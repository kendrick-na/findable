# ✅ Findable 투두리스트 — ⚠️ **근거·판정 이력 보관용으로 격하** (2026-08-12 세션N-24)

> 🔴🔴 **남은 일은 이 파일이 아니다. 단일 진실 = ⭐`_투두_단일진실_N24.md`**
> 이 파일은 **969줄이라 끝까지 읽히지 않아** *"코드로 할 게 없다"* 오진을 유발했다(4회 반복).
> → **미완 목록은 N24 파일로 이관.** 이 파일은 **왜 그렇게 판정했나(실측 근거)** 추적용으로만 쓴다.
> ⭐ 특히 살아있는 가치 = **§"만들지 말 것"**(882~918행)의 실측 기각 근거 10건.

> 🧭 방향·함정·판단은 ⭐**`_START_HERE_세션N23.md`** (⛔N22 이하 전부 폐기).
> ~~**이 파일은 "무엇이 남았나"의 단일 진실이다.**~~ → **격하됨(위 참조)**
> 갱신: **2026-08-11 밤 (세션N-23)** — ✅**S6 완료** + ✅**S7 1차 완료·배포·눈확인**
>
> ## ✅ S0~S6 완료 + S7 1차 완료 (커밋 `bcde96e`·`96b9fe7`·`505ddce`·`1a11e3f`)
> **S7-a**(숫자·돈) · **S7-b**(`/history` 자동갱신) · **S7-c**(조판·용어·화살표·드롭다운폭).
> 검증: app lint 0 · tsc 0(양쪽) · build 양쪽 · 테스트 **133/133** · 뮤테이션 2건 ·
> 눈확인 **1440px+390px**.
>
> ## 🔴🔴 엔진 수 진상 — **재조사·일괄수정 금지** (N-23 확정)
> 진단서의 *"7↔8 불일치 → 28곳 통일"* 은 **오답이었다.** 두 숫자는 다른 것을 센다:
> **본류 진단=항상 7**(`runner.ts:306`) · **영어만=4**(`GLOBAL_4`) · **8=브리핑 버튼을 따로 누른 뒤**.
> → 랜딩·요금제의 "7개"는 **틀리지 않았다**. 진짜 구멍은 **「영어만」 1건**이었고 고쳤다.
>
> ### ⭐ 다음 착수 = **S7 잔여 약 22건** — 감사 끝났고 목록이 있다(§N23 §5)
> 🔬 41건 전건 실측: **7건 이번 완료 · 7건 이미 고쳐져 있었음 · 5건 부분 · 나머지 열림**.
> 🎓 **진단서는 낡는다** — 그대로 믿었으면 7건을 재작업할 뻔했다.
> ⏳ 카카오페이 = **답장 대기**(먼저 보낼 것 없음) · 🔴 P0 지불의사 = 👤사용자 몫
>
> ## 🔬 세션N-19 전략 감사 — **S2 기재가 절반 오탐이었다**
> 착수 전 코드 실측. **그대로 만들었으면 있는 것을 또 만들 뻔했다**(3화면 낭비).
>
> | 투두 기재 | 코드 실측 | 판정 |
> |---|---|---|
> | `/actions`·`/history` 빈 상태 부실 | `/actions` 는 **이미 4요소 충족** | ❌ 오탐 |
> | `compare`·`sources` 에 빈 상태 이식 필요 | **이미 있음**(compare 는 2분기까지) | ❌ 오탐 |
> | — | 🔴 `/history` **완전 공백**(`jobs.map()` 빈 `<ul>`) | ✅ 진짜 |
> | — | 🔴 **빈 상태에서 영업 2종 렌더**(설계 §4-2 위반) | ✅ 진짜·미착수 |
> | — | 🔴 **`UpgradeLadder` 가 꺼진 기능 판매**(이메일) | ✅ **신규 발견** |
>
> 🎓 **교훈 재확인**: "빈 상태를 4화면에 이식"처럼 **묶음으로 적힌 항목은 건별로 실측**해야 한다.
> 3건 중 2건이 오탐이었고, 대신 **문서에 없던 결함 2건**이 같은 화면에서 나왔다.
>
> ## 🔴 순서를 바꿨다 — **S5(SSR)를 S4(용어) 앞으로**
> 근거: S4 는 화면 문구를 한국어화하는 일이고, S5 는 그 문구를 **크롤러에 노출**시키는 일이다.
> S4→S5 순서면 **영문 용어가 먼저 색인**돼 두 번 일한다. → **S5 먼저, 그 다음 S4.**
>
> ## 🔴 P4 KIPO 상표를 앞으로 당김 (👤 대표님 몫)
> `findableapp.com`(오스트리아 동일명 GEO)과 충돌 중 + 한국은 **선출원주의**.
> 코드 트랙과 **독립적으로 지금 진행 가능한 유일한 방어**라 뒤로 미룰 이유가 없다.
>
> ## 🔴 세션N-18 이 바꾼 것 (먼저 읽을 것)
> 1. 🔴🔴 **cron 인증 구멍을 닫았다** — `x-vercel-cron: 1` 헤더 한 줄로 **외부인이 유료 측정을
>    무제한 실행**시킬 수 있었다(프로덕션에 `CRON_SECRET` 이 없어 그 헤더로 폴백하고 있었다).
>    `packages/security/cron.ts` 로 단일화(fail closed) · 라이브 **401 재검증 완료**.
>    ⛔ **`x-vercel-cron` 폴백을 되살리지 말 것.**
> 2. 🔴 **cron schedule 은 항상 UTC 였다** — `0 2 * * *` 를 모두가 "새벽 2시"로 읽었지만
>    실제로는 **오전 11시 KST**. → UTC 15/16/17(=KST 0/1/2시)로 교정.
> 3. ✅ **PDF 3개월 실패 = 복구 확정**(5/5 생성). 확인 과제 **종결**.
> 4. ✅ **S0·S1 배포 완료** — `/billing` Scale 노출(💰못 팔던 것) · og:image 발행 · 거짓 판매문구 제거.
> 5. ⚠️ **S1-3 의 `/brand` 지적은 오탐이었다** — 정직한 화면이다(진짜 측정 버튼 있음).
>
> 🔴 **지금 정할 것 = 👤판단 1건** — **잠긴 유료 3종 처리**(채널톡식 시간축 전환이 유력)
> ✅ **세션N-16 배포 완료**: ~~D 밀도축소~~ ✅ · ~~A 빈상태(A1·A2)~~ ✅ · ~~**C 처방 전면공개**~~ ✅ → **다음 = B 원문신뢰**
> 🔴 **신규 발견 2건**: ①잠금 화면(`compare`·`sources`)이 **지어낸 숫자**(42%·31%·18%)를 깔고 있다
> ②**PDF 리포트가 5/3부터 100% 실패**(원인 미확증 · 확증엔 진단 1건 💰승인 필요) → 아래 P1.6 참조
> ✅ **"크레딧이 없어 새 진단이 안 돈다" 는 틀린 기재였다** — 엔진별 개인 키로 이미 우회돼 있고
>   7/30 이후 크레딧 에러 **0건**. **새 진단은 지금도 돈다**(원가 150~300원이라 💰승인 후 실행).
>
> ✅ **세션N-14 성과**: 오류 전수 해결(측정0건 손실단언·엔티티 49건·daum오염·하이라이트 버그)
> + 에러 301건 분류 + **리서치 3트랙 완료** → `UIUX_대개선_설계_v2_2026-08-10.md`
>
> 🔴 **왜 통합·재정리했나**: 세션N-10 이 `_START_HERE` 만 보고 "백로그 비었다"고 틀렸고,
> N-11 도 `투두리스트` 만 보고 같은 말을 했다가 `투두_마스터` 에 8건이 더 있는 걸 뒤늦게 발견했다.
> N-13 은 여기에 **없던 결제 트랙 2건**(`_카카오페이_심사보완/`)을 발견해 편입했다.
> **문서가 흩어지면 반드시 누락된다.** → 남은 일은 이 파일에만.
>
> ⚠️ **이 목록도 낡는다 — 착수 전 반드시 코드로 실측할 것.**
> N-11 실측에서 문서상 미완 **6건이 이미 구현**돼 있었다(#48·#67·#68·#76·#61·#84).

---

## 🎯 제품 기준 (세션N-16 확정) — 🔴 **"최소 기능"이 아니라 "고객 페인을 푸는 최고의 제품"**

> 👤 **사용자 정정(2026-08-10)**: *"내가 얘기했던 'MVP'라는 표현은 정정할게. MVP가 최소 기능 제품이라는
> 뜻인지 몰랐어. **고객들에게 선보일, 고객들의 페인을 해결하고 니즈를 해결해줄 최고의 제품**을 만들어."*
>
> 🔴 **이 문장이 이 프로젝트의 판단 기준이다.** 앞으로 우선순위 다툼이 생기면 이 기준으로 판정한다.
> **"작게 내보내자"는 더 이상 근거가 아니다.** 대신 *"이게 고객의 페인을 실제로 푸는가"* 를 묻는다.

### 🔴 이 기준이 바꾼 판단 (실제로 뒤집힌 것)

| 예전 기준(MVP) | 새 기준(최고의 제품) | 결과 |
|---|---|---|
| 처방 1건만 주고 나머지는 가입 유도에 쓴다 | **처방은 우리가 이기는 지점 — 아끼면 안 된다** | ✅ **5건 전부 공개**(`12a00de`) |
| 잠금 화면에 가짜 예시라도 깔아 가치를 암시 | **지어낸 숫자는 페인을 풀지 않는다** | ⬜ 실제 진단 링크로 교체 예정 |
| 게이팅은 많이 잠글수록 유료 전환에 유리 | **잠금은 "당했다"는 느낌 → 시간축으로** | 👤 결정 대기 |

### 🎯 고객의 진짜 페인 (리서치 확정 · 재리서치 금지)

> 12개 도구 리뷰어의 **독립 반복 지적**: ***"측정은 잘하는데 무엇을 해야 할지는 안 알려준다"***
> 사용자 반응 원문: ***"이걸 어떻게 고치는지 더 알고 싶다"*** 📕`06번:176` · `01번`

🔴 **고객의 페인은 "내 점수를 모르는 것"이 아니다.** 점수는 경쟁사도 다 보여준다.
진짜 페인은 **"그래서 내일 아침에 뭘 하면 되는데?"** 이고, 여기가 **카테고리 전체의 공백**이다.
→ **우리 `/actions`·`geoActions` 가 그 공백을 메우는 유일한 자산.** 여기에 자원을 몰아준다.

---

## 🧭 로드맵 — 페인 해결 순서 (세션N-16 재편)

| 단계 | 무엇 | 고객 페인 | 상태 |
|---|---|---|---|
| **1** | **처방 전면 공개 + 실행방법** | *"뭘 해야 할지 모르겠다"* ← **최대 페인** | ✅ **완료**(`12a00de`) |
| **2** | **처방을 믿게 만들기**(B 원문신뢰) | *"이 처방이 맞는지 어떻게 믿나"* | ✅ **완료**(`f6de40a`) |
| **3** | **처방 실행 추적**(완료 체크→before/after) | *"해봤는데 효과가 있었나"* | 🟡 코드 있음·**실고객 필요** |
| **4** | **보고 가능하게**(PDF 복구) | *"대표님께 보고해야 한다"* | 🔴 원인 미확증·💰승인 필요 |
| **5** | **잠금 정직화** | *"돈 냈는데 열리는 게 없다"* | 🟡 **가짜 숫자 제거 ✅**(`4292055`) / 시간축은 👤 **가격 결정 대기** |

> ⭐ **왜 이 순서인가**: 2·3·4는 **1이 있어야 의미가 생긴다**(처방이 없으면 믿을 것도, 추적할 것도,
> 보고할 것도 없다). 5는 **실결제 0건**이라 아직 아무도 안 당했다.

### 🔴 1·2 를 끝내고 보니 — **남은 것은 전부 "나 혼자 못 끝내는 것"이다**

🔬**세션N-16 재감사 결론**: 화면에서 코드만으로 개선할 수 있는 것은 **1·2로 사실상 소진됐다.**
남은 3·4·5는 **성격이 다르다** — 기술이 아니라 **판단·돈·고객**이 관문이다.

| 남은 것 | 진짜 관문 | 내가 할 수 있는 것 |
|---|---|---|
| **3. 실행 추적** | 🔴**실고객이 처방을 눌러야** 데이터가 생긴다(`ActionCompletion` **0건**) | 코드는 **이미 준비됨** — 기다리는 것 외 없음 |
| **4. PDF** | 💰**진단 1건(150~300원) 승인** 없이는 원인 확증 불가(가설 2건 이미 반박됨) | 승인되면 즉시 |
| **5. 잠금** | 👤**"기능이냐 기간이냐" = 가격 정책** | 가짜 숫자 제거는 지금도 가능 |

⭐ **그래서 다음 우선순위는 "코드"가 아니라 👤`P0 실고객 지불의사`다.**
3·4·5 가 전부 그 뒤에 줄 서 있고, 리서치도 *"실고객 없이 만들면 검증 없이 커진다"* 고 말한다.
(사용자 판단 "나중에"였으나, **이제 그것이 유일한 병목**임이 실측으로 드러났다.)

---

## 📌 재편 근거 — 깔때기 DB 실측

### 실측한 깔때기 — 🔴 구멍이 어디인지 숫자가 말해준다

| 단계 | 실측 | 판정 |
|---|---|---|
| 무료 진단 | **72건** (고유 이메일 **26명**) | ✅ 입구는 돈다 |
| → 가입 | **6명** (26명 중 · **23%**) | 🟠 절반 이하 |
| → 브랜드 등록 | 7개 — **전부 조직 1곳(우리)** | 🔴 **외부 고객 0** |
| → 반복 측정(Tracking) | 54행 — **브랜드 7개 중 2개만** | 🔴 5개는 0회 |
| → 조치 완료(ActionCompletion) | **0건** | 🔴 |
| → 결제 | **0건** | 🔴 |

🔴 **가장 큰 구멍**: 가입자 **6명 중 5명이 로그인하면 "빈 상태(설명 벽)"** 를 본다.
`psb***`·`twi***`·`ken***`·`nay***`·`ike***` — 실측으로 확인. 데이터가 보이는 사람은 **1명뿐**.

### 그래서 순서를 바꾼다 — **A를 쪼개고 앞으로 당긴다** (→ 이후 C를 최우선으로 재조정)

⭐ **B·C·D 는 "이미 온 사람에게 더 잘 보여주는 일"이고, A 는 "온 사람을 안 잃는 일"이다.**
지금 병목은 **유입이 아니라 착지**다(72건이 들어와서 5명이 빈 화면을 본다).

> 🔴 **2차 재조정(같은 세션, 사용자 기준 정정 후)**: 위 판단은 *"깔때기를 잇는다"* 관점이었다.
> **"최고의 제품"** 기준으로 다시 보니 **C(처방)가 1순위**였다 — 깔때기를 이어도
> **도착지에 고객이 원하는 게 없으면** 소용이 없기 때문이다. → C 를 먼저 끝냈다(`12a00de`).

- ✅ **D 완료** — 밀도(진실거울 1,409→676px). 무료진단 화면은 이제 읽을 만하다.
- 🔴 **A 를 A1/A2 로 쪼갠다** ← **지금 착수**
  - **A1. 빈 상태에 "내 진단"을 잇는다** — 🔬코드 실측상 대시보드는 **이미 이메일로 무료진단을
    매칭**한다(`page.tsx:53`). 즉 **다른 주소로 가입하면 조용히 빈 화면**이 된다.
    → 빈 상태에서 *"진단한 적 있나요? 그때 쓴 주소로 로그인하면 이어져요"* 를 **말해준다**.
    **원가 0 · 새 진단 불필요 · 24명(진단만 하고 미가입)이 돌아올 길**이다.
  - **A2. 설명 벽 → 샘플 리포트 실물**(C트랙 스티비 패턴) + **자기 소멸**(Metabase) +
    🔴**로딩 중 빈 상태 금지**(Raycast).
- 🟠 **B·C 는 A 뒤로** — 순서 자체는 유지(B 원문신뢰 → C 처방승격).
- ⏸️ **P3(D1·D2·D4)·P5 는 그대로 보류** — 근거 불변(시계열 1일 · 실고객 0).

### 묶기 — 같은 파일·같은 화면은 한 번에

- 🔗 **A1 + A2 + 잠긴 유료 3종** = **전부 `apps/app` 로그인 후 화면**이다. 한 묶음으로 본다.
- 🔗 **P2 lint 부채**(`audit-result.tsx` 27건)는 **B·C 가 그 파일을 크게 손댈 때** 함께 —
  단독으로 열지 않는다(기존 판정 유지).

---

## 📊 한눈에 — 지금 무엇이 남았나 (세션N-17 전면 재정리 · **2026-08-11 UIUX 진단 반영**)

> 🔴 **세션N-17 자기정정 2건** (되풀이 금지):
> ① *"코드로 할 게 없다"* 고 말했으나 **틀렸다** — 투두 뒷부분(P3·P4·P5)을 안 읽고 단정했다.
>    ⚠️ N-13 도 같은 실수를 했고 이 파일에 경고가 이미 있었다 → **3번째 반복.**
> ② *"UI/UX 는 리서치대로 다 개선했다"* 고 답했으나 **틀렸다** — 감사 대상이 **무료진단 결과화면 1개뿐**이었고
>    대시보드 12화면은 감사된 적이 없었다(`billing` 문서 언급 **0회**).
> 🎓 **교훈: 이 파일은 끝까지 읽어라. 그리고 "다 했다"고 말하기 전에 화면을 눈으로 봐라.**

### 🏁 최우선 트랙 = **UI/UX 결함 88건** (2026-08-11 신규 · 공인 프레임워크 진단)

> 📕 기획서 = ⭐**`UIUX_대개선_설계_v3_2026-08-10.md`** (v2 폐기 · 6축: IA·플로우·인터랙션·비주얼·**카피**·**SEO/GEO**)
> 📕 진단서 = `_진단_UIUX_v3/` 4개 · 기준 = **Apple 공식 8원칙 + NN/g 10 휴리스틱 + Vercel WIG**
> 🔬 방법 = 에이전트 99개(화면별 독립 감사 + **지적마다 반박 전담 검증자**)

| 진단 대상 | 원 지적 | ✅확인 | 오탐률 | critical |
|---|---|---|---|---|
| **무료진단 결과화면**(고객 첫 접점) | 41건 | **36건** | **12%** | **10건** |
| 대시보드 6화면 | 47건 | 32건 | 32% | 7건 |
| **합계** | **88건** | **68건** | — | **17건** |

🔴 **개별 88건 뒤의 근본 원인 5개** (기획서 v3 §2 — 개별 수정보다 이게 본질):
| # | 원인 | 대표 증거 |
|:--:|---|---|
| ⑤ | 🔴🔴**숫자가 서로를 반박한다** | 63%(25/32)→실제78% · 「8곳 모두」vs「80%」 · 엔진수 7↔8 · 「214시간 전 + 24시간 캐시」 |
| ① | 빈 상태가 제품을 설명 안 함 | `/actions`·`/history` 가 제목+버튼 하나 또는 완전 공백 |
| ② | 파는 것과 실제가 다름 | `alerts` 이메일 알림(꺼져있음) · `brand` "측정 시작"(등록만 함) |
| ③ | 파는 자리에서 이름·가격을 숨김 | **`/billing` 에 Scale(월99만) 없음** · "Growth에서 해제하기"(가격 없음) |
| ④ | 내부 용어·영업이 화면을 덮음 | 첫화면 40%가 영업 · CTA 4개 · `Korean Entity Grounding` |

### 🔢 미완 항목 전수

| 구분 | 남은 일 | 착수 가능? | 관문 |
|---|---|---|---|
| 🎨 **P1.7 UIUX** | 결함 68건 중 **원인①②③⑤ 착수분 완료** → 남은 축 = **원인④**(S3'·S4) + SSR(S5) + IA(S6) | ⭐**지금**(원가 0) | 없음 — **코드로 다 가능** |
| 🔴 **P0** | 지불의사 검증 | ✅ | 👤**사람을 만나야 함**(불변 1순위) |
| 🟠 **P1** | 정기결제(빌링키) · 웹훅 시크릿 | ⏳ | 💳채널키(카카오페이 승인 후) |
| 🟡 **P2** | lint 부채 27건 | ⚠️ | 손대지 않기로 판정 · **단 원인⑤ 수정이 같은 파일이라 함께 볼 것** |
| 🟢 **P3** | D4 기간선택 · D2 이벤트핀 · D1 알림UI · 재측정메일 | ❌ | 📊측정 5~6회 누적 |
| 🔵 **P4** | 엔진수 게이팅 · 파트너 특전 · KIPO 상표 | 🟡 | 👤정책 결정 |
| ⚪ **P5** | GEO 실행 에이전트 · RAG · durable · Prisma Ext | ✅ | 🟡가능(권고 10월+) |
| ⬜ 확인 | crew 로그(`refineApplied`) | ⏳ | 다음 crew 실행 시 |
| ~~⬜ 확인~~ | ~~PDF 복구 실물~~ | ✅ | **종결**(N-18 실측 5/5 생성) |
| 🔵 신규 | **app `/favicon.ico` 404**(콘솔 에러) | ✅ | 기존 결함·기능영향 없음. web 엔 있고 app 엔 없다 → 파일만 추가하면 끝 |
| 🔵 신규 | **cron 스케줄 실동작 확인** | ⏳ | UTC 17시(=KST 새벽 2시) 교정 후 **첫 자동 실행이 실제로 도는지** DB 로 1회 확인(원가는 원래 도는 작업분) |

---

## 🎯 전략적 착수 순서 (2026-08-11 확정 — 왜 이 순서인가)

> 🔴 **판정 기준**(사용자 정정): *"최소 기능이 아니라 **고객 페인을 푸는 최고의 제품**"*
> 🎯 성공 조건(사용자 정의): *"고객사·회사 직원이 봐도 **직관적이고 이해 쉬울 것**"*

| 단계 | 무엇 | 왜 이 순서인가 | 원가 |
|:--:|---|---|---|
| ~~**S0**~~ | ✅ **숫자 정합성**(원인⑤) — **완료·배포됨**(`500be5f`, N-18 배포) | 다른 모든 개선의 전제였다 | 0원 |
| ~~**S1**~~ | ✅ **고장 수리 3건** — **완료·배포됨**(`2f6cdd3`) | Scale 노출·og:image 발행·거짓문구 제거. ⚠️`/brand` 지적은 **오탐**이었다 | 0원 |
| ~~**S2'**~~ | ✅ **빈 상태 3대 결함** — **완료·배포·라이브 검증**(`87c1a5e`·`fa7ebff`) | 아래 §S2' 상세 | 0원 |
| ~~**S3'+S4**~~ | ✅ **용어 한국어화 + 가격 표기 + `lang="ko"`** — **완료·배포·라이브 검증**(`0611c2d`·`11f63cb`) | 아래 §S3'+S4 상세 | 0원 |
| ~~**S5**~~ | ✅ **진단결과 SSR** — **완료·배포·라이브 재실측**(`d76a22c`·`eb1f115`) | 아래 §S5 상세 | 0원 |
| ~~**S6**~~ | ✅ **IA 3단 + 이름통일 + 표시 정직성** — **완료·배포·눈확인**(`bcde96e`·`96b9fe7`) | 아래 §S6 상세 | 0원 |
| ~~**S7 1차**~~ | ✅ **숫자·돈 + 죽은화면 + 조판** — 완료·배포·눈확인(`505ddce`·`1a11e3f`) | 아래 §S7 상세 | 0원 |
| ~~**S7 2차**~~ | ✅ **www 이탈복귀 + 인용범례 + 척도설명 + 빈상태폭 + 영어혼재** (`f6ed9bf`) | `_START_HERE_세션N23.md` §5 | 0원 |
| ~~**S7 3차**~~ | ✅ **순위 표기 단일화 + 가정 7필드 근거** (`3b50c85`·`b59d3f9`) | `_START_HERE_세션N23.md` §5-1 | 0원 |
| ~~**S7 4차**~~ | ✅ **이력 총건수 + 감성배지 설명** (`ed41515`·`de54576`) | `_START_HERE_세션N23.md` §5-2 | 0원 |
| **S7 잔여** | 2건(1=large·2=미발현) — **사실상 소진** | `_START_HERE_세션N23.md` **§5-3** | 0원 |

> 🔴 **S7 이 소진되면 남는 건 코드가 아니다.** 회사 병목은 **P0 실고객 지불의사 검증**
> (👤사람을 만나야 함)과 **KIPO 상표**다 — 다음 세션은 이걸 먼저 짚을 것.

> ⛔ **S7 에서 "하지 않기로 판정한" 2건 — 되살리지 말 것**
> ① **결제 상태 3줄**: `Organization` 스키마에 다음 결제일·카드 끝 4자리가 **없다**
>    (`planExpiresAt` 은 만료일). 없는 데이터를 화면에 만들 수 없다.
> ② **잠금 카드 `min-h-[60vh]`**: 잠금 3화면이 전부 `preview` 를 넘겨 **닿지 않는 죽은 코드**.

### ✅ S7 1차 — 숫자 정합성·죽은 화면·조판 (2026-08-11 세션N-23)

| 항목 | 무엇 | 상태 |
|---|---|---|
| **S7-a①** | 🔴 「영어만」 진단은 실제 **4 엔진**인데 "7개"로 팔았다 → 선택지에 **AI 4곳** 명시 + 빠지는 엔진 설명 + 히어로·단계라벨 정정 | ✅ |
| **S7-a②** | 🔴 **Enterprise 만 VAT 안내 없음**(연 3천만원에 300만원이 계약 때 등장) | ✅ |
| **S7-a③** | `연 ₩30M~` → **`연 3,000만원~`**(app·web 동시 · 다른 카드는 전부 "월" 슬롯이라 오독) | ✅ |
| **S7-b** | 🔴 `/history` 가 **측정이 끝나도 영원히 「측정 중」** → 진행 중일 때만 15초 폴링 + 상한 40회 + 수동 버튼 | ✅ |
| **S7-c** | 처방 제목 12px회색 → **h2**(우리가 이기는 섹션이 가장 약했다) · **"다른 층"** 내부용어 · **`↗ -46%p`** 방향 오독 · 업종 드롭다운 `w-fit` | ✅ |

🐛 **테스트·스크린샷이 내 버그 3건을 잡았다**: ①effect 의존성에 `timedOut` → 폴링 되살아남
②mock router 를 매 렌더 새 객체로 만들어 재구독 ③**히어로가 폼 선택과 다른 숫자**(코드로는
안 보였고 **모바일 스크린샷**이 잡았다).
⭐ **벽시계 상한은 테스트 불가**(가짜 타이머가 `Date.now()` 를 안 돌린다) → **틱 수로 센다**.
⚠️ **N-22 가 "오탐"이라 한 업종 드롭다운은 진짜였다** — `SelectTrigger` **기본값이 `w-fit`**.
   🎓 *"내 파일에 없다" ≠ "적용 안 된다"* — 디자인 시스템 기본값을 열어야 한다.

### ✅ S6 — IA·이름·표시 정직성 (2026-08-11 세션N-22 완료·배포·라이브 눈확인)

> 👤 승인: **"ⓐ+ⓑ+ⓒ 전부 묶기"** + **"잠금 4개는 「분석」 그룹으로 그대로 노출"**(접지 않음).

| 단계 | 무엇 | 상태 |
|---|---|---|
| **S6-a** | `/brand` 이름 **3개→1개**(「브랜드·측정」) + 폼 「브랜드 추가」 + 🔴**거짓 설명문 수정** | ✅ |
| **S6-b** | 사이드바 **3단 그룹화**(⭐실행·측정·분석) — 처방을 최상단 | ✅ |
| **S6-c** | 비주얼·인터랙션 — **4건이 아니라 3건**(#3 오탐) | ✅ |

**S6-a 거짓 설명문**: *"바로 측정을 시작할 수 있어요"* → 실제로는 **등록만** 한다
(측정은 `StartTrackingButton`). → **"등록하면 측정 버튼이 생겨요. 측정은 1~3분 걸려요."**

**S6-c 결과**:
| # | 결함 | 처리 |
|:--:|---|---|
| 1 | 블러 프리뷰 대비 1.74:1 | ✅ `opacity-30 blur-[5px]` → `opacity-60 blur-[3px]`(`aria-hidden` 유지) |
| 2 | `(준비 중)` 이 완성 기능과 똑같은 주황 체크 | ✅ **`ready` 플래그를 데이터로** + 시계 아이콘·회색·문구 3중 구분 |
| 3 | 업종 드롭다운 너비 불일치 | ❌ **오탐** — `w-fit` 은 배지 2곳뿐, `SelectTrigger` 는 기본 전폭 |
| 4 | `/history` 가 실패·대기 행에도 "결과 보기 →" | ✅ **행 전체가 링크였다**(문서 기재보다 심각) → `completed` 만 링크 |

🔴 **#2 에서 문자열 파싱을 쓰지 않았다** — 라벨의 `(준비 중)` 을 파싱하면 문구가 바뀔 때
**조용히 깨진다**. 플래그가 단일 진실이고, 테스트가 *"라벨에 그 문자열을 박지 마라"* 를
**양방향으로** 고정한다(올바른 값 요구 + 틀린 값 부활 차단).

⛔ **잠금 4개는 접지 않았다**(👤결정) — `compare`·`sources` 는 **작동하는 기능**이라
접으면 *"있는 기능을 안 팔고 있었다"*(N-17 이 고친 결함)의 재발이다. 그룹화로 해결됐다.
🔴 **Gauge 는 `Act` 를 마지막에 두는데 「실행」을 최상단에 올렸다** — 리서치 확정 페인이
우리 차별점이라서다. **의도된 변경**(되돌리려면 실행을 맨 아래로).

### ✅ S5 — 진단결과 SSR (세션N-19 완료·배포·라이브 재실측)

> 🔴 **아이러니를 없앴다.** "AI 인용되게 해주는 서비스"를 파는데 **우리 결과 페이지가
> AI 크롤러에게는 빈 페이지**였다. 📕근거: GPTBot·ClaudeBot·PerplexityBot 는 **JS 미실행**.

| 항목 | 이전 → 지금 (라이브 정적 HTML 실측) |
|---|---|
| `SK하이닉스`(브랜드명) | **0회 → 10회** |
| `HyperCLOVA`(엔진명) | **0회 → 2회** |
| `등장률` | **0회 → 2회** |
| 처방 제목(`근거 문장` 등) | **0회 → 3회** |

- ⚠️ **전면 재작성 회피**: `audit-result.tsx` 는 **3,926줄**이고 폴링·상태가 얽혀 있다
  → 기획서 §6-1 처방대로 **요약부만** 별도 서버 컴포넌트로 추가(로직 무수정).
- 🔒 **개인정보 구조적 차단**: 기존 "공유 뷰 이메일 숨김"은 `window.location.search` 를 읽는
  **클라이언트** 판정이라 서버에선 못 쓴다. → 요약 함수가 **이메일을 받지도 않게** 설계 +
  DB select 에서 `email` 제외. **라이브 재검증: 신청자 이메일 유출 0**(푸터 사업자 주소만).
- 🔴 **산식 복제 금지**: 저장값을 그대로 옮긴다. 순수 함수를 `packages/audit/ssr-summary.ts` 로
  빼서 **테스트로 고정**(`apps/web` 엔 테스트 러너가 없다) · **뮤테이션 검증 2건**.
- ⚠️ **필드명은 라이브 API 로 실측**: `metrics.enginesCovered` 는 **중복 포함 응답 목록**이라
  (29개 응답 = 고유 8엔진) 길이를 그대로 쓰면 *"AI 29곳"* 이라는 거짓말이 된다 → `new Set()`.
  ⛔ `geoScore`·`recognitionRate` 는 실측 회차에 **없다**(null) → 쓰지 않았다.

🐛 **배포 후 스크린샷이 내가 만든 것의 결함 2건을 잡았다**:
①요약이 아래 상세와 겹쳐 **사람은 같은 걸 두 번 읽었다**(S3' 첫화면 정리 직후에 내가 중복을
얹은 셈) → **`<details>` 로 접음**(크롤러는 접힌 내용도 읽는다 · 숨김 아님).
②*"지금 할 일"* 제목 아래에 `kind=avoid`(*"이건 하지 마세요"*)·`rank_strategy`(*"이미 1순위"*)가
섞여 있었다 = **할 일이 아닌 것** → 요약에서 제외(상세엔 그대로 보인다).

### ✅ S3'+S4 — 용어·가격·줄바꿈 (세션N-19 완료·배포·라이브 검증)

> 🔬 **S3 와 S4 를 합쳤다**: 감사 결과 S3 의 핵심(빈 상태 영업 2종·CTA 4개)은
> **S2' 에서 이미 끝나 있었고**, 남은 건 전부 문구라 S4 와 같은 파일·같은 성격이었다.

| 무엇 | 바뀐 것 |
|---|---|
| 용어 7종 | `프롬프트 추적`→**질문 추적** · `Korean Entity Grounding`→**한국어 표기 통합 추적** · `Top 3 액션`→**먼저 할 일 3가지** · `API 액세스`→**API 연동** · `Mention/Citation 분리 집계`→쉬운 말 3줄 · `개선 처방의 채널`→**어디를 고쳐야 하는지** |
| 사이드바 | `실행 액션` → **`지금 할 일`**. 제품 안에 **이미 이 이름이 4곳** 있었다(메뉴만 딱딱) |
| 파트너 카드 | *"승인되면 파트너 전용 접근이 열립니다"*(자기참조) → **대상+혜택**. 🔬혜택은 코드로 확인한 것만(`decide.ts:91`=growth 부여) |
| 잠금 버튼(원인③) | `Growth에서 해제하기` → **`월 39만원 Growth로 열기`**. 🔒가격은 **카탈로그에서 읽는다**(하드코딩 금지) |
| 요금제 부연 | `{label, hint}` 구조 신설 — Growth 차별점이 **무슨 이득인지 화면에 없었다** |

🔴🔴 **최대 발견 = `app` 이 `lang="en"` 이었다**(스크린샷 눈확인):
부연이 *"정해드 / 려요"* 로 **단어를 쪼갰다**. 원인은 CSS 누락이 아니라 —
`globals.css:423` 에 `:lang(ko) { word-break: keep-all }` 이 **이미 있는데**
앱이 스스로를 영어라고 선언해 **한 번도 적용된 적이 없었다.**
→ `lang="ko"` **한 단어로 앱 전 화면의 한국어 줄바꿈이 고쳐졌다** + 스크린리더 발음 정상화.
🔬 안전 확인: app 은 locale 라우팅·i18n 패키지를 **쓰지 않는다**(전 화면 한국어).

🐛 **라이브 스크린샷이 추가로 잡은 2건**: ①Enterprise 만 `API 액세스` 잔존(같은 화면에서
두 이름) ②**web 요금제 표가 옛 용어 그대로** — `pricing.ts` 주석이 "web 과 함께 갱신"을
이미 요구하는데 app 만 고치면 **한 상품이 두 이름으로 불린다** → `TIERS_KO` 동기화(EN 은 유지).

✅ 검증: web·app tsc 0 · lint 0 · build 양쪽 · 테스트 **112/112**(신규 3) · 라이브 재검증

### ✅ S2' — 빈 상태 3대 결함 (세션N-19 완료·배포·라이브 검증)

> 🔬 **범위가 재조정됐다**: 문서의 "4화면 이식"은 **3화면이 오탐**이었다(위 감사표).
> 대신 진짜 결함 3건 + 중복 정리로 바꿨다. **순 +25줄**.

| # | 결함 | 수정 |
|:--:|---|---|
| 1 | 🔴 `/history` **완전 공백** — `AuditHistoryList` 에 length 가드 없음 → 빈 `<ul>` | 가드를 **컴포넌트 안**에 둠(호출부가 늘어도 재발 불가) |
| 2 | 🔴 **빈 상태에 영업 2종**(`PartnerCTA`·`UpgradeLadder`) — 설계 §4-2 위반 | 제거. ⚠️**지운 게 아니라 옮긴 것** — `hasData` 분기엔 그대로 |
| 3 | 🔴 **`UpgradeLadder` 가 꺼진 기능 판매** — *"이메일로 보내드려요"* | 실측: 프로덕션 env **50개 키에 `FINDABLE_ENABLE_DIGEST_EMAIL` 없음**(=꺼짐) → *"이력에 쌓아드려요"* 로 정직화 |

- ✅ **공용 `EmptyState` 승격** — 3벌 복제 마크업 단일화(`components/empty-state.tsx`)
- ✅ **`sources` 에 실제 회차 샘플 링크 추가**(지어낸 숫자 금지 방침 유지)
- 🐛 **스크린샷이 잡은 버그 1건**: `compare` 의 `no-ranking` 은 **이미 측정한 사람**인데
  버튼이 *"측정 시작하기"* 였다(설명문과 모순) → *"질문 추가하러 가기"*.
  ⚠️ 목적지를 `/search` 로 적었다가 **실측으로 정정** — `PromptWizard` 는 `/brand` 안에 있다.
- ✅ 검증: tsc 0 · lint 0(8파일) · build 통과 · 테스트 **109/109**(신규 3) · **뮤테이션 검증**
- ✅ 라이브: `/history` 안내 노출 확인 · 대시보드 빈 상태에 *"업그레이드하면…"* **없음** 확인

🔴 **내가 배포를 한 번 깨뜨렸다**(`dpl_7iejFziJ…`) — 원인은 프로덕션 코드가 아니라 **새 테스트**:
`window is not defined`. 이 저장소는 **vitest 설정 파일이 없어 환경 기본값이 `node`** 다.
⚠️**로컬 전체 실행에서는 통과했다**(다른 테스트가 만든 DOM 에 편승) → **단독 실행에서만 드러난다.**
→ 렌더링 테스트에는 **`@vitest-environment jsdom` 도크블록 필수**(`fa7ebff`).

🔴 **S0 를 먼저 하는 이유**: 진단이 찾은 critical 17건 중 **10건이 무료진단 화면**이고,
그 대부분이 **숫자 불일치**다. 이건 취향 논쟁이 없는 **산수 오류**라 고치면 확실히 나아진다.
반대로 S2~S6 를 먼저 하면 **틀린 숫자를 예쁘게 포장**하는 셈이 된다.

⚠️ **S0~S6 는 전부 화면 변경이다 → 착수 전 사용자 승인 필요**(프로젝트 규칙).
⚠️ **각 단계마다 스크린샷 눈 확인**(규칙: 테스트는 아는 것만 검증한다).

---

## 🔴 P0 — 회사 단일 장애점 (👤 사용자 몫)

- [ ] 👤 **지불의사 스모크테스트 (실고객 10~15명)** ⭐**불변 1순위** — 🕓**사용자 판단: 나중에**(2026-08-10)
  마케터(외국 브랜드 한국법인·K-뷰티 D2C)에게 수동 진단 리포트 → *"월 ₩99K 낼까?"* 관찰.
  질문지=`P0_지불의사_시뮬레이션_결과.md`. 측정 결함·원가 실측이 끝나 **기술적으로는 진행 가능**.
  ⚠️시뮬로 대체 불가(r=0.20 · [[reference_llm_persona_simulation_limits]]).
  🔗**이게 풀려야** P4 의 before/after·게이팅도 데이터에 근거해 만들 수 있다.

---

## 🟠 P1 — 결제 심사 트랙 (👤 사용자 몫, 마감이 있는 유일한 트랙)

> 📕 SoT = **`_카카오페이_심사보완/`** (문안·스크린샷·PPT 전부 여기)
> ⭐ **월요일 아침 실행 = `_카카오페이_심사보완/⭐월요일_아침_실행카드.md` 하나만 보면 된다.**
> 🔬 세션N-13 실측: 이 트랙은 **투두리스트에 아예 없었다**. 기록이 **8/4 에서 멈춰 있다**.

### ✅ 월요일 할 일은 **이미 끝났다** (2026-08-10 저녁 · 세션N-16 확인)
⚠️ **이 투두의 "① 포트원 문의 → ② 카카오페이 회신" 은 낡았다.** 실행카드 실측 결과:
- ~~① 포트원 단건결제 문의~~ → **폐기**. 전제가 틀렸다(포트원 콘솔에서 **이미 접수 완료**였음).
  `포트원_단건결제_추가문의.md` 는 **보내지 않았고 보낼 필요도 없다**.
- ~~② "8월 4일 접수" 날짜 함정~~ → **발송본에서 이미 삭제됨**. 사고 안 났다.
- ✅ **2차 회신 발송 완료** + **단건결제 신청서 최종 제출 완료**(`A0000MYQTZ9L8ZSI`).

### ✅ [2026-08-11 저녁] 4차 회신 발송 완료 — **정기결제 상품을 실제로 만들어서 제출**

심사관 3차 회신: *"정기결제로 접수한 건에 대해 **사이트 내 정기결제 상품이 없을 경우**
심사 진행이 어려우니 확인 후 회신"* → **취소하라는 뜻이 아니라 "있냐 없냐"를 물은 것**.

👤 **대표님 판단**(맞았다): *"저 준비계획이 필요한 거면 만들고서 메일 보내는 게 어때?
이미 정기결제는 꼭 필요한 거야"* → **말이 아니라 실물로 답해** 왕복 한 번을 없앴다.

🔬 **내가 틀렸던 것 2건**(대표님 지적·재검증으로 정정):
1. *"CID가 없으면 정기결제를 못 만든다"* → ❌ **테스트 CID `TCSUBSCRIP` 로 만들 수 있다**
2. *"카카오페이는 채널키 하나면 된다"* → ❌ **정기결제용 채널 별도 필요**(근거 없이 단정했었다)

✅ **구현·배포 완료**(`f806bbf`·`9eff045`): 빌링키 발급(EASY_PAY) · 첫 회 청구 ·
⚖️**웹 해지**(예약취소+빌링키삭제) · **사전 고지**(상품·금액·주기·차기결제일·해지방법+동의).
👤대표님이 포트원 콘솔에서 정기결제 테스트 채널 생성 → 채널키 env 등록.

🔴 **발송 전 자체 적발**(중요): 화면·PPT·메일이 모두 *"매월 1회 자동결제"* 라고 단정했는데
**2회차 자동청구(결제 예약)가 미연결**이었다(schedule 호출 0건). 이번 세션에서 `alerts`
잠금화면의 "이메일 알림"을 같은 이유로 고쳐놓고 **내가 만든 화면에서 반복할 뻔했다.**
→ 3곳 모두 *"지금은 첫 회만 결제되며, 자동 청구는 준비 중"* 으로 정직화.

---

### ✅ [2026-08-11] 3차 회신 발송 완료 — **결제경로(단건/정기) 자료 제출**

심사관 3차 요청(8/11): *"현재 사이트 내 **정기결제(구독 상품)만 확인**되오니,
결제경로(단건/정기 모두) 작성하여 파일 회신"*

🔬 **핵심 = 심사관이 반대로 이해했다.** 실제로는 **단건만 작동**하고 정기는 CID 미발급으로 미구현
(빌링키 호출 0건 실측). 원인은 화면의 "월 99,000원"·주문명 "월 구독" 표기.
→ v1 PPT 가 그 사실을 **작게** 적어 못 보고 지나친 것 → **v2 는 장 제목에 박았다**
(`【단건결제 — 현재 운영 중】` / `【정기결제 — 미운영 (CID 미발급)】`).

- ✅ 캡처 6장 자동 재촬영(`scripts/capture-payment-path.py` — **결제창까지 눌러서** 촬영)
- ✅ `결제경로_인디고차일드_v2.pptx` 8장 — 가이드 번호 그대로(4·4-1·5=단건 / 6·7=정기)
- ✅ **3중 검증 통과**: 좌표 이탈 0 · 금액 6종 카탈로그 전건 일치 · URL 4종 200 ·
  🔴**PPT 기재 계정으로 실제 로그인 성공**(billing 에 Starter·Growth·Scale·108,900 노출 확인)
- 🐛 검증에서 **잡아 고친 것 3건**: ①ID·PW 가 줄 중간에서 잘림(계정 오타 유발 → 지난 심사
  실패 지점) ②*"웹 해지 수단 제공"* 현재형 표기 → 실측상 해지 UI **없음** → "구현 예정"으로 정정
  ③메인·요금제 캡처가 126px 로 축소 → 뷰포트 캡처로 734px 확보

**현재 상태 = 심사 대기.** 👤할 일은 **답장을 기다리는 것뿐.** 먼저 보낼 것 없음.
| 심사관 답 | 할 일 |
|---|---|
| **승인** | 🔴 실행카드 §"되돌릴 것" **5건**(테스트 계정 비번·웹훅 시크릿·라이브 채널키·Client Trust) |
| "정기결제 화면 재요청" | CID 발급 여부부터 확인. **CID 없이는 캡처 불가**(결제창 호출 자체가 안 됨) |
| "단건도 안 보인다" | 테스트 계정 직접 확인 요청 — 8/11 실측으로 로그인·결제창 정상 확인됨 |

- [x] ~~🔴 VAT 정정과 제출 스크린샷의 금액 불일치~~ → ✅ **3차 회신에서 해소**
  결제창 캡처가 **요청받은 자료**가 되면서 "묻지 않은 쟁점"이 아니게 됐다(전 판단의 전제 소멸).
  새 캡처 = 108,900원 + 요금제 화면에 "VAT 별도 · 결제 금액 ₩108,900" 표기 → 별도 해명 불필요.
  ⚠️ 기존 `C_결제창.png`(99,000)는 **덮어쓰지 않고 보존**(무엇을 보냈는지 추적용).

### 🔴 승인 나면 남는 개발 (2026-08-11 갱신 — **대부분 끝났다**)

✅ **정기결제 자체는 구현 완료**(빌링키·첫결제·웹해지·사전고지). 아래 3건만 남았다.

- [ ] 🔴 **라이브 정기결제 채널키로 교체** — `NEXT_PUBLIC_PORTONE_CHANNEL_KEY_BILLING`
      (지금은 테스트 CID `TCSUBSCRIP`)
- [ ] 🔴 **결제 예약(2회차 자동청구) 연결** — `POST /payments/{paymentId}/schedule`(포트원 V2).
      ⚠️`packages/payments/portone.ts` 에 **취소 함수만 있고 생성 함수는 없다**.
- [ ] 🔴 **연결 후 사전고지의 "지금은 첫 회만 결제되며…" 줄 삭제**
      (`subscribe-button.tsx` — 주석에 지우라고 적어 뒀다). 안 지우면 **거짓 표기**가 된다.

🔴 **해지 순서 고정**: ①예약 취소 → ②빌링키 삭제. 뒤집으면 **무한 과금 사고**.

<details><summary>옛 기록 — 정기결제 미구현 시절 진단(2026-08-10 세션N-13)</summary>

### ~~🔴 승인 나면 개발이 남아 있다 — 정기결제(월 구독)가 미구현~~
> 🔬 **세션N-13 코드 실측(2026-08-10)로 새로 발견.** 이전 투두 어디에도 없던 항목이다.

**지금 상태**: **"월 구독"으로 팔고 결제창에도 그렇게 뜨는데, 실제로는 1회 결제 후 영구 부여다.**
4중 교차검증(전부 grep·코드 실측):

| 층 | 실측 | 파일 |
|---|---|---|
| 요금제 화면 | `period: "월"` · "월 30개 프롬프트 추적" | `apps/app/.../lib/pricing.ts:42` |
| **결제창 주문명** | **`Findable ${plan} 월 구독`** | `actions/billing/checkout.ts:87` |
| 결제 호출 | **`requestPayment`(단건)** — `requestIssueBillingKey` **0건** | `features/billing/upgrade-button.tsx:66` |
| 만료·갱신 | `planExpiresAt` **코드 사용 0건** · `grantPlan` 에 **만료 개념 없음** | `packages/auth/plan-grant.ts` |

🔴 **결과**: 이대로 결제를 받으면 **첫 달만 걷히고 그 뒤로 영구 무료 사용**이 된다.
매출 누수인 동시에 **"월 구독"이라는 판매 표시와 실제 동작이 다르다**(표시 정합 문제).
⚠️ 카카오페이 심사 회신에도 *"정기결제 : 월 구독 플랜 3종 자동갱신"* 이라고 적어 보냈다.

- [ ] 🖥️ **정기결제(빌링키) 구현** — ⚠️**채널키 2개(일반·정기)가 나와야 시작 가능** → 승인 대기
  1. 포트원 콘솔 채널 2개 등록 → env 분리(`..._CHANNEL_KEY` 단건 + `..._CHANNEL_KEY_BILLING` 신규)
  2. `requestIssueBillingKey({ billingKeyMethod: "EASY_PAY" })` 로 빌링키 발급
  3. 빌링키 저장 → `POST /payments/{id}/billing-key` → `POST /payments/{id}/schedule`
  4. 웹훅 `Transaction.Paid` 시 **다음 달 재예약** + `planExpiresAt` 실사용
  5. **웹 해지 UI** — 🔴**전자상거래법 의무**
     🔬**세션N-14 리서치로 판단 기준이 정량적임이 확인됨**(개정법 **2025-02-14 시행**,
     제21조의2 · 정기결제는 제13조 제6항): **가입 절차 단계 수 vs 해지 절차 단계 수를 비교**하고
     **진입 링크 접근성**을 비교한다. 규제 대상 = 숨은갱신·특정옵션 사전선택·잘못된 계층구조·
     **취소/탈퇴 방해**·반복간섭.
     → 🔴**해지 경로를 가입 경로와 "같은 단계 수·같은 접근성"으로 함께 설계할 것**(나중에 덧붙이기 금지).
     📕근거=`_리서치원본_UIUX_2026-08-06/07_B2B_SaaS_화면구성_실측_2026-08-10.md` §11
  🔴**함정**: 해지 때 `DELETE /payment-schedules` 를 빠뜨리면 포트원 리커버리가 계속 청구
  → **무한 과금 사고**. 해지는 **예약 취소 + 빌링키 삭제 둘 다**.

### 승인 회신이 오면 그날 할 것 (그전엔 하지 말 것)
- [ ] 👤 **`PORTONE_WEBHOOK_SECRET` 발급 → `findable-app` env + PortOne 콘솔 웹훅 URL 등록**
  🕓**지금 불필요 — 카카오페이 승인 후로 미룸**(2026-08-10 판정). 근거 3가지:
  ① 웹훅은 **배포 완료**(`app.findable.co.kr/webhooks/payments` 라이브 200).
  ② 시크릿이 없으면 `not_configured` 로그 후 **200 으로 조용히 닫는다** → 다른 기능 영향 0,
     **위조 시도도 무해**(서명 검증 통과 전엔 아무 것도 안 함).
  ③ 웹훅이 메우는 구멍은 *"결제됐는데 plan 이 free"* 인데 **결제 채널 승인 전이라 결제가 0건**이다.
  게다가 시크릿은 **웹훅 URL 등록과 세트로 발급**되므로 채널 확정 전에 하면 다시 손대야 한다.
  🔴**단, 첫 실결제 전에는 반드시 넣을 것** — 안 넣고 결제를 받으면 사고가 실제로 난다.

</details>

---

## 🔴 P1.5 — 전면 감사 미완 (세션N-13 이 남긴 것, **다음 세션 최우선**)

> 🧭 상세·근거는 `_START_HERE_세션N14.md` §1. 사용자 캡처 제보에서 시작한 감사다.
> ✅ 텍스트 오염(195건→0)은 **완료**(`2b81f5d`). 아래가 남았다.

- [x] ✅ 🔴 🖥️ **daum 무관 문서가 "AI 답변"으로 화면 노출** — **완료**(`9632fcc`, 세션N-14)
  판정용·표시용 텍스트를 분리했다. `analyzeText` 에 `displayText` 인자 신설 →
  **판정 입력은 기존과 완전히 동일**(promptDocs+brandDocs)하게 두고 `rawResponse` 만 필터.
  프롬프트 검색분은 브랜드가 없어도 남긴다(**경쟁 지형 자체가 근거**). 걸러낸 결과가
  비면 원문 폴백(빈 근거 화면 방지). 소급 데이터 무변경.
  🔬**뮤테이션 3종으로 가드 검증** · 신규 테스트 16건 · app 84/84 · build app·web 통과.

- [x] ✅ 🟡 🖥️ **감성 분포 분모** — **완료**(`9632fcc`) · ⚠️**투두 기재가 부정확했다**
  🔬실측: 화면 3곳(결과페이지·대시보드·PDF)은 **건수만 표기**해 나눗셈이 아예 없고,
  대시보드는 `total===0 → null` 로 **이미 방어돼 있었다**. *"화면이 긍정 15%로 보여준다"* 는
  코드에 없다(감사 중 화면 확인 없이 추정으로 적힌 것).
  → 그래도 **누구든 퍼센트를 추가하면 `0/0=NaN`** 이므로 `normalizeSentiment` 신설(표시 전용).
  ⚠️점수(`geoAxisScores`)는 쓰지 않는다 — 거기 `sentTotal===0 → 30점` 기준선을 바꾸면
  소급 점수가 흔들린다.

- [x] ✅ 🔴 **[부수 발견] 검색 하이라이트가 브랜드명을 쪼개고 있었다** — **점수 영향 있음**
  🔬**카카오 검색 API 라이브 실측**(curl 40건): `“마뗑킴·<b>조선</b><b>미녀</b>…”` 처럼
  브랜드명을 **토큰 단위로 쪼개** 각각 감싼다. `sanitizeEngineText` 가 태그를 **공백으로**
  치환해 `조선 미녀` 가 되면서 **브랜드가 통째로 사라졌다** — **조선미녀 10건 중 9건**.
  (설화수·메디큐브·올리브영은 한 덩어리라 0건 → **그래서 지금껏 안 보였다**.)
  🔴 표시만의 문제가 아니다 — `detectBrandMention`·`estimateShareOfVoice` 가 정제된
  텍스트로 판정하므로 **브랜드가 실려 있는 문서를 미언급으로 오판**해 **점수를 낮추고 있었다**
  (= daum 언급률이 유독 낮던 원인 중 하나).
  → 맞붙은 태그 경계(`</b><b>`)만 먼저 제거. **공백 치환 자체는 유지**(`A<br>B`→`AB` 뭉침 방지).
  ⚠️ 이건 **의도적으로 점수를 고치는 변경**이다(위 daum 항목의 "점수 무변경"과 별개).
  ⬜ **다음 진단부터 daum 언급률이 오를 수 있다** — 오르면 정상(측정이 정확해진 것).

- [x] ✅ 🔴🔴 **[최중대] 측정 0건인데 "월 800세션 손실" 단언하던 것 차단** — **완료**(`db9fd02`, 배포·검증)
  🔬**라이브 화면 실측으로 발견**: `apple.com` 은 엔진 **28개 전멸**인데 `completed` 로 저장돼
  *"0/0 · 0개가 애플를 알고 있습니다"* 아래에 **"놓치는 유입 800세션/월"** 을 단언했다.
  못 잰 걸 **0점**이라 부른 것(=체온계 안 켜졌는데 "체온 0도"). 해당 **7건**.
  → 규칙 **"잰 것만 말한다"**: 성공0=점수·손실 전부 숨기고 *"0점이 아니라 아직 모른다는 뜻"*,
    일부성공=**분모 표기**(*"AI 28곳 중 12곳에서 측정"*). **임계값 없음**(근거 없는 경계선 금지).
  ⚠️ 내가 초안에서 *"40% 미만 경고"* 를 제안했다가 **사용자 지적으로 철회** — 그 숫자는 근거가 없었다.
  📕단일진실=`packages/audit/measurement-coverage.ts`(화면·테스트가 같은 모듈. 복제 금지).
  ✅배포 후 실측: apple·nike=측정실패 안내 · SK하이닉스(29/29)=**정상 그대로**(과잉차단 0).

- [x] ✅ 🔬 **에러 301건 전수 분류** — **완료**(세션N-14). 코드 결함으로 계속 터지는 건 **없다**.
  | 원인 | 건수 | 판정 |
  |---|---|---|
  | 🔴 크레딧 계열(잔액·카드·무료제한) | **169건(56%)** | 👤 충전하면 해소 |
  | 🔴 5개 엔진 동시 인증실패 | 20건 | ✅ **7/29 1회성**(env parity 사고) |
  | 🔴 Gemini 모델명 오류 | 8건 | ✅ **이미 수정됨**(`gemini-2.5-flash`) |
  | 🟡 WebSocket/CDP 타임아웃 | 18건 | 🟡 Stagehand 베타(기본 제외) |
  | 🟢 검색결과 없음·브리핑 미노출 | 40건 | ✅ **정상 동작**(에러 아님) |
  ✅**최근 2회는 완전 정상**(8/1 SK하이닉스 29/29 · 8/3 claude.ai 22/22).

- [x] ✅ 🔴 **AI Gateway 크레딧 = 차단 요인이 아니다** — **세션N-16 실측으로 정정**
  ⚠️**투두·인계문서가 둘 다 틀렸다.** *"크레딧이 -$0.11 이라 새 진단이 안 돈다"* 는 **사실이 아니다.**
  👤사용자 지적(*"저번에 엔진별 개인 키 붙이지 않았나"*)이 맞았다.
  🔬**코드 실측**(`packages/ai/lib/engines/global-adapters.ts:151-185`) — Vercel Gateway 는 **폴백**일 뿐:
  | 엔진 | 실제 경로 | Vercel 크레딧 |
  |---|---|---|
  | chatgpt·claude | **Letsur**(`LETSUR_API_KEY`) | ❌ 안 씀 |
  | gemini | **구글 무료티어**(`GOOGLE_API_KEY`, 1,500회/일) | ❌ 안 씀 |
  | perplexity | **퍼플렉시티 공식 API**(`PERPLEXITY_API_KEY`) | ❌ 안 씀 |
  | 한국 3종 | 각자 개인 키(클로바·네이버·카카오) | ❌ 안 씀 |
  ✅프로덕션 env 에 **7개 키 전부 등록**(11~12일 전) · 🔬**DB 회차별 실측**으로 확정:
  크레딧 에러는 **7/29 이전에만** 발생(7/27 16건·7/29 16건×4회차) → **7/30 이후 전 회차 0건**
  (7/30 nike 34/35 · 8/1 SK하이닉스 **29/29** · 8/3 claude.ai **22/22**).
  🔴 즉 *"에러 301건 중 169건"* 은 **전부 과거 기록**인데 현재형으로 잘못 읽은 것이다.
  → **새 진단은 지금도 돈다.** 단 원가는 여전히 **150~300원/건**이라 💰**승인 후 실행** 방침은 유지.

- [x] ✅ 🖥️ **화면 텍스트 실물 확인** — **완료**(세션N-14) · 🔴**투두 기재가 틀렸음을 여기서 잡았다**
  무료 진단 결과 화면 **10개**를 라이브 캡처해 검사(`/tmp/findable-audit-shots/`).
  🔴**발견**: 투두의 *"오염 195건 → 0건"* 은 **저장 시점만** 고친 것이라, 그 전에 저장된
  회차는 그대로였다 — **5개 중 4개 화면에 `&#39;` `&#34;` 가 실제로 보이고 있었다**
  (SK하이닉스·기아·무신사·claude.ai). DB 실측 `AuditJob` **49건** 오염 잔존.
  → `stripMarkdown`(표시 직전)에서 디코딩(`930cc2e`). **저장 데이터 무변경**.
  ✅**배포 후 재검증: 10/10 화면 오염 0건.** 404 2종은 무해(구글 파비콘·Analytics 미설정).

- [x] ✅ 🟡 **[종결] 저장된 옛 회차의 daum 무관 문서 — 🔴필터를 만들면 안 된다**(세션N-16 실측)
  ⚠️**착수 직전 실측으로 "만들지 말 것" 판정.** 그대로 만들었으면 사고가 났다.
  🔬**전수 측정**(40회차·daum 블록 1,186개): 무관 **786개(66%)** · 필터 적용 시
  **근거가 0개가 되는 응답 77건**. N-14 가 겪은 함정과 같은 패턴이라 **눈으로 깠더니**
  이번엔 숫자가 진짜였다 — SK하이닉스 진단에 *"말왕-나무위키"·"Apple TV 4K"·"65-inch TVs"* 가 통째로 들어와 있다.
  🔴**결정적**: 그 전멸 응답들은 **이미 `brandMentioned=false`·`sov=0`** 으로 판정돼 있다
  (= 점수는 이미 정확하다. 새 필터가 고칠 것이 없다).
  🔴**그리고 화면에 렌더되지도 않는다** — `dedupeByEngine` 이 **엔진당 1장**만 쓰기 때문.
  🔬**라이브 문자열 검사로 확정**: `말왕`·`Apple TV 4K`·`65-inch` = **화면에 없음**.
  화면에 보이는 무관 블록은 **13개 중 2개(`바이브 코딩`·`남자 패션`) = 15%** 뿐이며
  이는 N-14 자기수정치(15%)와 **일치**한다.
  → **표시 필터 신설은 폐기.** 만들었으면 **멀쩡한 근거를 가진 77건을 빈 카드로** 만들 뻔했다.
  ⚠️🔬**규모 정정(세션N-14 자기수정)**: 처음 적은 **"283건 중 201건(71%)"** 은
  **분석 스크립트 버그**였다 — 블록 분리 정규식이 안 먹어 **13개 블록을 1덩어리로** 셌다.
  제대로 나눠 보니 SK하이닉스는 **13블록 중 11개에 브랜드가 실재**하고 무관은 2개
  (`바이브 코딩`·`남자 패션`)뿐. **실제 오염은 블록 단위 15% 수준**으로 추정.
  🔴 그리고 그 잘못된 기준으로 필터를 시뮬레이션했더니 **279건 중 120건(43%)이 근거 전멸**
  로 나왔다 — **그대로 만들었으면 멀쩡한 근거 11개를 지울 뻔했다**(시뮬레이션으로 차단).
  ⬜**착수 전 반드시 블록 단위로 재측정할 것.** 라이브 화면에서 눈으로 확인한 실제 사례:
  · **기아** → *"[1] 말왕 - 나무위키"*, *"[1] 현대자동차 - 나무위키"*, 그리고
    **"푸에기아, 아무아쥬 향 추천해줘"**(향수 갤러리 글이 `인용됨` 배지를 달고 표시)
  · SK하이닉스 → *"Apple TV 4K"*, *"65-inch TVs"* · claude.ai → *"소피 마르소 - 나무위키"*
  ⚠️ `9632fcc` 는 **새 진단부터** 적용된다 — 이미 저장된 회차엔 닿지 않는다.
  🔴**소급 데이터 수정은 금지**(점수·시계열)이므로, 해법은 **표시 직전 필터**여야 한다
  (`stripMarkdown` 에서 엔티티를 푼 것과 같은 방식). ⬜**다음 세션 0순위 후보.**

- [x] ✅ 🖥️ **경쟁사 화면 구성 대조** — **완료**(세션N-14) · 📕`UIUX_경쟁사_화면대조_2026-08-10.md`
  결론: **업계 표준(히어로 4지표·질문형 라벨)은 이미 따라가 있었다.** 안 보였던 이유는
  ①측정 데이터가 없으면 빈 화면만 뜸 ②화면이 실제로 틀린 말을 하고 있었음(오늘 5건 수정).
  🔴**대조하다 발견**: 유료 축 3개가 **전부 껍데기**(아래 신규 항목).

---

## 🎨 P1.6 — UI/UX 대개선 (세션N-14 신규, 리서치 3트랙 완료)

> 🧭 **단일 진실 = `UIUX_대개선_설계_v3_2026-08-10.md`** ⭐ **이것부터 읽을 것**
> ⛔ **v2 는 폐기**(근거만 유효) — 예전 이 줄이 v2 를 가리키고 있었다(세션N-19 정정).
> 📕 근거 원본 **7개** = `_리서치원본_UIUX_2026-08-06/`(06·07번이 2026-08-10 추가분)
> ⚠️ **`UIUX_대개선_기획서_2026-08-06.md`(v1)는 5건이 낡았다** — 상단 경고표 참조.
> ⚠️ **재리서치 금지**(A트랙 검색 200회 + B·C트랙 2회 소진).

### 지금 착수 가능 (원가 0 · 새 진단 불필요)
- [x] ✅ 🖥️ **A. 빈 상태를 제품으로** — **완료·배포·라이브 검증**(세션N-16, A1 `b6dd63b` · A2 `6c074b6`)
  - ✅**A1 이어붙이기**: 🔬대시보드는 **이미 이메일로 무료진단을 매칭**한다(`page.tsx:53`).
    즉 **다른 주소로 가입하면 결과가 조용히 사라진다.** 화면이 안 알려줘서 사용자는
    "내 진단이 없어졌다"가 아니라 **"원래 빈 서비스"** 로 읽었다 → 안내 + 로그인 주소 표기.
  - ✅**A2 샘플 리포트**: **실제 측정 회차**(SK하이닉스 29/29·처방 5건)를 `?shared=1` 로 링크.
    🔒공유뷰·본인뷰 **둘 다 신청자 이메일 숨김** 라이브 확인(푸터 사업자메일은 법정 표기라 정상).
  - 🔬**"로딩 중 빈 상태 금지" 는 코드 불필요로 판정** — 대시보드는 `"use client"` 없는
    **async 서버 컴포넌트**라 await 후 HTML 이 나간다 → **구조적으로 뜰 수 없다.**
    없는 버그를 막는 코드를 넣지 않았다.
  - ⬜**자기 소멸**(Metabase)은 **이미 구현돼 있다** — `jobs.length>0` 이면 빈 상태 자체가 안 뜬다.

- [x] ✅🔴 🖥️ **PDF 3개월 실패 — 🎉복구 확정**(세션N-17 수정 `0e79b29` → **세션N-18 실물 확인 완료**)
  ✅**2026-08-11 실측 종결**: 측정 5건(anua·medicube·tonymoly·innisfree·apple) **전부 `pdfUrl` 생성**.
  기준선이 "직전 6건 전부 null·마지막 성공 5/3" 이었으므로 **5/5 생성 = 복구 성공**. 이 항목 종결.
  🔴**아래 "새벽 2시 크론" 기재는 틀렸었다** — cron 은 **UTC** 라 `0 2 * * *` 가 **오전 11시 KST** 였다.
    그래서 확인이 계속 미뤄졌던 것(고장이 아니라 그 시각이 안 온 것). 세션N-18 이 UTC 17시(=KST 2시)로 교정.
  🔬**원가 0원으로 확증**(진단 실행 불필요 — 빌드 산출물 nft 실측):
  `@sparticuz/chromium` 은 바이너리를 `../bin/chromium.br` 처럼 **런타임에 문자열로 조립**해 찾는다.
  Next 추적기(@vercel/nft)는 **정적 분석**이라 그 경로를 못 본다 → **바이너리가 배포본에서 통째로 누락**.
  실측: **web 37개·app 27개 함수 중 `chromium.br` 포함 = 0개**. 함수는 뜨지만 실행파일이 없어
  launch 가 실패하고 `runner.ts` 의 `try/catch` 가 조용히 삼켰다(3개월간 무증상).
  ✅**수정**: 각 앱 `next.config.ts` 에 `outputFileTracingIncludes` → web 0→포함 · **app 0/27→25/27** ·
  최대 함수 **157MB / 한도 250MB**. tsc 0 · lint 신규 0 · build 양쪽 통과 · **로직 무수정**.
  ✅**web·app 프로덕션 배포 완료**(22:22·22:3x · project.json 원복 확인).
  🔴**기존 가설 2개 다 틀렸다(되살리지 말 것)**:
  | 가설 | 판정 |
  |---|---|
  | *"66MB > Vercel 50MB 한도"* | ❌ **한도는 비압축 250MB**(공식문서 실측). `pdf-generator.ts` 주석의 "50MB" **오기재가 3개월 오진의 출발점**이었다 → 주석 정정함 |
  | `serverExternalPackages` 누락 | ❌ **no-op** — chromium·puppeteer-core 는 Next 가 **이미 자동 opt-out** 하는 목록에 있다 |
  🔴⬜ **남은 것 = 실물 PDF 확인. 🚧내 자동화로는 불가함이 실측으로 확정됐다**:
  `/api/audit` 는 **BotID 가 첫 관문**이라 Playwright 는 **headless·실제 Chrome 채널 둘 다 403**
  (`자동화된 요청으로 확인되어 차단되었습니다`). `tier==="admin"` 우회는 **BotID 뒤에 있어 못 넘는다**.
  ⚠️**BotID 를 끄거나 우회하지 말 것** — 어뷰징 방어 4층의 1층이고, 지금 정상 작동 중이다.
  ✅**확인 경로 2개(둘 다 추가 코드 불필요)**:
  ① 👤**사람이 브라우저로** `www.findable.co.kr/ko/audit` 에 진단 1건 제출(원가 150~300원)
  ② 🟢**원가 0원** — 매일 **새벽 2시 자동 재측정 크론**(`/api/cron/auto-refresh-tracking`)이
     돌면 그 경로에도 바이너리를 넣었으므로 PDF 가 생성된다. **다음 새벽 이후 DB 확인**.
  🔬**확인 쿼리**: `AuditJob.pdfUrl` 이 **null 이 아니면 복구 성공**
  (현재 기준선: 최근 완료 6건 전부 `pdfUrl=null` · 마지막 성공 2026-05-03).

- [x] ~~🔴 🖥️ **[신규 발견] PDF 리포트가 3개월째 100% 실패 중 — 🔴원인 미확증**~~ ↑**위로 이관**
  🔬실측: **마지막 PDF = 2026-05-03**(olive.co.kr). 이후 **전량 실패**(완료 71건 중 PDF 13건, 전부 5/3 이전).
  코드는 매 진단마다 호출되지만 `try/catch` 로 **조용히 삼켜진다**(`runner.ts:452`).
  🔴**리서치상 이건 리텐션 핵심이다** — *"Day 30 종착점은 대시보드가 아니라 내보내기·보고"*,
  ***"보고 가능성(shareability)이 리텐션의 핵심"***. PDF 없는 Otterly·Rankscale 은 **리뷰에서 감점**.
  ⚠️**내 가설 2개가 실측으로 반박됐다 — 되풀이하지 말 것**:
  | 가설 | 반박 근거 |
  |---|---|
  | `app` 에 `BLOB_READ_WRITE_TOKEN` 없어서 | 🔴 토큰이 **있는** `web` 실행분도 똑같이 실패 |
  | 300초 타임아웃 초과 | 🔴 최근 40건 **평균 19초·최대 54초**(250초↑ 0건) |
  🟡**남은 유력 후보**(미확증): `@sparticuz/chromium` **66MB** vs Vercel 함수 **50MB 한도**
  (바이너리 폴더 날짜가 **5/2** = 실패 시작 하루 전). 단 **확증 못 함**.
  ⬜**확증 방법 = 새 진단 1건 실행해 `audit.pdf.failed` 로그 실시간 확인** → 💰**150~300원 승인 필요**.
  ✅**대체 수단은 이미 있다**: 공유 링크(`?shared=1`·개인정보 보호 검증됨) + CSV(`/api/export/tracking.csv`)
  → 리서치 3대 내보내기 수단 중 **2개는 작동 중**이라 급하진 않다.

- [x] ✅ 🔴 🖥️ **잠금 화면의 가짜 예시 데이터** — **제거 완료·배포·라이브 검증**(세션N-16 `4292055`)
  `compare` **42%·31%·18%** · `sources` **blog.naver.com 47/내 도메인 9/namu.wiki 4** =
  **실재하지 않는 수치**를 막대·표로 그리고 있었다. *"(예시)"* 라고 적어도 **화면은 숫자를 사실로 읽힌다.**
  → 정직한 설명문 + **실제 측정 회차 링크**(A2 와 동일 방식)로 교체.
  🔴**라이브에서만 잡힌 결함 2건**(코드만 봤으면 놓쳤다):
  ①**프리뷰 안 링크는 눌리지 않는다**(`pointer-events-none`+`aria-hidden`) → `sampleUrl` prop 신설해 카드 안으로
  ②**카드 상단이 잘리고 본문이 화면 밖으로 밀렸다** — 오버레이가 `absolute inset-0` 인데 프리뷰가 짧아져 무너짐
  → **겹치기 폐기, 쌓는 흐름**으로 전환. 🎓**"스크린샷으로 눈 확인" 규칙이 또 값을 했다.**
  ⚠️`alerts` 는 **손대지 않았다** — 거긴 지어낸 수치가 아니라 **설정 항목 설명**이라 성격이 다르고,
  *"없는 기능을 판다"* 는 문제는 👤**가격 정책 결정과 함께** 다뤄야 한다.
- [x] ✅ 🖥️ **B. 응답 원문 신뢰 3종** — **완료·배포·라이브 검증**(세션N-16 `f6de40a`)
  🔬**착수 전 감사에서 범위가 1/3 로 줄었다** — 3종 중 **2개는 이미 있었고 리서치 기준보다 낫다**:
  | 항목 | 실측 |
  |---|---|
  | ②출처 You/Other 분리 | ✅**이미 있음** — `classifySource` 가 **owned/media/community/other 4단**(Semrush 2분류보다 세분화) |
  | ③미언급 태그 | ✅**이미 있음** — `StatusPill` 3상태(인용됨/당신을 모름/오류) + 정렬 가중치 |
  | ①브랜드 하이라이트 | 🔴**없었음 → 구현** |
  → **원문이 길어 "어디에 우리가 나오는지" 눈으로 못 찾던 것**을 해결. 라이브 `<mark>` 11개·오탐 0.
  🔒**XSS 0**: `dangerouslySetInnerHTML` 미사용, **React 노드 배열**로 자른다.
  ⚠️정규식 이스케이프 필수(`claude.ai` 의 `.`) · **1글자 브랜드는 제외**(조사 오탐) ·
  🔴**`brand`(빈값이면 "당신의 브랜드") 말고 `brandName` 원본을 넘길 것** — 안 그러면 거짓 형광.
- [x] ✅ 🖥️ **C. 처방 전면 공개** — **완료·배포·라이브 검증**(세션N-16 `12a00de`)
  ⭐**리뷰어 전원이 "처방이 카테고리 공백"이라 지적** = 우리 유일한 구조적 우위인데
  🔴**우리가 80%를 잠가두고 있었다** — 생성 **5건** / 무료 화면 노출 **1건**.
  🔬DB 실측: 처방 **10건 전수 100% 가 `how`(구체 실행방법)를 이미 갖고 있다**(Princeton 논문 근거 포함).
  → **잠금 제거.** 1건 펼침 + 4건 `<details>` 접힘(밀도는 접기로 관리 · 클릭 한 번이면 열림).
  `<details>` 채택 = JS 상태 불필요 + **Ctrl+F·스크린리더가 접힌 내용도 찾는다**.
  🔒**유료 축 무사**: 가치역전 원인은 *"무료가 **반복 이유**까지 주는 것"*인데
  **처방은 1회성 aha 라 반복 이유가 아니다**(반복 이유=시간·비교·알림은 그대로 유료).
  🔴**부수 수정**: `how`·`evidence` 의 마크다운 `**` 가 **글자로 보이고 있었다**(기존 lead 카드도 동일)
  → `stripMarkdown` 적용. ✅tsc 0 · lint 신규 0(기준선 28→28) · 라이브 별표 0건.
  ⬜**남은 것 = IA 분리**(Scrunch `Monitoring`/`Insights` · Gauge Monitor→Analyze→Act)는
  대시보드 구조 작업이라 별도. 지금은 **무료 진단 화면에서 처방이 다 보인다**.
- [x] ✅ 🖥️ **D. 무료진단 밀도 축소** — **완료·배포·라이브 검증**(세션N-16)
  ⚠️**문서의 "15섹션" 은 틀렸다** — 🔬라이브 실측 **큰 블록 10개** · 파일 **3,812줄**(3,696 아님).
  🔬**진짜 범인을 실측으로 특정**: 「진실의 거울」 **1개 섹션이 1,409px = 전체의 20%**
  (엔진 11개 원문 카드를 전부 펼침). 히어로·처방은 멀쩡했다.
  → **기본 3장만 펼치고 나머지는 「AI N곳 더 보기」로 접기.** ⚠️삭제·요약 아닌 **접기**
  (원문은 진실거울의 증거라 없애면 제품 핵심이 사라진다 — 기존 코드 주석의 경고를 따름).
  🔴**설계 함정을 실측으로 회피**: 정렬이 `인용됨→모름→오류` 라 **단순히 앞 3장을 자르면
  "모름" 카드가 통째로 접힌다.** 모름 = **GEO 개선 1순위이자 우리가 파는 것**.
  실측 12건 중 `claude.ai`(모름 1장)·`tesla.com`(모름 2장)이 **전부 앞3장 밖**이었다.
  → `pickInitialCards()` 가 **모름 카드를 최소 1장 보장**한다.
  ✅**라이브 검증**: 진실거울 **1,409px→676px** · 전체 **7,167px→6,434px** ·
  claude.ai 접힌 상태 3장에 **HyperCLOVA X "당신을 모름" 이 실제로 보임**(스크린샷 확인).
  ✅tsc 0 · **lint 신규 부채 0**(기준선 1건 그대로 · 중첩삼항은 `expandLabel()` 로 분리) · build 통과.

### 👤 사용자 결정 대기
- [x] ✅ 🔴 **잠긴 유료 3종 처리** — **세션N-17 감사로 전제가 뒤집혔다**(`963ccc6`)
  🔴 **투두 기재가 틀렸다**: *"Growth 결제해도 열리는 게 사실상 없다"* 는 **사실이 아니었다.**
  🔬코드 실측 — 3종의 상태가 **서로 다르다**:
  | 화면 | 유료 결제 시 실제 |
  |---|---|
  | `compare` | ✅ **작동** — `buildCompetitorAnalysis` 실계산 + 측정 없으면 *"측정 시작하기"* 안내 |
  | `sources` | ✅ **작동** — `buildSourcesAnalysis` 실계산 |
  | `alerts` | 🔴 **유일한 껍데기** — 유료여도 *"준비하고 있어요"* |
  그리고 `compare` 에는 이미 *"숫자는 측정을 해야 나오기 때문에 미리 보여드리지 않아요"* ·
  *"**「준비 중」이 아니라 다음 행동을 준다**"* 가 **구현돼 있었다** — "거짓 잠금→정직한 안내" 는 이미 끝난 일.
  ⭐**그래서 진짜 결함은 반대 방향이었다 = "있는 기능을 안 팔고 있었다"**:
  `planCapabilities` 가 단일 진실이고 cron 이 실제로 재측정하는데(starter=주간·growth/scale=매일),
  요금제 표에는 **Scale 에만** *"일간 자동 갱신"* 이 적혀 **Starter·Growth 의 자동 재측정이 통째로 누락**.
  → Growth(청구 429,000원) 결제자가 **매일 갱신을 상위 플랜 전용으로 오해**하던 표기 결함.
  ✅**수정·배포**: KO·EN·app 3개 표에 *"주간/매일 자동 재측정"* 추가 · Scale 중복 표기 제거 ·
  `alerts` 는 **켜진 것(재측정)과 아직 아닌 것(알림 메일)을 분리**해 말하고 이력 링크 추가 ·
  프리뷰의 **지어낸 수치 3건 제거**(*"매주 월요일 09:00"·"5%p 이상 하락"·"즉시 알림"*).
  ✅**라이브 검증 5/5**(www 요금제: 신규문구 2건 있음·Scale 중복 없음·준비중 유지·VAT 문구 유지).
  ⚠️**"(준비 중)" 3건은 그대로 유지** — 실측상 **전부 정직한 표기**다(이메일 알림=프로덕션 env
  `FINDABLE_ENABLE_DIGEST_EMAIL` **미설정** · Notion Export 미구현 · "주간 자동 **리포트**"의
  메일도 미발송). 🔴**재측정과 리포트(메일)는 다른 기능** — 딱지를 떼면 없는 걸 파는 것이 된다.
  ⚠️**가격·VAT 무수정** — `listKrw`(세전)/`amountKrw`(VAT포함) 분리가 이미 정확
  (실청구 **108,900 / 429,000 / 1,089,000**).
  ⬜**남은 것 = `alerts` 실제 알림 구현**: 🔴**지금 만들면 안 된다** — 임계값을 정할 **분포가 없다**
  (시계열 1일). 근거 없는 경계선 발명 금지 규칙에 걸린다. **측정 5~6회 쌓인 뒤** P3 와 함께.

### 🔴 하지 말 것 (표본 부족)
- ❌ **"업계 N분위" 벤치마크 표시** — 조직 1개라 **비교 표본이 없다**. 표본 없이 분위를 쓰면 **숫자 조작**이고,
  오늘 고친 *"측정 0건인데 손실 800세션"* 과 **같은 잘못**이다. → 나쁜 점수는 **방향·기회 언어**로만.

---

## 🟡 P2 — 코드 (지금 착수 가능한 유일한 항목)

- [~] 🖥️ **`audit-result.tsx` lint 부채 — 🔬N-13 재실측 27건**(문서의 "20건"은 진단 상한 착시였다)
  `--max-diagnostics=200` 기준 규칙별 정확한 기준선:
  | 규칙 | 건수 | 판정 |
  |---|---|---|
  | `noNestedTernary` | **17** | 🟡 JSX 조건부 렌더 패턴. 위험 > 이득 |
  | `noExcessiveCognitiveComplexity` | 4 | 🟡 동상 |
  | `noVoid` | 2 | 🟡 동상 |
  | `noAlert` (813행) | 1 | ❌**고치지 말 것** — 아래 참조 |
  | `noImgElement`+`useImageSize` (3194행) | 2 | ❌**고치지 말 것** — 아래 참조 |
  | `noArrayIndexKey` (2752행) | 1 | 🟡 정적 배열(`findings`)이라 무해 |

  🔴 **N-13 판정: 4건은 "고치면 오히려 나빠진다"** (실측으로 확인)
  - `noAlert`(813) = 카카오톡 공유 클립보드 폴백 안내. **`apps/web` 에 `Toaster` 가
    마운트돼 있지 않다**(grep 0건) → toast 로 바꾸면 **알림이 아무것도 안 뜬다**.
    고치려면 `Toaster` 마운트가 선행이라 lint 정리 범위를 벗어난다.
  - `noImgElement`(3194) = **구글 파비콘 외부 URL**(`google.com/s2/favicons`).
    `next/image` 로 감싸면 외부 이미지를 최적화 서버에 태워 **비용↑·느려진다**. 지금이 맞다.
  - `useImageSize`(3194) = `h-3.5 w-3.5` 클래스로 크기 고정돼 **CLS 없음**.

  ⚠️**3,696줄**이라 나머지 23건도 지금 건드리면 결과페이지(제품 핵심 화면) 회귀 위험 > 이득.
  → **이 파일을 크게 손댈 때 함께** 정리 권고(= 기존 판단 유지).
  ✅a11y 3건은 N-11 이 해소(`113eb8b`) + lint 가 못 잡던 **`tabular-numser` 오타** 수정.
  ⚠️**총계 착시 주의**: Biome 는 **파일당 진단 상한**이 있어 앞 항목을 고치면 뒤가 드러난다
  → 증감은 **규칙별·행별로 `git show HEAD:` 기준선과 대조**할 것.

---

## ⬜ 확인 대기 (코드로 앞당길 수 없음)

- [ ] 🖥️ **crew 자기평가 실물 로그 확인** — 배포(8/9) 이후 **crew 실행 0건**(최신 8/1)이라
  `refineApplied=true` 가 아직 안 찍혔다. **새 진단 1건이 돌면 자동 확인**된다.
  ```
  로그: audit.crew.completed → refineAttempted / refineApplied / refineViolations / refineDurationMs
  DB : AuditJob.crewResult.refinement
  ```
  ✅**세션N-13 교차검증 완료 — 기능·안전성은 이미 검증됨**(커밋 `70e37bb`).
  프로덕션 **31건**(N-12 는 18건)을 `critiqueStrategist` 로직 그대로 재현:
  위반율 `executiveSummary` **31/31=100%·347자** · `whyThisOne` **31/31·234자** — N-12 와 **전건 일치**.
  배수별 발동률 1.5=**100%**·2.0=**81%**·**2.5=32%**(채택)·3.0=**13%** → 채택값 근거 확인.
  🔬**시간 위험 0/31**: crew total 최대는 130초가 아니라 **226초**(문서 정정)지만 그 회차는
  재작성 **비대상**이고, 발동 10건 중 최악이 `181+45=226초` < `CREW_TIMEOUT_MS` **270초**.
  ⚠️**실측 경로 함정**: `crewResult.strategist.`**`output`**`.executiveSummary` 다.
  `strategist.executiveSummary` 로 읽으면 **전건 `null`** → "필드가 없다"고 오진한다(N-13 이 겪음).

---

## 🟢 P3 — 데이터가 쌓여야 의미가 생기는 것

> 🔴 지금 만들면 **"작동 안 하는 버튼"** 이 된다.
> 공통 근거: `Tracking` **54행 · 측정 2회 · 시계열 1일 · 조직 1개**(실측).
> ⭐**측정 5~6회 쌓이면 셋을 한 묶음으로** 착수.

> ⭐ **UI/UX 개선은 여기 3건 빼고 전부 끝났다**(🔬N-13 실측, 2026-08-10).
> 세션N-9 가 D5·D6·D8·D9·D10·D11 + **질문별 성적**(벤치마크 최대 공백 8/15) + CSV 를
> 이미 구현·배포했는데 `UIUX_감사결과_3중` 문서가 8/7 에서 멈춰 "⬜ 남음"으로 적혀 있었다.
> → 그 문서 상단에 실측표를 박아 정정했다. **그 목록을 그대로 믿으면 만든 걸 또 만든다.**

- [ ] 🖥️ **D4 기간 선택** — 🔬실측: `sov-trend-chart.tsx` 에 기간 상수 **0건**(미구현 확정).
  시계열이 **1일**이라 칩을 만들어도 **누를 게 없다**
- [ ] 🖥️ **D2 자동 이벤트핀** — 델타가 `[0,0,0,0,+97]` 이고 +97 은 측정실패→복구라
  Sistrix 규격(15.5%p)을 적용하면 **핀이 측정 실패 지점에만 꽂힌다**
- [ ] 🖥️ **D1 이상감지 알림 UI** — 🔬실측: `alerts/page.tsx:51` **"준비하고 있어요"** 그대로
  (76줄 껍데기). 임계값을 정할 **분포 자체가 없다**. D2 와 같은 임계 계산을 공유하므로
  **함께 하면 원가 절반**
- [ ] 🖥️ **재측정 알림 메일 실제 발송** — 코드는 완료(`0f1cf93`)·**기본 꺼짐**.
  켜기 = `FINDABLE_ENABLE_DIGEST_EMAIL=1`. 👤사용자가 **메일 본문을 눈으로 확인한 뒤** 켤 것.

---

## 🔵 P4 — 👤 정책 결정이 관문 (코드는 작거나 없음)

- [~] 🖥️ **엔진 수 게이팅** — 📕`엔진수_게이팅_판단재료_2026-08-08.md`
  ⭐**권고 = 레버로 쓰지 말 것**(실측 3건이 반대):
  ①원가 논리 불성립(비싼 건 claude+chatgpt **92%**, hyperclova 0.30원·gemini/daum 0원)
  ②**무료에서 한국 엔진 빼면 차별점 소멸** ③언급률 대부분 100%(naver 8/8, daum만 56%)라
  상위 티어를 **우리 데이터로 정당화 못 함**. 굳이 쓴다면 **뺄셈 아닌 덧셈**(growth+ 에 `BETA_ENGINES` 해금).
  ✅갱신 주기는 **이미 구현됨**(`autoRefreshHours`) · 프롬프트·브랜드 수도 이미 게이팅됨.
  ⚠️"스키마+러너 큰 작업"이라던 기재는 **과대평가** — `queryAllEngines(base, engineIds)` 가
  이미 목록을 인자로 받는다(`packages/ai/lib/engines/index.ts:92`). 진짜 제약은 **시간**(최대 298초/상한 300초).
- [ ] 👤 **파트너 특전 정책 확정** — 초기="Growth 무료+배지". 영구 할인 lock-in·상향 쿼터는
  결제 붙은 뒤. ✅배지 컴포넌트는 구현됨(`partner-badge.tsx`).
- [ ] 👤 **KIPO 상표 출원 (Findable/파인더블)** — Class 42(SW)·35(광고). 한국 **선출원주의**라 조기 유리.
  ⚠️`findableapp.com`(오스트리아 동일 이름 GEO) 글로벌 충돌 — 국내부터 선점.

---

## ⚪ P5 — 범위 큰 것 (데모데이 10월 이후 권고)

- [ ] 🖥️ **GEO 실행 에이전트** — 진단→리라이트→발행 준비. 경쟁사 6곳 전원 공백 = **최대 차별화**.
  ⚠️**자사 통제 소스만**(JSON-LD·자사 FAQ·구글비즈·위키데이터) + 인간 승인.
  ❌**나무위키 직접 편집 금지**(표시광고법).
- [ ] 🖥️ **RAG 파이프라인 4종** *(Day12 설계됨, 미구현 — 🔬실측: 스키마에 모델 없음)*
  - `Document`/`DocumentChunk`/`Embedding` 테이블 + pgvector (⚠️라이브 스키마 변경 고위험)
  - `scopedVectorSearch` raw SQL org 필터 (⚠️Extension 자동주입이 안 걸려 **손 주입 필수**)
  - `indexDocument` 인덱싱 파이프라인 (문서→청킹→임베딩→저장)
  - 임베딩 모델 small→large 확정 + 렛서 baseURL (차원·비용 결정)
- [ ] 🖥️ **durable 실행 전환(Vercel Workflow)** — crew 300초 타임아웃 위험 → WDK step crash-safe.
  ⚠️큰 변경. 🔗위 "시간이 진짜 제약"과 같은 뿌리. 🔬N-13 실측 최대 **226초**로 여유가 44초뿐.
- [ ] 🖥️ **Prisma Client Extension org where 자동주입** — RLS 준하는 강제력.
  ⚠️전체 쿼리 영향이라 고위험. 설계 확정 후.

---

## 🔴 만들지 말 것 (실측으로 판정 완료 — 되살리지 말 것)

- ❌🔴 **cron 가드에 `x-vercel-cron` 폴백 부활** (세션N-18) — 그 헤더는 **누구나 curl 로 붙인다**.
  예전 코드가 `CRON_SECRET` 없으면 그 헤더로 통과시켰고, 프로덕션에 시크릿이 **없어서**
  외부인이 **유료 측정을 무제한 실행**시킬 수 있었다(실측 증명: 측정 5건 실행됨).
  Vercel 공식 인증 수단은 **`CRON_SECRET` Bearer 뿐**이다.
  → 가드는 `packages/security/cron.ts` **한 곳**만 쓴다(fail closed).
- ❌ **`vercel.json` 에 주석용 커스텀 키**(`_주석` 등) — 스키마가 `additionalProperties: false` 라
  **거부된다**. 설명은 라우트 파일 주석에 둘 것.
- ❌ **cron schedule 을 KST 로 착각한 채 수정** — **항상 UTC**(KST=UTC+9). Hobby 는 **하루 1회만**
  (더 잦은 식은 **배포 실패**) · 정밀도 **±59분**(정시 도착 전제 로직 금지).
- ❌ **"팩트정합률 전면화"** — 세션K가 **이미 폐기**하고 "브랜드 인지율"로 개명했다.
  이유: **사실 검증을 안 하는데 이름이 사실 검증으로 읽혔다**(기아 측정에서 **향수·야구단**이
  언급으로 잡혀도 "팩트정합률 100%"). 전면화 = **결함으로 회귀**.
  "빨간 밑줄 사실오류 판정"을 넣지 않은 것도 **명시적 전략 판정**("진단까지가 안전선").
- ❌ **before/after 케이스 1건 (코드로)** — 🔬성립 조건 3개 중 2개 부재:
  ①시점 다른 측정 ✅(설화수 17건) ②**"무엇을 조치했다" 기록 = `ActionCompletion` 0건** 🔴 ③인과 🔴.
  🟡**세션N-13 진전**: ②의 **원인이 실측으로 밝혀지고 코드는 고쳐졌다**(아래 완료 항목).
  0건은 버그가 아니라 **입구가 좁아서**였다 — 무료 진단자는 완료를 저장할 곳이 없었다.
  이제 눌리면 쌓인다. 단 **여전히 실고객이 눌러야 하므로** 이 항목 자체는 아직 ❌.
  게다가 **있는 데이터는 반증에 가깝다** — 설화수 언급 16→27 상승분은 **측정자가 전량 사용자 본인**이고
  **우리 측정기 개선 시기와 겹친다**(5/2 stub 12건 · 7/27 에러 16건 회차).
  내놓으면 VC 에게 **정확히 반대로 읽힌다**.
  👤**실고객이 진단→조치→재측정을 거쳐야만 가능**(=P0 이 선행). 그때 `ActionCompletion.sovAtCompletion`·
  `recognitionAtCompletion` 컬럼이 **이미 준비돼 있어** 코드 추가는 거의 없다.
- ❌ **naver 어댑터 Daum식 정합(P0-c)** — Daum=**스니펫 raw** / naver=**HyperCLOVA 합성**이라
  구조가 다르다(P0-c 가 풀던 "사과-오렌지" 문제가 naver 엔 **없다**).
  실측 언급률 **naver 8/8=100%** · daum 만 56%. 적용하면 브랜드명 단독 검색을 섞어
  **없는 언급을 만든다**(감사 8번이 제거한 **가짜 1위 폴백과 같은 계열**).
- ❌ **진실거울(무료 aha)을 대시보드로 복사** — 유료 축(**시간·비교·알림**)을 안 바꾸는 오답.
- ❌ **`/en` 리다이렉트 "수정"** — 결함이 아니다. `urlMappingStrategy:"rewriteDefault"` 라
  기본 로케일 `en` 은 접두사 없이 서비스되고, 브라우저는 `Next-Locale` 쿠키로 정상 전환된다.
  (크롤러 문제는 hreflang·sitemap 으로 **이미 해소**.)
- ❌ **crew 자기평가에 zod `.max()` 강제** — 넣으면 검증 실패로 `output: null` → **위반율이
  100% 라 리포트가 전멸**한다(N-13 재확인: 31/31). 지금의 "감지 후 1회 압축, 실패 시 원본 유지"가
  **나빠질 수 없는 구조**다.

---

## ✅ 최근 완료 (다시 하지 말 것 — 상세는 `투두_마스터.md`)

**세션N-13 (2026-08-10)** — 커밋 9건, **app 배포됨**
- 🔴**화면 텍스트 오염 전수 해소**(`2b81f5d`) — 저장된 **1,779건** 감사 → 오염 **195건이 0건**.
  사용자 캡처 제보(네이버 브리핑에 HTML 코드 노출)에서 시작.
  근본 원인 = `stripHtml` 이 ①숫자형 엔티티를 **못 잡고**(868회 통과) ②**공백 치환**이라
  `It&#39;s`→`It s` 로 글자를 훼손하고 ③**순서가 역전**돼 있었다. + 네이버가 **고지문 문구를
  바꿨는데** 우리 목록이 안 따라감. → `packages/ai/lib/engines/sanitize.ts` 신설(단일 진실),
  4개 어댑터 전부 적용. ⚠️**이중 인코딩**(`&amp;#39;`) 실재 → 상한 3패스.
- 🔴**검증 도구 2종**(`10d1c57`) — `scripts/verify-app.py`(화면 자동 검증) ·
  `scripts/findable_admin.py`(Clerk API). **"직접 눌러보세요"를 없앴다.**
  만들자마자 결함 발견: 승인된 파트너가 폐기값 `plan=insider` 라 **특전을 못 받고 있었다** → 정정.
- 🔬**교차검증 2회로 결함 5건** — 전부 통상 검사 통과 상태였다.
  특히 **`"use server"` 는 동기 함수 export 불가** → tsc·lint·테스트 통과인데 **빌드만 실패**.

**세션N-13 (앞부분)** — 커밋 `70e37bb`·`bc48646`·`e311556`·`977cc15`
- 🔴**무료 진단자도 액션 완료 체크 가능**(`ActionCompletion` 0건의 진짜 원인 해소)
  · 원인은 버그가 아니라 **입구가 좁아서** — 처방은 보이는데 완료를 저장할 곳이 없었다
    (`ActionCompletion.brandId` 는 `Brand` FK 필수, 무료 진단자는 `Brand` 가 없다)
  · 이제 완료를 누르면 서버가 `domain` 으로 Brand 를 도출/생성해 연결한다
  · ⚠️**의도된 부수효과**: 첫 완료 시 `/brand` 목록에 등록된다(자동 측정은 시작 안 함)
- 🔬**교차검증 2회로 결함 5건**(전부 통상 검사를 통과한 상태였다):
  ①`"use server"` 파일이 **동기 함수 export 불가** → tsc·lint·테스트 통과인데 **빌드만 실패**
  (→ 순수함수를 `apps/app/lib/domain.ts` 로 분리) ②도메인 형식 검증 누락 ③브랜드 한도 우회
  ④**완료 시점 SoV 유실**(기능 목적 자체가 무력화 — `metrics.sov` 실재 확인해 연결)
  ⑤Brand 생성 후 `/brand` 미갱신(안내 문구와 화면 불일치)
- 🔴**도메인 정규화가 세 파일에 복제**돼 있던 것 발견 → `@/lib/domain` 단일화.
  실측 근거: `sulwhasoo.com` 과 `www.sulwhasoo.com` 이 **DB에 둘 다 존재**(안 고치면 Brand 가 갈라짐)
- **crew 자기평가 교차검증**: 표본 18→31건으로 N-12 수치 **전건 재현** · 시간 위험 **0/31**
- **문서 정정**: crew total 최대 130초→**226초** · `UIUX_감사결과_3중` 이 8/7 에 멈춰
  **이미 만든 8건을 "⬜ 남음"으로 적고 있던 것** 정정 · 실측 경로 함정(`strategist.output.…`)
- **투두 전면 재정리** + 누락돼 있던 **결제 심사 트랙 편입** + **정기결제 미구현 발견**

**세션N-12 (2026-08-09)** — 커밋 `efef8c2`, 배포됨
- **crew 자기평가 1패스(critique→refine)** + `MODEL_FAST` 죽은 코드 해소
- 착수 전 전수 감사로 **약속 위반 100%** 발견(최장 347자 = 약속 80자의 4.3배)
- ⚠️`MODEL_FAST` 는 **지금 이득 없음**: FAST 와 DEFAULT 가 같은 haiku-4.5.
  `FINDABLE_CREW_MODEL_FAST` 로 더 싼 모델을 지정해야 효과(배선만 열어둔 상태)

**세션N-11 (2026-08-08)** — 커밋 7건, web·app 배포
- 🔴**결제 웹훅을 죽은 앱(`apps/api`)에서 `apps/app/app/webhooks/payments/` 로 이동** + 회귀 테스트 18건
- **재측정 알림 메일**(±3점 변화시만·고장난 회차 제외·기본 꺼짐)
- 🔴**SEO 기반 복구**: `/robots.txt` **404→200** · `/sitemap.xml` **307→500 → 200** ·
  **hreflang 신설**(ko·en·x-default) · `og:locale` 언어별 · **URL이 `http://`로 나가던 버그**
- **a11y 3→0** + `tabular-numser` 오타 · **평문 이메일 로깅 6곳 마스킹**(`maskEmail` 승격)

**세션N-10 이전**: 감사 8번(가짜 1위 제거·분모 도입) · 무료진단 히스토리 · 유닛 이코노믹스 ·
대시보드 D-항목 · 파트너 승인 · 요금제 게이팅 축 통일 · 측정 파이프라인 P0 결함 수정 ·
진실거울 · GEO 코파일럿 · 네이버 브리핑 · 로그인 브랜딩 · 소셜 3종 — 전부 완료·배포.
