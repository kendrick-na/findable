# 🎯 Findable 마스터 투두 — ⚠️ **완료 기록·설계 근거 보관용**

> 🔴 **남은 일은 여기 보지 말 것. 단일 진실은 `투두리스트.md` 다** (2026-08-08 세션N-11 통합).
> 세 문서에 미완이 흩어져 있어 **누락 사고가 두 번** 났다 — 세션N-10 은 `_START_HERE` 만 보고,
> 세션N-11 은 `투두리스트` 만 보고 각각 "백로그 비었다"고 틀렸다(이 파일에 RAG·crew 등
> **8건이 더** 있었다). → **남은 일은 `투두리스트.md` 에만 적는다.** 이 파일은 이제
> **완료 로그 + 설계 근거** 보관용이며, 아래 `- [ ]` 항목들은 `투두리스트.md` 로 이관됐다.
>
> 🧭 방향·함정은 **`_START_HERE_세션N12.md`**.
> ⛔ 본문에 나오는 `_START_HERE_세션N10`·`N9`·`N11` 링크는 **전부 폐기된 파일**이다.
> ⚠️ 아래 각 세션 섹션의 "미배포" 표기는 **지난 기록**이다(이후 세션에서 전부 배포됨).


> **단일 진실.** "투두 이어서 하자"/"파인더블 투두 이어가자" 하면 이 파일. 상세 근거는 `실행백로그.md`(Day별)·`투두리스트.md`(측정결함 세션).
> 갱신: 2026-07-30(세션 E-3 종료). 완료 시 `✅(날짜)` + 근거 한 줄. 성격 태그: 🖥️코드=Claude착수가능 / 👤사용자몫(실고객·행정) / 🔬검증=실행·실측.

## 🏁 세션N-9 (2026-08-07) — **대시보드 감사 D-항목·벤치마크 공백 전부 완료 · 전량 배포**

> 📕 상세·판단근거·함정은 **`_START_HERE_세션N10.md`**. 여기엔 결과만 남긴다.

**커밋 11건 · web·app 모두 프로덕션 반영 · 워킹트리 깨끗**

| 커밋 | 내용 |
|---|---|
| `fba654e` | **D6** 히어로 카드 링크 + **잠금 사전고지**(문서의 "3장 전부 링크"를 2장으로 정정) |
| `d15b27e` | 🔴 라이트 OS 주 숫자 **비가시**(대비 1.06:1 → **17.90:1**) |
| `7cc4713` | **D8** 감성 스택바 + `긍정 15%` 오독 차단 + 티어 비중 보정 |
| `cea0b84` | **D5** 감성 이전 기간 비교 → 히어로 3장 모두 맥락 확보 |
| `48ca5ff` | **D10** 브랜드 전환 + `측정 2회` 거짓 숫자(org 총계 혼입) |
| `9b3b2f6` | 시각 타임존 "결함" **철회 — 내 오진**. ⚠️재조사 금지 |
| `3448fd6` | 정의 없는 CSS 토큰 2종(`--findable-danger`·`--findable-warn`) |
| `9132c2c` | **D9** 순위 스파크라인 → 3장 리듬 완성 |
| `e8132c0` | **밀리는 질문** — 벤치마크 채택률 **8/15 최대 공백** |
| `367c659` | **D11** 헤더 상주 지표(호출부 14곳 수정 0) |
| `dda3607` | 인계 문서 |

**🔬 실측으로 문서를 정정한 것 4건** (전부 실측이 옳았다)
1. **D4 = 보류 판정** — `Tracking` 54행·측정 2회·시계열 1일. 기간 칩이 **작동 안 하는 버튼**이 된다.
   D2 자동핀·D1 알림과 같은 병목인데 D4만 1순위에 남아 있던 누락.
2. **D6는 2장만 링크** — `/compare`·`/sources`가 **유료 게이트 + 가짜 예시**(42%·31%·18%).
   그냥 링크면 막다른 카드를 **미끼**로 바꾸는 것.
3. **브랜드 "9종" → 실측 7종**(측정된 건 2종).
4. **"promptStats 버림"은 틀렸다** — `buildGeoActions`로 넘어가 **처방 카드로 이미 사용 중**.
   진짜 공백은 **질문별 성적 화면 부재**였다.

**🔴 남은 코드 항목 = 감사 8번 하나(보류 권고)**
`matches.length`를 살리면 *"5개 중 1번째"* 가능. 그러나 **소급 불가**(기존 54행 전부 null) ·
**변별력 없음**(순위 보유 26건 중 **23건이 1위**) · **화면은 세션N-8이 이미 정직하게 처리**
(`"AI 답변 목록에서 우리 자리 · N개 응답 평균"`). → D4·D1·D2와 같은 묶음으로.

**👤 이제 사용자 몫만 남았다**: ①실고객 10~15명 지불의사(불변 1순위) ②신규 화면 눈으로 확인(로그인 필요)

---

## 🧭 (지난 기록) 세션N-8 — M1 해결 + 점수 불일치 발견·수정

커밋 `79df764`. **미배포 커밋 11개**(`0c81770`~`79df764`). web tsc 0 · app tsc 0 · lint 30→30.

### ✅ 세션N-8에서 한 것
1. **M1 결정·구현 완료**(사용자 승인 = **A안+B안 병행**). 5축을 2단 구조로 —
   인지가 **선행 축**, 감성·노출은 들여쓰기 + 실효 상한 병기(`27/40`→`27/34`).
   점수 무변경(**3,840 케이스 격자 회귀 0건**). 실화면 확인 완료.
2. 🔴 **그 과정에서 3개월 묵은 결함 발견 → 수정**: **화면 점수 ≠ 메일·OG 점수**가
   **71건 중 58건(82%)**, 최대 32점(Olive 화면 66 / 메일 34). 화면만 `new Set()` dedup을
   채점기에 넘겨 **세션M의 F11을 되돌리고 있었다**. 저장값(응답 단위)이 맞다고 실측 판정
   (고유 엔진 방식은 인지도 **만점 35%** vs 응답 단위 4% = F11이 없애려던 증상).
   → 4경로(화면·공유·메일·OG) 전건 일치.

### ✅ 감사 코드항목 **4~10번 전부 완료** (커밋 5개, 전 항목 실화면 확인)
4 빈카드→행동카드 · 5 매출위계강등(주숫자=세션) · 6 처방숫자혼재(+132.4% 제거) ·
7 인용출처 등급표기 · 8 순위 척도(분모는 **만들 수 없음을 코드로 확인** → 지어내지 않음) ·
9 `translate="no"`(12개 요소) · 10 임계값 3중복제 제거(전 구간 대조 불일치 0건)

### ✅ 세션N-8 후반 = **web 배포 + 대시보드 D3·D7·D2 완료**
- 🚀**web 프로덕션 배포 완료**(닥터오엣 70→60 검증). 🔑**배포=CLI직접**(`npx vercel --prod --yes`)
  ·GitHub자동배포 아님·**브랜치 무관**(feature가 main보다 122개 앞서나 충돌0)
- **D3** X축 거짓정밀도(같은날 중복시 시각병기) · **D7** 라벨정정+CSV+공유리드젠분기 ·
  **D2** 수동주석(`Annotation` 모델 신설)
- 🔬**리서치 재대조로 3건 판단이 바뀜** — 감사문서를 그대로 믿지 않은 게 맞았다:
  ①D3 감사 사례("설화수 13건")가 **틀림**(실제는 AuditJob 폴백의 메디큐브)
  ②D7 공유링크를 대시보드에 새로 뚫으려 했으나 **무료진단이 이미 조건을 갖춤**→DB변경0
  ③**D2를 "자동 급변감지"로만 좁혀 읽은 게 내 실수** — 원본에 GSC 수동주석이 있었다
- 🔴**자동핀·D1 알림 보류**: 실측 델타 `[0,0,0,0,+97]`(+97=측정실패→복구). 임계값 뭘 잡아도
  **핀이 측정실패 지점에만** 꽂힘. 주1회 갱신이라 분포형성 2~3개월 → 재측정 쌓인 뒤

### ⭐ 다음 작업 = **D4(기간) → D5·D6·D8·D10 묶음**
(D6는 목적지 라우트가 **전부 존재** → 링크만. D9는 데이터계층 변경 필요라 뒤로)

### (완료) 이전 순서 = 대시보드 D3→D2→D1
(이 순서가 원가 절반 — 같은 임계 계산 공유. D3=X축 거짓 정밀도 → D2=이벤트핀 → D1=알림 실구현)
그 외 D4 기간선택·D7 CSV/공유링크가 P0~P1.
⬜ **후속(신규 측정 필요)**: 8번 **진짜 분모** — `estimateMentionPosition`이
`matches.length`(목록 총 항목 수)를 함께 반환·저장해야 *"5개 중 1번째"* 를 쓸 수 있다.

### 🎓 세션N-8 교훈
- **표시 구조를 바로잡으면 숨은 계산 결함이 따라 나온다.** M1은 "화면 배치" 작업이었는데
  인지도를 위로 올리자마자 점수 불일치가 드러났다. UI 작업을 "미관"으로 낮춰 보지 말 것.
- **스크린샷을 눈대중으로 통과시키지 말 것**(N-7 교훈 재확인). 화면엔 85%가 떴는데
  DB 실측은 68%였다. 렌더 성공 ≠ 숫자 정확.
- **`git stash` 금지 재확인** — `stash@{0}`에 2026-07 낡은 stash 그대로 있음(건드리지 않음).

## 🧭 (이전) START HERE (2026-08-07 **세션N-7: 3중 감사 + 정합성·정직성 수정**)

**커밋 10개** `0c81770`~`65e21df`. 워킹트리 깨끗 · web tsc 0 · app tsc 0 · lint 32→30(2건 감소).
🔴 **전부 미배포** — 사용자가 배포 시점을 정하기로 함.

### 📕 먼저 읽을 것 (이 순서로)
1. **`UIUX_감사결과_3중_2026-08-07.md`** ← **미수정 항목 단일 진실.** ⚠️재감사 금지
2. `UIUX_대개선_기획서_2026-08-06.md` §2(가치역전)·§10(실행계획 1~4단계)
3. 리서치 원본 `_리서치원본_UIUX_2026-08-06/` ⚠️**재리서치 금지**(검색 200회 소진)

### ⭐ 다음 작업 = 감사결과 문서의 착수 순서 그대로
🖥️ **4번 우측 빈 카드 → 행동 카드**(M) → 5번 매출 위계 강등(M) → 6번 처방 숫자 혼재(S) →
7번 인용출처(S) → 8번 순위 분모(S) → 9번 `translate="no"`(S) → 10번 임계값 3중복제(M)
그다음 **대시보드 D1~D11**(D3→D2→D1 순서가 원가 절반: 같은 임계 계산 공유)

👤 **사용자 결정 대기 = M1**(점수 60%가 인지도 종속인데 독립처럼 나열 → 처방 방향 오독).
   A/B/C안이 감사결과 문서에 있음. **코드보다 정의가 먼저.**

### ✅ 세션N-7에서 고친 것 (커밋 5개)
| 커밋 | 내용 |
|---|---|
| `93bee93` | **정합성**: "AI 몇 곳이 아나"가 3곳에서 달랐음(제목6/KPI 6-7/진실거울**5**)→전부 6곳. `truth-mirror` `dedupeByEngine`가 엔진별 **첫 응답만** 채택해 질문2에서 언급한 엔진을 "모른다"로 집계하던 버그. 86%가 두 지표(`sov`≠`mentionRate`)→라벨 구분. `ViralBar` stub 보정 누락→공유점수≠화면점수 위험 제거. **정직성**: "이번 주 1건 액션" 하드코딩 삭제·버튼이름 불일치(없는 버튼 찾게 함)·공유링크 `shared=1`로 제3자에게 이메일 숨김. **규율**: 저점 빨강 제거(GSC 안티패닉)·5축 %통일·해요체 10건·`keep-all` 전역·자간 8건 |
| `0d3d409` | **없는 기능을 팔고 있었음** — "주간 자동 리포트"·"이메일 알림"·"Notion·Google Docs Export" 구현 0건인데 6곳에서 판매. 결제 후에야 알게 되는 구조 → 체크 대신 **시계 아이콘+흐린 글씨+"(준비 중)"** (한/영·web/app 전부) |
| `07c808b` | **변별력 없는 경쟁지형 필터** — 실측 5건 중 3건이 사실상 동률(Haegyung 2·2·2·2·2·2). **단 잘 되는 케이스는 유용**(SK하이닉스 마이크론·삼성전자·인텔) → 삭제가 아니라 **격차 5%p 기준으로 걸러내기**. 판정은 `@repo/audit` 단일진실에, 처방은 화면별로(무료진단=숨김 / `/compare`=고지문구). 제목 "경쟁 지형"→"AI가 함께 거론한 이름들" |
| `65bda4f` | **점수 기준선 한 줄** — 게이지 바로 아래 "그래서 좋은 거냐". 🔬**자기정정**: 첫 문안이 총점을 **인지 축**으로 설명해 모순(클로드 총점73인데 인지 85%)→총점이 뜻하는 것만 말하도록 교체 |
| `65e21df` | **원문 접기** — excerpt 최대 3,908자가 잘림 없이 렌더 → max-h+페이드+"전체 보기". **7,461px→6,044px**(스크롤 7.5→6.0회). ⚠️삭제·요약 아니라 접기(진실거울 증거라 없애면 핵심 소실) |

### 🔴 세션N-7 교훈 (반복 금지 — 이번에 실제로 사고를 냈다)
1. **완료 보고를 검증 없이 믿지 말 것.** N-5의 *"해요체 26→0건"*은 **따옴표 리터럴만 센 것**이라
   **백틱 템플릿 5건(h1 제목 포함)을 놓쳤다.** N-5의 *"1단계 완료"*도 원안 7개 중 6개만 했고
   **스파크라인이 조용히 빠졌다**(번호 재배치로 항목이 사라졌고 완료표에도 안 적힘).
   → **완료 주장은 grep 패턴까지 의심할 것.** 문자열 검색은 따옴표·백틱·JSX·i18n 사전을 모두 봐야 한다.
2. **"불가능"이라는 결론을 에러 실물 없이 쓰지 말 것.** N-5가 사용자에게 넘긴 1순위(Clerk 키)는
   **불필요**했다 — 키는 web/.env.local에 있었고, 진짜 원인은 **CSS 중복 import**였다.
3. **"판정 불필요"에도 근거가 필요하다.** N-5의 *"섹션 13개 → 중복 아님 → 불필요"*는
   센 기록만 있고 **중복 판단 기록이 0**이었다. 실제로는 한 섹션이 전체 39%를 먹고 있었다.
4. **감사자 주장도 실측할 것.** 3중 감사가 낸 항목 중 상당수가 참이었으나,
   "선이 절반에서 끊긴다" 같은 내 관찰은 **SVG path 좌표를 재보니 오독**이었다(전폭 정상).

## 🧭 (이전) START HERE (2026-08-06 **세션N-6: 화면 확인 성공 + 거짓비교 버그 수정**)

커밋 `0c81770`(CSS) · `b30ca60`(브랜드 필터). app tsc 0 · web tsc 0 · lint clean · next build 통과.
🔴 **아직 미배포** — 배포는 사용자 승인 후.

### ✅ 세션N-5가 "불가능"이라 한 화면 확인 = **실제로는 가능했다**

세션N-5는 *"Clerk 키가 없어 로컬 로그인 불가 → 사용자만 발급 가능"* 으로 1순위를 사용자에게 넘겼다.
**전제가 둘 다 틀렸다**:
1. **Clerk 키는 이미 있었다** — `apps/web/.env.local`에 `sk_test`/`pk_test` 실값 보유(app 폴더만 보고 "없다" 단정).
   app `.env.local`로 복사해 해결(`DATABASE_URL` 포함 3개).
2. **진짜 막은 것은 Clerk가 아니라 CSS였다** — `styles.css`의 중복 `@import "tailwindcss"`가
   폰트 `@import`보다 앞에 Tailwind 5,000줄을 인라인 → CSS 파싱 에러로 **전 라우트 500**.
   Clerk 키를 다 채워도 이것 때문에 화면이 안 떴다. (⚠️ 부수 함정: 빈 문자열 env 19개가
   zod `.optional()`을 통과 못해 부팅 실패 → 주석처리로 해결)

**교훈**: "환경변수가 없어서 못 한다"는 결론을 **에러 메시지 실물로 확인하기 전에 쓰지 말 것.**
실제 첫 에러는 env가 아니라 CSS였고, 두 번째는 "키 없음"이 아니라 "빈 문자열"이었다.

### 🔴 화면을 처음 보자마자 나온 버그 = 거짓 비교 (수정 완료)

**AuditJob 폴백에 브랜드 필터가 없어 서로 다른 회사를 비교**하고 있었다:

| 화면 표시 | 실제 계산 |
|---|---|
| 클로드 86% **↑+7%p** | 7%p = 클로드(86) − **SK하이닉스(79)** |
| "지난 측정 1.3번째에서 0.3 올랐어요" | 클로드 vs **SK하이닉스** |
| 추세선 1개 | **브랜드 9종**의 SoV를 한 선으로 이음 |

원인 구조 = **같은 의도의 두 구현 중 폴백만 필터가 빠짐.** Tracking 경로는 처음부터
`run.brandId === latest.brandId`로 정확히 걸고 있었다(세션N-2 `sovLabel` 사고와 같은 "쌍둥이 구현" 패턴).
`sovDeltaPoints` 혼합은 **세션N-5 이전부터 존재**했고, N-5가 순위힌트·감성추세 2개 소비자를 추가해 확대했다.

**수정 후 실측**: 클로드(1건)→거짓배지 제거·기존 폴백문구 "비교는 2회차 측정부터" /
설화수(12건)→"지난 측정 1.2번째에서 0.1 올랐어요"(같은 브랜드 실제 개선)·추세선 단일브랜드 정상

### ✅ 1·2단계 화면 검증 결과 (1440px·390px 스크린샷)
히어로 3장 질문형 라벨·티어("잘 보이는 편"/"가장 먼저"/"우호적")·운영지표 회색 한 줄 강등(A안)·
"지금 할 일 보기" 카드·감성 2계열 차트(오렌지↔단청) **전부 의도대로 렌더**. 콘솔 에러 0.
모바일 390px에서 오버플로 없음(`min-w-0` 가드 작동). 빈 상태도 정상("두 번째 측정을 하면…").

### ⬜ 다음 순서
1. 👤 **배포 승인** — 위 2커밋(app만 영향). 승인 시 app 배포(⚠️`project.json` swap→원복)
2. 🔬 **로컬 화면 확인 방법이 확보됐다** — 재현 절차는 아래 "로컬 app 화면 확인" 참고.
   앞으로 UI 작업은 **배포 전에 눈으로 볼 수 있다**(세션N-5의 최대 병목 해소)
3. ⏸️ 세션N-5 잔여: `/compare`·프롬프트 마법사 직접 사용 · 4단계(주간 이상감지·이벤트핀·CSV)

### 🔧 로컬 app 화면 확인 (재현 절차 — 다음 세션이 그대로 쓸 것)
```bash
# 1) Clerk·DB 키를 web → app 으로 (값은 web/.env.local 에 실재)
#    CLERK_SECRET_KEY · NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY · DATABASE_URL
# 2) app/.env.local 의 빈 문자열 키는 주석처리 (zod optional이 ""를 거부)
# 3) 기동 — VERCEL=1 필수(Neon WebSocket 함정)
cd apps/app && VERCEL=1 bun --bun next dev -p 3002
```
⚠️ **로그인은 여전히 자동화 불가** — 가입 화면에 **Cloudflare Turnstile**(봇 검사)이 있어
헤드리스 브라우저로 못 뚫는다. 또 DB는 프로덕션인데 Clerk 키는 dev 인스턴스라
**org id가 안 맞아 Tracking 경로가 빈다**(→ AuditJob 폴백만 렌더됨).
→ **화면 확인은 `(authenticated)` 그룹 밖에 임시 라우트**를 만들어 실제 컴포넌트에
실제 DB 데이터를 넣는 방식으로 했다(확인 후 삭제). 새 최상위 라우트는 **dev 서버 재기동 필요**.

## 🧭 (이전) START HERE (2026-08-06 세션N-4: UI/UX 대개선 기획 완료 → **다음=1단계 착수**)

**코드 변경 0건.** 이번 세션은 **리서치 + 기획**. 커밋 `d213718`(기획서) · `3345c29`(리서치 원본).

### 🔍 0단계 = **기획서 점검부터** (사용자 지시, 코드보다 먼저)

기획서는 **한 세션에서 급히 쓴 것**이라 검증이 안 됐다. 착수 전에 점검한다:

| # | 점검 항목 | 방법 |
|---|---|---|
| 0-1 | **기획서 내부 모순** — §2(진실거울 이식 안 함)와 §4·§10이 서로 어긋나지 않나 | 통독 |
| 0-2 | **파일 경로·줄번호가 실제와 맞나** — `runner.ts:354`·`dashboard-kpis.tsx` 등 | 코드로 확인 |
| 0-3 | **"데이터 이미 있다" 재확인** — `averageMentionPosition`·`sentimentDistribution` | DB 재조회 |
| 0-4 | **리서치 원본 5파일과 기획서 결론이 일치하나** | 대조 |
| 0-5 | **누락된 UX 항목** — L-1~L-5 중 기획서에 안 들어간 것 있나(L-4는 후순위 처리됨) | `_START_HERE_세션L_UX개선.md` 대조 |
| 0-6 | **인용 금지 5건이 기획서 본문에 잘못 섞여있지 않나** | grep |

⚠️ **이 세션 교훈**: 기획서를 쓰는 동안 **자기정정이 3번** 났다. 즉 급히 쓴 결론에 오류가 섞일 확률이 높다.
**문서의 결론이 아니라 전제를 코드로 확인**할 것(세션N-2 교훈).

### ⭐ 점검 통과 후 = **1단계 대시보드**

📕 **먼저 읽기**: [`UIUX_대개선_기획서_2026-08-06.md`](UIUX_대개선_기획서_2026-08-06.md) §10 실행계획
📕 근거 원본: [`_리서치원본_UIUX_2026-08-06/`](_리서치원본_UIUX_2026-08-06/) **⚠️재리서치 금지**(검색 200회 소진)

| # | 작업 | 파일 | 리스크 |
|---|---|---|---|
| 1-1 | `DashboardData`에 `averageMentionPosition`·`sentiment` 배관 | `apps/app/(authenticated)/lib/dashboard-data.ts` | 🟢 |
| 1-2 | KPI **4장 → 히어로 3장 + 보조 한 줄**, 라벨 질문형 | `components/dashboard-kpis.tsx` | 🟢 |
| 1-3 | 델타 배지 **빨강 완화**(GSC 안티패닉) | 동일 | 🟢 |
| 1-4 | **"지금 할 일 N건"** 카드 → `/actions` | `page.tsx` + 신규 | 🟢 |
| 1-5 | Sentiment 추세 | `sov-trend-chart.tsx` | 🟡 |

✅ **데이터는 이미 DB에 있음** — 실측 `metrics.averageMentionPosition`=1 · `metrics.sentimentDistribution`={neutral:17, positive:3}
→ **측정·원가·DB 마이그레이션 전부 0.** "저장돼 있는데 화면까지 안 오는 것"을 잇는 작업

**바뀌는 모습**
```
현재: [최신 SoV] [엔진 커버리지] [총 측정 횟수] [마지막 측정]   ← 4장 중 2장이 운영지표
이후: [AI가 우리를 아나? 42% ↑3%p] [몇 번째로 말하나? 1.8번째] [좋게 말하나? 긍정 15%]
      측정 12회 · 2일 전 · AI 5곳 중 3곳에서 등장   ← 작게 회색
      [지금 할 일 3건 →]                            ← 신규
```

### ❓ 착수 전 사용자 확인 1건 (⚠️CLAUDE.md: UI는 승인 후 반영)
**운영지표(총 측정 횟수·마지막 측정) 처리?**
- ⭐**A안**: 작은 회색 한 줄로 강등 (히어로 3장 유지 — Stripe 4장·GSC 기본2개 관행)
- B안: KPI 6장으로 늘림 (⚠️리서치 경고 *"KPI를 늘리지 말고 줄일 것"*에 반함. Profound가 *"버튼 잔뜩 달린 고급차"* 소리 들은 이유)

### 🔴 기획의 핵심 결론 3개 (착수 전 필독)
1. **가치 역전의 진짜 원인** = 섹션 개수가 아니라 **무료가 "반복 확인할 이유"까지 준 것**(Docker형 실패)
   → **유료 축 = 시간·비교·알림.** ⚠️**진실거울 대시보드 이식은 오답**(무료 aha 복사는 축을 안 바꿈)
2. **진짜 공백 = 처방** — 12개 툴 리뷰어가 독립적으로 *"측정만 하고 뭘 할지 안 알려준다"* 지적.
   Findable `/actions`가 카테고리 1군이 못 하는 것 → **1급 시민으로 승격**
3. **0점 처리는 이미 업계 관행**(따라잡을 것). 기준선 = **AthenaHQ의 단일 원인 도메인 추적**

### ⚠️ 이번 세션 자기정정 3건 (반복 금지)
①*"경쟁사가 0점화면 안 만든다=차별화"* → **12개 툴로 반박.** 원인=**Profound·Peec 2개만 보고 일반화**(세션M-3 교훈② 재발)
②히어로 KPI에서 **Visibility와 SoV를 합침** → 업계는 **다른 지표**
③*"Profound UX 나쁘다"* → **G2 4.6/5에 칭찬 많음.** "버튼 잔뜩" 인용만 골라 오해 유발

### 🔴 인용 금지 (원문 추적 실패 — 대외자료 사용 금지)
*"McKinsey 전환율 21%"* · *"NN/g loss-framing"* · *"챔피언 승률 4.5배"* · *"reverse trial 14~21%"* · *"인지부하 55% 감소"*

---

## (이전) START HERE (2026-08-04 세션N-3: 게이트 오프라인 재실행 + 시뮬 대체가능성 종결)

**코드 변경 0건.** 이번 세션은 전부 **실측·리서치**다. 커밋 `ddf2cb2` 이후 코드 무변경,
`228d2d1`(관측 심기)은 **이미 배포됨**(커밋 8/3 21:36 → 프로덕션 배포 그 이후. 세션N-2 문서의
"미배포"는 낡은 기록이었고 실측으로 정정).

### ✅ 게이트 오프라인 재실행 — 원가 0원, 새 측정 0건

**방법**: 실제 프로덕션 게이트(`__internal.needsVerification`)를 esbuild로 번들해 **그대로 import**.
재구현 아님. 저장된 `AuditJob` 71건 × 응답 2,026건에 재실행. LLM 호출 0.

**🔴 세션N-2의 핵심 가설이 반박됨**

| 지표 | 실측 |
|---|---|
| 게이트가 실제 판단한 응답 | 1,334건 (측정실패·stub 509 / absent 183 제외) |
| `viaRule` — LLM 안 감 = **오인이어도 confirmed** | **641건 (48.1%)** |
| `viaLlm` — LLM 판정 도달 | **693건 (51.9%)** |

세션N-2는 *"`viaLlm`≈0이면 게이트가 오인을 못 잡는 신호"* 라고 적었다. **`viaLlm`은 51.9%다.**
절반 이상이 이미 LLM 판정에 도달하고 있었다.

**왜 세션N-2는 반대로 봤나** — `viaLlm` 통과 사유가 답이다:
`한글 2~3자` **530건(76%)** · 되물음 정규식 135 · 모른다 정규식 13 · 영문소문자3~12자 15.
세션N-2의 *추론*(오인은 텍스트 정규식에 안 걸리고 브랜드명 형태만 남는다)은 **맞다**. 그런데
**실제 DB의 브랜드명이 대부분 한글 2~3자**였다(설화수·기아·현대·토스·나이키·달바·아누아·클로드)
→ 대량 통과. 세션N-2의 오류는 **브랜드명을 손으로 골라 테스트한 표본 편향**(SK하이닉스·삼성전자 등
긴 이름 위주)이었다.

**🎯 진짜 결함은 표기법 비대칭 (이번 세션 최대 발견)**

| 브랜드 | `viaLlm` |
|---|---|
| `설화수` (한글) | 25~27건 |
| **`Sulwhasoo` (영문)** | **0건** |

**같은 브랜드인데 표기법만 바꾸면 게이트가 완전히 열린다.** 같은 패턴:
`메디큐브`(llm 1~3) vs `Medicube`(llm 0) · `라운드랩` vs `Roundlab`(0) · `Olive`(0) · `SK하이닉스`(1).

### 🔴 그런데 지금 고치면 안 된다 — 641건의 정체

| 641건 브랜드 분포 | 건수 |
|---|---|
| **메디큐브 + Medicube** | **321 (50%)** |
| Sulwhasoo | 76 |
| 라운드랩 45 · 조선미녀 42 · SK하이닉스 22 · 기타 10개 | 나머지 |
| **브랜드 종류 총** | **15개** |

그리고 **측정자 실측**: 71건 중 **48건(68%)이 사용자 본인**(nay***@gmail 27 · ken***@indigochild 14 ·
org 7). 나머지 23명은 각 1건씩. 고유 도메인 29개.

→ **이 데이터는 사용자 자신의 테스트 데이터다.** 지금 게이트를 튜닝하면 **자기 테스트 브랜드
15개(절반이 메디큐브)에 과적합**된다. 실고객이 자기 브랜드를 넣으면 이름 분포가 완전히 달라진다.

**LLM 693건 판정도 보류** — 원가는 실측 **약 614원**(haiku·건당 0.89원, `cost.ts` USD_TO_KRW=1380 기준)
으로 싸다. 하지만 표본이 메디큐브 50%라 **어느 쪽 결과가 나와도 일반화 불가** =
614원으로 사는 건 정보가 아니라 **정보처럼 보이는 숫자**. 실고객 데이터 쌓인 뒤 돌리면 같은
614원이 진짜 답을 준다.

### 🔬 시뮬레이션으로 실고객 검증을 대체할 수 있나 — **불가. 재리서치 금지**

사용자 질문("시뮬을 실검증처럼 더 할 수 있나, 깃허브에 없나")에 대한 리서치. 결론 = **대체 불가**,
증거가 유독 명확하다.

| 연구 | 결과 |
|---|---|
| [컬럼비아 19연구 arXiv:2509.19088](https://arxiv.org/abs/2509.19088) — 실제 인간 13,506명, **1인당 질문 500개+**로 만든 트윈 | 트윈↔인간 **r=0.20** · 분산축소 140/154 · **상세페르소나 vs 인구통계만 = 차이없음(p=0.37)** · 실험효과 재현 ~50% |
| [When Synthetic Users Fail arXiv:2607.26348](https://arxiv.org/abs/2607.26348) | 세그먼트 격차 **2~4배 과장** → **절반은 엉뚱한 세그먼트 지목**. 과장 때문에 **틀린 답이 확신에 차 보임** |
| [Simulated Customers Never Walk Away arXiv:2606.20708](https://arxiv.org/pdf/2606.20708) | 시뮬 고객은 **이탈하지 않음**. 가장 알아야 할 결과가 "안 사고 떠남"인데 못 만듦 |
| 컬럼비아 (초합리성) | 매력·타협효과 재현 실패 = **가격 티어 선택을 움직이는 심리를 못 만듦** |

**가격 상한은 분포 꼬리에 있는데 합성 응답은 꼬리를 지운다.**

**잘 된 사례도 조건이 정반대**: [Park et al. 1,000명](https://arxiv.org/pdf/2411.10109) 85% 달성 =
**실제 인간 1,052명과 각 2시간 인터뷰**가 재료(→ *"사람을 만나라"는 논문*).
[SSR ρ=0.90~0.92](https://arxiv.org/html/2510.08338v1) = 생활용품 CPG·인간 9,300명, **WTP·가격 결과 없음**.
[Brand et al. HBS](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4395751) WTP 수요곡선 = *그럴듯함* 시연,
후속연구에서 **부호 뒤집힘 다수**.

**Findable은 최악의 조건**: 신뢰조건([Jia et al.](https://arxiv.org/pdf/2605.10659))=단봉분포·학습데이터
잘 표현된 집단·단순질문. **한국+B2B+신규카테고리+니치 = 4축 전부 실패.**
**B2B SaaS 구매의향 검증연구는 아예 0건.**

**GitHub**: WTP용 검증 도구 **0개**. [microsoft/TinyTroupe](https://github.com/microsoft/TinyTroupe)(7.5k,활발)가
가장 근접 · [verbalized-sampling](https://github.com/CHATS-lab/verbalized-sampling)(788)은 **분산축소 교정용**으로 유용.
스탠포드 generative_agents(21.8k)는 정지·용도 다름. Van Westendorp/Gabor-Granger를 페르소나에
적용한 검증 오픈소스 **0개**.
**상용**(Aaru $1B·Synthetic Users·Qualtrics)은 전부 자체주장. [Aaru 90%는 "LLM이 그 데이터를 이미
학습했기 때문"이라는 독립 비판](https://measuringu.com/review-of-experiments-with-synthetic-users/).
**ESOMAR·Bain 모두 "실인간 검증의 보완물, 대체물 아님"으로 규정.**

⚠️ **기존 시뮬 12명 결과(평균 4.3/10, 2명만 지불) 해석 주의**: 비관적이라 "보수적이라 안전"하다고
읽으면 안 된다. **r≈0.2에서 나온 비관은 보수가 아니라 노이즈다.** 낙관이 시장 좋음의 증거가
아닌 것과 대칭.

**시뮬의 검증된 쓸모 = 3가지** (버리지 말고 자리를 바꿀 것):
①사람 만나기 **전** 반대·거절 사유 뽑아 약한 프레이밍 죽이기 ②인터뷰 가이드·질문문구 사전테스트
③**실제 녹취를 페르소나에 먹인 뒤** SSR+verbalized sampling → 그때부터 증폭기로 작동.
→ 실고객 인터뷰는 시뮬을 피해가는 비싼 길이 아니라 **시뮬이 작동하게 만드는 입력**이다.

### ✅ 투두 정합성 정리 (세션N-3 후반, 코드로 전수 확인)

**🔴 투두 낡음이 실제 사고를 냈다** — 세션N-3 전반에서 *"다음 후보 = 프롬프트 마법사"* 로 제시했으나
**7/30에 이미 구현된 기능**이었다(커밋 `030190b`). 투두가 ⬜로 남아 있어서 그걸 믿고 오제시.
→ **표준 백로그 8항목 전부 코드로 재확인**해서 상태를 바로잡음(위 섹션).

| 항목 | 투두에 적혀있던 것 | 실측 |
|---|---|---|
| 1 프롬프트 마법사 | ⬜ 미구현 | **✅ 구현됨**(680줄 3파일) |
| 2 자동갱신 cron | ⬜ 미구현 | **✅ 구현+vercel.json 등록**(`0 2 * * *`) |
| 5 Sentiment | ⬜ 미구현 | **🟡 절반**(저장·단건표시 O, **추세만** 남음) |
| 7 게이팅 프롬프트수 | ⬜ 미구현 | **✅ `plan.ts` promptLimit 5/30/150/500/2000** |
| 6 주간메일 · 8 CSV | ⬜ | ⬜ **미구현 확인**(맞음) |

**tsc 실측**: web 0 · app 0 (CLAUDE.md 기재 "api 2·studio 1"은 세션C에서 해소됨)
**L-2 진실거울 app 이식**: 미이식 확인(`truth-mirror.tsx`는 www에만 존재) — 투두 기재 맞음

### 🔬 "AI 답변에서 우리가 밀리는 질문 리스트" 확인 결과 = **재료는 있고 저장이 없다**
P1a(시뮬 페르소나)가 콕 집어 요구한 항목. 코드 확인:
- `runner.ts:354` **`promptStats` 이미 계산됨** = `{text: 질문원문, hit: 언급엔진수, total: 측정엔진수}`
- `buildGeoActions` 에 전달돼 액션 생성에 쓰인 뒤 **버려진다**
- **result JSON 미저장** — DB 실측: 최상위 키에 `promptsCount: 4`(개수)만 있고 질문별 결과 없음
- 화면에서 프롬프트별 통계를 읽는 곳 **0곳**(`prompt-wizard`는 질문 *제안* 기능으로 무관)
- 🔴**세션N-2 `mentionQuality`와 완전히 같은 패턴** — 계산·전달까지 하고 버림

⚠️ **단 지금 저장 추가해도 화면은 빈다**(신규 측정부터만 쌓임 = N-2 관측과 같은 제약).
실고객 0명 상태에서 만들면 또 "사용자 본인 테스트 데이터"가 된다. → **실고객 후로 유보.**

### ⬜ 다음 (세션N-3 판단)
0. 👤⭐ **사용자가 `/compare`·프롬프트 마법사를 직접 써보고 이상한 곳 지적**
   — 만들어졌지만 **사용자가 써본 기록이 없다.** 만든 사람도 안 쓰는 기능은 실고객도 안 쓴다.
   근거 없이도 확실히 옳은 유일한 다음 수(실고객 0명이라 기능 우선순위의 근거가 원천 부재)
1. ⏸️ **`promptStats` 저장 + "밀리는 질문" 화면** — 재료 확인됨, **실고객 후 착수**(위 ⚠️)
2. ⏸️ **투두4번(오인 배지)·게이트 확장 = 실고객 데이터 대기** (보류 아님, 상태 변경)
   — 시뮬 12명 중 **아무도 이걸 이유로 지갑을 열지 않음**. 641건은 자기 테스트 데이터
3. 👤 **실고객 10~15명** (사용자가 "인터뷰는 나중에"로 유보) · 카카오페이 CID 대기
4. 🖥️ F2 both 반쪽측정 · F5 나머지 언어↔엔진 서브셋 · lint 51건

> ⚠️ **세션N-3 교훈 (착수 전 필독)**: 이 세션에서 **자기정정 2건**이 났다.
> ①"관측은 신규 측정부터만" 문서 문구를 검증 없이 받아 *"측정을 3~5건 돌려야 한다"* 고 답 →
> 사용자 반박("왜 돌려야 해?") → **오프라인 재실행으로 0원 해결.**
> ②"원문 2,026건 있으니 공짜" → **응답 개수만 세고 스키마를 안 봄.** `rawResponse` 0건,
> 실제로는 `excerpt`(최대 4000자, 판정기는 1200자만 씀)에 담겨 있었다.
> 공통 원인 = **필드 이름·문서 문구만 보고 내용을 안 열어본 것.**
> **규칙: 결론을 말하기 전에 코드·데이터를 열고, 확인한 것과 추측을 구분해 보고할 것.**

---

## (이전) START HERE (2026-08-03 세션N-2: 판정 관측 심기 + sovLabel 정정)

**커밋 `228d2d1`** (미배포 — www 배포 필요). 측정·점수·화면 **무변경**, 시계열 연속.

### 🔴 이번 세션 최대 발견 — 투두 4번("판정 사유 화면 노출")은 **착수 전제가 틀렸다**

투두에 *"표시 레이어만 = 리스크 낮음"* 으로 적혀 있었으나, **표시할 데이터가 대부분
고객에게 존재하지 않는다.** 게이트 실측으로 확인:

`needsVerification`(mention-verdict.ts:98)은 아래 중 하나만 걸려야 LLM 판정에 보낸다 —
`되물음 정규식` OR `모른다 정규식` OR `영문소문자 3~12자` OR `한글 2~3자`.
그런데 **오인(different_entity)은 정의상 되묻지도 모른다고도 하지 않는다**
(AI 가 자신 있게 엉뚱한 설명을 한다) → 텍스트 정규식 둘 다 안 걸리고, **브랜드명 형태**만 남는다.

| 판정 도달 ✅ | 판정 못함 ❌ (오인이어도 조용히 `confirmed`) |
|---|---|
| 클로드·기아·현대·네이버·토스·설화수·무신사 | **SK하이닉스·삼성전자·메디큐브·라운드랩·Findable·이터널에디션즈·당근마켓·오늘의집** |

→ **4글자 이상 한글·대문자 섞인 영문 브랜드는 완전히 오인당해도 정상 집계된다.**
F12 가 *"판정기가 이미 구분하니 화면에 드러내면 된다"* 고 적었는데, **클로드가 한글 3자여서
게이트를 통과한 예외**였다. F12 자신이 경고한 *"claude.ai 는 최악의 예외"* 가
**게이트 통과 여부에서도** 예외였다. → UI 를 먼저 만들면 **표본 1의 일반화**.

### ✅ 완료 (커밋 `228d2d1`)

**1) 판정 사유 관측 심기** — 정보가 러너에서 죽고 있었다(참조 0건 + 로그 0건).
- `runner.ts`: `engineResponses` 에 `mentionQuality`·`verdictVia` 저장.
  **JSON result 라 DB 마이그레이션 없음** = Prisma TCP 차단(P1001) 함정 회피.
- `mention-verdict.ts`: 브랜드별 판정 분포 로그 1줄(`mention.verdict.distribution`).
  응답당 로그는 측정 1건에 22~28줄이라 과함 → 브랜드 단위 집계.
- **`viaRule`/`viaLlm` 분포도 함께 로깅** — `viaLlm`≈0 이면 **게이트가 오인을 못 잡는 신호**.
  이게 다음 판단의 핵심 지표다.

**2) `sovLabel` → `scoreTierLabel`**(동작 0 변경, 이름·주석만)
- 호출부는 처음부터 **GEO 총점**을 넘겼는데 이름·주석은 SoV 기준이었다(값은 맞고 이름이 거짓).
  SoV 는 총점에서 **10점 배점뿐** → 이름을 믿고 SoV(0~100%)를 넣으면 **등급 전면 오류**.
  같은 SoV↔총점 혼동이 **과거 OG 이미지를 이미 한 번 깨뜨린 전례**가 있다(결함감사 §OG).
- 임계값 **76/51/26 이 3곳 중복**임을 주석으로 못박음 — ①화면 ②`lead/route.ts` `tierLabel`(메일)
  ③`og/route.tsx` `tier`(공유이미지). **현재 3곳 일치 확인.**

**3) 🔬 5번(카피 정합) 점검 결과 = 문제 없음** — "GEO 점수 N점" 카피는 **전부 계산값**이고
화면·OG·메일 3곳이 `geoAxisScores` 단일 진실을 쓴다. 랜딩 사전(ko.json)·PDF 에
`"평균 N점"`·`"상위 N%"` 같은 **고정 숫자 약속 0건**. → **F11 여파 카피 수정 불필요.**

### 🔬 자기정정 1건 (세션M-3 교훈 적용 결과)
"SoV 눈금을 총점에 적용해 등급이 박하다"고 판단했으나 **실측이 반박**.
이론만점 99 · 인지100%+감성절반긍정 = **80점 → 리더 도달 가능**. 눈금은 정상.
→ **임계값은 손대지 않았다.** F11 점수하락(3~11점)으로 등급이 박해진 건
부작용이 아니라 **변별력 확보의 결과**일 수 있어 제품 판단 사항으로 남김
(실고객 0명이라 정답이 데이터에 없음).

### ⬜ 다음 세션 (우선순위)
1. 🚀 **www 배포**(커밋 `228d2d1` 미배포) → 이후 측정부터 판정 분포가 쌓인다
2. 🔬 **분포 확인 후 4번 재판단** — 로그 `mention.verdict.distribution` 조회
   - `different_entity` 유의미 → UI 분기(오인 배지 + **원문 노출**. 지금은 미언급이면 excerpt 를 가림)
   - **0 에 가까움 → 진짜 문제는 UI 가 아니라 게이트** → `needsVerification` 확장 검토
     ⚠️ 단 이건 **측정 변경** = 원가·시계열 영향 → 별건으로 다룰 것
   - ⚠️ 관측은 **신규 측정부터만** 쌓인다(기존 71건 소급 안 됨). 실고객 없으면 관측도 빈 채로 남는다
3. 👤 **P0 실고객 10~15명**(불변 1순위)
4. 🖥️ F2 both 반쪽측정 · F5 나머지 언어↔엔진 서브셋 · lint 51건(audit-result 32건)

### 💳 카카오페이 심사 — 2차 보완 회신 완료 (2026-08-04)

**심사 상태: 🟡 보류 → 3건 대응 완료·회신 발송. 포트원·카카오페이 답변 대기.**

| # | 심사관 2차 요청 | 대응 |
|---|---|---|
| 1 | 테스트 계정이 **인증번호 때문에 로그인 불가** | ✅ **Clerk `Client Trust` OFF** + 실측 검증 |
| 2 | *"**정기결제로 접수**됨 → **단건결제 추가는 포트원**으로 문의"* | ✅ 아래 §정정 — **실제는 반대**였다 |
| 3 | 전자계약서 **반려** → 재제출 | ✅ 사용자 재제출 완료 |

**🔴 §정정 — 접수 상태는 심사관 말과 반대였다 (2026-08-04 실측 종결)**

| 출처 | 주장 | 판정 |
|---|---|---|
| 카카오페이 심사관 | "**정기결제**로 접수됨 → 단건 추가하라" | ❌ **틀림** |
| 포트원 콘솔 캡처 | 카카오페이 1건 = **"간편결제 일반결제"** | ✅ 맞음 |
| 포트원 CS 회신 | *"포트원을 통해 이미 접수된 **일반결제** 외, **정기결제를 추가 접수**해주시면 됩니다"* | ✅ **확정** |

→ **실제: 일반결제 접수됨 · 정기결제 미접수.** 사용자가 *"일반결제도 담았던 것 같은데"* 라고
의심해 콘솔 확인 → 포트원에 되물어 확정. **추측으로 신청했으면 중복 접수로 3~4주 날렸다.**
(교훈: 심사관·상대방 말도 **전제로 신뢰하지 말고 콘솔·1차 출처로 교차검증**. 세션N-2 교훈과 동일)

✅ **정기결제 추가 접수 완료(8/4)** — 콘솔 카카오페이 **진행중 2건**:
   ①간편결제 **일반결제** = **입점 심사중** ②간편결제 **정기결제** = 신청 접수 및 계약 진행 중
   신청 시 입력한 서비스 기간 = **"결제 즉시 이용, 월간 구독 30일 자동갱신"**
   (근거: 이용약관 *"결제 즉시 효력이 발생"* — legal `[slug]/page.tsx`. 환불=결제일 7일 내 미사용분)
✅ **카카오페이 3차 회신 발송(8/4)** — 접수 상태 정정 + **"PG사에 직접 접수된 정기결제 건이
   남아있는지 확인" 요청**(포트원 정책: 직접 접수분은 취소 후 포트원 경유로 재접수해야 함)

**⏱️ 심사 기간**: 포트원 안내 **1건당 3~4주 · 두 건 각각 별도 진행**(서로 안 막음)
👉 **단건·정기 둘 다 필요**(사용자 확인: 일회성 상품 + 월 구독) → **채널 2개 = 채널키 2개.**

**1번 원인 = Clerk `Client Trust`** (우리가 켠 MFA 아님 — Clerk 가 2025-11 자동 활성화)
- credential stuffing 방어. 발동 조건 **3개 동시**: ①비밀번호 로그인 ②MFA 미설정 ③**새 기기**
  → 심사관은 항상 새 기기 → 코드 요구 → 그 메일이 **우리 메일함**으로 옴 = 영구 차단
- 증상: `/sign-in/factor-two` + "You're signing in from a new device." + 6자리 코드칸
- 조치: **Clerk Dashboard → Configure → Protect → Rules → Client Trust → Manage → 비활성**
  (⚠️ **Production 인스턴스**에서 수행)
- ⭐ Client Trust 는 **비밀번호 로그인에만** 적용 → 소셜/매직링크는 애초에 발동 안 함
- 유지된 방어: Lockout policy · Bot sign-up protection · User enumeration protection
- **실측(끈 뒤)**: 로그인 시 코드 없이 대시보드 ✅ → `/billing` ✅ → [Starter 시작하기] →
  **결제창 정상**(카카오페이·토스페이·카드 9종 · `Findable starter 월 구독` 99,000원) ✅

📕 SoT = `_카카오페이_심사보완/` (git 제외 — 테스트 계정 PW 평문 포함)
   `회신메일_2차_문안.md`(발송본과 동일) · `포트원_단건결제_추가문의.md`

**🖥️ 채널키 2개 받으면 착수**(설계·법령 조사 완료, 재조사 금지)
1. 포트원 콘솔 채널 2개 등록 → env 분리(`..._CHANNEL_KEY` 단건 + **`..._CHANNEL_KEY_BILLING` 신규**)
2. 정기 = `requestIssueBillingKey({billingKeyMethod:"EASY_PAY", issueName(카카오페이 필수)})`
3. 빌링키 저장 → 첫 결제 `POST /payments/{id}/billing-key` → 다음 달 `POST /payments/{id}/schedule`
   (**포트원이 스케줄 제공 = 월간 크론 불필요**) → 웹훅 `Transaction.Paid` 로 재예약(체인)
4. 실패 재시도 = **포트원 콘솔 "리커버리"** 켜기(직접 구현 불필요)
5. **웹 해지 UI** ⚖️전자상거래법 제5조 제4항(웹 가입시켰으면 웹 해지 의무)
- 🔴 함정: ①웹훅 body JSON 파싱하면 서명검증 실패(`await req.text()` raw) ②**해지 시 예약 취소
  누락 → 리커버리가 계속 청구 = 무한 과금** ③웹훅 재전송 5회 → 멱등 필수

**🔴 심사 통과 후 반드시 되돌릴 것**
1. **Clerk `Client Trust` 재활성화**(같은 경로 Enable. 재활성 전파시간·기존 신뢰기기 처리는
   Clerk 문서에 명시 없음 → 켠 뒤 한 번 검증)
2. 심사용 계정 `kendrick+pay@indigochild.kr` **비밀번호 변경 또는 삭제**(메일에 평문 노출됨)
3. 라이브 채널키 교체 + **재배포 필수** → 소액 실결제 1건 → plan 자동부여 확인

> ⚠️ **세션N-2 교훈**: 투두에 적힌 *"리스크 낮음"* 을 **검증 없이 신뢰했다가** 착수 방향이
> 뒤집혔다. 문서의 **결론**이 아니라 **전제**를 코드로 확인할 것. 세션M-3 교훈 ②
> (표본 1로 일반화 금지)를 이번에 **하마터면 그대로 반복**할 뻔했다.

---

## (이전) START HERE (2026-08-03 세션N: 카카오페이 심사 보완 + 결제 동선 복구)

**🔴 이번 세션의 최대 발견: 결제창이 아예 안 뜨는 상태였다(실고객 결제 불가). 원인 제거·배포 완료.**

### ✅ 완료 (커밋 3건, web·app 모두 배포)

**1) `658a12e` 결제 동선 복구**
- **www 요금제 유료 3티어 CTA 가 `/contact`(상담)으로 가서 결제 입구가 막혀 있었다.**
  결제 코드는 앱에 완성인데 입구만 없던 상태 → `app.findable.co.kr/billing` 로 연결.
  `Tier` 에 `app?: boolean` 플래그 추가(하드코딩 아님, `env.NEXT_PUBLIC_APP_URL` 사용). 한/영 6곳.
- **www `/[locale]/checkout` 데모 페이지 삭제.** 고아 페이지(진입 링크 0건)·로그인 밖이라
  결제자 uid 를 몰라 plan 부여 불가 = "결제는 되고 플랜은 안 붙는" 위험 + 화면에 테스트
  카드번호(4242…)·"환경변수 미설정" 경고 노출. 세션E-3 앱 이관 후 남은 잔재.
- `checkout/success` 의 폐기된 링크 → `/pricing`. `api/payments/verify` 는 레거시 표시(호출부 0).
- 푸터에 **호스팅서비스 제공: Vercel Inc.** 추가(전자상거래법 시행령 제11조의4, 기존 누락).

**2) `cbd5156` 🔴 COEP/COOP 완화 — PortOne 결제창 차단 해소 (핵심)**
- 증상: `/billing` 결제 버튼 → `[PortOne] Failed to load window.PortOne`
- 원인(실측): nosecone 기본값이 cross-origin isolation 을 켬(`COEP: require-corp` +
  `COOP: same-origin`) → PortOne SDK 가 주입하는 `cdn.portone.io/v2/browser-sdk.js` 가
  `ERR_BLOCKED_BY_RESPONSE.NotSameOriginAfterDefaultedToSameOriginByCoep` 로 차단.
  **CDN 은 정상(200/241KB) — 우리 헤더가 막던 것.**
- 조치: `crossOriginEmbedderPolicy: false` + `crossOriginOpenerPolicy: same-origin-allow-popups`
  (PortOne 결제창은 IFRAME·POPUP·REDIRECTION 3방식 → 팝업 opener 통신 필요.
   `unsafe-none` 전면해제 대신 최소 완화값). 나머지 보안헤더 기본값 유지.
- ⚠️ `packages/security` 는 **web·app 공용** → 두 앱 모두 재배포 필요.
- 검증: SDK `[200]` + `window.PortOne=object` · 결제창 실제 표시 확인(카카오페이·토스페이·카드 9종)

**3) `9da3850` `_카카오페이_심사보완/` git 제외**(테스트 계정 PW 평문 포함. 과거 커밋 유출 0건 확인)

### 📮 카카오페이 심사 — 보완자료 회신 발송 완료 (2026-08-03)
- 심사관(김지은) 요청 = **결제 경로 캡처 + 테스트 계정**. PPT 7슬라이드(캡처 6장)+메일 회신.
- 심사용 계정 = `kendrick+pay@indigochild.kr`(PW 는 `_카카오페이_심사보완/회신메일_문안.md`).
  ⚠️ 심사 종료 후 **비밀번호 교체 권장**.
- ⚠️ 결제창 캡처에 "실제 결제가 안되는 테스트입니다" 배지 노출 → 가리지 않고 메일에
  테스트 채널 사유를 명시(자료 위조 회피). PG 심사에서 통상 이대로 통과.

### 🔴 정기결제 = 다음 최우선 (사용자 지적으로 방향 정정)
- **자기정정**: 처음에 "정기결제 운영하지 않습니다"로 회신하려 했다 → 사용자 지적
  *"B2B 서비스고 우리는 구독제할 때 돈 버는 건데"*. **월 구독이 본질이고 신청서에 정기결제
  특약도 동의한 상태**라, 그 문구는 **사업모델 축소 신고**였다(단건 전용 승인 시 정기결제
  재심사 필요). → "정기결제가 주요 방식 + **정기결제용 CID 발급 요청**"으로 교체.
- **🔑 게이트 = 카카오페이 정기결제용 CID.** 포트원 공식 문서 원문 확인:
  *"카카오페이는 일반결제용 상점아이디와 정기결제용 상점아이디가 **각각 발급**됩니다"*
  (테스트 CID 도 일반 `TC0ONETIME` / 정기 `TCSUBSCRIP` 로 분리). **단건 채널키로는 빌링키
  발급 불가** → 콘솔에 채널 2개 등록 + env 에 정기결제 채널키 추가 필요.
- **구현 설계는 조사 완료**(재조사 금지) → 📕`_카카오페이_심사보완/회신메일_문안.md` 하단.
  요약: `requestIssueBillingKey({billingKeyMethod:"EASY_PAY", issueName(카카오페이 필수)})`
  → 빌링키 서버 저장 → 첫 결제 `POST /payments/{id}/billing-key` → 다음 달
  `POST /payments/{id}/schedule`(**포트원이 스케줄 제공 = 월간 크론 불필요**) →
  웹훅 `Transaction.Paid` 로 다음 달 재예약(체인) → 실패는 **콘솔 "리커버리" 기능**.
- **🔴 함정 3개**: ①웹훅 body 를 JSON 파싱하면 서명검증 실패(`await req.text()` raw 필수)
  ②**해지 시 예약 취소 누락 → 리커버리가 계속 청구(무한 과금 사고)** ③웹훅 재전송 5회 → 멱등 필수
- **⚖️ 법적 의무**: **웹에서 해지 가능해야 함**(전자상거래법 제5조 제4항 — 웹 가입시켰으면 웹 해지) ·
  무료→유료 전환 14일 전/가격인상 30일 전 동의(제13조 제6항, 위반 시 영업정지 3개월 또는 과태료)

### 📌 참고: 통신판매업 신고 — **지금은 불필요**(재조사 금지)
- 「통신판매업 신고 면제 기준에 대한 고시」(공정위고시 제2020-11호) 제2조 제1항 제1호
  **직전년도 거래 50회 미만** → 실매출 0 이므로 면제(제2호 간이과세자와 OR 조건).
- 푸터에 **신고번호 넣지 말 것** — 제10조 표기 목록에 없고(신고번호 의무는 제13조=신고한 경우),
  없는 번호는 허위 표시. 카카오페이 필수서류(사업자등록증·정산계좌·신분증)에도 없음.
- 🔴 **연 거래 50회 초과 + 간이과세자 아님 → 이듬해 신고 의무**(미신고 3천만원 이하 벌금).
  신고 시 **구매안전서비스 이용확인증은 PG사 발급** → PG 계약이 선행. 등록면허세 매년 발생.

### ⬜ 다음 세션 (우선순위)
1. 👤 **카카오페이 심사 회신 대기** → 정기결제용 CID 받으면 🖥️ 빌링키 구현 착수(위 설계대로)
2. 👤 심사 통과 시 **라이브 채널키 3개 교체**(`findable-app` Vercel env) → **재배포 필수** → 소액 실결제 1건
3. 👤 **P0 실고객 10~15명** (불변 1순위, 단일 장애점)
4. 🖥️⭐ **판정 사유를 화면에 드러내기** (측정 무변경 = 시계열 연속, 리스크 낮음)
   - **문제**: 화면이 `"당신을 모름"` 으로만 말한다. 실상은 훨씬 강한 판매 근거 —
     하이퍼클로바는 클로드를 **"네이버가 만든 AI"**라고 답한다(= 잠재고객이 **경쟁사 제품으로 오인**).
     "노출 없음"이 아니라 **브랜드 안전성 리스크**다.
   - **할 일**: ①`mention-verdict.ts` 판정 사유가 result 에 저장되는지 먼저 확인(없으면 저장 추가)
     ②진실의 거울 문구를 사유별 분기(**다른 대상 착각**+원문인용 / 정말 미인지 / 되묻음)
     ③⚠️측정 로직은 건드리지 않는다(표시 레이어만).
   - 📕근거 = `구조감사_언어축_2026-08-02.md` §F12
5. 🖥️ **랜딩·OG·메일·PDF 카피 정합** — F11(인지도 빈도화)로 점수가 3~11점 내려갔다.
   `"GEO 점수 N점"` 류 카피가 화면과 어긋나는지 점검.
6. 🖥️ 세션M-3 잔여 = **F5 후 재측정으로 언어별 데이터 확보**(그 전엔 시장분해 UI 복원 금지)
   - 갈리면 → `MarketRegionCards` 를 **언어축**으로 재작성 + `SHOW_MARKET_CARDS=true`
   - 안 갈리면 → 시장 분해 폐기, 기존 **`Naver × Global AI` 카드만 유지**
   - 💡그 카드가 이미 잘 만들어져 있다(언급률 표시·주범 엔진 지목·0을 "기회"로 프레이밍)
     → 새로 만들지 말고 **확장**하는 방향
7. 🖥️ **F2 both 반쪽측정** — ⚠️원가 재계산 동반(`FREE_AUDIT_AVG_COST_KRW`·일일예산).
   단 F5로 28→22회로 줄어 여유 생김 → 실제 증가폭 계산 후 결정.
8. 🖥️ lint 51건 정리(전부 기존 오류·스타일. `audit-result.tsx` 32건 집중. tsc 는 0)

> ⚠️ **세션M-3 교훈 (착수 전 필독)**: 이 세션에서 **자기정정 5건**이 났다
> (분해축 promptLang→엔진권역→**둘 다 오답**·F11과장·F6미실행·프롬프트병기 철회).
> 공통 원인 = **숫자가 갈리면 맞다고 착각**한 것. 규칙:
> ①코드 짜기 전에 **"고객 입장에서 말이 되나"** 를 소리 내어 확인
> ②**표본 1로 일반화 금지**(claude.ai 는 최악의 예외 케이스였다)
> ③실측이 예상과 같게 나오면 **왜 같은지 이유를 캘 것**(40/40/40의 원인은 F5였다)

---

## (이전) START HERE (2026-08-03 세션M-3: 시장축 오답 롤백 + F5 수정)

**🔴 세션M-2 의 "시장 분해"는 축이 틀려 롤백했다. 반드시 먼저 읽을 것.**

**사용자 지적**: *"국내 중심이면 네이버·다음 AI만 측정하는 거냐? 한국 사람도 ChatGPT 많이 쓰는데."*
→ **맞다.** 권역을 **엔진 국적**으로 갈라서, "국내 중심"을 고르면 **한국인이 가장 많이 쓰는
ChatGPT 가 점수에서 빠지는** 상태였다.

**올바른 축 = 질의 언어** (엔진 국적 아님)
- 한국 시장 = **한국어로 물었을 때** 7 엔진 전부의 답 (ChatGPT 포함)
- 글로벌 시장 = **영어로 물었을 때** 글로벌 AI 의 답

**왜 오답에 도달했나 (같은 실수 반복 금지)**
언어축 프로토타입이 40/40/40 → "언어축 무효"로 결론내고 엔진 국적으로 갈아탔다.
**똑같이 나온 이유를 캐지 않은 게 실수**다. 원인은 F5(모든 질문이 7엔진 전부로 감)였고,
축이 틀린 게 아니라 **측정이 고장나 있던 것**. 엔진 국적은 숫자가 87 vs 0 으로 갈려
"맞다"고 착각했다. **숫자가 갈리는 것 ≠ 의미가 맞는 것.**

**✅ 세션M-3 조치**
1. **롤백 배포 완료** — 결과페이지 시장 카드(`SHOW_MARKET_CARDS=false`) · 앱 타깃시장 선택칸 제거
   (runner 의 `regions[]` 적재는 유지 = 데이터는 남기되 노출만 차단)
2. **F5 수정 완료·배포** — 영어 질문을 한국 검색엔진에 보내지 않는다
   - 근거 실측(Tracking): **daum 한국어 71%(5/7) vs 영어 0%(0/2)**
   - 한국어 → 7 엔진 / 영어 → 글로벌 4 엔진
   - **동반 수정**: `promptStats` 고정폭 슬라이싱 → 누적 오프셋
     (시뮬레이션: [7,7,4,4]일 때 고정폭이면 **3건 오귀속**. 감사가 경고한 함정을 실제로 피함)
   - 부수 효과: both 원가 **28회 → 22회**

**🚀 다음 (세션N) — 순서 중요**
1. ⚠️ **F5 배포 후 재측정으로 언어별 데이터 확보** — 기존 데이터는 **영어 프롬프트 전체 2개**라
   언어축 유효성을 증명할 표본이 없다. **증명 전 시장 분해 UI 복원 금지.**
2. 언어축으로 시장 분해 재설계 → UI 복원
3. F2 `both` 반쪽측정(⚠️원가 재계산) · 랜딩·OG·메일 카피 정합(점수체계 변경 여파)
4. L-3 대시보드 IA · L-2 · L-5 · daum 질의무관 필터 · costKrw · UPSTASH/ARCJET
5. 👤 **P0 실고객 10~15명(불변 1순위)** · 카카오페이

## (이전) START HERE (2026-08-03 세션M-2: 시장 분해 + 점수 체계 변경) ⚠️시장축 부분은 위에서 롤백됨

**✅ 세션M-2 완료 (커밋 8개, www 3회·app 1회 배포 READY·project.json 원복 확인)**

**발단**: 사용자 *"모두가 글로벌 브랜드 및 기업은 아니다"* — 국내전용 병원·학원·B2B에
"글로벌 0점"을 보여주면 안 된다. SEO 20년 선례 조사 후 설계 확정.

**🎯 확정된 설계 = "시장 = 점수의 분모"** (Semrush 직수입)
📕`타깃시장선언_SEO선례_2026-08-02.md` **재리서치 금지**
- 업계는 시장을 **가중치가 아니라 분모**로 다룬다. 선언 안 한 시장은 분모에서 빠져
  **0이 산출될 수 없다**(감점이 아니라 비해당). Ahrefs 는 국가별 API 에서 DR 을 아예 제외.
- **0과 N/A 구분이 핵심.** 0="했는데 실패", 미표시="우리 시장 아님".
  기존 제품의 문제 = **N/A 를 0 으로 렌더링**하던 것.
- **단일 시장 고객은 숫자 1개 그대로.** 통합점수가 사라지는 게 아니라 **이름이 붙는 것**
  (`GEO 62` → `한국 시장 62`). 오히려 글로벌이 분모에서 빠져 **점수가 올라간다.**

**✅ 구현 완료**
1. `packages/audit/market-scope.ts` 신설 — 권역 분리·시장 추정·분모 필터
2. runner → `result.marketScope`·`regions[]` 적재(기존 순수 함수 재호출, 새 채점 로직 0)
3. 결과페이지 **시장별 점수 카드**(라벨 의무·비선언 숨김·업셀은 카드 밖 하단)
4. 앱 브랜드 폼 **타깃 시장 Select** + `Brand.marketScope`(DB enum 추가) + 승계 배선
5. **F11 점수체계 변경** — recognitionRate 고유엔진→**응답빈도**
6. **F7** 한/영 프롬프트 등가화 · **F10** PDF 측정언어 표기

**📊 실측 효과 (통합 1개 숫자가 가리던 것)**
| 브랜드 | 기존 | 변경 후 |
|---|---|---|
| SK하이닉스 | 68점 | **한국 41(언급54%) · 글로벌 70(언급100%)** |
| 나이키 | 70점 | 한국 62(93%) · 글로벌 71(100%) |
| 해경 | 68점 | 한국 47(64%) · 글로벌 69(100%) |
→ "국내 AI가 우리를 모른다"는 **실행 가능한 진단**이 처음 산출됨.

**🔬 이번 세션의 자기정정 3건 (가설이 실측에 진 사례 — 같은 실수 반복 금지)**
1. **분해축**: "promptLang 으로 나눈다"(문서에 적어둔 처방) → **무효**. 프롬프트가 언어와
   무관하게 7엔진 전부에 나가 슬라이스 구성이 동일(40/40/40). 실제 축은 **engineId 권역**.
2. **F11 "게이트 항상 1.0"** → 과장. nvidia 0.71 존재. 다만 "빈도 정보 소실"은 유효.
3. **F6 "daum 이 공짜 점수"** → **실측 반박**. daum 59%로 **전 엔진 최하위**
   (chatgpt 100·hyperclova 90·naver 85). 게다가 그 보정은 P0-c 의 의도된 설계.
   → **F6 미실행 결정.**

**🚀 다음 (세션N)**
1. F2 `both` 반쪽측정 — ⚠️**원가 2배**(28→56 호출) → `FREE_AUDIT_AVG_COST_KRW`·일일예산 재계산 동반
2. F5 나머지 언어↔엔진 서브셋 — ⚠️`promptStats` 슬라이싱(runner.ts:265-277) 동반 수정 필수
3. L-3 대시보드 IA + Sentiment · L-2 진실거울 이식 · L-5 액션 동선
4. daum 질의무관 필터 · costKrw 컬럼 · autoRefreshHours · UPSTASH/ARCJET 키
5. [큰건] 반복측정 n=5~7 · SoV 분모 경쟁사 대비 · unaided 프롬프트
6. 👤 **P0 실고객 10~15명(불변 1순위)** · 카카오페이 · 브라우저 확인(시장 카드·시장 선택칸)

**⚠️ 세션M-2 함정**
- **Prisma TCP 가 sandbox 에서 막힘(P1001)** → enum·컬럼 추가는 **neon HTTP 드라이버로
  `CREATE TYPE`/`ALTER TABLE`** 직접 실행. `prisma db push` 는 `--skip-generate` 플래그도 없음(v7).
- `BrandLanguage` 와 `MarketScope` 는 **축이 다르다**(측정 입력 vs 점수 분모). 혼동 금지.
- 점수 체계가 바뀌었으므로 **랜딩·OG·메일의 "GEO 점수 N점" 카피가 화면과 어긋날 수 있음** → 점검 필요

## (이전) START HERE (2026-08-02 세션M: 업종편향 + 언어축 P0)

**✅ 세션M 완료 (커밋 `4242926`, www·app 배포 READY·project.json 원복 확인)**

**발단**: 사용자 질문 — *"한국에서만/미국에서만이 아니라 **전체적으로 자사 브랜드의 GEO 파워**를
키우자는 것인데 어떻게 분류·해결·제시해야 하나?"* → 이 질문이 업종편향보다 큰 구조 문제를 드러냄.

**① 업종 편향 = 3중 원인이었음(프롬프트 하드코딩만이 아니었다)**
- 🔴**실측**: AuditJob **71건·Brand 7건 전부 `industry=null`** — route 가 zod 로 받기만 하고
  **저장 안 함** + crew-runner 가 **조회도 전달도 안 함** + 프롬프트에 K-뷰티 하드코딩.
  → crew 는 업종을 알 방법이 **원천적으로 없는** 상태에서 K-뷰티 예시만 보고 추론했고,
  "소비재 커뮤니티" 틀을 유지한 채 서브레딧만 반도체용(r/buildapc)으로 바꿔 끼웠다.
- 수정: `packages/ai/lib/industry-profile.ts` 신설(명시→사전→LLM→미확인 4층, **audience
  b2b/b2c/mixed 축 병행** — 반도체는 b2b_saas 가 아니라 제조 B2B) · crew 3개 에이전트에서
  업종 고유명사 전량 제거→런타임 주입 · **channel enum 확장**(기존이 소비재뿐이라 B2B 액션을
  도출해도 담을 칸이 없었음) · 배선 4곳 · 앱 업종 수정 UI · DB enum `manufacturing` 추가(적용완료)
- 검증: SK하이닉스→제조·산업재/B2B, **"뷰티·패션 커뮤니티"가 금지채널로 프롬프트에 명시**됨

**② 언어축 P0 3건** (📕`구조감사_언어축_2026-08-02.md`)
- **F8 캐시가 언어를 무시** — 캐시 키가 domain 뿐. **라이브 실측으로 오배급 확인**
  (medicube·sulwhasoo·roundlab 이 ko/both 혼재 → ko 요청에 both 결과 배급).
  리드 metadata 엔 "요청한" 언어가 적혀 **CRM 까지 오염**되고 있었음.
- **F5** HyperCLOVA 시스템 프롬프트가 `query.language` 무시하고 한국어 고정(글로벌 어댑터는 분기)
- **F9** crew-runner 가 language 를 SELECT 하고 안 넘기는 **dead read** → ko 전용 측정에도
  영문 담당 Alex 실행 = **업종편향 재발 경로**. 언어별 분석가 분기 + 중복카운트 dedup
  (엔진 7개인데 LLM 에 "12개"로 전달되고 있었음)

**🔴 남은 구조 문제 (미해결 — 다음 세션 핵심)**
- **F1/F3 언어 분해 부재** = 사용자 원 질문의 답. 실증: **한국에서만 보이는 브랜드와
  해외에서만 보이는 브랜드가 5축 전부 동일한 39점**. 어느 시장이 약한지 산출 불가.
  최소 경로 = `flat` 에 promptLang 부착(runner.ts:211-220 이 **이미 태깅 중**) →
  aggregateAudit·geoAxisScores **기존 함수 3회 재호출**(새 로직 0) → UI.
- **F2 both = 각 언어 반쪽 측정**("권장"인데 한국어 4개 중 2개만). ⚠️수정 시 원가 2배 → 예산 재계산
- **F6 엔진 가중치 전무**(daum=검색스니펫·LLM아님인데 chatgpt와 1표 동일, daum만 브랜드명
  별도검색 보너스). ⚠️지금 바꾸면 **기존 시계열 끊김** → P0 실고객 검증 후로
- **F7 한/영 프롬프트 비등가**(ko[0]="X 추천해줘"=전제 vs en[0]="What is X?"=개방형)
- F5 나머지: 언어↔엔진 서브셋 분리는 `promptStats` 슬라이싱(runner.ts:265-277)이 깨져 보류

**🎯 시장별 점수 표시 전략 = 사용자와 재논의 중(미확정)**
- 리서치 결론: **통합 GEO 점수 공식을 발행한 툴이 업계에 0곳**(전부 "필터로 쪼개보기").
  근거 = 📕`지역언어축_경쟁사_리서치_2026-08-02.md` **재리서치 금지**
- 학술: **언어가 브랜드 정체성보다 18배 큰 변수**(언어 26.5% vs 브랜드 1.5%, arXiv 2607.13304)
- ⚠️**사용자 지적(반영 필요)**: *"모두가 글로벌 브랜드는 아니다"* — 국내전용 병원·학원·B2B 는
  영어권 0점이 정상. **타깃 시장 선언** 개념 필요. GSC·Ahrefs·Semrush 등 SEO 선례 조사 미완(재실행 필요)

**⚠️ 세션M 함정**
- `prisma db push` 는 **`--skip-generate` 플래그 없음**(Prisma 7). 또 sandbox 에서 Prisma TCP 가
  막혀 P1001 → **enum 추가는 neon HTTP 드라이버로 `ALTER TYPE ... ADD VALUE`** 로 적용함
- **app 로컬 빌드 실패는 Clerk env 부재 탓, 변경 전에도 동일**(코드 무관). web 은 빌드 통과
- ultracite 가 `apps/app/app/(authenticated)/...` 경로를 괄호 때문에 못 읽음 → **해당 디렉터리로 cd 후 실행**
- 검증 hook 의 "generateObject removed in v6"·"모델 슬러그 하이픈"은 **오탐**
  (설치본 ai@6.0.170 에 generateObject 실재 확인 · Letsur 슬러그는 하이픈이 정상)

## (이전) START HERE (2026-08-02 세션L 종료: 전환장치·원가방어4층·전수감사v2)

**✅ 세션L 완료 (커밋 8개, www 2회+app 1회 배포 전부 READY·project.json 원복 확인)**
1. **L-1 확정(로그인 강제 ❌)** + 가입유도 4장치 `342a361` — A소유권(emailMasked·"같은 주소로 가입"), B시점vs추세, C약점앵커(EngineGapCta), D액션티저(처방 1건 공개+건수)
2. **어뷰징·원가 리서치** `161a54a` — 📕`무료진단_어뷰징_원가방어_리서치_2026-07-31.md` = **재리서치 금지**. 메모리 [[reference_findable_free_tier_cost_defense]]
3. **원가방어 4층 완성** `495d7dc`+`a378693`+`e3a3fa3` — ⓪BotID(www curl→**403 라이브 실측**·봇 원가0·라우트3개 보호·instrumentation-client 등록) ①도메인캐시24h(이메일우회 무력화, 이메일50개 공격 12,500→250원=98%절감) ②IP당 신규도메인 2건/24h(env `FINDABLE_IP_DAILY_NEW_DOMAINS`, IPv6 /64묶음, 캐시히트·실패는 쿼터 제외) ③전역 일일예산 5만원=200건(env `FINDABLE_DAILY_FREE_BUDGET_KRW`) → **최악 하루지출 5만원 고정**
4. **캐시 투명성** `bf08a97` — MeasuredAtNotice("N시간 전 측정"·10분내 미표시) + 재측정 링크는 app으로(www로 보내면 캐시가 같은 결과 반환=죽은버튼)
5. **결과페이지 전수감사 v2** `ddecd99` — 📕`결과페이지_전수감사_2026-08-02.md`. 사용자 8건+자체 3건 수정: 규모 자동인식(inferBrandSize: SK하이닉스→large)·갭카드 엔진별 칩+"언급"용어·actions.ts 마크다운 제거·**app /actions 이메일 폴백**("나머지 N건" 약속 이행)·briefingPrompt 저장+표기·업셀 3분기 카피·dataGaps 접힘·액션 IA(티저를 진실거울 직후로+출처 명시)·경쟁사 한/영 alias 병합(BRAND_KEY_ALIASES, 세션J 회귀 24종 충돌0)

**⚠️ 세션L발 함정 (신규)**
- **BotID가 프로덕션 curl을 차단** — `/api/audit`·`*/crew`·`*/briefing` POST는 **브라우저 폼 제출로만** 라이브 테스트 가능. GET은 curl OK.
- briefingPrompt 표기는 **신규 측정부터**(구 job은 일반 안내문 폴백).
- BotID 보호경로 추가 시 instrumentation-client.ts `initBotId({protect})`에도 **반드시 등록**(누락하면 checkBotId 실패).
- `pnpm --filter web add`가 워크스페이스 링크를 끊을 수 있음(@sentry 사라짐) → 이후 `pnpm install`로 복구.

**🚀 다음 세션(M) 투두 — 우선순위 순**
1. 🖥️⭐ **crew 에이전트 업종 편향 수정** — 반도체 회사(SK하이닉스)에 "Sephora·Amazon 리뷰"·"Reddit r/buildapc 댓글" 처방이 나감. `packages/ai`의 crew 에이전트 프롬프트에 소비재 예시 오염. 사용자 지적 ⑥("말이 이상하다")의 진짜 뿌리. **P0 실고객 데모 전 필수.** 업종 감지→예시 분기.
2. 🖥️ **L-3 대시보드 IA + Sentiment(백로그5)** — 히어로 KPI를 Peec 표준 3개(Visibility/**Position**/Sentiment)로. 현재 둘 다 없음. 리서치는 `측정정확도_전면진단_2026-07-31.md` §3에 있음(재실행 금지).
3. 🖥️ L-2 나머지(app 대시보드에 진실거울 이식) · L-5(대시보드→/actions 동선)
4. 🖥️ daum 질의무관 스크랩 필터(전수감사 §C 보류분, "파워 몇으로 갈지" 커뮤글)
5. 🖥️ 소소: costKrw 컬럼(예산 정밀화) · autoRefreshHours 게이팅 재검토(경쟁사 전원 daily=우리 free=null은 열위) · UPSTash/ARCJET 키 부재(문의폼 무방비)
6. 👤 **P0 실고객 10~15명(불변 1순위)** · 카카오페이 **심사중**(통과→채널키 3개 교체=실결제) · 브라우저 확인 2건(SK하이닉스 job 재열람=규모 대기업 자동선택 확인 · 무료진단 이메일로 가입→"실행 액션"에 5건 폴백 확인) · BotID Deep Analysis 켤지(선택, 건당 1.4원)

## (이전) START HERE (2026-07-31 세션K-2 종료: 액션 레이어 신설)

> UX 개선 투두 L-1~L-5는 `_START_HERE_세션L_UX개선.md` 참조(세션L에서 L-1·L-2 일부 완료).

**🎯 세션K-2: "그래서 뭘 하라고?"에 답하는 액션 레이어 신설·배포. 커밋 `4152f67`+`62efdc7`.**
**다음 = ⭐UX 개선(사용자 지시 대기분: ①무료진단 게이팅 ②측정시작 vs 무료진단 중복 ③대시보드 IA 6분할 ④진단페이지 구조) → 그 다음 👤카카오페이·P0 실고객.**

**✅ 세션K-2 완료 — 액션 레이어 (📕SoT=`액션레이어_설계_2026-07-31.md`)**
- **발단**: 사용자 "액션이 굉장히 부실하다. 뭘 하라는 건지 이해도 안 되고 맞는 가이드인지 확인도 어렵다."
  실측 → `buildTopRecommendations()`가 if/else 4분기뿐 → **나이키·Haegyung·5throck 전부 동일 문장**(이름만 교체).
- **근거 확보(GEO 논문 PDF 원문)**: Table1 인용문추가 +41% > 통계 +31% > 출처인용 +27% > 베이스라인 > **키워드스터핑 역효과**.
  **🎯Table2 = 같은 최적화가 1위엔 −30.3%, 하위엔 +115.1%** → 후발주자 무기, **경쟁사 전원 미반영**.
- **신설 5종**(`packages/audit/actions.ts`): 순위전략 · 프롬프트갭 · 출처포트폴리오 · 콘텐츠보강 · **하지말것 명시**(llms.txt·키워드스터핑·스키마맹신 = 업계 차별화). MAX 5개(1위 불만이 "압도적이다").
- **구체화**: 처방에 **실측 도메인·건수** 삽입 → *"AI는 우리 사이트보다 blog.naver.com를 더 많이 보고 있습니다(47건 vs 자사 9건). ①그 채널 확인 ②공식 답 만들기 ③그 채널에 직접 올리기"*. **거짓말 불가·고객이 직접 확인 가능.**
- **루프 닫기**: `ActionCompletion` 모델(additive·db push·라이브확인) + `/actions` 페이지 + 사이드바(**게이팅 없음**).
  완료 체크 → 다음 측정에 **`완료 후 +N%p`** 배지. ⚠️Peec조차 impact 측정이 beta.
- 검증=app·web·audit·database tsc 0·ultracite 0. www·app 배포 READY·`/actions` 307·project.json 원복.
- ⬜남음: 대시보드→액션 동선(사이드바뿐) · www진단과 app액션 분리 · 문장처방 LLM판 · Ghost Citation · 신선도감사.

## (이전) START HERE (2026-07-31 세션K: 측정 정확도 전면 수술)
**🔴 세션K: 사용자 문제제기("동명 브랜드·다른 답변인데 인용됐다 함·무명인데 고점")가 전부 사실로 입증 → 판정·점수·인용 3층 수정·배포. 커밋 `bcd5488`.**
**다음 = ①반복 측정 n=7~10 원가 판단(현재 1회, 측정당 392호출) ②UX 개선(사용자 지시 대기: 무료진단 게이팅·대시보드 IA 6분할·액션 레이어) ③👤카카오페이 심사·P0 실고객.**

**✅ 세션K 완료 — 측정 정확도 (📕SoT=`측정정확도_전면진단_2026-07-31.md`)**
- **실측 결함**: kia.com 28/28 언급 중 **"푸에기아(향수)"·"기아 타이거즈"** 포함 · forget.sh SoV89인데 **4엔진 전부 브랜드 모름**(Claude=BTS노래·Perplexity=영단어 뜻풀이) · 무명(윈디플로96·forget89)이 **Tesla69보다 높음** · **"팩트정합률"이 사실 검증을 전혀 안 함**.
- **수정 4층**: ①`packages/ai/lib/mention-verdict.ts` 신설(4단계 판정·**모호할 때만 LLM**·**객관식+NIL**·실패시 폴백=회귀0) ②runner 집계 **전** 교정(flat/tagged 동시 → SoV·점수·Tracking·표시 단일진실) ③geo-score **인지도 게이트**(0/7 인지=총점 0, 기존 74) ④"팩트정합률"→**"브랜드 인지율"** + 인용 관련성 필터(제외 건수 화면 고지).
- **✅회귀(실LLM)**: 나이키 94→**94(오탐0)** · 엔비디아 62→62(LLM 2회) · 기아 100→**93(향수 차단)** · forget.sh 83→**7**.
- ⚠️**1차 실패 교훈**: 프롬프트를 "이 답변이 브랜드를 인지·설명하는가"로 물었더니 LLM이 "브랜드 **소개글**인가"로 해석 → **나이키 오탐 4건**(문자열 매칭보다 나쁨). **"여기 나온 이 이름이 그 브랜드가 맞는가"**로 좁혀 해소. **경쟁사 나열형 답변도 엄연한 인지.**
- **원가**: 명확 브랜드 LLM 0~2회(+1.5%). 한글 2~3글자·영문 소문자 단어만 전수 판정(+30~40원/측정).
- **업계 실태**: 동명이인 해법 **공개 0곳**. Ahrefs "string matches"·Scrunch "pattern matching"·daydream "exact whole-word matching" **자인**. → 한국어는 겹침 빈도 높아 실질 차별화.
- ⬜**미착수**: **반복 측정 n=7~10**(Evertune 100회·daydream 20회·논문 n=7~8. 단 5회부터 수확체감, 우선순위=언어>모델>프롬프트변형>반복) · SoV 분모 경쟁사 대비(Profound·Otterly) · 프롬프트에서 브랜드명 제거(unaided).

## (이전) START HERE (2026-07-31 세션J: 백로그 3·4 완료·배포)
**🎉 세션J: 경쟁사 SoV 대시보드 + 인용 출처(Mention/Citation 분리) 구현·배포. 커밋 `7b08cb5`.
다음 = ①카카오페이 심사통과→채널키→app env 교체(실결제, 👤대기) ②P0 실고객 10~15명(👤, 단일장애점) ③백로그 5·6·8(Sentiment·주간메일·CSV).**

**✅ 세션J 완료 — 백로그 3·4(경쟁사 SoV + Sources 탭):**
- **🔴 발단(가치 역전 발견)**: `/compare`가 **유료 유저에게 "준비 중입니다" 빈 화면**을 띄우고 있었음. 반대로 **무료 진단 유저는 www에서 경쟁사 벤치마크를 이미 보고 있었음**(`competitor-benchmark.tsx`). 돈 낼수록 덜 보이는 상태 → 신규 개발이 아니라 **죽은 화면 소생**이 실제 과제였음.
- **설계 원칙 = 추가 AI 호출 0원**. 이미 적재된 Tracking(`rawResponse`·`citedSources`)만 재해석. 러너·측정 경로 무변경 → 원가 증가 0.
- **데이터 계층**: `apps/app/lib/db/scoped.ts`에 **`scopedLatestRunTracking()`** 신설 — 최신 측정 1회분만(run 키=brandId+trackedAt). 대시보드 select가 성능상 뺀 rawResponse·citedSources를 여기서만 싣고 take 120으로 페이로드 제한. **org 필터를 2단 쿼리 양쪽에 유지**(brandId URL 위조 차단).
- **집계**: `(authenticated)/lib/analysis-data.ts` 신설. ①경쟁사=`@repo/audit` extractCompetitorLandscape 재사용(www 검증 로직 그대로, org는 excerpt 1500자가 아닌 **전문**이라 표본이 더 큼) ②**Mention(본문 언급) vs Citation(출처 링크) 분리** ③출처 유형 분류(자사·커뮤니티·위키·언론·기타)=한국 GEO 차별화 축 ④HTML 엔티티 디코딩(`&#39;` 노출 해소, 세션G 잔존 이슈).
- **화면**: `/compare` 실데이터 리더보드(순위·SoV·평균노출순위·내 순위, 유료+미측정은 "준비 중" 아닌 **측정 CTA**) · **`/sources` 신설**(Mention/Citation KPI·유형 구성·엔진별 언급vs인용·도메인 목록·**진단 문장**) · 사이드바 "인용 출처"(growth 게이팅).
- **🔧 경쟁사 필터 보강** `packages/audit/competitor-extract.ts`: 실측에서 **서술구가 경쟁 브랜드로 유입**("최고의 쿠셔닝과 일상적인 편안함"·"착용 목적"). 기존 필터는 접미사만 봐서 미탐 → 조사·서술어미(**2어절 이상일 때만** 적용=실브랜드 보호) + 분류명사 규칙 추가. **회귀검증: 실브랜드 31개 통과(오탐 0)·서술구 10개 차단·www 무료진단 4건 결과 무회귀**(공유 파일이라 교차검증 필수였음).
- **라이브 DB 실측 근거**: 나이키=아디다스·뉴발란스·아식스·호카·푸마 정확 추출 · **커뮤니티 50% vs 자사 9%**("AI가 우리 사이트가 아니라 남의 블로그를 근거로 삼음") · **claude는 언급 5/5인데 인용 1건**(Mention/Citation 분리해야만 보이는 진단).
- 검증=app·web·@repo/audit tsc 0·ultracite 0(내 파일). 배포=`readyState:READY`·project.json 원복 확인·라이브 `/sources`·`/compare` 307 sign-in 게이트 정상(라우트 실재 증명).
- 🔬 **남은 검증(👤)**: 로그인 후 실브라우저로 두 화면 렌더 확인(브라우저 필요).
- ⚠️ **알려진 한계**: 인용 데이터에 **질의 무관 기사 섞임**(daum 엔진이 "집 사기"·"Apple TV" 등 반환) — 저품질 인용 소스 필터(감사 §15)와 같은 뿌리, 미해결 잔존.

**✅ 세션I 완료 — 결제 라이브화 착수(포트원):**
- **사업자 정보 www 푸터 표기·배포** — PG 심사 "사업자정보 유무" 요건 충족. `footer.tsx`에 상호(인디고차일드)·대표(나현덕)·사업자등록번호(534-15-01132)·주소·전화(010-8958-2547)·이메일(kendrick@indigochild.kr). 라이브 확인. ⚠️통신판매업 신고번호는 미신고→나중에 추가.
- **카카오페이 온라인 가맹점 신청서 제출 완료** — 업종코드 58222(응용 소프트웨어 개발·공급업)·상품 3개(Starter/Growth/Scale 구독)·수수료 카드 3.2%·정산계좌(나현덕 명의)·정기결제 특약 동의. 심사 대기. 👤파트너어드민(pg.kakao.com) 등록 확인 필요.
- **🔑 결제 코드 = PortOne V2**(`packages/payments/portone.ts`·`apps/app/app/actions/billing/checkout.ts`). PG 무관, **채널키만 교체하면 실결제**. env 3개=`PORTONE_API_SECRET`·`NEXT_PUBLIC_PORTONE_STORE_ID`·`NEXT_PUBLIC_PORTONE_CHANNEL_KEY`(현재 토스 테스트채널). **심사통과→라이브 채널키 발급→이 3개 교체(app Vercel env)→실결제 오픈**(코드 준비완료, 키만).

**⚠️ 결제 인프라 판단(리서치 확정) — PG별 비용/무게:**
- **간편결제(카카오페이·토스페이·페이코·Npay) = 가입비0·연회비0·보증보험0** (진짜 무료, 팔릴때 수수료만). 대형사 자체 리스크관리라 보증보험 불필요.
- **카드 직접 PG(토스페이먼츠·KPN·KCP·이니시스) = 보증보험 필수**(포트원 공식: "PG 계약시 반드시 보증보험, PG별 각각"). 토스페이먼츠=가입비22만+연11만+보증보험(⚠️포트원 가입비면제 프로모션 2024.6.30 종료). KPN=가입비무료지만 인감증명서·실물등기·보증보험. → **지금 매출0에선 카드 직접PG 붙일 이유 없음.**
- **결론: 카카오페이(무료)만으로 시작. 카카오페이 안에 카드결제 포함(MAU 2300만).** 카드 자동결제(빌링키) 필요시 실매출 확인 후 토스페이먼츠. KPN·KCP 신청분은 서류 안내면 2주 뒤 자동취소(방치).
- 🔶 토스페이 = 신청했다 하나 메일 안옴 → 포트원 콘솔서 "작성중/접수완료" 상태확인 필요(무료라 담아두면 좋지만 없어도 카카오페이로 결제됨).
- ⚠️정기결제(빌링키) 코드 미구현 = 카카오페이/토스 정기결제 "결제창 호출 방식"으로 나중에 붙일것(REST API방식 아님=카드정보 직접취급 회피).

**(이전) 🎉 세션H 3건 완료+실브라우저 스모크 통과: ①P5 8-d ②프롬프트 자동 제안 마법사 ③자동 갱신+게이팅(백로그 2·7). + 요금제 tier 정합(Scale 카드·Enterprise 배너)·비홈 헤더.**

**✅ 실브라우저 E2E 스모크 통과(2026-07-30, 사용자+DB교차검증):**
- 마법사 제안 생성(24개)→승인 저장(나이키 5개)→**무료 상한 5 강제**("24개 중 5개" 토스트)→growth 승격(Clerk metadata `{role:admin, plan:growth}` + DB `Organization.plan=growth` 둘 다)→**rate-limit 해제**(유료 24h우회, 나이키 측정중)→**러너가 저장 프롬프트 사용 확정**(나이키 Tracking 5개=마법사 저장분 "러닝화/신발/스포츠화"와 완전일치, 고정4 아님). 마법사→저장→러너 전 경로 닫힘.
- ⚠️게이팅 plan 소스 2곳: rate-limit·프롬프트·브랜드 상한=Clerk metadata(getCurrentPlan) / 자동갱신 cron=DB Organization.plan. 결제 grantPlan은 둘 다 세팅. 테스트는 수동 2곳.
- ⚠️rate-limit "25시간인데 막힘"은 오진이었음: 나이키 실제 16시간 전 측정=정당한 24h 차단(버그 아님). 시각 저장 전부 UTC 정상 확인(start-tracking 수정 시도했다가 불필요 판명·되돌림).

**세션H-3 완료(다시 하지 말 것) — 자동 갱신 스케줄 + 게이팅(백로그 2·7):**
- **게이팅 단일진실** `packages/auth/plan.ts` — `planCapabilities(plan)`→`{promptLimit, brandLimit, autoRefreshHours}`. PLAN_META blurb의 약속숫자를 코드가 강제: free 5·starter 30·growth 150·scale 500·ent 2000 프롬프트 / autoRefresh free=null(수동만)·starter=168h(주간)·growth+=24h(데일리). 업계화폐=추적 프롬프트 수(Peec식).
- **프롬프트 저장 게이팅** `suggest-prompts.ts` — 하드코딩 MAX_SAVE 폐기→플랜 promptLimit 강제(누적기준: 기존 auto Prompt수+이번분). 상한도달=차단+안내. 초과=capped 신호(saved<clean)→UI가 "N/M개 저장·요금제보기" 토스트(⚠️silent truncation 방지). 서버판정(getCurrentPlan, 우회불가).
- **자동 갱신 cron** `apps/web/app/api/cron/auto-refresh-tracking/route.ts` + vercel.json(schedule `0 2 * * *`·maxDuration 300) — 유료 org 브랜드 주기 재측정. ⚠️**cron은 세션없음→plan 진실=DB `Organization.plan`**(Clerk metadata 아님). 경과기준 분기(주간/데일리 같은 cron서 자연분리)·원가보호 MAX_TRIGGERS_PER_RUN=5(오래된것 우선)·진행중 중복skip·AuditJob FK생성후 runAuditJob 직접실행(start-tracking 6~7 cron판). 저장 프롬프트 있으면 러너가 우선사용.
- **안전**: 실 org 2개 다 free→cron dueCount=0·triggered=0(유료 전까지 no-op, 서프라이즈측정·비용 0). 무료 email진단 회귀0. 검증=auth·web·app tsc0·ultracite 전파일0.
- 🚧 배포: web(cron+vercel.json)·app(게이팅) 순차. web 백그라운드 진행중.
- ✅게이팅 3축 완결: promptLimit(suggest-prompts)·autoRefreshHours(cron)·**brandLimit(assign.ts 신규생성 게이트, free1/starter3/growth5/scale+무제한, 기존 그랜드파더링)**. ⚠️실 free org 1개가 이미 7브랜드(내 테스트데이터)=신규추가만 차단·기존유지(정상).
- ⚠️자동갱신 즉시성은 Hobby 하루1회 제약(유료 스케일시 `0 */6 * * *`).
- 🔖 세션H 커밋 5개(f9d89f6 이후): 세션G잔여·P5 8-d·마법사+게이팅+cron·docs·brandLimit. 브랜치=feature/login-branding-2026-07. 워킹트리 클린.

**세션H-2 완료(다시 하지 말 것) — 프롬프트 자동 제안 마법사(표준 백로그 1):**
- **전략 결정**: 측정 반영=「승인분 저장만(1단계)」 · 개수=「고정 ~24개」. 근거=러너 전면전환/플랜차등은 원가·429·tier 3중불일치 리스크 → 게이팅(프롬프트 수=업계화폐)은 백로그 2·7의 러너 파라미터화와 함께.
- **생성기** `packages/ai/lib/prompt-suggestions.ts` — `suggestTrackingPrompts(brandName,domain,lang)`. brand-identity.ts 패턴 복제(generateObject+zod+Letsur haiku+confident 게이트). 브랜드형/경쟁형 균형(P0-a 계승, interleave)·ko/en·상한24. 실패/비확신→정적 폴백(고정4 골격). 경쟁사 후보 dedupe(≤8).
- **서버액션** `apps/app/app/actions/brand/suggest-prompts.ts` — 2동작: `suggestPromptsAction`(생성, 저장X)·`saveApprovedPromptsAction`(승인분만 upsert). 보안=start-tracking 불변식(orgId 세션도출·scopedBrandById로 org소속 검증 후 write). 저장=tracking.ts와 동일 `brandId_text` upsert+`isAutoGenerated:true`, category 힌트→enum(brand→recommendation·competitor→comparison).
- **러너 연결** `packages/audit/runner.ts` — `resolveRunPrompts(brandId,...)`: brandId 있고 저장 프롬프트 있으면 우선(상한 `RUNNER_PROMPT_LIMIT=8`, createdAt asc), 없으면 기존 `generateAuditPrompts` 폴백. **무료 email 진단은 brandId 없어 항상 폴백=회귀0.** ⚠️게이팅 시 이 상수 파라미터화 자리.
- **UI** `apps/app/(authenticated)/features/brand/prompt-wizard.tsx` + brand/page.tsx 배선 — [AI 추적 질문 제안받기]→체크박스 승인(기본 전체선택)→[선택 저장]. StartTrackingButton 패턴(서버액션+sonner+refresh). a11y=label htmlFor+Checkbox id.
- 검증: app tsc 0·ai(신규파일) 0·audit tsc 0·ultracite 5파일 0. ⚠️hook의 generateObject-removed/모델슬러그 경고는 **오탐**(ai@6.0.116이 generateObject 유지, brand-identity.ts가 프로덕션서 사용중, 모델ID=Letsur 검증된 실슬러그).
- **DB 사전확인**: nvidia에 기존 auto Prompt 4개=generateAuditPrompts "both" 출력과 동일(세션E-2 측정이 tracking.ts로 적재)→러너 신동작이 nvidia엔 무변화(회귀0). 마법사 저장분은 upsert 병합, createdAt asc라 레거시4 먼저·이후 마법사분이 남은 슬롯.
- 🚧 배포: 세션H-2 백그라운드 진행중(project.json swap·revert 체인). 라이브 스모크(마법사 클릭→제안→저장→재측정시 저장프롬프트 사용)는 로그인 필요=👤 or 다음 세션.

**세션H-1 완료(다시 하지 말 것) — P5 8-d:**

**세션H 완료(다시 하지 말 것) — P5 8-d 4스텝 전량:**
- ①`history/page.tsx` FK OR 쿼리 추가(page.tsx와 동일 OR: email∪organizationId) → org 측정이 이력에도 노출 ✅
- ②검증 = `apps/app` tsc 0 · ultracite 0 ✅
- ③app 배포 ✅ — `readyState:READY`·`app.findable.co.kr` aliased·project.json 원복 확인(projectId→web). 빌드 로그: 테스트 2/2·Next16 컴파일 71s·정적 19p.
- ④라이브 검증 ✅ — DB실측: 엔비디아(nvidia.com) Tracking **20행·1 run·SoV 85%·5/5엔진**(chatgpt/claude/hyperclova/naver 4/4언급, daum 1/4). 8-d foldTrackingRuns 로직을 실데이터로 재현→raw SQL과 sov·coverage 완전일치. 대시보드 `/`→307 sign-in 게이트 정상, 새 x-vercel-id 서빙.
- ⚠️**run 1개라 추세 차트는 점 1개**(정상, 결함 아님). 선 그래프 원하면 nvidia 재측정 1회 필요(측정≥2회=시계열).
- (이미 세션G에 되어 있던 3파일 재확인 완료: scoped.ts scopedTracking 첫 활성화+`ScopedTrackingRow` · dashboard-data.ts `buildTrackingDashboardData`(run 접기=brandId+trackedAt, SoV=언급행/전체행, 최신 브랜드 스코프, 빈 rows→null) · page.tsx Tracking 우선+AuditJob 폴백+8-b FK OR+`hasData`.)

**⚠️ parity 스크립트 교훈**: `check-app-env-parity.sh`가 `PORTONE_API_SECRET`을 MISS로 오탐(실제 app prod에 14h전 등록·Encrypted 존재, pull값만 빈값이라 스크립트가 오판). Encrypted 키의 빈 pull값을 MISS로 찍는 버그 있음 → 다음에 스크립트 개선 여지.

- git 미커밋 6파일 = 8-d 3개(app) + history/page.tsx(세션H) + 세션G 배포분 2개(web copilot-chat.tsx=마크다운 렌더러·packages/ai naver-briefing-adapter.ts=광고/생HTML/prose 게이트, **www에 이미 배포됨**). 커밋은 사용자 결정.
- 세션G 상세(결과페이지 데모 결함 4건 해소·발표 완료)=메모리 project_findable_master_todo 세션G 섹션.

## (이전) 🧭 START HERE (2026-07-30 세션F 종료)
**⏰ 최우선(사용자 지시): 오늘 15시경까지 발표자료 제작. 어떤 발표인지 세션 시작 시 사용자에게 확인(후보: KAIST PRD 발표 등). 그 후 나머지 투두.**

**세션F 최종 상태: 결과페이지 결함 24건 전량 수정·배포·라이브검증 완료.** 상세=`결과페이지_결함감사_2026-07-30.md`(§1~24). 핵심:
- GEO 채점 `geo-score.ts` 단일진실화 · 매출모델 v2(3요소, 리서치=`매출임팩트_벤치마크_리서치_2026-07-30.md`) · 브리핑 파서/판정/병합 3중 수정 · 4에이전트 게이트(org 허용) · 코파일럿 부활(Letsur 라우팅) · 크루 재시도 · UX slop 제거(아이브로우 29곳·이모지·글로우)
- **🎉 엔진 7개 완전체**: GOOGLE(app 등록)+PERPLEXITY(재발급→app·www 동일키, 옛키 삭제완료). kia.com 28/28 오류0 검증. 실값 백업=apps/web/.env.local
- 🟢잔여: 크루 에이전트 간헐 Letsur 400(재시도로 완화, 1명 실패해도 리포트 생성됨·관찰중) · 브리핑 미노출 검색어시 기사스트립 저장 · 저품질 인용소스 필터 · UX 심화(impeccable 풀패스)
- ⚠️브리핑/크루 재실행법: completed는 API 409 → DB에서 briefingStatus/crewStatus 초기화 후 POST

## (이전) 🧭 START HERE (2026-07-30 세션F: 결과페이지 결함 대청소)
**한 줄 상태: 사용자 문제제기(nvidia 영상)발 결과페이지 결함 17건 전수 감사→코드 수정 전량 완료·배포. GEO 채점=`packages/audit/geo-score.ts` 단일진실화(UI·리드메일·OG·PDF 통일).**

**세션F 완료(다시 하지 말 것)** — 상세=`결과페이지_결함감사_2026-07-30.md`:
- 헤드라인 모순("SoV 85인데 절반 누락")·회복매출 +₩0·"28개 엔진"·경쟁사 오염("그래픽카드 추천")·진실거울 벽글/넘침/오류분모·표 raw 노출·excerpt 하드컷(→4000자 문장경계)·Visibility Gap 카피/고지·매출 규모프리셋 3종·GEO 채점 재설계(감정 중립 무벌점·노출품질 HHI·인지 오류제외)·PDF "112회 호출" 버그.
- ⓪스모크: www Clerk pk_live 확인(accounts.dev 0)·TTFB 0.13s·sign-up 200·contact 실폼. 실브라우저 몫(신규가입 여정·4242 결제·메일 수신)은 👤 잔존.
- **엔진 복구 반전**: gemini·perplexity 키는 **이미 www에 등록돼 있었음**(2일 전, 투두의 "키 없음"은 낡은 정보). 진짜 문제=app에 없어서 org 측정만 Gateway 크레딧 에러. GOOGLE_API_KEY=app 등록 완료(로컬 실값 발견·검증). **👤PERPLEXITY_API_KEY만 app에 재입력 필요**(Sensitive 재열람 불가, 로컬 실값 없음): `cd apps/app && vercel env add PERPLEXITY_API_KEY production`.

## (이전) 🧭 START HERE (2026-07-30 세션E-3 최종)
**한 줄 상태: 실유저 유입 대비 대청소 + 로그인404·성능 근본해결 + P5 8-b + Clerk 키 통일. 커밋 9개·배포 6회, 전부 라이브.**

**세션E-3 후반 추가 완료(다시 하지 말 것):**
- **🔑 로그인 404 근본해결 ✅라이브검증** — 원인=www Vercel env `NEXT_PUBLIC_APP_URL`이 `https://findable.co.kr`(app.누락)→sign-in 링크 www행→/ko/sign-in 404. 값 교정+**web·app env.ts 프로덕션 빌드 assertion**(app. 없으면 빌드 실패=재발봉쇄). 라이브 확인: 링크=app.findable.co.kr/sign-in ✓. (세션C 커스텀API 문제와 별개 건이었음)
- **⚡ www 로딩 근본해결 ✅라이브검증** — 원인=`[locale]` generateStaticParams 부재→전 라우트 매요청 SSR(no-store·TTFB 1.5s). revalidate만으론 ƒ 유지(실측). layout `generateStaticParams(ko·en)`+홈1800s·pricing/contact/audit 3600s → **PRERENDER→HIT, TTFB 0.13s**. 동적API 페이지(checkout/success·legal·audit/[jobId])는 자동 ƒ 유지.
- **✅ P5 8-b 완료** — AuditJob nullable org/brand FK+인덱스(db push), start-tracking forward-fill, backfill 6건(org: 프리픽스. 무료 54건=D1대로 NULL). ⚠️순서 교훈=**db push 후에만 새 Prisma client 배포**. Prisma7은 `--skip-generate` 없음.
- **🔧 Clerk 키 통일** — www가 pk_test(dev 인스턴스 loved-robin-66) 로드하던 것 발견→www env를 pk_live+sk_live(클립보드→CLI, 채팅 노출0)로 교체. **⚠️새 세션 첫 확인: www 재배포 완료 여부 + 홈 HTML에서 clerk.accounts.dev 사라졌는지** (`curl -s https://www.findable.co.kr/ko | grep accounts.dev` → 0건이어야).
- 참고판정: "껐다 켜도 자동로그인"=Clerk 세션쿠키 정상동작(버그 아님). 포트원=월 순거래액 5천만↓ 무료, PG 카드수수료만 판매자 부담(~3% 가정).

**(전반) 대청소 완료:**

**세션E-3 완료(커밋 4개, 전부 배포) — 다시 하지 말 것:**
- **측정 UX 직관성 ✅** `4d03863` — 용어 "측정 시작" 통일(사이드바·대시보드·브랜드페이지·버튼), 측정 진입점 /brand 단일화(www 무료진단 외부링크 폐기), **대시보드·이력이 org 측정(email=`org:{orgId}`)도 표시**(결과 안 보이던 버그 해소, P5 8-c 전 다리), rate-limit org스코프+플랜차등(무료 24h+요금제 안내 토스트, starter+ 즉시 재측정), 측정 진행 폴링(8s)→완료 토스트+refresh, 브랜드 행에 마지막 측정 상태·시각·결과 링크.
- **메뉴 감사 ✅** `07f4a3b` — 잠금 "Pro에서 해제"→PLAN_META 기반 "Growth"(폐기 플랜명 버그), 미구현 검색창 제거(/search 깨진 /audit 링크→/brand), 인사이트 5→2(GEO 리포트·벤치마크만). parity 스크립트 버그 수정(MISS 크래시·Sensitive는 WARN·GOOGLE/PERPLEXITY OPTIONAL).
- **출시차단 5건 ✅** `16e12ab` (플로우 전수감사 팀에이전트가 발견, 코드 재검증 후 수정) — ①org 0개 신규가입자 500크래시→**CreateOrgGate**((authenticated) layout, Clerk 표준 CreateOrganization)+(authenticated)/error.tsx ②/contact 죽은 구인템플릿→실폼(이름/이메일/문의+contact 서버액션 Resend) ③진단결과 UpsellCard에 **가입 CTA**(무료로 계속 추적하기→APP_URL/sign-up) 추가(전환루프 완결) ④리드메일 결과링크 APP_URL→**WEB_URL 우선**(404 해소) ⑤checkout/success 앱링크·hero sign-in env화·"한국 최초" 카피 제거.
- **결제→plan 자동화 ✅** `0a9604d` — **앱 /billing에서 로그인 상태 PortOne 카드결제**. `actions/billing/checkout.ts`: createCheckoutIntent(서버가 paymentId에 세션 uid 임베드+금액 확정)→위젯→verifyPaymentAndGrant(PAID·KRW·카탈로그 역산→**grantPlan 자동**, uid 대조로 리플레이 차단). starter·growth·scale 카드결제, enterprise 상담. env 미설정시 상담 폴백. app 의존성 @portone/browser-sdk·@repo/payments 추가, findable-app env 3개(PORTONE) CLI 등록+로컬 .env.local 복사. ⚠️**토스 테스트 채널=실과금 아님**(실결제 전 👤포트원 라이브 채널 키 교체)·자동갱신(빌링키) 미구현.
- **리서치 자산 2건**(팀에이전트): ①GEO 경쟁사 12개 UX·게이팅·가격 조사+적용 TOP10 ②전체 유저플로우 A~F 전수감사. → 아래 "표준 기능 백로그"에 반영.

**🔬 배포후 스모크(다음 세션 첫 확인)**: ①신규계정으로 가입→조직생성 게이트→브랜드 등록→측정 시작→완료 토스트 ②/billing 카드결제(테스트카드 4242…)→plan 반영 ③www /contact 제출→메일 수신 ④진단결과 가입 CTA 노출.

## 🆕 표준 기능 백로그 (경쟁사 표준 — 2026-07-30 리서치 근거)
> 사용자 지시: "대중적으로 이런 플랫폼들이 제공하는 기능 다 기획해서 진행". 근거=Profound·Peec·Otterly·Semrush 등 12개 조사.
1. ✅**(2026-07-30 커밋 `030190b`)** **프롬프트 자동 제안 마법사** — 구현 완료. 실측 파일:
   `features/brand/prompt-wizard.tsx`(256줄)·`actions/brand/suggest-prompts.ts`(188)·`packages/ai/lib/prompt-suggestions.ts`(236).
   ⚠️**세션N-3에서 이 항목이 ⬜로 남아 있어 "다음 후보"로 오제시됨** → 투두 낡음이 실제 사고를 냈다
2. ✅**(2026-07-30 커밋 `030190b`)** **자동 갱신 스케줄** — `apps/web/app/api/cron/auto-refresh-tracking`
   실존 + `vercel.json` crons 등록됨(`0 2 * * *`). 플랜별 주기는 `plan.ts` `autoRefreshHours`
   (free=null·starter=168h 주간·growth=24h 데일리)
3. ✅**(2026-07-31 세션J)** **Mention/Citation 분리 + Sources 탭** — `/sources` 신설. 언급vs링크인용 분리·출처 유형(자사/커뮤니티/위키/언론)·엔진별 대조. 실측: 나이키 커뮤니티 50% vs 자사 9%
4. ✅**(2026-07-31 세션J)** **경쟁사 SoV 대시보드 1급 배치** — `/compare` "준비 중" 빈 화면 → 실데이터 리더보드. ⚠️경쟁사 **자동 추출**(답변 파싱)으로 구현, 등록 시 수동 입력 UI는 미구현(자동으로 충분한지 실고객 반응 후 판단)
5. 🟡**절반 완료** **Sentiment 지표** — 측정·저장·**단건 표시까지 됨**(`runner.ts:396` 저장,
   `truth-mirror.tsx:253` 엔진별 긍/중/부 점 표시). **남은 것 = "추세"뿐**(시계열 그래프).
   → 신규 구현 아님. 기존 값을 시계열로 묶는 작업
6. 🖥️ **주간 이메일 리포트** — 미구현 확인(cron 3개는 sweep-stuck·daily-ops-digest·auto-refresh뿐).
   Resend·`@repo/email` 있음. 리텐션 최저비용
7. ✅**(구현됨)** **게이팅 주축=프롬프트 수** — `packages/auth/plan.ts:115~129` `promptLimit`
   실존: free 5 · starter 30 · growth 150 · scale 500 · enterprise 2000. `brandLimit`·`autoRefreshHours` 병행
8. 🖥️ CSV Export → (후순위) Looker/API. **미구현 확인**(csv 검색 결과 proxy.ts 오탐뿐)
- 참고 오픈소스: Elmo(오픈소스 AEO, IA 참고서)·gego(Go GEO 트래커 스케줄러)·geo-optimizer-skill·llms.txt 생태계. 관리자/테스터 부여는 이미 있음(파트너 승인=growth 자동, Clerk metadata 수동).

## 🔧 플로우 감사 잔여 (2026-07-30 발견)
- ✅ **www 비홈 헤더 부재 해소**(세션H) — pricing·audit·contact에 기존 Header 컴포넌트 마운트(옵션 A: 비홈만·홈은 hero 인라인 nav 유지). 로고·요금제·로그인·가입 진입점 라이브. (blog·case 등 나머지 비홈은 필요시 동일 패턴 추가)
- ✅ **플랜 tier 정합 해소**(세션H) — pricing 페이지 재구성: Scale(₩990K) 카드 노출(각주→카드)·Enterprise 카드→하단 배너(판매X 상담형)·4카드 유지(Free/Starter/Growth추천/Scale). 근거=GEO경쟁사 상위티어 노출+B2B 3~4tier 전환최적(5tier 마비). 결제 catalog·plan.ts와 5-tier 정합. ⚠️Starter도 growth잠금은 설계상 맞음(starter=쿼터만 큰 진입, 심화기능=growth+). CTA는 아직 /contact(포트원 라이브 전이라 의도적).
- 🟢 /ko 하드코딩 상수화·verify 라우트 console.log·진단 폴링 7분 초과시 타임아웃 카피·footer 용어 드리프트

## (이전) 🧭 START HERE (2026-07-30 세션E-2 종료)
**한 줄 상태: 전략리뷰 로드맵 P1·P2·P3 완료 + P5 8-a(Tracking 라이브 적재) 성공. 데이터 배관 전 구간 라이브 검증 끝. 다음은 P5 8-b~d(대시보드 전환) 또는 UI/UX 직관성.**

**이번 세션(E·E-2) 완료 — 다시 하지 말 것:**
- **P2 ✅** `0bb6d75` — 러너를 `@repo/audit` 공유패키지로 승격(git mv 10). app 서버액션 `startOrgTracking`이 `runAuditJob` 직접 실행 → www 크로스오리진 fetch·CORS·크로스쿠키 원인째 소멸. 구 www org route·proxy CORS 삭제.
- **P1 ✅** `e2704a5` — Clerk 웹훅을 미배포 apps/api→apps/app 이설(git mv). 웹훅 실행0회가 org 0행 직접원인이었음. svix 의존만 추가.
- **P3 ✅판단** — 코드 무변경이 정답(Clerk 이미 표준·satellite 하드코딩0). 3값은 👤대시보드 몫.
- **배포+P1·P2 라이브검증 ✅** — www·app 배포. 웹훅 org.created 테스트→DB org 생성 실증. "추적 시작"→Brand·AuditJob completed 실증.
- **파비콘/앱아이콘 ✅** `2061436` — next-forge 기본→Findable F. 마크(icon.svg·apple-icon).
- **사이드바 /brand 링크 ✅** `e39ca51` — 링크 없어 URL직접입력만 됐음(P2 검증중 발견).
- **P5 8-a ✅🎉** `f300d0b` — flag ON+엔진키 app 반영 후 nvidia.com "추적 시작"→**21초 실측→Tracking 20건·Prompt 4건 적재**. org 시계열 데이터 토대 라이브에서 첫 적재. D5대로 실패엔진(gemini·perplexity 키없음) Tracking 제외.
  - ⚠️**세션E-2 실수2건(교훈)**: P2때 러너 엔진키(LETSUR·CLOVA·NAVER·KAKAO) app 누락→Gateway폴백→크레딧에러 / 고칠때 `vercel env pull` 마스킹값([SENSITIVE]) 그대로 add→Unauthorized. 실값=`apps/web/.env.local`. → 재발방지=`scripts/check-app-env-parity.sh`(배포전 실행). 상세=[[feedback_vercel_multiapp_env_parity]].
  - **"AI Gateway 크레딧 소진"은 오진이었음**: Findable은 Letsur 직접프로바이더로 chatgpt·claude 크레딧 우회. app에 LETSUR_API_KEY 실값 있으면 됨.

**👤 사용자 대시보드 몫(이미 설정 완료, 참고용):** 절차문서=`docs/_적용/P3_사용자_대시보드_확인절차.md`. Clerk 웹훅(app.findable.co.kr/webhooks/auth 7이벤트)+findable-app env(CLERK_WEBHOOK_SECRET·엔진키 5종·AI_GATEWAY·AUDIT_DUAL_WRITE_ENABLED=true). ⚠️gemini(GOOGLE_API_KEY)·perplexity(PERPLEXITY_API_KEY)는 로컬에도 실값 없어 미설정=그 2엔진만 폴백/제외.

**🚦배포·검증 실측교훈(중요):** app 배포=project.json swap→배포→**원복 필수**(포그라운드 타임아웃시 원복 지연 주의, 백그라운드 권장). app 배포 전 `bash scripts/check-app-env-parity.sh`. 무인증 검증은 www `/api/audit`(POST email·domain·language)로 러너 엔진경로 확인 가능. DB검증=`cd packages/database && set -a && . ../../.env.local && set +a && node --input-type=module`. ⚠️모니터 쿼리서 `result->'cost'`의 작은따옴표를 SQL이 식별자로 오해→JSON경로는 파라미터/js에서 파싱.

- **P0 남은 것 = 👤 지불의사 실고객 10~15명 테스트(사용자 몫). 회사 단일 장애점.**

## 🎯 다음 순서 추천 (2026-07-30 세션H 최종)
- ✅ **P5 8-d 종료**(세션H) — 20번 데이터 목표(runner→Brand/Tracking 적재+org 시계열 대시보드) 완전 완결.
1. **🖥️ 프롬프트 자동 제안 마법사**(표준 백로그 1) — 브랜드 등록 시 AI가 추적질문 20~30개+경쟁사 후보 자동생성→체크박스 승인. **온보딩 체감 최대**. audit 러너 재사용+LLM 호출 1개 추가. Peec·Semrush 표준.
2. **🖥️ 자동 갱신 스케줄 + 게이팅(프롬프트 수 주축)** — free=주간/유료=데일리 cron 재측정. 백로그 2·7 + 기존 엔진레버 통합. 업계 화폐(Peec 25/100/300).
3. **🖥️ (선택) 스모크 4종** 실브라우저 — 신규가입 여정·/billing 4242 결제→plan·www /ko/contact 메일·진단결과 가입CTA. (세션F에서 www 쪽 curl 스모크는 통과, 실브라우저는 👤 잔존)
4. 👤 포트원 라이브 채널 발급(실결제 전) · 플랜 tier 정책 결정(Starter도 growth 잠금 모순) · www 비홈 헤더 결정
5. 👤 P0 지불의사 실고객 10~15명 (회사 단일 장애점)

## (이전) 다음 순서 추천 (2026-07-30 세션E-2 종료 기준)
로드맵 P1·P2·P3 + P5 8-a 완료. 데이터 배관 라이브 검증 끝. 남은 것을 "가치×전제확실성×위험"으로:

1. **🖥️⭐ 핵심 기능 UI/UX 직관성 (세션E-2 사용자 강력 지적) — ✅완료(세션E-3 `4d03863`)** — **핵심(GEO 측정=브랜드 추적)이 직관적이지 않음.** 사용자(만든 사람)조차 "브랜드 추적이 무슨 기능이냐" 물음=제품 근본문제. **실측된 용어 불일치**: 같은 'GEO 측정 실행'을 사이드바 "새 측정"(→www /audit 외부링크=무료진단)·대시보드 "새 측정 시작하기"·브랜드페이지 "추적 시작"(app org측정)으로 **제각각**. "측정"vs"추적" 혼용, 무료진단(www)과 org추적(app)이 기능 다른데 이름 안 구분. + 진행상태 안 보임(토스트만, 결과 30초~3분 후 대시보드)·브랜드 등록 이유 설명 부재. → 용어 통일·행위 수렴·진행상태 표시·온보딩 카피. 대상=`apps/app/(authenticated)/{page.tsx,components/sidebar.tsx,brand/page.tsx,features/brand/*}`. UX스킬(impeccable/taste-skill) 활용. **+rate-limit 안내**: "이미 24시간 내 측정했어요"만 뜨고 왜·구독하면 되는지 안내 없음(사용자: "구독제에 따라 안 되면 그렇게 안내하면 됨"). 도메인 전역 하드 dedup(org무관·우회불가·카운트다운없음)=`start-tracking.ts` DAY_MS → 플랜별 차등(유료=즉시재측정)+안내로.
2. **🖥️ P5 8-b~d — 20번 데이터 목표 완성** — 8-a(Tracking 적재) 통과했으니 2단계: AuditJob에 nullable org/brand FK 추가(additive·라이브안전)·기존 AuditJob 54건 backfill(email→org)·대시보드를 email스코프→Tracking 읽기로 전환·scopedTracking 활성화·relationMode 재검토. 설계=`20번_runner_Brand_Tracking적재_설계.md` §7 스텝8 + "2단계" 섹션. 재사용=tracking.ts·persistAuditTracking.
3. **🖥️ payments 자동화 — ✅완료(세션E-3 `0a9604d`, 웹훅 이설 대신 앱 내 결제로 근본 해결)** — apps/api Stripe 웹훅은 next-forge 템플릿 잔재(PortOne 무관)라 이설 무의미 판명. 로그인 상태 앱 결제+서버 검증+grantPlan으로 완결.
4. **🖥️ (선택) gemini·perplexity 엔진 복구** — GOOGLE_API_KEY(무료 티어)·PERPLEXITY_API_KEY 발급→app+www env. 현재 이 2엔진만 폴백/제외라 7→5엔진 측정. 커버리지 100% 원하면.
5. **🖥️ 2번 AI 엔진 수·갱신주기 게이팅 레버** — 카테고리 최강 매출무기. P5 위에서(엔진수=플랜별 러너 파라미터). 스키마+러너.
6. **🖥️ 8~13 RAG 6개** — Day12·13 설계만. pgvector 설치됨. 라이브 스키마 고위험. 매출 안급하면 후순위.
7. **🖥️ P6 — 배포 전 스모크 체크리스트 + env 값 assertion** — check-app-env-parity.sh 확장(NEXT_PUBLIC_APP_URL `app.` 값검증 등, dev 안깨지게). 로드맵 P6.

- **결제 3번 후속(선택)**: verify는 금액검증·plan역산까지. 완전 자동 grant는 **결제를 apps/app 이관** 필요(현재 web checkout 로그인밖). 위 3번(payments 웹훅 이설)과 함께 검토. grantPlan·서버카탈로그 준비됨. 유료고객 생기면.

### 배포법 (실측 검증됨 2026-07-29)
- **web(findable)**: 루트 링크가 findable이니 그대로 `vercel deploy --prod --yes`.
- **app(findable-app)**: `cp .vercel/project.json .vercel/project.json.web-bak && cp apps/app/.vercel/project.json .vercel/project.json` → `vercel deploy --prod --yes` → **`mv .vercel/project.json.web-bak .vercel/project.json` 원복 필수**.
### 검증
- 앱별 `cd apps/<앱> && npx tsc --noEmit`(현재 web·app·api·studio 전부 0) · 루트서 `npx ultracite check <파일>`(대괄호 경로는 그 디렉토리 cd 후 파일명만, 안되면 `ultracite fix`).
### DB 스크립트
- `cd packages/database && set -a && . ../../.env.local && set +a && <명령>`. Neon HTTP는 `node --input-type=module` + `@neondatabase/serverless` neon().
### ⚠️ 하지 말 것 / 사용자 TODO
- **로그인: 커스텀 useSignIn/useSignUp 흐름 다시 만들지 말 것.** clerk-js 6.x와 안 맞아 깨진다. 표준 `<SignIn/>`/`<SignUp/>`/`<AuthenticateWithRedirectCallback/>`만. (react@7 라인 없음=nextjs@7이 react@6 의존이 정상.) 상세=`_소셜로그인_인계_START_HERE.md`.
- 👤 **KIPO 상표 출원**(Findable/파인더블, Class 42·35) · 👤 노출된 Firecrawl `fc-`키 폐기→재발급→Vercel findable env 교체.

---

## 🔴 P0 — 남은 것 (1건, 사용자 몫)
- [ ] 👤🔬 **지불의사 스모크테스트 (실고객 10~15명)** — 마케터(외국법인·K뷰티 D2C)에 수동 리포트 → "월 ₩99K 낼까?" 관찰. 질문지=`P0_지불의사_시뮬레이션_결과.md`. 측정결함·원가 실측 다 끝나 이제 진행 가능. **회사 단일 장애점.** *코드 아님.*

> P0 코드 완료분(2026-07-28~29): ✅crew 타임아웃 정합 ✅stuck 스윕 cron ✅db 스코핑 헬퍼 ✅pgvector 설치 ✅진단 원가·레이턴시 실측(1건=105원) ✅/audit E2E 재검증 → 하단 "완료 로그" 참조.

---

## 🟡 P1 — painkiller 증거 · 파트너/이코노미 · RAG · 보안

### 파트너 / 이코노미 (2026-07-28 세션 파생)
- [ ] 🖥️ **신규 게이팅 레버: AI 엔진 수·갱신 주기** — Profound식 1→3→10, 주간vs일간. 카테고리 최강 레버. ⚠️스키마+러너 큰 작업. → `packages/ai`·`runner.ts`·`plan.ts`. 근거=`요금제_이코노미_설계.md` §2·§5
- [x] 🖥️ **결제 웹훅 → 자동 plan 세팅** — PortOne 결제 성공 시 plan 자동. rate-limit(이메일+도메인) 병행. → `packages/payments`·webhook  ▶ ✅(2026-07-29 세션C) 안전범위 완료=grantPlan 공용화(@repo/auth/plan-grant)+서버 금액카탈로그(@repo/payments/catalog)+verify 금액검증·plan역산. ⚠️완전 자동 grant는 결제를 apps/app 이관 후속(현재 web checkout 로그인밖).
- [x] 🖥️ **파트너 승인/거절 알림** — 승인·거절 시 메일/앱내. → `actions/partner/decide.ts`+`packages/notifications`  ▶ ✅(2026-07-29 세션C) partner-decision.tsx 템플릿+decide.ts가 Clerk userId→이메일 조회 Resend 발송(best-effort). apps/app에 @repo/email 추가.
- [x] 🖥️ **파트너 특전·배지 확정** — 초기 "Growth+배지". 별도 배지 컴포넌트 미구현(현재 문구만). 영구할인은 결제 후. → `plan.ts`·배지  ▶ ✅(2026-07-29 세션C) partner-badge.tsx(plan과 별개축·진실=DB status=approved)→layout getMyPartnerStatus→sidebar 병기.
- [ ] 👤 **KIPO 상표 출원 (Findable/파인더블)** — Class 42·35. 선출원주의라 조기 유리. ⚠️findableapp.com 글로벌 충돌. 근거=[[project_findable_pricing_economy]]

### painkiller 증거 · 리텐션
- [x] 🖥️🔬 **팩트정합률 전면화 + before/after 케이스 1건** ⭐다음추천 — vitamin→painkiller 증거. → audit UI + 실측 1건  ▶ ✅(2026-07-29 세션C) 재검토=수익번역카드(RevenueImpactCard)·팩트정합률(진실거울 자기채점) 이미 라이브. 정합률 전면화는 팀 안전선이라 의도적 미확대. 수익카드에 KPI 프레이밍 한줄 강화. before/after는 20번(runner적재) 후.
- [x] ✅(2026-07-29) **audit 히스토리/시계열(email 기준)** — 대시보드 SoV 추세 차트+KPI(직전 대비 델타)+이력 리스트 **이미 라이브**. `page.tsx`·`dashboard-data.ts`·`sov-trend-chart.tsx`·`audit-history-list.tsx`·`history/page.tsx`. ⚠️단 데이터=`AuditJob`(email). "scopedTracking 첫 실사용"은 오판이었음(아래 참조).
- [x] 🖥️ **(근본·큰작업) audit runner → Brand/Tracking 적재 + org 스코프 시계열** ✅1단계+경로개선 완료 — 설계=`20번_runner_Brand_Tracking적재_설계.md`(팀16). **✅세션D**: 1단계 dual-write 코드11+파일·라이브배포(flag OFF)·Prompt @@unique·Engine seed9·트리거버튼. **✅세션E**: P1(웹훅 이설=근본원인A 해소)+P2(러너 @repo/audit 공용화=트리거 CORS경로 폐기·app 서버액션 직접호출). **🚧남은 것=P5(flag ON+org 시계열 2단계)** → "다음 순서 추천" §2. 재사용 자산=`packages/audit/tracking.ts`(persistAuditTracking)·`apps/app/app/actions/brand/start-tracking.ts`·Prompt유니크·Engine seed.

### RAG / 에이전트 (Day12·13 설계됨, 미구현)
- [ ] 🖥️ **Document/DocumentChunk/Embedding 테이블 + pgvector** — org키 직접, vector(N) 차원=모델 종속. ⚠️라이브 스키마 변경 고위험. → `schema.prisma`  ※실측: 모델 없음
- [ ] 🖥️ **`scopedVectorSearch` raw SQL org 필터** — Extension 자동주입 안 걸림→손 주입 필수. → `apps/app/lib/rag/scoped.ts`
- [ ] 🖥️ **`indexDocument` 인덱싱 파이프라인** — 문서→청킹→임베딩 첫 호출→저장. → `apps/app/lib/rag/`
- [ ] 🖥️ **임베딩 모델 small→large 확정 + 렛서 baseURL** — 차원·비용 결정. → `models.ts`·`keys.ts`
- [ ] 🖥️ **crew 자기평가 1패스(reflection)** — critique+haiku 채점→score<70 refine 1회. → `crew/agents.ts`·`orchestrator.ts`
- [ ] 🖥️ **MODEL_FAST(haiku-4.5) 실사용** — `void MODEL_FAST` 미사용, critique 채점용. → `crew/agents.ts`

### 보안 / 운영 / 스코핑
- [x] 🖥️🔬 **미들웨어 보호 라우트 실동작 확인** — `proxy.ts` 존재(Next16 방식). ⚠️"middleware.ts 미발견"은 옛 정보 → **실동작·Core3 정합 확인만**. → `apps/app/proxy.ts`·`packages/auth/proxy.ts`  ※실측: proxy.ts 존재  ▶ ✅(2026-07-29 세션C) 검증만=Clerk authMiddleware 정상(Next16 proxy.ts). 변경 불필요.
- [x] 🖥️ **assignBrandOwner Server Action + Form** — 오류 6유형+org 이중방어, useActionState 인라인. → `apps/app/features/brand/`  ▶ ✅(2026-07-29 세션C) actions/brand/assign.ts(org 이중방어·오류6유형)+features/brand 폼+/brand 페이지. scoped.ts 재사용.
- [x] 🖥️🔬 **Clerk 웹훅 User/Org 동기화 확인** — → `apps/app/api/webhooks/clerk/route.ts` [확인필요]  ▶ ✅(2026-07-29 세션C) 검증만=웹훅 이미 존재(apps/api/app/webhooks/auth/route.ts). user/org 이벤트=analytics. DB미러링 없는건 설계(Clerk가 유저·조직 단일진실). 변경 불필요.
- [x] 🖥️ **관리자 콘솔 최소판(apps/studio)** — 빈 껍데기(0파일). 읽기전용(audit현황·stuck·crew분포·Lead). admin 역할 게이트(이미 role=admin 있음). → `apps/studio/app/`  ※실측: 0파일  ▶ ✅(2026-07-29 세션C) studio 아닌 apps/app/admin/ops로 구현(studio 미배포 빈껍데기라). 읽기전용 audit현황·stuck·Lead·파트너대기+사이드바 링크.
- [x] 🖥️ **운영 집계 cron(daily digest)** — 어제 audit 집계, 읽기전용. → `apps/api/app/cron/daily-ops-digest/`  ▶ ✅(2026-07-29 세션C) apps/web/app/api/cron/daily-ops-digest(직전24h 집계 로깅)+vercel.json 0 0 * * *. web에 배치(api 미배포).
- [x] 🖥️ **api 타입오류 2 · studio 타입오류 1 정리** — 진단표 §3. ※원인확인(2026-07-28): api 2건=keep-alive `database.page`(Page 모델 스키마에 없음). api 미배포라 방치중. 고치려면 Page 모델 추가 or keep-alive를 `SELECT 1`로 교체. → `apps/api/app/cron/keep-alive`·`apps/studio`  ▶ ✅(2026-07-29 세션C) keep-alive database.page→SELECT 1 교체. api 2건 해소, studio 원래 0 → web·app·api·studio 전부 tsc 0.

---

## 🟢 P2 — 리텐션 / 장기 해자 / 정리

- [ ] 🖥️ **주간 재측정 + 이메일 알림** — cron 리텐션. keep-alive 패턴 재사용. → cron
- [ ] 🖥️ **GEO 실행 에이전트** — 진단→리라이트→발행준비. 6개 경쟁사 전원 공백=최대 차별화. ⚠️자사 통제 소스만+인간 승인. ❌나무위키 직접편집 금지(표시광고법). *데모데이(10월) 후.*
- [ ] 🖥️ **durable 실행 전환(Vercel Workflow)** — crew 300초 타임아웃 위험→WDK step crash-safe. ⚠️큰 변경.
- [ ] 🖥️ **Prisma Client Extension org where 자동주입** — RLS 준하는 강제력. ⚠️전체 쿼리 영향 고위험. 보류(설계확정 후).
- [ ] 🖥️ **lint 부채 정리** — audit-result.tsx 복잡도·651건 pnpm fix. 크게 손댈 때.
- [ ] 🖥️ **`/en` 홈 title 한국어 현지화** — dictionary meta. → `apps/web` i18n

---

## 📌 문서-only 정정 (코드 아님, PRD/전략 반영 대기)
- North Star 순서 정정(ARR=NorthStar/액션=선행), 유닛이코노믹스 리스크 PRD 추가, 타깃 세일즈 순위(외국법인 상향), "1/3 가격" 카피 톤다운. → 🟡 실고객·실영업 데이터 후 확정.

---

## ✅ 완료 로그 (다시 하지 말 것)

### 2026-07-30 세션 E-2 (배포 + P1·P2 라이브 검증 완전 성립)
- **배포** ✅ www·app 프로덕션 반영(새 AI Gateway 키·P1 웹훅·P2 러너). app=project.json swap→배포→원복(⚠️포그라운드 타임아웃으로 원복 지연 있었음, 처리함).
- **사용자 대시보드 설정** ✅ Clerk 웹훅 엔드포인트(`app.findable.co.kr/webhooks/auth`, 7이벤트) + findable-app env 3개(CLERK_WEBHOOK_SECRET·AI_GATEWAY_API_KEY[AI Gateway서 신규발급, www도 동일키 통일]·NEXT_PUBLIC_APP_URL). ⚠️Vercel Sensitive env는 값 재열람 불가→기존 www키 복사 불가라 신규발급이 정답.
- **P1 라이브 검증(결정적)** ✅ Clerk `organization.created` 테스트→DB에 Organization row 실제생성 확인(Acme Corp, 검증후 삭제). 전경로 작동. `user.created` 테스트는 Clerk 예시 email 빈배열이라 코드 정상 skip(org로 검증).
- **P2 라이브 검증(결정적)** ✅ "추적 시작"→rate-limit 토스트(서버액션 실행·CORS0). DB: Brand 2개(아누아·메디큐브, 실제 org 스코프)+org AuditJob themedicube.co.kr **completed**(러너 app 완주·AI키 작동). 세션D 빈 Brand테이블 채워짐.
- **사이드바 /brand 링크 버그 수정** ✅ 커밋 `e39ca51`. WORKSPACE_NAV에 BuildingIcon "브랜드 관리" 추가(P2 검증 중 발견—링크 없어 URL직접입력만 가능했음).

### 2026-07-29 세션 E (전략리뷰 로드맵 P2·P1 코드 + P3 판단)
- **기준선 커밋** ✅ `b6305e7` — 세션D 누적 미커밋 119개(20번+파트너승인+소셜로그인+요금제+docs)를 한 스냅샷으로 봉인. 마케팅 페이지 수십개=biome 포맷터 노이즈로 확인(로직0). P2 diff 격리 목적.
- **P2 러너 @repo/audit 공용화** ✅ `0bb6d75` — 러너10개 `apps/web/lib/audit/*`→`packages/audit/*`(git mv, rename보존). 신규 `@repo/audit`(package.json·tsconfig·keys.ts, @repo/ai 구조복사). runner의 유일 앱결합 `@/env`(AUDIT_DUAL_WRITE_ENABLED)→패키지 `keys()`, web env.ts는 extends `audit()` 합성. 소비자 재배선(web route3·UI4는 import경로만). **app `startOrgTracking` 서버액션 신설**(runAuditJob 직접호출)+버튼 fetch제거. **구 www /api/audit/org route(265줄)·proxy CORS분기(21줄) 삭제.** diff=+76/−338. 검증=web·app·audit tsc0·server-only 클라누수0·무료audit 회귀0. **CORS 원천 소멸이 핵심 성과.**
- **P1 Clerk 웹훅 app 이설** ✅ `e2704a5` — `apps/api/app/webhooks/auth/route.ts`→`apps/app/app/webhooks/auth/route.ts`(git mv, 로직0). 웹훅 실행0회가 org0행/AuditJob54행 직접원인(뿌리2). app이 auth/webhooks env·@repo/auth·database 이미 보유→인프라0추가, svix 의존만 추가. top-level(=(authenticated)밖)=public, proxy clerkMiddleware auth 강제안함→Clerk POST통과. ensure-org.ts 폴백 위상 명시(1차소유자=웹훅). **payments 웹훅·cron은 apps/api에 남음=별도부채**(→다음순서 §3). app·api tsc0.
- **P3 Clerk 3값 정합** ✅판단(코드 무변경) — 실측: Clerk provider=순수 ClerkProvider·proxy=표준 clerkMiddleware·satellite 하드코딩0·로그인 표준컴포넌트 유지. 고칠 코드 없음(로드맵 "위험 낮음, 설정값"). 3값은 👤대시보드 몫. env `z.url()` 값검증은 dev localhost 깨지므로 P6로. 절차문서=`P3_사용자_대시보드_확인절차.md`(P1+P2배포env 통합).

### 2026-07-29 세션 C (1~27 밀어붙이기 + 로그인 근본해결 + 배포)
- **15번 브랜드 소유자 지정** ✅ `actions/brand/assign.ts`(org 이중방어·오류6유형)+폼(useActionState)+`/brand` 페이지. scoped.ts 재사용.
- **17번 관리자 운영콘솔** ✅ `admin/ops/page.tsx`(읽기전용: audit현황·stuck·Lead·파트너대기)+사이드바 "운영 현황" 링크. studio 아님(app/admin). 서브에이전트.
- **18번 daily digest cron** ✅ `apps/web/cron/daily-ops-digest`(직전24h 집계 로깅)+vercel.json `0 0 * * *`. web에 배치(api 미배포). 서브에이전트.
- **배포** ✅ web·app 프로덕션 반영(www.findable.co.kr / app.findable.co.kr). project.json swap→app→원복. 이전세션 미배포분(원가실측 등)도 함께 반영.
- **🔑 로그인 근본해결** ✅ **3주 묵은 문제 종결.** 429가설 오진 판명. 진짜원인=커스텀 실험API(signIn.sso/finalize·수동콜백)가 clerk-js 6.x 미지원→400 session_exists·Oops·루프. 해결=**Clerk 표준 `<SignIn/>`/`<SignUp/>`/`<AuthenticateWithRedirectCallback/>`로 전면교체**+(unauth)layout 서버 redirect가드+shared.tsx삭제. 실사용자 구글로그인·세션·재방문 확인. 인계문서·메모리 정정. ⚠️커스텀 useSignIn 다시만들지말것.

### 2026-07-29 세션 C-초 (1~7·14·16·19 — 코드부터 확인 방식)
- **14번 proxy 보호 라우트** ✅ 검증만 — Clerk authMiddleware 정상(Next16). "middleware.ts 미발견"은 옛 오해. 변경 불필요.
- **16번 Clerk 웹훅 동기화** ✅ 검증만 — 웹훅 **이미 존재**(`apps/api/app/webhooks/auth/route.ts`, 투두 경로와 달랐을 뿐). user/org 이벤트 처리 O(analytics). DB 미러링 없는 건 **설계**(Clerk가 유저·조직 단일 진실, 앱이 database.user/org를 read 안 함). 변경 불필요.
- **19번 타입오류 청산** ✅ keep-alive `database.page`(모델 없음)→`SELECT 1` 교체. **api 2건 해소, studio 원래 0 → web·app·api·studio 전부 tsc 0.** CLAUDE.md "api 2·studio 1" 부채 청산.

- **1번 지불의사 시뮬레이션(페르소나 12명)** ✅ 결과=`P0_지불의사_시뮬레이션_결과.md`. 지불의향 7+ = 12명 중 2명. **핵심병목: 가격저항≈0, 진짜 벽="내 분기 KPI 문제인지+돈으로 얼마인지 번역 부재".** ⚠️실고객 검증(P0)은 여전히 사용자 몫.
- **3번 결제→자동 plan(안전범위)** ✅ `packages/auth/plan-grant.ts` 공용 grantPlan 신설(파트너 decide.ts가 재사용, 인라인중복 제거) + `packages/payments/catalog.ts` 서버 금액↔plan 카탈로그(클라 금액 불신) + verify 라우트가 실결제금액을 카탈로그와 대조·KRW검증·plan역산. ⚠️grant 실연결은 **결제를 apps/app(로그인)으로 이관** 후속작업(지금 web 결제는 로그인밖·test이메일 하드코딩이라 userId 없음). PortOne=PG통합레이어(실PG=토스). Stripe 웹훅은 있으나 PortOne용 아님.
- **7번 painkiller 증거** ✅ 재검토 결과 **수익번역카드(RevenueImpactCard)·팩트정합률(진실거울 자기채점) 이미 라이브**. 정합률 전면화는 팀 "안전선"(오판→신뢰붕괴)이라 의도적 미확대가 맞음. before/after는 20번(runner적재) 후에나 실값. → 수익카드에 **KPI 프레이밍 한 줄**만 강화(시뮬 병목 직격). `revenue-impact-card.tsx`.
- **4번 파트너 승인/거절 알림 메일** ✅ `packages/email/templates/partner-decision.tsx`(승인=Growth+대시보드CTA / 거절=사유+재신청) + decide.ts가 결정 후 Clerk에서 userId→이메일 조회해 Resend 발송(best-effort, 실패해도 결정성공). apps/app에 `@repo/email` 의존성 추가.
- **5번 파트너 배지** ✅ `partner-badge.tsx`(plan과 별개 축, 진실=DB status=approved) → layout에서 getMyPartnerStatus로 isPartner 판정 → sidebar "현재 플랜" 옆 병기. 승인 파트너만 노출.

### 2026-07-29 세션 B (투두 재검토·정정)
- **"audit 히스토리/시계열" 재검토** ✅ 코드 실독 결과: 히스토리/시계열 UI는 **이미 라이브**(대시보드 SoV 추세+KPI 델타+이력, `history/page.tsx`). 데이터=`AuditJob`(email). `scopedTracking`이 겨냥한 `Tracking`·`Brand`는 **빈 테이블**(runner가 `AuditJob`만 write, grep으로 write 코드 전무 확인). → 투두 착수순위 재정렬: 다음=**결제 웹훅** / painkiller UX. "runner→Brand/Tracking 적재"는 별도 근본작업으로 분리. 코드 무변경.

### 2026-07-29 세션 (P0 코드 소진 + 네이버 브리핑 해결)
- **네이버 AI Briefing** ✅ Browserbase CDP(wss 구조적 불가, region·timeout 무관 실측배제)→**Firecrawl `/v2/scrape` HTTP** 어댑터 전면교체. 라이브: 아누아 mentioned=true·rawLen=704·**7.7초**·에러0. 무료1000/월·KR캡차 뚫림·DOM파싱 정규식. env=`FIRECRAWL_API_KEY`. 상세=[[reference_vercel_browserbase_stagehand]]. ⚠️검색어 20~40%만 브리핑노출(미노출=정상 빈결과).
- **진단 1건 원가·레이턴시 실측** ✅ `EngineResponse.usage`+`cost.ts`→`result.cost`+로그. **1건=105원**(claude~50·chatgpt~48, 한국엔진 합4원, gemini/daum 무료). 28호출·~20초. 무료진단1000건=10.5만원=원가 무시가능. ⚠단가 추정치.
- **pgvector 설치** ✅ Neon `vector 0.8.0` 설치(RAG 게이트). 기능자체는 미구현.
- **crew 타임아웃 정합** ✅ CREW_TIMEOUT 720→270초(<함수300<STALE900). stuck 물리적원인 해소.
- **stuck 스윕 cron** ✅ web(findable)에 배치·하루1회(Hobby). crew-runner 270s가 1차·이건 2차안전망. 유료전환시 `*/15`.
- **db 스코핑 헬퍼** ✅ `lib/db/scoped.ts`(requireOrg·scopedBrands·scopedTracking·scopedBrandById). ⚠호출처0=예방적·미배포. ⚠️(2026-07-29 정정)첫 사용처를 "audit 히스토리"로 봤으나 **오판**: Tracking/Brand가 빈 테이블(runner 미적재)이라 scopedTracking은 아직 쓸 데가 없음. 실사용은 "runner→Brand/Tracking 적재" 근본작업 이후.
- **/audit E2E 재검증** ✅ 라이브 2건 관통, sov93 일관, 회귀0.

### 이전 세션
- **파트너 승인 기능** ✅(2026-07-28) 신청→승인/거절·3중 게이트·라이브. [[project_findable_partner_approval]]
- **요금제 이코노미 통일** ✅(2026-07-28) insider/pro폐기→결제축(free/starter/growth/scale/enterprise). isPaid=growth. [[project_findable_pricing_economy]]
- **측정 파이프라인 결함 P0~P1** ✅(2026-07-27) 프롬프트·브랜드한글화·Daum정합·SoV정합·출처필터·마크다운. 설화수 실측검증.
- **SoV→매출 번역 카드·경쟁사 벤치마크 카드** ✅(2026-07-27)
- **Clerk 프로덕션 전환·소셜 로그인 3종** ✅(2026-07-23~27) 라이브. [[reference_findable_app_dashboard_deploy]]
- **crew 영구 stuck 가드**(부분)·**진실거울·GEO 코파일럿·네이버 브리핑 on-demand** ✅ 라이브.
