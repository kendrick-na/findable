# 📋 남은 일 단일 진실 — 2026-08-17 세션N-37

> ⛔ **이 문서가 남은 일의 단일 진실이다.** `_투두_단일진실_N34.md` 는 **폐기**(아래 §0 이 정정).
> 🔴 문서 기재를 옮기지 않았다 — **코드·DB·실화면으로 실측**해 다시 판정했다.
> 📕 설계 근거 = `재설계안_v4_전면_2026-08-15.md` · 함정 = `[[reference_findable_traps]]`

---

## 0. 🔬 오늘 실측이 뒤집은 것 4가지

| 기존 인계 | 실측(2026-08-17) |
|---|---|
| *"Tracking 75행 = P0 실고객이 없어서"* | 🔴 **틀림.** 코드가 흘리고 있었다(N-36 수정) → 오늘 **218행** |
| *"측정일 1일이라 추세 불가"* | ⚠️ **절반 맞음.** 2회차를 **아무도 안 돌린 것**이었다 → 돌리니 **즉시 그려짐** |
| *"탭 재편은 데이터가 없어 보류"* | 🟡 **근거 약해짐.** 추세 가능 브랜드 2→**3/8**, cron 이 매일 1일씩 채운다 |
| *"모바일은 v4 P0-4 대로 다 깨질 것"* | 🔴 **틀림.** 가로스크롤·한글잘림·표붕괴 **전부 이미 해결됨**(경쟁사 4곳은 실패) |

### ⭐ 그래서 바뀐 전략
**"없는 걸 만드는 일"보다 "만들어 놓고 아무도 못 보던 걸 여는 일"이 훨씬 싸고 효과가 크다.**
오늘 87원 측정 1건으로 **잠겨 있던 화면 4개**(추세 그래프·순위 비교·감성 변화·메모)가 열렸다.

---

## 1. ✅ 끝난 것 (되살리지 말 것)

| 작업 | 근거 |
|---|---|
| 지표 사전(축 5개) · 감성 정직화 · 로딩 상태 · 0결과 3요소 | N-34 |
| 날조 제거 · 실측 데이터 교체 · 소셜프루프 | N-32·N-33 |
| **Tracking 유실 수정** | N-36 `123e14c` — 오늘 실측 확인(75→218행) |
| **관리자 측정 콘솔**(1건 측정·수정·삭제) | N-37 — cron 5건(435원) 말고 **1건(87원)** |
| **측정 타임아웃 수정** | N-37 — 동기 대기 300초 사망 → `after()` 백그라운드, **50초 완료** |
| **모바일 반응형 기본기** | 실측: 가로스크롤 0 · 한글 잘림 0 · 표→카드 전환 ✅ |

---

## 2. 🔴 P0 — 지금 착수 (막는 것 없음)

### P0-A. 모바일 화면 분할 【M】 ⭐ **최우선**
`[실측]` 대시보드 세로 **5,892px** = 스크롤 15번. 「Growth 업그레이드」가 맨 아래라 **아무도 못 본다.**
- v4 P0-4 확정안 = **하단 탭 4개 + 「더보기」 시트**(👤 162프레임 근거로 결정 완료)
- ⚠️ 단 **탭 8개가 아니라 4개**다 — 지금 있는 화면(한눈에·지금 할 일·질문별·순위)로 충분
- 🔴 **P0-5 규칙 준수**: 측정 0건이면 탭바를 렌더하지 않는다(빈 탭 금지)

### P0-B. 모바일 헤더에 브랜드·점수 【S】
`[실측]` PC 우측 상단 `sulwhasoo.com 95%` 가 모바일에서 **사라진다**(`sm:` 이상만).
→ 모바일 사용자는 **지금 어느 브랜드를 보는지 모른다.** 경쟁사 4곳 전부 항상 표시.

### P0-C. 2회차 유도 【S】 ⭐ **가성비 최고**
`[실측]` 오늘 87원 1건으로 잠긴 화면 4개가 열렸다. 그런데 **그걸 유도하는 장치가 화면에 없다.**
- 1회차만 있는 브랜드에 *"한 번 더 재면 변화를 볼 수 있어요"* + 버튼
- 🔴 이건 UI 가 아니라 **제품의 핵심 루프**다(경쟁사가 「추세」를 파는 이유)

---

## 3. 🟠 P1 — 근거는 있으나 순서가 뒤

| # | 작업 | 규모 | 막는 것 |
|:--:|---|:--:|---|
| P1-D | **진실의 거울 앱 이관** | M | 없음. web 에만 있다(어댑터 1개). ⭐4곳 중 1곳만 보유 = **우리 무기** |
| P1-E | **로그인 화면 소셜프루프** | S | 없음. **경쟁사 4/4 보유, 우리만 빈 화면** |
| ~~P1-F~~ | ~~탭2 「질문별」 빈 상태~~ | — | ❌ **기각 — 대상 0건**(아래) |
| ~~P1-G~~ | ~~탭3 엔진 권역 분리~~ | — | ✅ **완료 N-38**(아래 §7) |
| ~~P1-H~~ | ~~블로그 500~~ | — | ✅ **완료 N-37 `6590391`** — 이 표가 낡아 있었다(N-38 정정) |

🔴 **P1 은 전부 끝났다.** 남은 것은 §8 브리핑(👤 지시) 과 아래 P2 뿐이다.

---

## 7. ✅ P1-G 완료 (2026-08-17 세션N-38) + 🔴 신규 발견

### 한 일
`sources-board.tsx` 「AI별 등장·출처」를 **한국/글로벌 권역으로 분리**. 한국을 **위**에 세운다
(경쟁사 4곳 랜딩 fetch 에서 한국 엔진 언급 **0건** = 우리만 재는 축이라 아래로 밀지 않는다).

| 무엇 | 근거 |
|---|---|
| 권역 그룹 + 그룹 등장률 | `market-scope.ts` 재사용(새 분류 로직 0) |
| **빈 권역은 섹션째 미렌더** | 못 잰 것에 `0%` 금지(apple.com 유형) |
| 🔴 **`인용 0` 을 둘로 가름** | hyperclova → 「출처 안 밝힘」 · 나머지 0 → `인용 0` |
| 엔진 이름표 누락 2건 | `naver-briefing`·`chatgpt-web` 이 **슬러그로 노출**되고 있었다 |

### 🔴 v4 문서 정정 — 「인용 못 내는 엔진 3종」은 **틀렸다**
> v4 §탭4 함정: *"chatgpt·claude·hyperclova 가 인용을 구조적으로 반환 안 함"*

**코드·DB 실측 결과 구조적 0 은 `hyperclova` 하나뿐이다.**
chatgpt·claude 는 `extractCitedSources()` **본문 URL 폴백**이 있다.
DB 실측(`Tracking` 전수): **chatgpt 인용 33건** · claude 1 · gemini 5 · **hyperclova 0** · perplexity 0.
→ 셋을 묶어 *"출처를 안 밝히는 AI"* 라 적었으면 **거짓 안내**가 화면에 나갔다.

### 🔴 스크린샷이 잡은 버그 2건 (테스트 440개·뮤테이션 4종 **전부 통과한 상태**)
1. **막대가 전부 100%** — 실측상 대부분 엔진이 등장률 100%라 비율 막대는 **정보가 0**이었다.
   8/8 과 13/13 이 같은 길이 → 구별 불가. **막대 제거**(정보 없는 요소가 자리만 제일 크게 먹었다).
2. **모바일 390px 에서 우측 잘림** — 「출처를 밝히지 않는 AI」가 `...않는 AI` 로 잘렸다.
   고정폭 4칸이 안 들어감 → 유동폭 + 문구 단축(「출처 안 밝힘」).
   ⭐ 규율 재확인: **빌드·테스트 통과는 검증이 아니다. PNG 를 열어야 한다.**

---

## 8. 🔵 네이버 AI 브리핑 — **본류 편입 진행**(👤 결정 2026-08-17)

> 📕 **기획서 = `브리핑_본류편입_기획_2026-08-17.md`** (착수 순서 B-1~B-5 는 거기)
>
> 이력: N-38 이 *"신규 P0 후보"* 로 올림 → 👤 *"본류에 넣을 정도로 중요한 엔진 같지는 않다"*
> 로 근거 부족 지적(맞는 지적 — 근거가 *"경쟁사가 안 잰다"* 하나뿐이었다)
> → 👤 *"네이버 AI 탭 그거 본류에 넣는 기획 진행하고"* 로 **편입 결정**.
>
> 🔴 **단 기획 과정에서 더 큰 문제를 찾았다 — 그냥 넣으면 거의 전량 미노출로 찍힌다.**
> 브리핑은 *정보형*(효과·후기·장단점)에만 뜨는데 본류 프롬프트는 **전량 추천형**이다.
> → 「8번째 엔진」이 아니라 **「질의 축 하나 추가」**로 설계해야 한다. 상세=기획서 §2.

### ⚠️ 근거로 쓰면 안 되는 숫자
`types.ts:12` — *"검색 점유율 20%, 점유율 40% 확대 예정"*
→ **저장소·문서 전수 검색 결과 출처가 어디에도 없다.** 이 숫자를 인용하지 말 것
(사실 자동생성 금지 원칙). 편입 근거는 아래 실측으로만 세운다.

### 뒤집은 것
> ❌ 기존 주석(`runner.ts:365`): *"본류는 항상 7엔진. 브리핑은 느려서 on-demand 버튼"*
> ❌ 어댑터 주석: *"네이버 캡차 뚫는지 **라이브 검증 필요**"*

**둘 다 낡았다.** 프로덕션 이력 97건 실측:

| 시기 | 방식 | 결과 |
|---|---|---|
| **7/29~8/1** (Firecrawl 전환 후) | HTTP scrape | 🟢 **6/7 성공 · 5~10초** |
| 5/08~7/28 (Browserbase) | WebSocket CDP | 🔴 전멸(timeout·`101`·429) |

- **캡차는 이미 뚫린다.** 유일 실패도 캡차가 아니라 *"이 질의엔 브리핑 미노출"*(정상 동작)
- **5~10초** = 본류 7엔진과 같은 자릿수 → *"30초~3분 약속"* 배제 근거 **소멸**
- 브랜드 언급률 **6건 중 5건 `true`**(정답형 질의라 실제로 등장)
- 비용도 작다: 프롬프트 전량이 아니라 **후보 3개 중 뜨면 즉시 중단** = 잘 되면 1콜

### 🔴 그런데 **본류 편입은 아직 하면 안 된다** — 파서가 오염돼 있다
저장된 브리핑 본문 실측(`excerpt`):

| 브랜드 | 판정 | 내용 |
|---|:--:|---|
| NVIDIA·SK하이닉스 | 🟢 정상 | 진짜 AI 브리핑 요약 |
| 아누아·설화수 | 🟡 **면책문구만** | *"AI 브리핑 실험 단계로…"* 네이버 고지문을 본문으로 긁음 |
| **현대** | 🔴 **광고 삼킴** | *"현대효과 관련 **광고** … 한빛프로이드**최면센터**"* |
| **테슬라** | 🔴 **블로그 잡탕** | *"일론머스크 다이어트로 유명한 **위고비**…"* |

🔴 **현대·테슬라는 `brandMentioned` 판정까지 오염됐을 수 있다**(광고문에서 "현대"가 잡힘)
= **없는 언급을 세는** 유형(N-31 "푸에기아→kia" 와 동종).
원인 추정: `extractBriefingBlock` 이 정규식 문자열 파싱이라 **블록 경계를 못 잡고 광고·블로그까지 삼킨다.**

### ⬜ 착수 순서 (상세=기획서 §6)
| # | 작업 | 선행 |
|:--:|---|---|
| **B-1** | **파서 오염 수정** ⭐지금 착수 가능 | 없음 |
| B-2 | 브리핑 질의를 `Prompt`(`isAutoGenerated:false`)로 적재 | B-1 |
| **B-3** | **Firecrawl 크레딧·요금 확인** 🔴👤 코드로 못 푼다 | 없음 |
| B-4 | `runner.ts` 본류 편입(`after()` 백그라운드) | B-1·B-2·B-3 |
| B-5 | 화면: 브리핑 행 + 「질의 축 다름」 표기 | B-4 |

🔴 **B-1 은 본류 편입과 무관하게 지금 필요하다** — on-demand 버튼을 누르는 순간 오염된다.
🔴 **B-3 이 비면 편입하면 안 된다** — cron 이 매일 도는데 크레딧이 마르면 브리핑만 조용히
   실패하고, 그게 *"네이버가 우리를 모른다"* 로 집계된다.

---

## 4. ⏸️ P2 — 조건이 붙는 것

| # | 작업 | 조건 |
|:--:|---|---|
| P2-I | **대시보드 탭 8개 전면 재편** | 🟡 추세 3/8 → ⚠️ **"8/20 재판정"은 폐기**(§9 — cron 구조 실측) |
| P2-J | **web 부분가림(자물쇠)** | 👤 **무료/유료 경계 미정**(`audit-result.tsx` 8개 중 어디까지) |
| P2-K | 순위카드 `/compare` 유료 경계 | 👤 가격 정책 |
| P2-L | 7일 트라이얼 | ⛔ **카카오페이 심사 후** · 법적 고지 동반 |
| P2-M | 구매의도 탭 | `Prompt.category` 되살리기 선행 |
| P2-N | 커머스 2단계 | ⬜ Q4 · `Product` 모델 XL |

---

## 5. 🔴 코드 밖 (진짜 병목)

| 항목 | 상태 |
|---|---|
| **P0 실고객** | 유저 0명 · 유료 0건. **모든 것의 뿌리** |
| 카카오페이 심사 | 🟡 심사 중(~9월초). **화면 동결 중** — [[project_findable_kakaopay_review_freeze]] |
| Starter `1개`→`3개` | 승인 후 배포하면 자동 해소 |

---

## 6. ⛔ 되살리지 말 것

**탭2 「질문별」 빈 상태**(P1-F) — 🔬 실측 2026-08-17: **측정 있는데 질문 0 = 0/8 브랜드.**
  N-36 이 폴백 프롬프트를 항상 심게 고친 뒤로 **구조적으로 안 나는 상태**가 됐다.
  (토니모리만 `0/0` 인데 그건 `DashboardEmptyState` 담당) → 만들면 아무도 안 보는 화면.

랜딩 정렬·타이포(오탐) · 전면 0결과 화면(대상 0건) · 「AI별」 분리(이미 있음) ·
감성 임계값 발명 · web GEO 5축 라벨 · `packages/payments`(심사 중) ·
**모바일 가로스크롤 탭 8개**(한글은 2.5개만 보인다 — v4 P0-4)

---

## 9. 🔴 P2-I 재판정 — **"cron 이 매일 채운다"는 틀렸다** (N-38 실측)

### 9-a. ⚠️ 먼저: 로컬 DB 가 **가짜를 보고 있었다**
N-38 이 처음 *"Tracking 75 · 추세 0/3"* 이라 보고했으나 **틀린 DB 였다.**

| | 로컬 `.env.local`(`ep-holy-dream`) | **진짜**(`ep-plain-sun`) |
|---|:--:|:--:|
| Tracking | 75 | **239** |
| 추세 가능 브랜드 | 0 | **3** |
| `isAutoGenerated:false` | **0** | **16**(N-36 폴백 적재분) |

원인 = N-36 이 만든 **`dev-local` 스냅샷 브랜치**가 **N-36 수정 직전 상태**로 굳어 있다.
🔴 **로컬에서 데이터를 세면 N-36·N-37 작업분이 안 보인다.** 대조법 =
`_백업/.env.local_20260817-003504.bak`(전환 전 URL 보존)로 같은 쿼리를 두 번 돌린다.
→ **인계서 숫자(218→239)가 맞았다. 로컬 실측을 근거로 판정하지 말 것.**

### 9-b. 🔴 진짜 결함 — 8/20 에 다시 봐도 저절로 안 늘어난다
날짜별 적재(진짜 DB): `08-17:21행/1브랜드` · `08-16:143행/6브랜드` · `08-14:21` · `07-30:34` · `07-29:20`
→ **매일 1일씩 쌓이는 패턴이 아니다.** 8/15 는 아예 없고 8/16 에 6브랜드가 몰렸다.

cron 은 **살아 있다**(`auto-refresh-tracking` · `0 17 * * *` · 8/16 적재 시각 17:31~17:50 과 일치).
문제는 **구조**다:

| 제약 | 값 | 결과 |
|---|:--:|---|
| `MAX_TRIGGERS_PER_RUN` | **5** | growth org 브랜드가 **7개** → 한 번에 5개만, 나머지는 다음 날 |
| growth `autoRefreshHours` | 24 | 브랜드당 하루 1일씩만 늘어난다 |
| 🔴 **설화수 org = `free`** | `null` | **자동 갱신 대상이 아니다 → 영원히 안 늘어난다** |

⭐ **"추세 가능 3/8" 중 설화수는 cron 이 만든 게 아니라 사람이 수동으로 돌린 것**(8/14·8/17).
   즉 인계서가 센 3 은 **자동으로 유지되는 수가 아니다.**

### 9-c. ⬜ 그래서 P2-I 는 "날짜 대기"가 아니라 **조건부 해제**로 바꾼다
- ❌ 폐기: *"8/20경 재판정"* — 날짜를 기다리는 근거가 없다
- ✅ 해제 조건: **growth org 7브랜드 중 5개 이상이 측정일 2일 이상**
  (cron 이 5개씩 도니 며칠이면 도달한다. 도달 여부는 **진짜 DB 로** 확인할 것)
- ⚠️ 별건 후보: `MAX_TRIGGERS_PER_RUN=5` < 브랜드 7 → **2개는 항상 뒤로 밀린다.**
  상한을 올릴지 여부는 원가 판단(1건 87원)이라 👤 결정 사항
