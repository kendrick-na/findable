# 🔧 실행 백로그 — 문서 → 코드 다리 (⚠️ **상태 표기 낡음**)

> 🔴 **남은 일의 단일 진실 = ⭐`_투두_단일진실_N24.md`** (2026-08-12 세션N-24).
> ⭐ **단, 이 파일에만 있던 살아있는 항목 5건을 N24 §2 로 건져 올렸다**:
> BL-Day17-02(장애 알림) · 17-03(로그 도메인) · 17-04(퍼널 이벤트) ·
> crew route org 스코핑 · D16-4(함수 타임아웃 정합).
> → **N23 인계문서가 "코드 소진"이라 단정한 근거가 이 누락이었다.**

> 🔴 **이 파일의 `⬜` 를 "남은 일"로 믿지 말 것.** 상태 갱신이 멈춰 있다 —
> 2026-08-08 세션N-11 표본 실측에서 `⬜` 5건이 **전부 이미 완료**였다
> (api 타입오류 → tsc 0 · `scoped.ts` 존재 · `proxy.ts` 존재 · crew 타임아웃 가드 존재 · pgvector 설치).
> **남은 일의 단일 진실 = `투두리스트.md`.** 이 파일은 **근거(어느 문서에서 나왔나) 추적용**으로만 쓴다.
>
> 실습·기획 문서에서 나온 결론을 **실제 Findable 코드 작업**으로 변환하는 곳.
> 각 항목은 근거(어느 문서) + 대상(어느 코드) + 우선순위.

## 규칙
- 문서에서 "이걸 바꿔야 한다"가 나오면 여기 한 줄 추가.
- 형식: `[우선순위] 할 일 — 근거(문서) → 대상(코드 경로) — 상태`

---

## 📋 백로그

### 🔴 P0 — 4개 페르소나 팀리뷰 수렴 (Day07 코드 전 실검증)
- [P0] **지불의사 스모크테스트** — K-뷰티/D2C 그로스 10~15명에 수동 SoV리포트 → "월₩99K 결제?" + 결제링크 클릭 관찰(측정만 vs 액션 A/B). 근거(day-02/페르소나_검토_종합 §F, 4명 수렴) → 코드 아닌 실고객. **회사 단일 장애점** — ⬜
- [P0] **HyperCLOVA 단일 Vertical Slice** — 도메인→API→인용파싱→SoV 1건 관통 + 호출당 실비용/레이턴시 실측. 근거(CTO §G, Day01 최대리스크) → `apps/` — ⬜
- [P1] **Naver Cue:→AI Briefing 대상 정정** — 2025.3 폐지(사실오류). 근거(CTO) → 시장분석·PRD 전체 — ⬜
- [P1] **North Star 순서 정정** — ARR=NorthStar/액션=선행지표(현 제안은 거꾸로). 근거(CFO·CMO 반대) → PRD §4.1 — ⬜
- [P2] **유닛이코노믹스 리스크 추가** — CAC·전환율·COGS·Churn 명시. 근거(4명 공통 누락) → PRD Risks — ⬜

### Day01 진단에서 나온 것
- [P1] api 타입오류 2건 수정 — 근거(day-01_진단/진단표.md §3) → `apps/api/` — ⬜
- [P1] studio 타입오류 1건 수정 — 근거(진단표 §3) → `apps/studio/` — ⬜
- [P2] lint 651건 정리(`pnpm fix`) — 근거(진단표 §3) → 전체 — ⬜
- [P0] `/audit` 진단 엔진 end-to-end 실동작 검증 — 근거(진단표 §6 최대리스크) → `apps/*/audit` — ⬜

### Day02 시장분석에서 나온 것 (블라인드 재검증 발견)
- [P2] **USP "1/3 가격" 문구 정정** — Otterly $29 반례. → ✅ **문서 반영 완료**(PRD §2.4-5, COMPETITORS §4.5 정정). 남은 코드작업: `apps/web` 랜딩 카피에서 "1/3 가격" 강조 톤다운 → ⬜(Day07~)
- [P1] **North Star 재정의** — 측정(Paid ARR) → 액션실행(`action.executed`×`sov.delta`). → 🟡 **PRD §4.1에 재검토 제안 병기**(미확정, 실고객 검증 후). 확정 시 `apps/*` 이벤트 로깅 — ⬜
- [P2] **타깃 세일즈 순위** — 외국법인(강제수요·경쟁0) 상향. → 🟡 **PRD §3.1에 제안 병기**(실영업 데이터 후 확정) — ⬜
- [P1] **v1에 "주간 액션 카드" 포함 검토** — 차별화가 개선(액션)인데 v1.5로 미룸. → 🟡 **ROADMAP v1.5에 제안 병기**(팀 논의 후). 채택 시 `apps/app` — ⬜

> 반영 원칙: 저위험·확신(USP 문구)=문서 실수정 / 고위험·방향결정(North Star·타깃·범위)=제안 병기(원본 유지, 실검증 후 확정). AI 분석으로 제품 방향 즉시 안 덮음.

### Day10 Slice2(브랜드 담당자 배정)에서 나온 것
- [P1] **assignBrandOwner Server Action** — 오류 6유형(AUTH-401/403·BRAND-404·RULE-409·INPUT-422·500) + org 스코핑 이중방어. 근거(day-10 slice2_implementation_note §3) → `apps/app/features/brand/actions.ts`(신규 함수, 저위험) — ⬜
- [P1] **AssignOwnerForm + 목록 assignee 표시** — useActionState 인라인 메시지(흰화면 방지). 근거(§3 ②③) → `apps/app/features/brand/components/·queries.ts` — ⬜
- [P2] **Brand.assigneeUserId 컬럼 migration** — ⚠️고위험(라이브 Neon DB 스키마 변경). 컬럼 존재 여부 [확인필요] 먼저. 근거(§3 ④ 실코드 판단) → `packages/database/prisma/schema.prisma` — 🟡 보류(확인 후)
- [P2] **org 스코핑 A/B 실행 검증** — Clerk 테스트 org 2개로 BRAND-404·0건 확인. 근거(day-10 slice2_verification RLS 등가) → 실행 검증 — ⬜

### Day11 인증/RLS 치환에서 나온 것
- [P0] **미들웨어 보호 라우트 실동작 확인 + Clerk Core 3 마이그레이션** — apps/app에 `middleware.ts` 실측 미발견. ⚠️Clerk Core 3(2026.3)에서 `authMiddleware()` **제거**됨→`clerkMiddleware()`+`createRouteMatcher`로 교체 필요. `auth()` async 확인. 근거(day-11 auth_rls_setup §1) → `apps/app/middleware.ts`·`packages/auth/proxy.ts` — ⬜
- [P0] **`lib/db/scoped.ts` 스코핑 헬퍼** — `requireOrg`·`scopedBrands`·**`scopedTracking`(brand 경유, org키 없는 모델 유일방어)**. 근거(§2, 실측=org키 3/9모델뿐 Tracking 없음) → `apps/app/lib/db/scoped.ts`(신규, 저위험) — ⬜
- [P1] **Clerk 웹훅 User/Org 동기화 존재 확인** — 교재 profiles 트리거 등가. 근거(§3) → `apps/app/api/webhooks/clerk/route.ts` [확인필요] — ⬜
- [P2] **Prisma Client Extension org where 자동주입** — 앱레벨 스코핑 실수방지(RLS 준하는 강제력). ⚠️전체 쿼리경로 영향 고위험. 근거(§2 재검토제안, Day04 §5각주) → `packages/database` — 🟡 보류(설계확정 후)
- [P1] **org-A/B 교차 검증 실행** — Clerk 테스트org 2개로 7케이스. 근거(day-11 account_ab_test) → 실행검증 — ⬜

### Day12 RAG/문서 임베딩에서 나온 것 (블라인드 재검증 발견)
- [P0] **Document/DocumentChunk/Embedding 테이블 설계** — org키 직접(Tracking 교훈), vector(N) 차원=모델 확정 종속. 근거(day-12 rag_embedding_design §4·§5) → `packages/database/prisma/schema.prisma` — 🟡 보류(라이브 스키마 변경 고위험, D12-1 게이트)
- [P0] **`scopedVectorSearch` raw SQL org 필터 헬퍼** — Extension 자동주입 안 걸림 → `WHERE organizationId` 손 주입 필수. 근거(A-12-O2 §2·L3) → `apps/app/lib/rag/scoped.ts`(신규, 저위험) — ⬜(pgvector 준비 후)
- [P1] **임베딩 모델 small→large 확정 + 렛서 baseURL 치환** — 코드=text-embedding-3-small+OPENAI직접 / 계획=large+렛서. 차원·비용 결정. 근거(D12-1·D12-2) → `packages/ai/lib/models.ts`·`keys.ts` — 🟡 보류(결정 필요)
- [P1] **`indexDocument` 인덱싱 파이프라인** — 문서→청킹→models.embeddings 첫 실제 호출→저장. 근거(A-12-O2 §2.2) → `apps/app/lib/rag/` — ⬜
- [P0] **Neon pgvector 확장 설치 확인** — `CREATE EXTENSION vector`. 근거(rag_embedding_design F6) → Neon 콘솔 — ⬜ [확인필요]
- [P1] **org-A/B 벡터검색 7케이스 실행** — V4(필터제거 유출재현) 포함. 근거(A-12-O2 §4) → 실행검증(Clerk 테스트 org) — ⬜

### Day13 에이전트 워크플로/자기개선에서 나온 것 (블라인드 재검증 발견)
- [P0] **crew 타임아웃 가드 실구현 + 주석 거짓문구 정정** — orchestrator.ts:16 주석은 "재시도1회+타임아웃60초" 약속하나 코드에 없음(grep 0건, D13-2). AbortController/Promise.race 도입, 없는 "재시도" 문구는 정정. 근거(A-13-O1 §3 D13-2, A-13-O2 §2) → `packages/ai/lib/crew/orchestrator.ts` — ⬜ [Mastra abortSignal 지원 확인필요]
- [P0] **crew 크래시 stuck 감지** — `after()` 백그라운드라 프로세스 죽으면 crewStatus="processing" 영구 stuck(D13-3). crewStartedAt 15분 초과+processing=stale로 재실행 허용(스키마 무변경, 저위험). 근거(A-13-O1 §3 D13-3, A-13-O2 §3-①) → `apps/web/lib/audit/crew-runner.ts`·`apps/web/app/api/audit/[jobId]/crew/route.ts` — ✅ (2026-07-23) route에 STALE_AFTER_MS=15분 stale 재실행 가드 + crew-runner에 CREW_TIMEOUT_MS=12분 withTimeout. web tsc0/lint/build 통과. ⚠️런타임 E2E(실제 stuck job 만들어 재클릭 복구)는 미검증
- [P1] **analyst 자기평가 1패스(reflection)** — 현재 순수 feed-forward, 자기개선 루프 없음(D13-1). critiqueSchema+haiku 채점→score<70이면 refine 1회(N=1 종료조건). 근거(A-13-O1 §4-A, A-13-O2 §4) → `packages/ai/lib/crew/agents.ts`(critique 에이전트)·`orchestrator.ts` — 🟡 보류(지연·비용 실측 후)
- [P1] **MODEL_FAST(haiku-4.5) 실사용 전환** — `void MODEL_FAST` 미사용(D13-4). critique 채점용으로 활용(별도 모델 발급 불필요). 근거(A-13-O1 §3 D13-4) → `packages/ai/lib/crew/agents.ts` — 🟡 보류(critique 품질 A/B 후) [확인필요]
- [P1] **crew/route.ts org 스코핑 확인** — jobId로 AuditJob 조회 시 `where:{organizationId}` 강제 여부 실측(Day11 Tracking 우회 교훈). 근거(A-13-O2 §6) → `apps/web/app/api/audit/[jobId]/crew/route.ts` — ⬜ [확인필요]
- [P2] **durable 실행 전환(Vercel Workflow)** — `after()`→WDK step 기반 crash-safe resume. crew "2~10분"이면 300초 함수 타임아웃 위험(D13-3). ⚠️큰 변경, Day16 배포 트랙과 연계. 근거(A-13-O1 §4-C, A-13-O2 §3-②) → `apps/web/lib/audit/` — 🟡 보류(Day16)

### Day14 운영 자동화/관리자 콘솔에서 나온 것 (블라인드 재검증 발견)
- [P0] **stuck 스윕 cron(audit+crew)** — Day13 crew stuck P0를 cron으로 승격 통합(D14-4). `sweep-stuck-jobs`: status IN(queued,processing) 또는 crewStatus=processing이 createdAt/crewStartedAt 15분 초과 시 failed 정리(updateMany 멱등, 스키마 무변경). keep-alive 인증패턴 재사용. 근거(A-14-O1 §4-A, A-14-O2 §2) → `apps/api/app/cron/sweep-stuck-jobs/route.ts`(신규)·`apps/api/vercel.json` — ⬜ [Vercel cron 주기제한 확인필요]
- [P1] **운영 집계 cron(daily digest)** — 어제 audit 총건/성공/실패/평균소요·crew완료율·신규Lead 집계, log+Day17 알림 자리. 읽기전용. Day04 자동화후보①② 흡수. 근거(A-14-O1 §4-B, A-14-O2 §3) → `apps/api/app/cron/daily-ops-digest/route.ts`(신규) — ⬜
- [P1] **관리자 콘솔 최소판(apps/studio 채우기)** — 빈 껍데기(소스0, D14-2). 읽기전용 대시보드(audit현황·stuck목록·crew분포·최근Lead). ⚠️org 스코핑 아닌 **admin 역할 게이트**(크로스-org 정당하되 admin만, Day11과 반대방향). 근거(A-14-O1 §4-C, A-14-O2 §4) → `apps/studio/app/` — ⬜ [Clerk admin role 설정여부 확인필요]
- [P2] **콘솔 수동 재처리 버튼(쓰기)** — stuck job 수동 재큐잉 등. ⚠️실데이터 변경+admin 감사로그 필요. 근거(A-14-O2 §1) → `apps/studio` — 🟡 보류
- [P2] **주간 SoV 재측정 배치** — Day04 자동화후보③. 전략백로그 "주간 재측정+이메일 알림"과 병합(리텐션). 근거(A-14-O1 §4-D) → cron 라우트 — 🟡 (전략백로그 항목과 통합)

### Day15 사용자 테스트/PDCA에서 나온 것 (2026-07-24)
- [완료] **crew queued 굳음 사각지대 보강** — Day13/14 stuck 가드 E2E 검증 중 발견: queued 전환 직후 runner processing 전환 전 크래시하면 crewStatus="queued"+crewStartedAt=null로 굳어 route stale 판정(startedAt!==undefined) 못 받아 영구 stuck. → queued 전환 시 crewStartedAt 함께 세팅(멱등, 스키마무변경). 로직시뮬 6/6·web tsc0·biome. → ✅ **commit 2ce7f1b**(2026-07-24, feature/login-branding-2026-07). ⚠️라이브 DB stuck 주입 E2E는 미실행(Day17)
- [🔧 진행중] **stuck 스윕 cron 실코드 반영** — ⚠️**Day17 재검증(2026-07-28)으로 상태 정정**: "미반영(명세만)"이 아니라 **오늘 다른 세션이 실제 구현했으나 미정리·untracked**. 실측: `apps/web/app/api/cron/sweep-stuck-jobs/route.ts`(자체완결, web/vercel.json cron 등록 `0 1 * * *`) + `apps/api/app/cron/sweep-stuck-jobs/{route.ts,sweep.ts}`(헬퍼분리, keep-alive:25가 하루1회 합승 호출) = **web·api 이중구현**. 둘 다 `git ??`(untracked). → **BL-Day17-01에서 단일화 결정 필요**. 근거(A-14-O2 §2, A-16-O2 §1, **A-17-O1 D17-1**) → 사용자 확인 후 정리+커밋

### Day16 배포·CI에서 나온 것 (2026-07-27)
- [확인사실] **★D16-4 타임아웃 불일치 = stuck의 물리적 원인** — crew route 함수 상한 `maxDuration=300초`(apps/web/vercel.json) vs crew-runner `CREW_TIMEOUT_MS=720초`. 함수가 먼저 죽어 crewStatus="processing" 굳음. **runner 내부 가드만으로 stuck 못 막음이 실측 확정** → cron 스윕 물리적 필요성 근거. 후속판단: 함수 300↑ or CREW_TIMEOUT 300↓ 정합(Day17 연계) → `apps/web/vercel.json`·`apps/web/lib/audit/crew-runner.ts` — ⬜
- [🟡] **GitHub Actions 최소 CI 게이트** — `.github/workflows/` 부재(D16-1). CI는 Vercel Git 자동배포에 위임 중, 머지 전 lint/tsc 게이트 없음. `ci.yml`로 `turbo lint typecheck`만(빌드는 Vercel 중복 배제). ⚠️현재 1인·feature 직접배포엔 과투자→기여자 2인+/main 보호 도입 시 활성화. 근거(A-16-O1 §2-A, A-16-O2 §2) → `.github/workflows/ci.yml`(신규) — 🟡 제안·미도입
- [P2] **배포 롤백·프리뷰 운영 런북** — Vercel Instant Rollback·Preview URL·Production 승격 흐름 문서화(신규 코드 아님). 근거(A-16-O1 §2-C) → docs 런북 — ⬜(Day18 보안/런북 연계)

### Day17 로그·이벤트·알림에서 나온 것 (2026-07-28)
> 근거: A-17-O1~O3. next-forge로 3축 인프라(Logtail·Sentry·PostHog·Knock·Resend) 이미 연결됨 → gap은 도구가 아니라 정리·혼용.
- [P0·저위험] **BL-Day17-01 sweep-stuck-jobs 단일화 + 커밋** — web·api 이중구현·untracked(D17-1). 안 A=web 정본(배포앱 우선, api 미배포면 죽은코드) 권장. ⚠️**오늘 다른 세션 작업 진행중** → 의도 확인 선결 후 정리. 헬퍼는 `apps/web/lib/audit/sweep-stuck-jobs.ts`로. 근거(A-17-O2 §1) → 사용자 확인 후 — 🔧
- [P1·저위험] **BL-Day17-02 운영자 장애 알림** — stuck 스윕이 `log.warn`만 남고 아무도 안 봄(D17-5, "알림을 로그로 대신" 혼용함정). 임계 초과 시 Sentry `captureMessage`(warning) — **추가 의존 0**(Sentry 이미 연결). env `SWEEP_ALERT_THRESHOLD`(기본3). 근거(A-17-O2 §2) → `sweepStuckJobs()` 말미 — ⬜(sweep 단일화 후, Day18 런북 연계)
- [P2·저위험] **BL-Day17-03 로그 도메인 확장** — 로깅 audit 편중, 결제·브랜드·인증 무로그(D17-2). audit식 `도메인.대상.동작` 키 추가(`checkout.paid`·`brand.registered` 등). 근거(A-17-O2 §3) → PortOne 콜백·Day07/10 Server Action — ⬜
- [P2·저위험] **BL-Day17-04 전환 퍼널 이벤트** — PostHog 캡처가 클라3+webhook만, 중간단계 끊김(D17-3). `report_viewed`·`pricing_viewed`·`checkout_started` 추가 → 지불의사 검증(P0) 정량근거. 근거(A-17-O2 §4) → audit UI·pricing·checkout — ⬜
- [P2·중위험] **BL-Day17-05 Knock in-app 트리거** — Knock 연결만·발송 트리거 0(D17-4 반쪽구현). 선결: audit(익명 email)→계정 매핑(요금제/파트너 승인 연계). 근거(A-17-O2 §5) → `@repo/notifications` 트리거 — 🟡 선결 후

### Day18 보안·개인정보·런북에서 나온 것 (2026-07-28, 사전작성)
> 근거: A-18-O1~O3. 보안 인프라(Arcjet·usageGate·secret위생)는 견고(A-)→gap은 개인정보 정합·런북.
- [✅완료] **BL-Day18-01 처리방침 정합 정정** — ★PIPA: IP 실제수집(route.ts:146)하나 방침 §1 미고지(D18-1)+결제 위탁사 방침(토스/PayPal)≠실제(PortOne)(D18-2). **2026-07-29 실반영**: `apps/web/app/[locale]/legal/[slug]/page.tsx` KO/EN 4곳 정정 — privacy §1(IP 자동수집 추가+결제사 PortOne)·§2(IP 이용목적 (5)추가)·§3(IP 보존=기간 미명시 완곡 "목적 달성 시 파기", ∵파기cron 미도입=BL-18-03)·terms 제4조(PortOne). updated 날짜 7/29 갱신. **web tsc 0·biome 통과**. 백업=`legal/[slug]/_백업/page.tsx.v1.bak`. ⚠️미배포(git 미커밋·워킹트리) → 배포는 별도. 근거(A-18-O2 §1)
- [✅완료] **BL-Day18-02 최소 장애 대응 런북** — 런북 부재·대표1인 암묵지(D18-4). **2026-07-29 작성**: `docs/_적용/런북_장애대응.md`. 공통헤더(1차대응=대표·관측3종 Sentry/Logtail/Vercel·판단원칙) + 5유형(①사이트다운=Instant Rollback ②stuck=sweep 수동트리거 ③엔진장애=env 비활성 ④결제실패=PortOne 대사 ⑤개인정보 요청=email 기준 조회·삭제) + §6 알림연계(BL-17-02). 근거(A-18-O2 §2)
- [P2·중위험] **BL-Day18-03 IP 자동 파기 cron** — 방침에 보존기간 쓰면 파기로직 필수(안그러면 방침이 거짓말). auditJob createdAt 6개월↑ ipAddress=null, sweep cron 합승, 스키마무변경. 근거(A-18-O2 §3) → sweep-stuck-jobs — 🟡 방침 보존기간 확정 후
- [P3·저위험] **BL-Day18-04 Upstash rate-limit 중복 정리** — usageGate(DB 24h)로 충분, Upstash는 env 참조만. 근거(A-18-O1 D18-3) → `packages/rate-limit` — ⬜

### Day19 프로덕션 릴리스·릴리스 노트에서 나온 것 (2026-07-29, 사전작성)
> 근거: A-19-O1~O3. 배포 자동화(Vercel)는 있으나 릴리스 관리 공백. main 대비 feature 13커밋+미커밋 111파일.
- [✅완료] **BL-Day19-01 릴리스 노트 도입** — CHANGELOG 0(D19-2). **2026-07-29 생성** `docs/_적용/릴리스노트.md`: 형식 확립(✨새기능/🐛개선/🔒내부/⚠️주의/🔙롤백) + 이번 릴리스(main..feature 13커밋) [AI번역 초안]. ⚠️실릴리스 전 사용자 검수 필요. 근거(A-19-O2 §2)
- [P0·중위험] **BL-Day19-02 미커밋 111파일 커밋 분할** — untracked 50(sweep이중구현·digest cron)·modified 61(홈·audit·legal)(D19-3). 5묶음(①sweep단일화 ②digest ③legal ④홈 ⑤audit). ⚠️**BL-Day17-01 sweep 단일화 선결·실험분 확인·secret 재확인·사용자 승인**. git 조작이라 저위험 아님. 근거(A-19-O2 §1) → git — ⬜
- [P1·문서] **BL-Day19-03 릴리스 전략 결정** — 13커밋 전량머지(안A) vs 안정분 우선(안B, 권장). 롤백 계획(스모크+Instant Rollback) 세트. 근거(A-19-O2 §3) → 결정 사항 — ⬜
- [고위험] **BL-Day19-04 feature→main 머지·프로덕션 배포** — 위 전부 + 명시 승인 후. Vercel 자동배포라 머지=즉시 프로덕션(D16-1). 근거(A-19-O1 §3) → git·Vercel — ⬜(승인 필수)
- [✅확인사실] **실제 활성 PG = PortOne 단독** (2026-07-29 재검증) — PortOne=apps/web checkout+verify 실경로 / Stripe=apps/api webhook 잔재·api 미배포라 실동작X. 방침엔 PortOne만 표기.
- [✅확인사실] **auditJob.ipAddress 무기한 보존** (2026-07-29 재검증) — 수집(route.ts:146·156)만·파기 코드 0건·cron 3개 중 파기 없음. → 방침 보존기간 명시 시 BL-Day18-03(파기 cron) 세트 필수.
- [P1] **결과 후 crew 액션 자동 노출(P2)** — 결과 도달 시 crew not_requested→수동 트리거라 핵심가치(액션) 한 단계 숨음. 자동 트리거 or 라우팅 변경(M). 근거(A-15-O1 P2) → `apps/web/audit/[jobId]` — ⬜(실사용자 검증 후)
- [P2] **7개 엔진 약속↔결과 stub 문구 정합(P1)** — 랜딩 "7개" vs 결과 "미연결" 기대 불일치. "다음 측정부터 포함"(못 지킬 약속) 제거+라이브 수 강조. ⚠️실사용자 이탈 데이터 없는 가설. 근거(A-15-O1 P1) → `audit-result.tsx:973` 배너 — 🟡(실검증 후)

### Day03 PRD에서 나온 것
- [P1] **액션카드 v1 승격 검토** — Day02 발견 반영. PRD v1에서 Could로 배치, Day05 백로그에서 v1 승격 재검토. 근거(PRD_v1 §2·§6) → `apps/app` 액션카드 UI(승격 시) — ⬜
- [P1] **North Star 이벤트 로깅** — `action.executed`·`sov.delta`. North Star 재검토 확정 시 필요. → `apps/*` 이벤트 계측 — ⬜
- [P2] **Constraints 가격 재확인** — Clerk·Neon·Vercel·HyperCLOVA 공식 가격/한도 [확인필요]. → PRD Constraints 갱신 — ⬜

### 2026-07-22 전략 재검토(서브스택 결합+세계적상품)에서 나온 것
> 근거: `서브스택_결합_기획/MASTER_Findable_2.0_통합기획서.md`, `추가기능_후보_교차매트릭스.md`. 10에이전트 종합.
- [완료] **문서 Cue: 폐기 + AI Briefing 교체** — ROADMAP·PRD·PRD_v2·ARCHITECTURE·COMPETITORS 5개 문서 팩트 교정. Cue: 2026-04-09 종료 반영 → ✅ (2026-07-22)
- [완료] **COMPETITORS 한국경쟁 반영** — GPTO(Primer+Hashed)·오픈타임 등 진입, "무경쟁·First-mover" 주석 병기 → ✅ (2026-07-22)
- [P0] **네이버 AI Briefing 측정 켜기** — `naver-briefing-adapter.ts` 완성인데 `runner.ts:117` env(`FINDABLE_DISABLE_NAVER_BRIEFING`)로 비활성. 광고주 30초 약속 보호 위해 "네이버 전용 측정" 별도 버튼(비동기+알림)으로 노출. 한국 독점 차별점. → `apps/web/lib/audit/runner.ts`·audit UI — ⬜
- [P0] **진실거울 랜딩** — 7 AI가 브랜드를 뭐라 아는지 원문 나란히+팩트정합률. 이미 어댑터·`naver-vs-ai-gap.tsx`·크루 존재, UI 재조합. 3초 wow. → `apps/web/app/[locale]/audit/` — ⬜
- [P0] **포지셔닝/서사 전환** — "GEO측정"→"네이버 AI 가시성 셀프서브", 소유권서사("AI노출은 빌린것/자산은 적립"). 코드 0, 랜딩 카피. → `apps/web` 랜딩 dictionary — ⬜
- [P1] **audit 히스토리/시계열** — `Tracking`/`Brand`/`Report` 테이블 스키마 완성인데 앱 사용 0(schema.prisma:54-149). 1단계=email기준 이전 audit 대비 점수변화(스키마무변경), 2단계=로그인시 Tracking 적재. → `apps/web/lib/audit/runner.ts`·`apps/app` — ⬜
- [P1] **코파일럿 챗** — 크루 4에이전트(Mastra `.generate()` 대화가능)에 챗 UI만. crewResult 컨텍스트 주입. → 챗 API 라우트 + `apps/web/audit` 컴포넌트 — ⬜
- [P1] **팩트정합률 전면화 + before/after 케이스 1건** — 자기채점 넘어 실증. vitamin→painkiller 증거. → audit 결과 UI + 실측 1건 — ⬜
- [P2] **GEO 실행 에이전트**(진단→리라이트→발행준비, ⚠️자사통제소스만+인간승인) — 6개 경쟁사 전원 공백. ⚠️나무위키 직접편집 금지(표시광고법). → 크루 확장 — 🟡 데모데이 후
- [P2] **주간 재측정+이메일 알림** — cron 패턴(`apps/api/app/cron/keep-alive`) 재활용. 리텐션. → cron 라우트 — ⬜
- [P3] **귀속픽셀(F8)** — ⚠️PIPA(IP·리퍼러 개인정보) 검토 필수. 데모데이 후. → v1.5 F8 — 🟡 보류

---

## ✅ 완료
<!-- 완료 항목 이동 -->
