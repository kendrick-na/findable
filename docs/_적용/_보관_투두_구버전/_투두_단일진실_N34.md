# 📋 v4 전체 투두 정리 — 단일 진실 (2026-08-16 세션N-34)

> ⛔ **이 문서가 남은 일의 단일 진실이다.** v4 §6 착수순서표·§8 미해결표는 **여기로 흡수**됐다.
> 🔴 **문서 기재를 그대로 옮기지 않았다** — 항목마다 코드·DB로 실측해 상태를 다시 판정했다.
> 📕 근거: `_실측_0결과_분포_N34.md`(전수 95건) · `_START_HERE_세션N34.md`

---

## 1. ✅ 끝난 것 (되살리지 말 것)

| v4 순번 | 작업 | 결과 |
|:--:|---|---|
| 1 | 랜딩 정렬·타이포 | ❌ **취소**(N-32 재실측: `keep-all` 0곳 · 정렬 경고는 **중앙정렬 오탐**, 중심오차 0.00px) |
| **2** | **지표 사전 + 분모** | ✅ `44edaf2`·`7bbe478` — 축 5개 전부 닫힘(아래 §1-a) |
| **3** | **「평가」 탭** | ✅ `e8337b0`·`3cd65a7` — **감성 정직화 + 섹션**으로 형태 변경(아래 §1-b) |
| **4** | 0결과 3요소 | ✅ `4bc27bc` — **엔진 카드 단위로 축소**(전면 화면은 대상 0건) |
| — | A 모션 · B 결함 · D 실데이터 · E 소셜프루프 | ✅ N-32·N-33 |
| — | 죽은 홈 컴포넌트·사전 키 | ✅ `e4af992` **-833줄** |

### 1-a. 지표 사전 축 5개 (v4 §4-a-2) — **전부 완료**
| 축 | 무엇 | 커밋 |
|:--:|---|---|
| ① | 분모 축 3종 명명(응답·고유엔진·성공행) | `44edaf2` |
| ② | 방향 역전 표시(`낮을수록 좋음`) | `44edaf2` |
| ③ | 0 vs null 통일 | `44edaf2` |
| ④ | 지표 이름 통일(`언급률`→`등장률`) | `44edaf2` |
| ⑤ | 인용 정의·용어 충돌 해소(`인용됨`→`우리를 말함`) | `9ba3acf` |

### 1-b. 🔴 v4 가 몰랐던 것 — 「평가 탭」은 데이터부터 썩어 있었다
탭을 만들기 전 감사에서 **결함 2건**이 나왔다. 그냥 만들었으면 **틀린 숫자를 더 큰 화면에** 걸었다.
- 화면이 상태를 **좋게 반올림**: 긍정 12~15%인 브랜드 3개가 전부 「우호적」 → 「중립적」로 교정
- `별로` 가 **조사**(`용도별로`)를 부정으로 셈 → 실측 4건 전부 조사형이라 키워드 제거

---

## 2. ⏸️ 보류 — 데이터·정책이 막고 있다 (코드 문제 아님)

| v4 순번 | 작업 | 막는 것 |
|:--:|---|---|
| **6** | **탭 전체 재편(L)** | 🔴 `Tracking` **75행 · 브랜드 3개**뿐. 탭 8개 재편 = **빈 탭 만들기**. → **P0 유입 선행** |
| **7** | 7일 트라이얼(L) | ⛔ **카카오페이 심사 후.** 기획만 보존 · `packages/payments` 미접촉 |
| — | 커머스 2단계(XL) | ⬜ Q4 |
| — | 순위 카드 `/compare` 유료 경계 | 👤 **가격 정책 판단** — 경쟁사 비교는 유료가 맞다고 봄 |

> 🔴 **v4 §8-5(`Tracking` 행수 실측)는 완료됐고, 답은 "6번을 하지 말라"였다.**
> 75행·브랜드 3개 · **전 브랜드 측정일 1일**(추세 불가). 적재 3중 조건이라
> **무료진단(비로그인)분은 한 줄도 안 쌓인다** — v4 예측이 맞았다.

---

## 3. ⬜ 지금 할 수 있는 것 (근거 있음 · 규모 순)

| # | 작업 | 규모 | 상태 |
|:--:|---|:--:|---|
| ~~A~~ | `/` 이력 섹션 제거 | XS | ✅ `065204a` |
| ~~B~~ | 고아 화면 2개 | S | ✅ `065204a` — `/webhooks` **삭제** · `/admin/evidence` **연결**(정반대 판정) |
| ~~C~~ | 「AI별」 분리 | M | ❌ **기각** — 이미 잘 만들어져 있고(`sources-board:151`), 무료 쪽엔 G-2 `byEngine` 이 있다. 분리 = **유료 경계 재설계**라 👤 정책 |
| **D** | 🔴 **`loading.tsx` 신설**(v4 P0-2) | **M** | ⬜ **다음** — 아래 |

### 🔴 D — v4 P0-2 가 탭 재편에 묶여 **같이 멈춰 있었다**
`[실측 2026-08-16]` `loading.tsx` **repo 전체 0개** · `<Suspense>` **0건**.
`/` 는 `currentUser`→`auth`→`getCurrentPlan`→`auditJob.findMany(20)`→`scopedTracking`
→`scopedAnnotations` 를 **순차 await** 한 뒤에야 첫 픽셀을 그린다.

🔴 v4 는 이걸 *"탭 재편(6번)의 전제"* 라며 묶었는데 **탭 재편이 보류**라 함께 멈췄다.
→ **분리한다.** 로딩 상태는 탭이 없어도 지금 화면에 필요하다
  (§2-d 「4곳 공통 실패 #5 = 로딩 실패 미설계」를 우리가 그대로 재현 중).
⚠️ 단 **눈으로 확인하려면 로그인이 필요**하다 → 배포 후 검증이 남는 항목.

---

## 4. 🔴 코드 밖 — 진짜 병목

| 항목 | 상태 |
|---|---|
| 🔴 **배포** | **19커밋 미배포**(N-31부터). 고친 게 **고객에게 하나도 안 갔다**. 👤 보류 지시 중 |
| 🔴 **대시보드 눈확인** | G-1·G-2 를 **실제 대시보드로는 못 봤다**(로컬 Clerk 개발 인스턴스라 로그인 불가) → **배포 후 필수** |
| 🔴 **P0 실고객·유통** | 유저 0명 · Tracking 브랜드 3개. **모든 시간축 기능의 뿌리 원인** |
| ⬜ 법무(트라이얼 자동갱신 고지) | v4 §8-8 · 7번과 함께 |

---

## 5. ⛔ 되살리지 말 것

| 대상 | 이유 |
|---|---|
| **랜딩 정렬·타이포(v4 1번)** | 재실측에서 **오탐 확정**(중심오차 0.00px) |
| **전면 「0 결과」 화면** | 대상 **0건 / 95건**(전수) |
| **web GEO 점수 5축 라벨** | v4 §4-a-1 ⑧ *"잘 정리돼 있다"* · N-17 이 척도 오독을 이미 고침 |
| **web `sentimentLabel`** | S7-4차 *"없는 차이를 만들지 않는다"* 판단이 옳다 |
| **감성 임계값(±2)** | 근거 없는 경계선 발명 + 저장된 95회차 재해석 |
| `packages/payments/*` | 다른 세션 미커밋 + 심사 중 |

---

## 6. 📌 이 정리가 v4 를 고친 곳 (7세션 연속 인계서 오류)

| v4 기재 | 실측 |
|---|---|
| §4-a-1 ④ *"`measurement-coverage` 운영 0건"* | ⚠️ **앱을 잘못 짚음** — `apps/web` 은 3곳에서 쓴다 |
| §4-a-1 ③ app `AI N곳에 물어` | ✅ **이미 고쳐져 있었다**(`4993b0c`) |
| §6-4 0결과 3요소 | 🔴 대상 **0건** → 엔진 단위로 축소 |
| §6-6 탭 재편 | 🔴 데이터 없음 → 보류 |
| §6-3 「평가」 탭 *"쿼리 변경 0 · 가성비 최고"* | ⚠️ 맞지만 **데이터가 썩어 있었다** — 정직화가 선행 |
| §8-5 Tracking 행수 | ✅ 실측 완료 = **75행·브랜드 3개** |
