# 🎯 Findable 남은 일 — **단일 진실 (세션N-25 전면 재정리)**

> 갱신: **2026-08-13 (세션N-27)** · 이전판 = `_투두_단일진실_N24.md`(→ **근거·판정 이력 보관용으로 격하**)
> 🧭 **새 세션은 `_START_HERE_세션N28.md` 를 먼저 열 것** — 승인 대기 설계안이 거기 있다.
> 🔴 **"무엇이 남았나"의 단일 진실은 이 파일이다.**
> ⛔ 다른 문서의 `⬜` 를 믿지 말 것 — `실행백로그.md`·`투두_마스터.md` 는 2026-07 에 멈췄다.

---

## 0. 📊 지금 상태 (실측)

| 항목 | 값 |
|---|---|
| 테스트 | **309/309** (N-25 종료 249 → N-26 **+60**) |
| tsc | web 0 · app 0 |
| lint | 신규 0 · 🔴 **기존 부채 실측 65건**(문서가 23건이라 적었다 — N-26 정정) |
| 배포 | web·app **둘 다** 배포·확인(N-26) |
| 🔴 유저 | **0명** · 유료 고객 **0명** · 조직 1개(자체 테스트) |

⚠️ **다른 세션이 `packages/payments/` 를 동시 작업 중**(`billing-cycle.ts` 신설 · `portone.ts` +68줄).
→ **결제 트랙은 이 세션이 건드리지 않았다.** 충돌 방지.

---

## 1. 🔴🔴 이 프로젝트의 진짜 병목 — **코드가 아니다**

```
트래픽 0 → 유저 0 → 유료 0 → 매출 0
```

세션N-25 가 고친 것은 전부 *"고객이 왔을 때 배신하지 않게"* 하는 작업이다.
**그런데 아직 고객이 오지 않는다.** 코드 품질이 병목이 아니다.

### 👤 사람이 움직여야 하는 것 (불변 1순위)
| # | 할 일 | 내가 도울 수 있는 것 | 내가 못 하는 것 |
|:--:|---|---|---|
| **P0** | 🔴🔴 **마케터 10~15명 실제 인터뷰** | 질문지·대상 리스트·거절사유 정리·녹취 분석 | **사람을 만나는 것** |
| **P1** | 🔵 **KIPO 상표 출원**(선출원주의 · 시간이 불리) | 지정상품 분류·선행상표 검색 정리 | 출원·수수료 납부 |
| **P2** | 🟠 카카오페이 승인 회신 대기 | — | 상대방 회신 |

⛔ **시뮬레이션으로 P0 대체 불가**(r=0.20). *"비관적으로 돌리면 보수적"* → **아니다, 노이즈다.**
⭐ **세션N-25 가 퍼널 계측을 깔았다** → 이제 인터뷰에 *"제출 N건 중 거절 M건"*,
   *"무료 체험 다 쓰고 가입 화면 본 K명"* 을 **숫자로** 들고 갈 수 있다.

---

## 2. 🖥️ 코드로 지금 할 수 있는 것 (원가 0 · 승인 불필요)

> 정렬 = **제품 가치 순**. 크기(S/M/L)는 참고값.

| # | 할 일 | 크기 | 근거 | 상태 |
|:--:|---|:--:|---|:--:|
| **1** | ~~**429 에러에 문의 링크**~~ | S | 퍼널감사 | ✅ **완료**(세션N-26 · `b7d890b` · 배포·라이브 번들 확인) |
| **2** | 🟡 **`/en` `<title>` 한국어 노출** | S | 🔴 **세션N-26 재현 실패** — 아래 §2-1 | ⛔ **보류** |
| **3** | ~~요금제 「현재」 배지~~ | S | 실측 | ✅ **완료**(N-26 · `f46d6bc`) |
| **4** | ~~`/history/[jobId]` 앱 내재화~~ | **L** | 🔴 **N-27 재평가: 착수 권하지 않음** — 아래 §2-3 | ⏸️ **보류** |
| **5** | **lint 부채** — 🔴 N-27 실측 **81건**(65 도 낡았다) | M | ⚠️ 고객에게 안 보인다 · 단독으로 열지 말 것 | ⬜ |
| **7** | 🆕 ~~**가입·결제한 고객에게 「가입하세요」**~~ — 목록에 **없던 결함**(N-26 이 #4 파다가 발견) | M | 실측 | ✅ **완료**(N-26 · `75438e1`) |
| **8** | 🆕 ~~**메일 발송 실패해도 "곧 보내드려요"**~~ — 목록에 **없던 결함**(약속↔실제 감사로 발견) | S | 실측 | ✅ **완료**(N-26 · `4760a39`) |

### 🔒 미해결로 등록한 보안 항목
| # | 항목 | 판정 |
|:--:|---|---|
| **6** | ~~공유링크 이메일 노출~~ | ✅ **완료**(N-26 · `3633629`) — **서버측 소유자 판별** 도입. `@repo/audit/ownership` · fail-closed · 컴파일 산출물에서 `...a?{emailMasked...}:{}` 확인 |

---

### 🔍 세션N-26 **투두리스트 자체 감사** — 실측으로 4건 정정

> 🔴 이 목록의 **순서와 크기가 실제와 어긋나 있었다.** 착수 전 실측이 규칙인 이유.

| # | 문서의 주장 | 실측 | 판정 |
|:--:|---|---|---|
| 2 | `/en` `<title>` 한국어 노출 | `en.json` 한글 **0건** · `packages/seo/*` 하드코딩 **0건** | 🔴 **재현 실패** |
| 4 | 유료 고객이 제품 밖으로 나간다 | 이미 `target="_blank"` + **"새 탭"** 글자 고지(`S7-2차`) | 🟡 **과장** |
| 5 | lint 부채 **23건** | 실측 **65건** | 🔴 **약 3배 과소** |
| 6 | 공유링크 이메일 노출(맨 아래) | 라우트에 인증 호출 **0개**, 방어는 `window.location`=클라이언트 전용 | 🔴 **가장 심각한데 맨 아래** |

#### §2-1 `/en` 제목 — ⛔ **재조사 전 조건 확인**
한국 IP 는 `/en` → `/ko` 로 **307 리다이렉트**된다(`proxy.ts` 지역 판정).
→ 👤 **해외 IP·VPN 없이는 이 항목을 확인할 수 없다.** 사전 확인 없이 다시 파지 말 것.
코드상으로는 사전·SEO 패키지 모두 깨끗하므로 **결함이 없을 가능성**이 높다.

#### §2-2 #4 축소 재정의 — *"밖으로 나간다"* 가 아니라 *"로그인을 모른다"*
`audit-history-list.tsx:124` 는 이미 **새 탭**으로 열어 대시보드를 잃지 않게 한다
(세션 S7-2차가 고쳤고 *"새 탭"* 이라고 글자로도 밝힌다).
→ 남은 실익은 **www 결과 페이지가 로그인을 인식하지 않는 것**뿐이다. L 을 통째로
   들이기 전에 *"그래서 고객이 무엇을 못 하나"* 를 먼저 한 줄로 답할 것.

> ⭐ **그 질문의 답을 세션N-26 이 찾았고, 그게 #7 이 됐다**(아래).
> *"로그인을 모른다"* 의 **가장 비싼 증상**은 화면 이동이 아니라
> **가입·결제한 고객에게 「가입하세요」를 띄우는 것**이었다.
> → #4(L)를 통째로 하지 않고 **그 증상만 M 으로 잘라 먼저 고쳤다.**

---

### ✅ 세션N-26 완료 — 429 문의 링크 (`b7d890b`)

서버가 *"문의해 주세요"* 라고 안내하면서 **클릭할 데를 주지 않던 것**을 연결했다.
- `shouldOfferContact`(순수함수) 신설 → **`budget`·`ip_capped` 에만** 노출.
  ⛔ `invalid`(오타 = 고객이 고칠 수 있음)·`bot` 은 제외 — 문의함 오염 방지.
- 판정을 **한 번만** 내서 계측과 화면이 같은 값을 본다
  (*"이벤트엔 `ip_capped` 인데 링크는 없다"* 어긋남 차단).
- 검증: 262/262(+13) · web·app tsc 0 · 린트 34건(**변경 전과 동일**) ·
  라이브 번들에서 `문의하기`/`Contact us`/`/contact` 확인 · `/ko/contact` HTTP 200.
- ⭐ **가드 양방향 확인**: *"항상 true"* 뮤테이션에 **4건 실패** · 렌더 제거에 **1건 실패**.

> 🔴 **IP 상한 값(2건)은 그대로 두었다.** `ip_capped` 계측이 쌓인 뒤 **분포를 근거로**
> 판단한다. 지금 2→5 로 올리면 **근거 없이 숫자를 발명**하는 것이다(이 프로젝트 금지사항).
> 📌 판단 조건 = PostHog 에서 `ip_capped` 가 유의미하게 잡힐 때.

### ✅ 세션N-26 완료 — 🔒 공유링크 이메일 노출 (`3633629`) · **#6 정석 해결**

`/api/audit/[jobId]` 가 **소유 검사 없이** `emailMasked`·`emailDomain` 을 항상 줬다.
화면 방어(`?shared=1`)는 `window.location` 을 읽는 **클라이언트 전용**이라 두 구멍:
①주소창 URL 을 그대로 복사해 보내면 표식이 없어 제3자가 본다 ②**API 직접 호출 시 방어 0**.

- `@repo/audit/ownership` **순수함수** 신설 — 판정 규칙은 **앱 대시보드와 동일**
  (로그인 이메일 ∪ org FK ∪ 레거시 `org:{orgId}`). 두 벌이 되면
  *"대시보드엔 내 측정인데 결과 페이지는 남이라고 한다"* 가 된다.
- 🔴 **fail-closed** — 비로그인·판별 실패는 비소유. 단 **결과 조회 자체는 살린다**
  (이메일 표기 하나로 500 을 내면 고객이 결과를 통째로 못 본다).
- ⛔ **과잉 매칭 금지** — 같은 도메인 동료·gmail `+alias`/`.` 은 **남**이다.
  (과소 매칭보다 **과잉 매칭이 위험**: 남의 진단을 내 것으로 판정한다.)
- ⚠️ **필드를 비운다**(가리지 않는다) — 마스킹 값이라도 보내면 네트워크 탭에 남는다.

> 🔬 **라이브 검증법**(BotID 가 curl 을 막아 실 job 생성 불가):
> **컴파일 산출물**에서 `...a?{emailMasked:...,emailDomain:...}:{}` 를 확인했다
> (`.next/server/chunks/695ea_*.js`). 게이트가 컴파일 후에도 살아있다는 직접 증거다.
> ⚠️ **범위 과장 금지** — jobId 는 UUIDv4 + `robots.txt` 제외라 **대량 수집 경로가 아니었다.**
> 소유자가 링크를 건넨 상대에게 새는 **표적형** 노출이었다.

### ✅ 세션N-26 완료 — 🆕 **#7 가입·결제한 고객에게 「가입하세요」** (`75438e1`)

> 🔴 **투두리스트에 없던 결함이다.** #4 의 *"그래서 고객이 무엇을 못 하나"* 를
> 파다가 나왔다 — 목록을 따르는 것보다 **목록을 의심하는 것**이 값을 냈다.

가입·결제를 마친 고객이 **자기 무료진단 결과**에서 심층분석을 누르면
*"무료 심층 분석을 이미 사용하셨어요. **가입하시면** 계속 이용할 수 있어요."* 를 본다.

🔬 **원인**: `canRunDeepAnalysis(job.email)` 은 **문자열만** 본다 → `org:` 로 시작하는
**앱에서 만든 측정만** 통과. 고객이 www 에서 받은 무료진단 job 은
**`email` 이 개인 주소 그대로** 남는다(**결제해도 안 바뀐다**).

🔴 **같은 job 을 두고 제품 두 곳이 서로 다른 말을 했다**:
| 어디 | 판정 근거 | 결론 |
|---|---|---|
| 대시보드(`(authenticated)/page.tsx:47`) | 로그인 이메일 ∪ org | **"내 측정"** |
| crew 게이트(`crew/route.ts`) | `job.email` 문자열 | **"남"** |

⭐ 앞 커밋 `4492022` 가 **스스로 적어둔 약속**이 *"실질은 **로그인 게이트**이고
free 플랜도 **가입하면 열린다**"* 였다 → **그 약속을 실제로 지키게** 만든 작업이다.

- 자격 = **소유자 OR 기존 tier**(`isAuditOwner` 재사용 = 대시보드와 같은 규칙)
- `resolveIsOwner` 를 **`_lib/owner.ts` 로 승격** — `[jobId]`(이메일 노출)와
  `crew`(자격)가 같은 판정을 쓴다. 복사하면 한쪽만 고쳐진다
- ⚠️ **과잉 개방 아님** — 비로그인·남의 진단은 그대로 차단(테스트로 고정)
- ⚠️ 원가는 **전역 일일 상한**이 막는다(자격 판정 **앞**에 유지 · **순서를 테스트로 고정**)

> 🔬 컴파일 산출물 확인:
> `await resolveIsOwner(e)||canRunDeepAnalysis(e.email)?null:await I(...)`

### ✅ 세션N-26 완료 — 🆕 **#8 메일 발송 실패해도 「곧 보내드려요」** (`4760a39`)

> 🔴 목록에 없던 결함. **"약속 ↔ 실제" 축으로 화면을 훑다가** 나왔다
> (#7 을 찾은 것과 같은 렌즈 — *"화면이 약속한 것을 제품이 지키는가"*).

*"풀 리포트 받기 · 무료"* 를 누르면 화면이 **언제나**
*"전체 리포트를 곧 메일로 보내드려요"* 라고 답했다. 그런데 서버는 발송 실패 시
`emailSent:false` 로 **정직하게 답하고 있었다** — 화면이 그 진실을 **계측에만**
쓰고 버렸다. → 고객이 **오지 않을 메일을 기다린다.**

- `getLeadResultMessage(emailSent, isKo)` 순수함수로 분기
- ⭐ 실패를 **에러로 말하지 않는다** — 리드 저장은 성공했고 **이 화면 자체가
  전체 리포트**다. *"다시 시도"* 가 아니라 **"여기서 보실 수 있어요"**
  (원칙: *"우리가 해준다"* ❌ → *"여기서 볼 수 있다"* ⭕)
- 서버: 메일 안 보낸 **3갈래 전부** `emailSent:false` 명시
  (예전엔 필드를 빼고 클라이언트 `?? false` 에 기댔다 = **우연히** 맞은 것)

> ⚠️ **범위 정직**: 프로덕션에 `RESEND_TOKEN` 이 **실제로 있다**(env 확인) →
> 상시 거짓말이 아니라 **발송 실패 시** 거짓말이었다. 과장하지 않는다.
> 🔬 라이브 번들 확인: `메일 발송에 실패했어요`·`right here on this page` 등 4문구.

### ✅ 세션N-26 완료 — 요금제 「현재」 배지 (`f46d6bc`)

조건이 `isCurrent && !tier.featured` 라서 **추천 플랜을 결제한 고객은 「현재」를
영영 못 봤다**(배지 슬롯 1칸을 「추천」이 차지). 하단 CTA 는 *"이용 중"* 이라
**같은 화면이 서로 다른 말을** 하고 있었다.
→ **사실이 마케팅을 이긴다**: 현재 플랜이면 「추천」이 자리를 양보한다.
⭐ 가드가 *"둘 다 켜짐"* 도 결함으로 고정한다(슬롯 1칸이라 줄이 밀린다).

### §2-3 🔴 #4 재평가 (세션N-27) — **L 을 쓸 값이 남아 있지 않다**

실측으로 다시 봤다:
- `apps/app` 에 `[jobId]` 라우트 **0개** — 전제는 맞다
- 히스토리는 이미 **새 탭**으로 연다(`audit-history-list.tsx:126 target="_blank"`)
- www 결과페이지는 로그인을 **전혀 모른다**(`audit/[jobId]/page.tsx` 에 `auth()` **0건**)

그런데 **그 무지가 만들던 가장 비싼 증상은 N-26 이 이미 고쳤다**
(가입·결제한 고객에게 「가입하세요」 → `75438e1`, crew API 게이트에서 소유자 판별).

🔬 지금 비로그인으로 결과페이지를 열면 제안은 **"Measure again (sign-up)"** 하나뿐이고,
   이건 비로그인에게 **정확한 제안**이다. → **고객이 못 하는 일이 남아 있지 않다.**

⏸️ **결론: L 을 들이지 않는다.** 유료 고객이 늘어 *"결과페이지에서 바로 재측정하고 싶다"* 는
   실제 요구가 나오면 그때 착수한다. 지금은 **유저 0명**이라 근거가 없다.

---

## 2-C. 🎨 UI/UX 정식 감사 — **승인 대기 (세션N-27 후반부)**

> 🔴 **`Skill(ui-ux-quality)` 정식 절차로 다시 감사했다.** 전반부(U1~U15)는 스킬을 안 쓰고
>    a11y 하나만 돌렸던 것이라 👤 지적을 받고 처음부터 다시 밟았다.
> 🔴 **측정만 하고 코드는 안 건드렸다** — 배포된 실사용 화면이라 승인 전 코드 금지.
> 📕 상세 = `_START_HERE_세션N28.md` §2·§3 (**재측정 금지 · 근거 전부 실측**)

### 👤 가 지목한 5건 — 전부 진짜 문제였다

| # | 지적 | 실측 판정 |
|:--:|---|---|
| ① | 엔진 열 스크롤 | 🔴 모바일 **313px 잘림**(653 vs 340). **네이버·다음이 화면 밖** = 차별점을 숨김 |
| ② | 엔진별응답 vs 진실의 거울 | 🔴 **중복 확정** — 두 섹션이 **동일 원문** 출력(`ia.md` 기준) |
| ③ | 96% vs 100% | 🔴 **분모가 다름**(둘 다 산술은 맞음): 응답 26/27 vs 엔진 7/7 |
| ④ | 대시보드가 무료진단과 같다 | 🔴 **구조가 실제로 같다** — **같은 러너 `runAuditJob`**. 유료는 *자동 재측정·이력*만 더 받는다 |
| ⑤ | 메뉴 느림 | 🟡 **홈만** 7.76초(콜드). 나머지 9개 0.49~0.75초 정상. 원인 = 홈 `await` **7개 직렬** |

### 도구가 잡은 것 (눈·axe 로는 못 봄)
```
🔴 근사 불일치 16건 · 세로간격 37종(목표≤6) · 한글 음수자간 4곳(결과페이지)
   모바일 11.0화면 · PC 6.8화면 · 질문 0개 · 경계침범 0 · 근접성역전 0
```

### ⬜ 승인 대기 — 8층 설계안은 `_START_HERE_세션N28.md` §2
👤 미답 2건: **Q1 범위**(A안 전부 / B안 느림+숫자만 / C안 위임) · **Q2 대시보드 비주얼**을 묶을지 뺄지

---

## 2-B. 🎨 UI/UX 감사 결과 — **실행 대기** (세션N-26 신설 · 전부 실측)

> 🔴 `ui-ux-quality` 스킬 정식 절차로 감사(measure·a11y·align·squint 4종 + IA 인벤토리).
> **근거는 전부 실측이다. 재감사 말고 바로 고칠 것.** 상세 = `_START_HERE_세션N27.md` §3
> ⚠️ playwright 필요 → **Findable 폴더에서** 스크립트 실행

| # | 할 일 | 실측 근거 | 영향 | 크기 |
|:--:|---|---|---|:--:|
| ✅ **U1** | `text-zinc-500` → `zinc-400` | **4.32:1 → 8.14:1** | 결과페이지 51곳 | S |
| ✅ **U2** | `ink-tertiary` `#62666d`→**`#7e8289`** | **3.62:1 → 5.41:1** | web+app 76곳 | S |
| ✅ **U3** | 모바일 메뉴 버튼 `aria-label` | axe **critical** | 스크린리더 차단 | S |
| ✅ **U9** | `search.tsx`·`search/page.tsx` 삭제 | 진입링크 **0** | ROT | S |
| ✅ **U10** | 오렌지 배지 글자 흰색→canvas | **2.58 → 8.09** | 8곳 | S |
| ✅ **U11** | `/pricing` 문장 속 링크 상시 밑줄 | 1.77:1 (색만) | 1곳 | S |
| ✅ **U12** | 🔴 헤더 흰배경 = 로고 안 보임 | **1.06 → 19.61** | **3페이지** | S |
| ✅ **U4** | 엔진 라벨 3쌍 겹침 | 모바일 3칸 전환 | **모든 모바일 방문자** | S |
| ✅ **U5** | `SelectTrigger` `w-full` | 297px → 195px 내 | WCAG 1.4.10 | S |
| ✅ **U6** | 이메일 `overflow-wrap` | 355px → 190px | WCAG 1.4.10 | S |
| ✅ **U7** | `prefers-reduced-motion` | 0건 → 전역 대응 | 전정장애 | S |
| ✅ **U8** | 한글 음수 자간 | 한글 **11→0**(영문 유지) | 가독성 | S |
| 🟡 **U9-b** | `webhooks/` 보일러플레이트 | `env.ts` 결합 | ⚠️ payments 세션 끝난 뒤 | S |

### ✅ U1·U2·U3 완료 (세션N-27) — 결과페이지 axe 위반 **51곳 → 0곳** (브라우저 실측)

🔴 **인계서 숫자 2건이 틀렸다. 실행 전에 검산해서 잡았다:**

1. **U2 제안값 `#72767d` 는 부족했다.** 검정(canvas)에서만 4.57 이고
   **카드 배경(surface-1~4)에서는 4.17~3.82 로 미달**이었다. `ink-tertiary` 는 카드 위에서도
   쓰는 토큰이라 그대로 넣었으면 *"127곳 해결"* 이 거짓이 될 뻔했다.
   → 👤 승인 받아 **`#7e8289`** 채택 = **5개 배경 전부 AA 통과**(최악 surface-4 에서 4.52).
2. **`text-zinc-600`(2.70:1) 이 목록에 아예 없었다.** U1 을 끝내고 재측정하니
   **14곳이 남았다**. zinc-500 보다 더 나쁜 값인데 감사에서 누락된 것.
   → 결과페이지 8곳 추가 수정 후 **0곳** 달성.

⚠️ **밝은 배경은 의도적으로 제외했다**(일괄치환 금지):
`packages/email/templates/*` · `logo-preview` 는 `bg-white`/`bg-zinc-50` 이라
zinc-400 으로 바꾸면 **4.83 → 2.56 으로 오히려 악화**된다. 총 13곳 보존.

🔬 검증: `a11y.mjs` 로 로컬 dev(:3111) 실측 · web·app `tsc` 0 · 테스트 309/309 ·
변경 105줄 **전부 색 클래스**(diff 로 확인) · 다른 세션의 `packages/payments/` 미접촉

### 🆕 U10·U11 — 감사에서 **새로 발견**(브랜드 색 결정 필요 → 👤 판단 대기)

| # | 문제 | 실측 | 왜 지금 안 고쳤나 |
|:--:|---|---|---|
| **U10** | 오렌지 배지/버튼 위 **흰 글자** | **2.58:1** (필요 4.5) | `--findable-primary #ff7a4d` 는 **D-062 브랜드 시그니처**. 글자를 검정(6.75)으로 바꾸거나 오렌지를 어둡게 = **브랜드 룩 변경** → 대표님 결정 사안. 8곳 |
| **U11** | 링크가 **색으로만** 구분 | **1.77:1** (필요 3.0) | `hover:underline` 이라 평소엔 밑줄 없음. 상시 밑줄로 바꾸면 디자인 인상 바뀜. 3곳 |

### 🔴🔴 U12 — **"워드마크 1.06:1 = 오탐" 은 오판이었다. 진짜 버그다.** (세션N-27 정정)

세션N-26 이 *"axe 가 투명 배경을 흰색으로 오인"* 이라며 기각했는데, **확인해 보니 아니었다.**
조상 체인을 타고 실제 계산값을 읽으니 `<header>` 의 `bg-background`(shadcn 라이트 토큰)가
**진짜로 흰색**(`lab(100 0 0)`)이었다. 페이지 배경만 검정이고 **헤더 띠만 흰색**이었던 것.

```
헤더 배경 lab(100 0 0) = 흰색   ←  bg-background (shadcn 기본 = 라이트)
워드마크  #f7f8f8      = 흰색   ←  색 지정이 없어 상속
→ 흰 글자 on 흰 배경 = 1.06:1 · 로고가 육안으로 거의 안 보였다
```

🔬 **스크린샷으로 눈으로 확인했다** — 측정값이 아니라 그림에서 로고가 사라져 있었다.
(교훈: `axe` 를 오탐이라 부르기 전에 **계산된 배경색을 직접 읽는다**)

| 무엇 | 전 | 후 |
|---|---|---|
| `<header>` 배경 | `bg-background`(흰색) | `bg-[var(--findable-canvas)]` |
| 모바일 메뉴 패널 | `bg-background`(흰색) | `bg-[var(--findable-canvas)]` |
| 워드마크 글자 | 색 지정 없음(상속→페이지마다 뒤집힘) | `text-[var(--findable-ink)]` |

영향 = 이 헤더를 쓰는 **`/audit`(주 전환 진입점)·`/pricing`·`/contact` 3페이지 전부**.
결과: 세 페이지 모두 axe **0건**(전 /pricing 3건·/audit 1건·/contact 1건).

⚠️ **`/contact` 는 원래 안 깨져 보였다** — 그 페이지만 로고 글자가 검정으로 상속됐기 때문.
헤더를 어둡게 고치자 이번엔 *그 페이지가* 깨졌고, 그래서 **글자색까지 명시**해 3페이지를 통일했다.
👉 *색을 상속에 맡기면 페이지마다 뒤집힌다* 는 게 이 버그의 진짜 원인.

### ✅ U13 — 모바일 가로 넘침 **해결** (원인은 헤더가 아니었다)

🔴 **내가 처음 지목한 원인(헤더 `container`+`absolute`)은 틀렸다.**
진짜 원인은 **두 개**였고 둘 다 따로 고쳐야 했다:
1. **SoV 차트 `grid-cols-7`** — 모바일에서 칸이 넘쳐 페이지가 419px 이 됐다(U4 로 해결)
2. **헤더 오른쪽 묶음의 `w-full`** — 모바일 버튼 2개(각 40px)가 든 부모를 50px 로
   짓눌러 메뉴 버튼이 `right=424` 로 화면 밖에 나갔다 → `min-w-0` + `shrink-0`

🔬 실측: **195·390·768·1440px 전부** `scrollWidth == viewport` · 넘친 요소 **0개**
🎓 교훈: *"stash 로 되돌려도 같은 숫자"* 는 **내 변경이 원인이 아니다**까지만 말해준다.
   **원인이 어디인지는 말해주지 않는다** — 넘친 요소를 직접 찾아야 했다.

### (구) U13 최초 보고 — 아래는 위 조사로 대체됨

`/audit` 모바일에서 **`scrollWidth 419 > viewport 390`** — 가로 스크롤이 생긴다.
모바일 메뉴 패널이 `left:-29px` 로 화면 밖에 걸쳐 **로고·메뉴 항목이 잘려 보인다**.
원인 = `container` + `absolute right-0` 조합.
🔬 **내 변경 때문이 아니다** — 헤더 수정을 `git stash` 로 되돌리고 재측정해도 419 로 동일(실측).
👤 레이아웃·디자인 변경은 승인 후 진행하는 규칙이라 **보고만 하고 손대지 않았다.**

### 👤 대표님이 승인한 신규 2건 (2026-08-13)
| 안 | 내용 | 크기 | 누가 |
|---|---|:--:|---|
| **A** | **`/admin/audits` 관리자 진단 목록 신설** — 운영자가 고객 측정을 볼 화면이 **제품에 없다**(admin 폴더에 `auditJob.findMany` 0건) | **M** | 나 |
| ✅ **B** | 🔴 **인계서가 틀렸다** — `FINDABLE_ADMIN_EMAILS` 가 아니라 **Clerk `publicMetadata.role`** (아래 §B-정정) | S | 👤 **이미 완료** |

### 🔴🔴 U14 — **web 이 라이트 OS 방문자에게 깨져 있었다** (세션N-27 최종 발견)

**`apps/web` 에 `forcedTheme="dark"` 가 없었다.** `apps/app` 은 세션N-19 에 같은 사고를
겪고 고쳤는데(그 주석이 지금도 `apps/app/app/layout.tsx:36` 에 남아 있다)
**web 에는 적용되지 않은 채 남아 있었다.**

```
DesignSystemProvider 기본값 = defaultTheme="system" + enableSystem
→ OS 가 라이트인 방문자는 shadcn 토큰이 라이트로 풀린다(--background: 흰색)
→ <header> 가 흰 배경 · 그 위 글자는 Findable 다크 토큰(흰색) = 대비 1.06:1
→ 「Product」·「Sign in」 버튼과 워드마크가 **안 보인다**
```

| OS 설정 | /(랜딩) | /audit | /pricing | /contact | 합계 |
|---|:--:|:--:|:--:|:--:|:--:|
| **라이트(수정 전)** | 0 | 2 | 2 | 3 | **7곳** |
| 다크(수정 전) | 0 | 0 | 0 | 0 | 0곳 |
| **수정 후 둘 다** | 0 | 0 | 0 | 0 | **0곳** |

🔴 **내가 이 세션 내내 "0건"이라고 보고한 것들은 전부 다크 OS 기준이었다.**
   측정 환경의 기본값이 버그를 가리고 있었다 → **OS 테마를 바꿔가며 재는 것**이
   이제 검증 절차에 들어가야 한다.
⚠️ `<html>` 에 클래스만 박으면 프로바이더가 덮어쓴다 → **`forcedTheme` 이어야** 한다.

### ✅ U15 — **진단 소요시간 약속이 화면마다 달랐다** (퍼널 감사 · 세션N-27)

*"화면이 약속한 것을 제품이 지키는가"* 렌즈로 퍼널을 훑다 나왔다(N-26 의 #7·#8 과 같은 렌즈).

🔴 **한 컴포넌트 안에서 자기모순**이었다:
```
footer-cta.tsx  eyebrow "3분 무료 진단"      ← 14행
                본문    "30초면 끝납니다"     ← 19행   두 줄 차이로 충돌
/audit page     "3분 안에" · "30초에 진단" · "입력 (30초)"  ← 한 페이지에 셋
```

🔬 **무엇이 사실인가**(재조사 금지):
- 엔진 주석은 일관되게 **"30초~3분"**(`packages/audit/runner.ts:4,246,306`)
- 결과페이지는 **최대 7분까지 폴링**(`audit-result.tsx:611` `MAX_POLL_MS = 7*60*1000`)
- 즉 **"30초"는 가장 빠른 경우일 뿐 약속으로 쓸 수 없다**

👤 대표님 판단 = **"3분"으로 통일**(보수적 — 약속은 반드시 지킨다).

| 고친 곳 | 전 → 후 |
|---|---|
| `hero.tsx` (한/영) | 30초면 진단 끝 → **3분이면 진단 끝** |
| `footer-cta.tsx` (한/영) | 30초면 끝납니다 → **3분이면 끝납니다** |
| `audit/page.tsx` | 30초에 진단 → **3분 안에 진단** |
| `case/a-brand` | 30초면 측정 끝 → **3분이면 측정 끝** |
| `report/k-beauty` | 30초 무료 진단 받기 → **무료 진단 받기** |

⭐ **일부러 안 바꾼 것**(전부 사실이라 정상):
- `"이메일과 도메인 입력 (30초)"` — 이건 **타이핑 시간**이지 진단 시간이 아니다
- 진행 중 문구 `"약 30초~3분 걸려요"` — **정직한 범위**라 그대로가 맞다
- 네이버 브리핑 `"약 30초~1분"` — 진단 본류와 **다른 작업**이다

🔬 실측: 랜딩 화면 텍스트에 "30초" **0회**·"3분" 3회 / `/audit` 는 "30초" 1회(입력단계뿐)

### 🔴🔴 §B-정정 — **"admin" 이라는 이름이 두 곳에 있고, 서로 다른 축이다**

인계서(그리고 그 앞 세션)가 *"`FINDABLE_ADMIN_EMAILS` 에 계정을 추가하면 관리자 화면이
열린다"* 고 적었는데 **틀렸다. 그 env 를 아무리 바꿔도 `/admin/*` 는 안 열린다.**

| | `FINDABLE_ADMIN_EMAILS` | `publicMetadata.role` |
|---|---|---|
| 정체 | **사용량 티어**(진단 무제한) | **플랫폼 운영자 권한** |
| 읽는 코드 | `packages/audit/usage-tier.ts:17` | `packages/auth/admin.ts` → `admin-role.ts` |
| 바꾸는 곳 | Vercel 환경변수 | **Clerk 대시보드 → Users → Metadata** |
| `/admin/*` 열리나 | ❌ **무관** | ✅ **이것만** |

🔬 실측: `/admin/{ops,audits,partners,evidence}` **4개 전부** + 사이드바 「관리자」 그룹이
   `isAdmin()`(Clerk role) 하나에 걸려 있다. `FINDABLE_ADMIN_EMAILS` 는 admin 라우트에서
   **참조 0건**.

✅ **결과: 이미 완료돼 있었다.** `nayoy2@gmail.com` 의 public metadata =
   `{"plan":"growth","role":"admin"}` → `role` 이 이미 `"admin"` 이라 **바꿀 것이 없었다.**
   (env 를 고치라는 지시를 그대로 따랐다면 **아무 일도 일어나지 않았을 것**)

⭐ 이 김에 **admin 게이트 테스트가 0개**였던 것도 메웠다(+8). 4개 라우트와 사이드바가
   전부 이 판정에 걸려 있는데 고정이 없었다 — 뚫리면 **남의 고객 진단 목록이 통째로 노출**된다.
   판정이 `isAdmin`·`requireAdmin` 에 **두 벌 인라인**으로 있던 것도 `hasAdminRole` 한 곳으로 합쳤다
   (한쪽만 고치면 *"목록은 막는데 서버액션은 통과"* 가 된다).
   🔬 뮤테이션 검증: 엄격일치 → truthy 로 느슨하게 바꾸니 **4건 실패**(가드 작동 확인).

### 🚀 세션N-27 배포 완료 (2026-08-13) — **프로덕션 실측 검증까지**

| 앱 | 배포 | 도메인 별칭 | 확인 |
|---|---|---|---|
| **app** | `findable-mtsva9z6t` | `app.findable.co.kr` ✅ | `/admin/audits` 307→sign-in(정상) |
| **web** | `findable-2s3zofrjp` | `www.findable.co.kr` · `findable.co.kr` ✅ | 아래 실측 |

⭐ **도메인 자동연결 함정은 이번엔 안 터졌다** — `vercel inspect` 로 Aliases 직접 확인했다.
   (메모리 `project_realestate_decision_app` 의 *"`--prod` 가 도메인 연결 안 됨"* 케이스와 다름)

🔬 **프로덕션 실측**(로컬이 아니라 라이브 사이트):
```
라이트·다크 OS × 모바일·데스크톱 × 4페이지 = WCAG A/AA 위반 0곳
U14  라이트 OS 에서도 html=dark · 헤더 rgb(1,1,2) · 로고 rgb(247,248,248)  ✅
U4   엔진 라벨 겹침 0 · U8 한글 음수자간 0 · U13 가로스크롤 390=390        ✅
U5   200% 확대(195px) 넘친 요소 0                                        ✅
U15  시간 약속 전부 "3분" 수렴 · JS 오류 0                                 ✅
```
📸 헤더 스크린샷 눈확인: 홈·제품·블로그·문의·로그인·시작하기 **전부 읽힌다**
   (배포 전에는 「제품」·「로그인」이 **흰 박스 안 흰 글자**였다)

⚠️ **"약 2분"은 오탐** — `audit-tracker-mock.tsx` 의 목업 대시보드 예시값이다
   (*"K-뷰티 Q1 트래킹"* 옆의 샘플 데이터). 고객 약속이 아니라 그림이다.

### ⛔ 감사에서 **기각**한 것 (되살리지 말 것)
1. **정렬 8종·간격 25종 전면 정리** — 일부는 **의도된 들여쓰기**. 1~3px 근사 불일치만
2. **엔진 카드 "중복"** — **오진**. 닮은 것이 **데이터의 사실**이다
3. **랜딩 11.7화면 줄이기** — 화면 수는 실격 기준이 아니다(Stripe 24.1)
4. **대비 1.06 로고** · **라벨 없는 입력** · **빈 구간 2.6화면** · **탭 잘림** — 전부 **오탐**

---

## 3. 🔗 P0 에 종속된 것 — **기다려도 안 풀린다** (4건)

> 🔴🔴 **세션N-25 재분류.** N-24 는 *"측정 5~6회 쌓이면 착수"* 라 적어 **시간이 지나면
> 풀리는 일**처럼 읽혔다. 실측하니 **영원히 안 쌓인다**:
> 자동 재측정 cron 은 `autoRefreshHours != null` 인 org 만 도는데
> (`auto-refresh-tracking:196`), **`free: autoRefreshHours: null`**(`plan.ts:120`)이고
> **유료 고객이 0명**이다. → 시계열은 1일에서 **멈춰 있다.**

- [ ] **D4 기간 선택 칩** — 시계열이 1일이라 누를 게 없다
- [ ] **D2 자동 이벤트핀** — 델타가 `[0,0,0,0,+97]`, +97 은 측정실패→복구
- [ ] **D1 이상감지 알림 UI** — 임계값을 정할 **분포 자체가 없다**
      🔴 **근거 없는 경계선 발명 금지**(*"40% 미만 경고"* 를 제안했다가 철회한 전력)
- [ ] **`alerts` 실제 알림 메일** — 코드 완료·**기본 꺼짐**. 👤 메일 본문 눈확인 후 켤 것

⚠️ **유료 축 3개 중 알림=0 · 비교=0 · 시간=절반** 이다(`UIUX_경쟁사_화면대조:113`).
🔴 **순서 경고**: *"유료 축을 먼저 채우고 무료를 줄인다. 반대로 하면 게이팅 이탈만 얻는다"*
   (최대 30% 경쟁사 이탈 — Artisan). **지금 무료를 조이면 안 된다.**

---

## 4. 💳 결제 트랙 — ⚠️ **다른 세션 작업 중** (건드리지 말 것)

카카오페이 승인 회신 대기 · 👤 먼저 보낼 것 없음.
- [ ] 라이브 정기결제 채널키 교체(지금 테스트 CID `TCSUBSCRIP`)
- [ ] **결제 예약 생성 함수** — 🔬실측: 취소·삭제만 있고 **생성이 없다**
      ⚠️ **다른 세션이 `billing-cycle.ts` 로 착수한 것으로 보인다**
- [ ] 연결 후 *"지금은 첫 회만 결제되며…"* 줄 삭제(안 지우면 **거짓 표기**)
- [ ] 👤 `PORTONE_WEBHOOK_SECRET` 발급 + 콘솔 웹훅 URL 등록 — 🔴 **첫 실결제 전 필수**

🔴 **해지 순서 고정**: ①예약 취소 → ②빌링키 삭제. **뒤집으면 무한 과금.**

---

## 5. ⚪ 범위 큰 것 (데모데이 10월 이후 권고)

- [ ] **GEO 실행 에이전트** — 경쟁사 6곳 전원 공백 = 최대 차별화
      📕 정의 = 🔴**고객 사이트 수정이 아니라 자체 블로그 플랫폼**([[project_findable_geo_agent_definition]])
      ❌ 나무위키 직접 편집 금지(표시광고법)
- [ ] **RAG 파이프라인 4종** — ⚠️ 라이브 스키마 변경 고위험
- [ ] **durable 실행 전환**(Vercel Workflow) — crew 최대 226초 / 상한 270초
- [ ] **Prisma Client Extension org 자동주입** — ⚠️ 전체 쿼리 영향

---

## 6. ✅ 세션N-25 완료 (커밋 9건 · 전부 배포·라이브 확인)

| # | 무엇 | 검증 |
|:--:|---|---|
| 1 | **처리방침 개정** — 수탁자 6곳 공개(제26조 제2항) + **AI 미학습 고지** 신설 + 기재 오류 3건 정정 | 라이브(KO) |
| 2 | **crew 전역 일일 상한**(KST 자정 기준 · 순수함수 분리) | 뮤테이션 6건 |
| 3 | **crew 무료 1회 개방** + *"무료"* 라 해놓고 403 뱉던 **거짓 제거** | 라이브 번들 |
| 4 | **실패가 체험분을 먹던 것** 수정 | 뮤테이션 2건 |
| 5 | **가입 카드 소유권 안내** + **공유링크 이메일 노출 방지** | 뮤테이션 2건 |
| 6 | **퍼널 계측 3종** + 🔴 완료를 *"시작"* 으로 세던 이벤트 정정 | 뮤테이션 3건 |

### 🔬 실측으로 **인계문서를 정정한 것 4건**
1. 🔴 **수탁자 목록이 양방향으로 틀렸다** — `AWS` 는 SDK **0건**(적었으면 허위 기재),
   개인정보를 실제로 받는 **PostHog**(이메일·이름·전화번호)·**Resend**·**Neon** 은 **누락**.
   ⭐ 배포 빌드 로그가 독립 확인("BetterStack Envvars not detected"·"Sentry No auth token").
2. 🔴 **"crew 원가 67%" 는 오독** — 원문은 *"crew 를 빼면 67% 절감"*(sonnet 시절)이다.
   실측 = 진단 87원 / crew 수십 원 · 원가율 **0.09%**. 유닛이코노믹스 결론이
   *"원가가 무서워서 못 푼다는 제약은 **없다**"*.
3. 🔴 **N-24 의 "Logtail 유출" 위험평가가 어긋났다** — BetterStack 은 **프로덕션 미활성**.
   실제 개인정보 전송처는 **PostHog** 였다(N-24 의 코드 수정 자체는 옳다).
4. 🔴 **#14 "진단↔계정 연결 없음" 은 전제 오류** — 대시보드가
   `identifiers=[로그인 이메일, "org:{orgId}"]` 로 조회하므로 **읽는 시점에 자동 연결**된다.

---

## 7. 🎓 세션N-25 가 배운 것

1. 🔴🔴 **인계문서·감사 결과도 틀린다.** 이번에 에이전트 1위 지목이 **두 번** 틀렸고
   (*"연결 코드 없음"* · *"돈 내도 crew 안 열림"*), 내가 **인계문서 숫자를 검증 없이 인용**해
   전략 추천 전체를 그 위에 세웠다(67% → 실제 0.09%).
   → **감사 결과는 탐색의 시작점이지 결론이 아니다.** 건별 실측.
2. 🔴 **되돌린 결정을 다시 뒤집지 않도록 git 이력을 먼저 본다.** 내 초안(*"crew 유료화"*)은
   `12a00de`(8-10)가 이미 **되돌린 결정**을 다시 잠그는 것이었다.
   👤 지시 원문: *"MVP 가 아니라 **고객 페인을 해결하는 최고의 제품**"*.
3. 🔴 **가드가 두 번 뚫렸다** — ①부정 고지의 *존재*만 검사(제목이 만족시켜 통과)
   ②헬퍼 이름 `toContain`(import 줄이 만족시켜 통과).
   → **존재 검사 금지.** *"긍정 서술이 없는가"* · *"호출 형태인가"* 로 판정.
4. ⭐ **계측이 있는데 거짓말할 수 있다** — `audit_started` 가 완료 시점에 발화해
   퍼널 이탈이 0으로 읽혔다. **없는 것보다 나쁘다**(틀린 답을 자신 있게 준다).
5. ⭐ **근거 없는 임계값 대신 관측 가능한 것으로** — crew 는 원가를 기록하지 않으므로
   금액 상한 대신 **건수 상한**을 걸었다.
6. 🔴 **어뷰징 방어와 고객 경험을 한 함수에 섞지 않는다** — 실패를 체험분에 세면
   *"우리 잘못인데 고객이 손해"* 가 된다. 역할 분리(상한=방어 / 쿼터=체험).

---

## 7-b. 🎓 세션N-26 이 배운 것 (가드가 두 번 뚫렸다 — 둘 다 뮤테이션으로 발견)

1. 🔴🔴 **정규식으로 블록주석을 지우지 말 것.** 게으른 수량자(`[\s\S]*?`)로
   JSX 주석을 지우려다 `audit-result.tsx` 에서 **40,570자**(실제 코드 대부분)가
   함께 사라졌다 — 앞쪽 `{/*` 와 **한참 뒤의** 주석 끝이 짝지어진다.
   → 가드가 **아무것도 검사하지 않는 상태로 통과**한다. **줄 단위 상태 기계**로 센다.
   ⚠️ 같은 버그가 `billing-badge` 가드에도 있었다(그 파일은 주석이 짧아 **우연히** 무사).
2. 🔴 **"형태"로 판정하면 뚫린다 — "어디에 있는가"로 판정한다.**
   `submitted ? (…"곧 메일로"…)` 형태만 막았더니 `{isKo ? "곧 메일로…" : …}` 로
   되돌리는 뮤테이션이 **그대로 통과**했다.
   → *"약속 문구는 **판정 함수 안에 1회만** 존재한다"* 로 바꿔 잡았다.
3. ⭐ **가드를 새로 짜면 반드시 뮤테이션을 돌린다.** 위 2건 모두
   *"테스트 통과"* 상태에서 **실제로는 안 잡히고 있었다**. 통과는 증거가 아니다.
4. ⭐ **막다른 길을 만들지 않는다** — 실패를 알릴 때도 *"지금 할 수 있는 것"* 을 준다
   (#8: *"다시 시도"* 가 아니라 *"이 화면에서 그대로 보실 수 있어요"*).
5. ⭐ **응답이 스스로 말하게 한다** — 클라이언트의 `?? false` 기본값에 기대면
   *"우연히 맞는"* 상태가 된다. 서버가 `emailSent:false` 를 **명시**해야 한다.

---

## 8. ⚠️ 착수 전 함정 (계속 유효)

- 🔴🔴 **엔진 수 재조사 금지** — 본류=**7** · 영어만=**4** · 8=브리핑 별도
- 🔴 **`apps/api` 는 죽은 앱** — 새 코드 넣으면 실행되지 않는다
- 🔴 **`apps/web` 에 테스트 러너 없음** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서 검증
- 🔴 새 `.test.tsx` = **`@vitest-environment jsdom` 도크블록 필수** · **단독 실행으로 먼저**
- 🔴 **DB 직접 조회 막힘**(한글 NFD + Prisma CJS) → **라이브 API `/api/audit/<id>`**
- 🔴 **BotID 가 curl 을 차단한다** → 진단·crew 경로는 **브라우저로만** 라이브 검증 가능
- 🔴 **biome 복잡도 20 · 중첩 삼항 금지 · 정규식은 최상위 상수**
- 🔴 **`Date.now()` 기반 상한은 테스트 불가** → 기준 시각을 **인자로 받는다**
- 🔴 **cron 은 UTC** — `0 17 * * *` = KST 새벽 2시. 일일 상한은 **KST 자정**으로 끊는다
- 🔴 로컬 dev·build 는 **`VERCEL=1`** · `"use server"` 는 **async 함수만 export**
- ⚠️ 요금제 문구는 **app·web 둘 다** · ⭐ **app 은 `lang="ko"`**
- ⚠️ biome 이 **대괄호 경로**(`[jobId]`)를 직접 못 읽는다 → 상위 디렉터리로 lint
- ⭐ **배포**: `npx vercel --prod --yes --scope nayoy2-7791s-projects`(web=루트 기본값)
  / app 은 `project.json` swap 후 **원복 필수**
- 👁️ **스크린샷·라이브 번들 확인** — 테스트는 아는 것만 검증한다

---

## 9. ⛔ 되살리지 말 것 (전부 실측 근거)

1. **엔진 수 "28곳 통일"** — 오답. 두 숫자는 다른 것을 센다
2. **결제 상태 3줄** — `Organization` 스키마에 필드가 **없다**
3. **처방 2건 병합** — 서로 **다른 질문**(영/한) + `@@unique` 라 **고객 완료표시가 되살아난다**
4. **감성 배지 변별력** — 데이터가 **실제로** 중립(18/3/null 8)
5. **순위 분모 노출** — `mentionListSize` 전부 null
6. **잠금카드 `min-h`** — 🔴 **코드에 없다**(주석 안)
7. **`x-vercel-cron` 폴백** — 외부인이 유료 측정을 무제한 실행할 수 있었다
8. **업계 N분위 벤치마크** — 조직 1개라 표본 없음 = 조작
9. **naver 어댑터 Daum식 정합** — 구조가 다르다(naver=HyperCLOVA 합성)
10. **팩트정합률 전면화** — 사실 검증을 안 하는데 이름이 그렇게 읽힌다
11. **`z.email()` 완화** — 🔴 **보안 경계**. `org:` 이름공간을 구조적으로 보호한다
12. **대법원 2020도13960 인용** — 그 판결은 **오히려 위탁을 인정**했다
13. 🆕 **crew 를 유료 전용으로 되돌리기** — `12a00de` 가 되돌린 결정이고, 업계 게이팅
    축 4개(프롬프트·엔진·브랜드·갱신주기)에 **처방은 없다**. HubSpot·AthenaHQ 는
    처방을 **무료로 준다**
14. 🆕 **처방(`buildGeoActions`) 잠그기** — *"우리가 이기는 유일한 지점"*
15. 🆕 **AWS 를 수탁자로 기재** — SDK **0건**. 런타임 감지 플래그일 뿐(허위 기재)
16. 🆕 **실패한 crew 를 체험분에 카운트** — 우리 잘못으로 고객이 손해본다

---

## 10. 🔴 다음 세션이 잊으면 안 되는 것

> **① 남은 일은 이 파일에만.** 다른 문서의 `⬜` 를 믿지 말 것
> **② 착수 전 실측** — 인계문서·감사 결과 **둘 다 틀린 적이 있다**(§7-1)
> **③ 방향을 뒤집기 전 `git log -S` 로 그 결정의 이력을 볼 것**(§7-2)
> **④ 가드는 존재 검사 금지** — 구조·호출 형태로 판정(§7-3)
> **⑤ 회사 병목은 여전히 P0 실고객.** 코드로는 계측까지가 최선이었다
> **⑥ ⚠️ 결제 트랙은 다른 세션이 작업 중** — 충돌 확인 후 착수
