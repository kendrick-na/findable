# ⛔ 격하됨 — 최신 단일진실은 `_투두_단일진실_N25.md`

> 2026-08-12 세션N-25 가 전면 재정리했다. **"무엇이 남았나"는 N25 파일을 볼 것.**
> 이 파일은 **근거·판정 이력 보관용**이다(왜 그렇게 판정했나의 실측 기록).

---

# 🎯 Findable 남은 일 — **단일 진실 (세션N-24 전면 정리)**

> 갱신: **2026-08-12 (세션N-24)** · 작성 근거 = 4개 문서(`투두리스트.md` 969줄 ·
> `투두_마스터.md` 1,132줄 · `실행백로그.md` 133줄 · `_START_HERE_세션N23.md`) **전량 통독**
> \+ 라이브·코드 **실측 9건**.
>
> 🧭 **방향·함정은 ⭐`_START_HERE_세션N25.md`** (⛔N23 이하 폐기).
> 🔴 **이 파일이 "무엇이 남았나"의 단일 진실이다.**
> ⛔ `_투두_단일진실` 이전판 없음(신규) · `투두리스트.md` 는 **근거·판정 이력 보관용으로 격하**.
> ⛔ `실행백로그.md`·`투두_마스터.md` 의 `⬜` 는 **믿지 말 것**(상태 갱신이 2026-07 에 멈춤).

---

## 0. 🔴 왜 이 파일을 새로 만들었나

세션N-23 인계문서는 *"코드는 사실상 소진, 남은 건 P0·상표뿐"* 이라고 단정했다.
🔬 **4개 문서를 끝까지 읽고 실측한 결과 그 단정은 틀렸다.** 코드로 지금 할 수 있는 일이
**11건** 남아 있다(§2). 그중 **3건은 실측으로 새로 확인**했다.

🎓 **이 오진은 4번째 반복이다** — N-10·N-11·N-13·N-17 이 각각 *"백로그 비었다"* 고 틀렸고,
`투두리스트.md:178` 에 *"3번째 반복"* 이라는 경고가 이미 적혀 있었다.
**원인은 게으름이 아니라 문서 분산**이다: 미완 항목이 4개 파일에 흩어져 있어
어느 하나만 읽으면 반드시 누락된다. → **그래서 이 파일 하나로 합쳤다.**

---

## 1. 🔬 이번 세션 실측으로 **새로 확인한 것 3건**

| # | 발견 | 근거 | 어디에도 없었나 |
|:--:|---|---|---|
| ① | 🔴 **요금제 배지가 유료 고객에게 「현재」를 안 보여준다** | `billing/page.tsx:216` = `isCurrent && !tier.featured`. Growth(=`featured`)를 결제한 고객은 **「추천」만** 보고 「현재」를 못 본다 → *"내가 지금 무슨 플랜인가"* 를 화면이 답하지 않는다 | N-23 은 이걸 *"미발현"* 이라 적고 넘겼다. **절반만 맞다** — 아래 §2-11 |
| ② | ✅ **app favicon 404 = 파일이 실제로 없다**(확정) | `apps/app/app/favicon.ico` **없음** / `apps/web/app/favicon.ico` **있음** | 원인이 확정 안 돼 있었다 → **1분 작업** |
| ③ | 🔴 **결제 예약 생성 함수가 정말 없다**(확정) | `packages/payments/portone.ts` export 7개 전량 확인 — `cancelBillingKeySchedules`(취소)·`deleteBillingKey` 는 있고 **생성(`POST /payments/{id}/schedule`)은 없음** | 투두 기재가 맞았음을 **확증** |

### 🔒 1-2. 보안 감사 — **crew route 는 안전하다**(세션N-24 · 코드 수정 불필요)

`실행백로그.md:66` 이 *"crew route 가 `where:{organizationId}` 를 강제하는지 확인"* 을
**P1 보안 항목**으로 남겨뒀다(근거: Day11 Tracking 우회 전력). 🔬**실측 결과 = 안전.**

| 검사 | 실측 |
|---|---|
| 조회부 | `findUnique({ where: { id: jobId } })` — `organizationId` **필터 없음** |
| 🔴 그럼 유출인가? | **아니다.** 이 라우트는 **비로그인 무료진단 경로**라 org 개념 자체가 없다(`apps/web`). 필터할 org 가 없으므로 그 지적은 **전제가 안 맞는다** |
| 그럼 실제 방어선은? | **① BotID**(자동화 차단) → **② `canRunDeepAnalysis(job.email)`**(유료·승인 게이트) |
| 🔴 게이트 우회 가능? | `canRunDeepAnalysis` 는 `email.startsWith("org:")` 면 **무조건 허용**한다. 그런데 공개 `/api/audit` 는 `email: z.email()`(`route.ts:36`) 로 검증한다 |
| 🔬 **`org:` 를 이메일로 넣을 수 있나** | ❌ **불가**. 실측(vitest 임시 테스트): `org:abc`·`org:org_123`·`ORG:abc`·**`org:x@y.com` 전부 `z.email()` 거부**. → `org:` 는 **서버가 인증된 `orgId` 로만** 만든다(`start-tracking.ts:157` 등) |

⭐ **결론: 권한 상승 경로가 닫혀 있다.** `org:` 접두사가 "인증된 서버만 쓸 수 있는 이름공간"
역할을 하고, 이메일 형식 검증이 그 이름공간을 **구조적으로** 보호한다.
⚠️ **그래서 `z.email()` 을 느슨한 문자열 검증으로 바꾸면 유료 게이트가 뚫린다** —
`route.ts:36` 은 **보안 경계**다(단순 입력 검증으로 보고 완화하지 말 것).

### 📊 1-3. 결제 퍼널 계측 — **P0 의 정량 근거**(세션N-24 완료·배포)

결제 흐름에 **분기점이 5개**인데 **전부 무계측**이었다 — *"몇 명이 결제를 눌렀고
그중 몇 명이 성공했나"* 조차 알 수 없었다(기존 계측 = 클라이언트 3종뿐).

| 분기 | 이벤트 | 뜻 |
|---|---|---|
| 버튼 클릭 | `checkout_started` | 의도 |
| intent 실패 | `checkout_failed(stage:intent)` | **우리 잘못** |
| 결제창 닫힘 | `checkout_failed(stage:widget)` | ⭐**지불의사 핵심 신호** |
| verify 실패·미부여 | `checkout_failed(stage:verify)` | 🔴**가장 위험**(돈 나갔는데 권한 없음) |
| 성공 | `checkout_completed` | 결제 **+ 권한부여** 둘 다 |

📕 단일 진실 = `packages/analytics/funnel.ts` (복제 금지 — 호출부는 여기만 부른다)

#### 🔬 백로그 원안에서 **바꾼 것 2건**(근거 있음 · 되돌리지 말 것)
1. ⛔ **`pricing_viewed` 를 만들지 않았다 — 중복이다.**
   `instrumentation-client.ts` 가 `defaults: "2025-05-24"` 로 초기화하고 그 프리셋은
   **`capture_pageview: "history_change"`** 를 켠다(🔬posthog-js 1.372.5 **소스 실측**).
   → 화면 조회는 **이미 `$pageview` 로 자동 수집**되고 SPA 이동까지 잡는다.
   ⚠️ 게다가 web `/pricing` 은 **ISR 1시간 캐시** 서버 컴포넌트라 `"use client"` 를 붙이면
   **그 캐시를 깨뜨린다** — 안 만든 게 성능에도 맞다.
2. ⭐ **정기/단건을 같은 이벤트 이름으로 통일**(`isSubscription` 속성으로 분해).
   나누면 *"결제 시도 몇 건"* 을 물을 때 두 이벤트를 더해야 하고 **한쪽을 빠뜨린다**.

#### 🔴 발명하지 않은 것
PG 실패 코드를 *"이건 고객 취소"* 로 **분류하지 않는다** — PortOne SDK 타입에 코드 목록이
**없다**(실측). 코드를 **그대로** 보내고 분류는 분포를 본 뒤에.
(단 "창을 그냥 닫음"은 응답 부재로 **구조적으로** 구분되므로 `window_closed` 로 표기.)

#### ✅ 라이브 검증
- **env parity 양쪽 확인** — `findable`·**`findable-app` 둘 다** PostHog 키 존재
  (🔴 한쪽만 보면 안 된다: 과거 P5 에서 app env 누락으로 측정 전면 실패한 전력)
- **배포 번들에 posthog 실재 확인** — 청크 `7ffa00c961ffb193.js` 에 `posthog.com`
  (키만 있고 클라이언트가 안 뜨면 이벤트는 사라진다 → 번들까지 확인)

### 🔒 1-4. 🔴🔴 개인정보 유출 차단 — **이번 세션 최대 발견**(세션N-24 완료·배포)

Clerk 인증 웹훅이 `log.info("Webhook", { id, eventType, body })` 로 **원문 JSON 전체**를
남기고 있었다. `body` 안에는 **이메일·이름·전화번호·아바타 URL** 이 평문으로 있고
(`handleUserCreated` 가 읽는 필드가 그 증거), **가입·정보수정마다 Logtail 에 적재**됐다.

⚠️ **로그는 지우기 어렵고 보존기간이 길다** → 필요 없는 개인정보는 **애초에 안 넣는다**.
→ 장애 조사에 실제로 필요한 것만(`id`·`eventType`). `id` 는 Clerk 식별자라 개인정보가
아니고 DB·PostHog 대조의 열쇠다.

⚠️ **`analytics.identify` 는 손대지 않았다** — 거긴 **CRM** 이고 개인정보 전송이 **의도된 것**이다.
여긴 **운영 로그**다. 목적이 다르므로 판단도 다르다. 🔴**둘을 같이 지우면 CRM 이 망가진다.**

🔗 선례 = 세션N-11 이 평문 이메일 로깅 6곳을 `maskEmail` 로 마스킹했다. **그때 누락된 분.**

#### 🔬 백로그 기재를 실측으로 정정 — **범위가 1/3 로 줄고 더 중대한 것이 나왔다**
`실행백로그.md:89` 는 *"결제·브랜드·인증 무로그"* 라 했으나 **틀렸다**:

| 도메인 | 실측 로그 키 수 | 판정 |
|---|:--:|---|
| audit | 52 | ✅ |
| **billing** | **11** | ✅ 이미 규칙 준수(`billing.verify.*` 등) |
| **payments.webhook** | **10** | ✅ 이미 규칙 준수 |
| brand·action | 11 | ✅ |
| 🔴 **auth 웹훅** | **4**(그중 **2건이 결함**) | ❌ **진짜 공백** |

🎓 내가 처음 `checkout.` 접두사로 grep 해 0건이 나온 것을 *"무로그"* 로 오독했다.
**접두사를 바꿔 다시 세니 21개가 있었다.** → *"묶음으로 적힌 지적은 건별로 실측"* 규칙이
또 값을 했다(불필요한 작업 회피 + **개인정보 유출 발견**).

#### 가드 = **소스 정적 검사**(모킹으로 우회 불가)
라우트 import 는 `svix`·`server-only` 스텁이 필요한데, 지키려는 성질은
*"로그 인자에 원문 payload 를 넣지 않는다"* 는 **정적 성질**이다 → 소스를 직접 읽어 단정.
🔴 **정규식이 0건을 잡아도 통과하는 함정**을 막으려 `logCalls.length > 0` 을 먼저 단정한다.
🔬 뮤테이션 2건: ①`body` 로깅 부활 → 2건 실패 ②키를 문장으로 → 1건 실패.
✅ 배포 후 라이브: auth 웹훅 **400**(무서명 거부) · 결제 웹훅 **200**(무영향).

### ⏱️ 1-5. 타임아웃 정합 — **이미 맞다. 코드 변경 불필요**(세션N-24 검증 · 재조사 금지)

`실행백로그.md:81`(D16-4)이 *"`maxDuration=300초` vs `CREW_TIMEOUT_MS=720초` 불일치 =
stuck 의 물리적 원인"* 이라 했다. 🔬**실측: 그 720 은 낡은 값이고 지금은 정합이다.**

| 값 | 실측 | 판정 |
|---|:--:|---|
| 함수 상한(`maxDuration`) | **300초** | — |
| crew 자체 타임아웃(`crew-runner.ts:28`) | **270초** | ✅ **함수보다 먼저 끝난다**(여유 30초) |
| N-13 실측 최악 crew | 226초 | ✅ 270 까지 여유 44초 |

⭐ **순서가 중요하다**: 자체 타임아웃이 함수 상한보다 **작아야** 스스로 정리하고 끝낼 수 있다.
지금이 그 상태다(270 < 300). 뒤집히면 함수가 먼저 죽어 `processing` 이 굳는다.

#### 🔬 감사 중 새로 본 것 — **본류 러너에는 자체 타임아웃이 없다**(그래도 지금은 괜찮다)
`packages/audit/runner.ts` 에는 시간 가드가 **0건**이고 엔진 어댑터에도 fetch 타임아웃이 없다.
그런데도 지금 손대지 않는 이유 3개:
1. 🔬 엔진은 **`Promise.allSettled` 병렬**(`engines/index.ts:95`) → 총시간 ≈ **가장 느린 1개**
   (합계가 아니다). 7개를 더해 300초를 넘길 구조가 아니다.
2. 📊 **실측이 여유롭다** — 최근 40건 **평균 19초·최대 54초**(N-13). 상한의 18% 수준.
3. 🛡️ **급사해도 영구 stuck 이 아니다** — 300초에 죽으면 `status=processing` 으로 남고
   **15분 뒤 sweep cron 이 `failed` 로 정리**한다(위 §1-1 에서 그 알림도 붙였다).

⛔ **그래서 타임아웃을 새로 발명하지 않았다.** 상한 값을 정할 **지연 분포가 없고**
(측정 2회), 근거 없는 경계선은 이 프로젝트가 금지한 것이다. 게다가 너무 짧게 잡으면
**정상적으로 느린 진단을 우리가 죽이는** 새 버그가 된다.
📌 **켤 조건**: 실측에서 200초 이상 회차가 나오면 그때 분포를 근거로 정한다.

⚠️ **`투두리스트.md:862` 의 *"최대 298초"* 는 근거가 없다** — 출처 표기가 없고
같은 문서의 실측치(평균 19초·최대 54초)와 **모순**이다. 인용하지 말 것.

### 🌏 1-6. 🔴 처방 채널이 타깃 시장을 무시하던 것 (세션N-24 완료·배포)

👤 **사용자 지적**: *"한국 서비스가 GEO 점수 올리려면 레딧이 아니라 네이버에 글을 써야 하는데,
엉뚱하게 레드홍에 올리라고 하면 이상하잖아."*

#### 🔬 교차검증이 내 분석을 **3번 뒤집었다** — 고친 곳이 최초 제안과 다르다
| 내 최초 주장 | 실측 |
|---|---|
| crew 프롬프트에 *"Reddit 40% 강조"* 하드코딩 | ❌ **파일 상단 주석**이었다. 실제 프롬프트의 Reddit 언급은 **법적 주의사항**("학술 archive만 합법") → **지웠으면 준법 가드를 삭제할 뻔** |
| 채널 목록이 영미권 편향 | ❌ **반대.** `industry-profile.ts` 는 **한국 기본값**이고 Reddit **0건** |
| crew 경로를 고쳐야 한다 | ❌ **crew 는 유료·승인자가 버튼을 눌러야만** 도는 on-demand(원가 67%라 기본 꺼짐) |

⭐ **진짜 결함은 내가 한 번도 안 본 곳에 있었다** — `buildGeoActions` 는 **모든 무료진단에서
항상** 돈다. **고객이 실제로 보는 처방은 여기서 나온다.**
🎓 교훈: *"어느 경로가 실제로 도는지"* 를 먼저 확인해야 한다. 나는 3세션째 **주석을 코드로
오독**하는 실수도 반복했다(`min-h-[60vh]` · `Reddit 40%`).

#### 🔴 결함 실체
`actions.ts` 가 `"네이버 지식iN·관련 카페"` 를 **문자열로 박고** 있었고, `ActionInput` 에는
`marketScope` 도 `language` 도 **없었다** = 시장을 알 방법이 **구조적으로 없었다**.
→ 해외 시장 브랜드에게도 *"네이버 지식iN에 답변하세요"* 가 나갔다.
🔬 원인이 하나 더: `runner.ts` 가 **처방을 만든 뒤에** `marketScope` 를 계산했다
(365행 처방 → 378행 시장). **순서상 알 수가 없었다** → 계산을 앞으로 옮겼다.

#### ⭐ 설계 판단 — **판정은 한 곳에서만**
처방의 시장 판정에 **점수의 분모를 정하는 것과 같은 `marketScope`** 를 쓴다.
여기서 따로 추정하면 **한 화면 안에서 시장 판정이 둘로 갈린다**.

#### 🔴 플랫폼 이름을 발명하지 않았다
글로벌 채널을 `Reddit·Quora` 로 박지 않고 **유형**으로만 말한다:
① Semrush *"Reddit 40.1%"* 는 **영어권 기준**이고 **자사 후속 연구에서 급락**(2025-06→2025-10)
② 이 제품 원칙은 *"잰 것만 말한다"* — 실제 인용 도메인은 `topDomains` 로 **이미 들어온다**

#### 📊 경쟁사 조사 — **업계 공백이 맞다**
🔴 **시장별로 처방을 다르게 주는 GEO 도구 = 확인된 사례 0곳.**
Profound(80개국)·Otterly(50개국)·Peec(115개 언어)·Ahrefs 전부 **측정만** 현지화하고
**추천 로직은 만국 공통**. CrowdReply 는 아예 Reddit/Quora/Wikipedia **하드코딩**.
업계 비판 실재: **Duane Forrester**(전 Bing 웹마스터 총괄) *"Your AI Visibility Strategy
Doesn't Work Outside English"*(2026-04).
⚠️ **위협**: 국내 **Anymorph** 가 *"네이버 측정 + 한국어 GEO 페이지 자동 생성"* 을 이미 주장 —
[[project_findable_geo_agent_definition]] 의 *"자체 블로그 플랫폼"* 정의와 **정면 겹침**. 실물 확인 권장.

#### ✅ 검증
테스트 **184/184**(+9) · 뮤테이션 2건(하드코딩 복원 →3건 실패 / Reddit 이름 박기 →1건 실패) ·
배포 번들에 **시장 분기 3종 전부 확인** · ⚠️ 기존 저장 회차는 **소급 안 됨**(정상 — 새 진단부터)

### 🔒 1-7. 처리방침 개정 — **인계문서 목록이 틀렸다**(세션N-25 완료·배포)

§4 가 지정한 수탁자 `AWS·Vercel·Clerk·PortOne·AI API` 를 **그대로 옮기지 않고 실측**했다
(프로덕션 `vercel env ls` 양쪽 + 코드 데이터흐름). **결과가 양방향으로 틀렸다.**

| 인계 기재 | 실측 | 판정 |
|---|---|---|
| **AWS** | SDK·의존성 **0건**. `AWS_LAMBDA_FUNCTION_NAME` 은 서버리스 **런타임 감지 플래그** | ❌ 적었으면 **허위 기재** |
| — | 🔴 **PostHog** — `identify` 로 **이메일·이름·전화번호** 전송 | ➕ **누락됐던 진짜 수탁자** |
| — | **Resend**(수신자 이메일) · **Neon**(가입자 정보 저장 실체) | ➕ 누락 |

⛔ **프로덕션에 없는 것은 넣지 않았다**(적으면 거짓): Stripe(**프로덕션 키 없음** — 결제는
PortOne) · BetterStack · Sentry · Knock · Arcjet · Upstash · Replicate.
⭐ **배포 빌드 로그가 독립 확인해줬다**: *"BetterStack Envvars not detected"* ·
*"Sentry No auth token"* → 미활성이 맞다.

🔴 **N-24 의 "Logtail 개인정보 유출" 위험평가는 방향이 어긋나 있었다** — Logtail
(BetterStack)은 **프로덕션 env 에 아예 없다**(=미활성). 실제로 개인정보가 나가는 곳은
**PostHog** 였다. ⚠️ **N-24 의 수정 자체는 옳다**(코드에서 원문 payload 제거). 다만
*"최대 발견"* 이라는 평가는 미활성 서비스에 대한 것이었다.

#### 🔴 기존 기재 오류 3건 — §4 가 지적하지 않은 것
1. 제2조 (4) *"GEO 모델 학습 데이터"* → **삭제**. 실측: 엔진 프롬프트에 **이메일 0건**,
   우리 개인정보는 이메일·이름뿐이라 **학습할 것이 없다**. 학습 자산인
   `Tracking.rawResponse` 는 **공개 브랜드에 대한 AI 답변**이라 개인정보가 아니다.
   → **하지 않는 처리를 자백**하는 문구였다. 대신 **부정 고지**를 신설.
   📕 근거: 경쟁사 전원이 반대로 쓴다(Anthropic *"may not train models on Customer
   Content"* · Google Workspace 계약 **12.11 Learning Limitation** · Notion · OpenAI 4.2조).
   B2B 에서 미학습 고지는 **차별화가 아니라 위생요인**(없으면 조달 탈락).
2. **AI 엔진 목록 불완전** — Perplexity·LETSUR·Vercel AI Gateway **누락**분 추가.
3. 최종 업데이트 날짜 갱신.

#### ⚠️ 모델 학습을 **나중에** 하고 싶어지면 (조건부 허용)
🔬 개인정보위 **제2025-011-031호**는 프롬프트의 LLM 학습을 제15조 제3항
**추가적 이용으로 적법 인정**했다. 단 조건이 붙는다:
**①고지 ②상시 옵트아웃(셀프서비스) ③식별정보 필터링** 3종 세트 + 시행령
제14조의2 **제2항**(지속적 발생 시 판단기준 **처리방침 공개 + CPO 점검** = 의무).
- 🔴 **같은 서비스 성능 개선**이면 제15조 제3항 가능 / **별개 신규 서비스**면 목적 외
  → 가명처리 or **별도 동의**(이루다 제2021-007-072호가 정확히 이 지점에서 깨졌다).
- 🔴 **애플 시리(2026-07 · 2억 5,200만)**: *"서비스 개선"* 은 그 자체로 적법근거가
  **아니고**, 원본에 동의를 받아도 **파생 데이터엔 별도 근거**가 필요하다.
- 🔴 **고객사 데이터를 받는 순간** 통합 안내서 **178면**(⚠️전략문서의 *"176면"* 은 오기)
  수탁자 조항이 살아난다 → **자기 이익 목적 이용 절대 금지**.
- ⛔ **2024-07 안내서를 근거로 쓰지 말 것** — 그건 **"공개된 개인정보"(웹 스크래핑) 전용**
  이고 고객 데이터엔 적용되지 않는다(2025-08 생성형 AI 안내서 소관).

#### ✅ 검증
테스트 **206/206**(+10) · web tsc 0 · ultracite 0 · 뮤테이션 3건 전부 실패 확인 ·
라이브(KO) 수탁자 6곳·부정 고지·날짜 확인.
⚠️ **EN 렌더는 한국 IP 에서 검증 불가** — `/en`→**307**→`/` 이고 Vercel 이 클라이언트가
보낸 `x-vercel-ip-country` 를 **무시**한다(N-24 와 같은 벽). 소스에 EN 8섹션 존재는 확인
(KO 8 + EN 8 = 16). → 👤 **해외 VPN 1회 접속**으로 확인 가능.

🎓 **가드가 작성 중 한 번 뚫렸다** — 처음엔 부정 고지의 **존재**만 검사했는데, 같은 문구가
**섹션 제목에도** 있어서 본문을 *"이용할 수 있습니다"* 로 뒤집어도 **통과**했다.
→ *"긍정 서술이 없는가"* 로 판정을 바꿔 잡았다. **존재 검사만 하는 가드는 호위병이 된다.**

### 🎯 1-8. crew "무료" 거짓 제거 + 무료 1회 개방 (세션N-25 완료·배포)

#### 🔴 결함 실체
모든 무료 진단 화면에 **항상** 뜨는 두 곳이 `베타 · 무료`·`할 일 뽑기 · 무료` 인데
누르면 **403**. 사이드바 *"먼저 할 일 3가지"* 는 crew 결과에서 오므로 **영원히 빈 카드**였고,
리포트 메일의 *"🎯 오늘 할 일"* 블록도 같은 이유로 빈 채 나갔다.
🔬 **표류였다**: 라벨=`f045cb9`(6-28) / 게이트=`b80db60`(7-27).

#### ⚠️ 403 문구 자체가 거짓
`canRunDeepAnalysis` 는 **`isPaid(plan)` 을 안 본다** — `org:`·admin·partner 만 본다.
→ 실질 **로그인 게이트**이고 **free 플랜도 가입하면 열린다**.
*"승인 파트너·유료 플랜에서 이용할 수 있습니다"* 는 **사실이 아니었다**.

#### 🔴🔴 내 초안이 교차검증에서 뒤집혔다 (기록해둘 가치가 있다)
초안 = *"정직한 유료 업셀로 전환"*. **반증에서 근거 4개 중 3개가 무너졌다**:
| 내 주장 | 실측 |
|---|---|
| *"crew 원가 67% 라 무료로 못 준다"* | ❌ **인계문서를 검증 없이 인용**했다. 그 67%는 *"crew 를 빼면 67% 절감"*(sonnet+자동실행 시절)이지 *"crew 가 67% 추가"* 가 아니다. 실측 = 진단 87원 / crew 수십 원, 원가율 **0.09%** |
| *"crew 가 유일한 자연 페이월"* | ❌ 처방(`buildGeoActions`)은 **이미 전량 무료 공개**(`12a00de`) |
| *"막아야 유료 축이 두꺼워진다"* | ❌ 유료 축은 `PLAN_CAPABILITIES`(프롬프트·브랜드·**갱신주기**)로 이미 구현. crew 는 거기 안 속한다 |
| *"경쟁사 대비 타당"* | ❌ 업계 게이팅 축 4개에 **처방 없음**. HubSpot Grader·AthenaHQ 무료티어는 **처방을 공짜로 준다** |

🔴 **결정적 선례**: `12a00de`(8-10)가 이미 *"처방 1건만 공개"* 를 **전면 공개로 되돌렸다**.
커밋에 👤 지시 원문이 있다 — *"MVP 가 아니라 **고객 페인을 해결하는 최고의 제품**.
그 기준으로 다시 보니 순서가 틀렸다 — **우리가 이기는 지점을 우리가 잠가뒀다**."*
→ 내 초안은 **두 달 전 뒤집은 결정을 다시 잠그는** 것이었다.

#### 구현
- `FREE_LEAD_CREW_QUOTA = 1` · `hasFreeCrewQuotaLeft()` (순수 함수) ·
  **전역 일일 상한**(`DAILY_CREW_CAP`, KST 자정 기준) 신설.
- 🔴 **평생 1회**(하루 1회 아님) — 매일 리셋되면 가입 이유가 사라진다(Docker형 실패).
- 🔴 **실패분도 카운트** — 실패해도 크레딧은 나갔다.
- ⚠️ **금액이 아니라 건수로** 상한 — crew 는 원가를 **어디에도 기록하지 않는다**
  (`cost.ts` 에 haiku 단가 없음). 금액으로 걸면 **측정된 적 없는 단가**를 곱해야 한다.
- UI 3상태 + 배지에 **횟수 선고지**(AthenaHQ 최대 불만 = *"크레딧 소진 예측 불가"*).
- ⚠️ 소진 판정은 **서버 플래그**(`signUpRequired`)로 — 문구로 갈랐다면 서버 문구를
  다듬는 순간 조용히 깨진다.

#### ✅ 유지한 것
네이버 브리핑의 `베타 · 무료` 는 **그대로** — 그 라우트엔 자격 게이트가 없어 **사실**이다.

#### 🔴 남은 퍼널 결함 (이번에 안 고침 · 아래 §2-14·15)
① **가입해도 빈손** — 진단 결과(`AuditJob`/`Lead`)와 Clerk org 를 잇는 코드가 **없다**.
   *"기준점 저장하기"* 를 누르고 가입하면 **빈 대시보드**. 이메일 프리필도 없어
   **다른 주소로 가입하면 결과가 영구 소실**(코드 주석이 자백하고 있다).
② **리드 후속 접촉 0건** — `Lead` 생성 3곳 / 소비 1곳(관리자 count). 크론 3개 전수
   확인 결과 리드 대상 발송 **0건**.

### 🔴 1-9. 🟡 **미해결로 남긴 것 — 공유링크 이메일 노출**(세션N-25 확인)

결과 페이지는 **비로그인 접근 가능**하고, `[jobId]/route.ts:130` 이 **소유 검사 없이**
`emailMasked` 를 항상 준다. 현재 방어는 `?shared=1` 표식뿐인데 그건 **공유 버튼으로
만든 링크에만** 붙는다 → 소유자가 **주소창 URL 을 그대로 복사**해 보내면 제3자에게
마스킹 주소가 보인다.
- 🔗 세션N-7 이 이미 같은 사고를 겪고 표식 방식으로 막았다(저위험 임시책).
- 정석 = **서버측 소유자 판별**(API 가 소유자에게만 `emailMasked` 를 준다).
- ⚠️ 세션N-25 는 **새 카드를 기존과 같은 수준**으로만 맞췄다(더 낫게도, 나쁘게도 아님).
- 📌 착수 조건: 결과 페이지에 인증 컨텍스트를 넣거나 API 에 소유 토큰을 붙일 때 함께.

### 🎓 1-10. 세션N-25 가 배운 것 — **감사 결과도 틀린다**

이번 세션은 **감사 에이전트의 1위 지목이 두 번 틀렸다**:
| 감사 주장 | 실측 |
|---|---|
| *"진단↔계정 연결 코드가 없다"*(퍼널 1위) | ❌ **이미 연결됨** — 대시보드가 이메일로 조회 |
| *"돈 내도 crew 가 안 열린다"*(반증 에이전트) | ❌ **열린다** — `start-tracking.ts:157` 이 `email:"org:{orgId}"` 로 만들고 그게 게이트를 통과 |
| *"이메일 프리필이 없는 건 결함"* | ❌ **의도적 미적용**(마스킹 주소라 오연결 위험) |

⭐ **그런데 감사가 무의미했던 게 아니다** — 틀린 1위를 파는 과정에서
**내가 직전에 만든 실제 구멍 2개**(소유권 안내 누락 · 공유링크 이메일 노출)를 찾았다.
🎓 교훈: 감사 결과는 **탐색의 시작점**이지 결론이 아니다. *"묶음으로 적힌 지적은
건별로 실측"* 규칙이 **에이전트 산출물에도 그대로 적용된다.**

### ⚠️ `[확인필요]` 로 남긴 것 1건 — **단정하지 않는다**
🟡 **외국 방문자가 한국어 화면을 보는지** — `/en` → `/` **307**(정상: `rewriteDefault` +
`defaultLocale:"en"` 이라 영어는 접두사 없음). 그런데 `/` 는 **한국 IP 인 내 위치에서만**
검증 가능했다(`x-vercel-id: icn1`, `lang="ko"` = **내 IP 기준으로는 정상**).
🔴 **Vercel 이 클라이언트가 보낸 `x-vercel-ip-country` 를 무시**하므로 미국 IP 위장 테스트가
불가능했다 → **외국 IP 경로는 검증하지 못했다.** 단정 금지.
→ 확인 방법 = 👤 **해외 VPN 1회 접속** 또는 Vercel 로그의 `x-vercel-ip-country` 분포 확인.
⛔ `투두리스트.md:912` 의 *"`/en` 리다이렉트는 결함 아님"* 은 **URL 구조에 대해서만 옳다**
(위 307 로 재확인). 그 판정이 **`<title>` 언어까지 면제해주는 것은 아니다** — 별개 항목.

---

## 2. 🖥️ 코드로 **지금** 할 수 있는 일 — 11건 (원가 0원 · 승인 불필요)

> 🔴 인계문서가 *"소진"* 이라 한 구간이다. 실제로는 아래가 남아 있다.
> 정렬 = **작업량 대비 효과 순**(위가 가성비 높음).

| # | 할 일 | 크기 | 근거·경로 | 관문 |
|:--:|---|:--:|---|:--:|
| ~~13~~ | ✅ **crew "무료" 거짓 제거 + 무료 1회 개방 — 완료·배포·라이브 번들 확인(세션N-25)**. 🔴 화면이 `베타 · 무료`/`할 일 뽑기 · 무료` 라 광고하고 **403** 을 뱉던 것(라벨=최초커밋 6-28 / 게이트=7-27 **표류**). 403 문구 *"유료 플랜"* 도 **거짓**이었다(실질=로그인 게이트). 아래 §1-8 | M | 퍼널감사 1·2위 | — |
| ~~12~~ | ✅ **처리방침 개정 — 완료·배포·라이브 확인(세션N-25)**. 수탁자 6곳 공개(제26조 제2항) + AI 학습 미이용 고지 신설 + 기존 오류 3건 정정. 🔴**인계문서 §4 의 수탁자 목록이 틀렸다**: AWS 는 **SDK 0건**(런타임 감지 플래그)이라 적었으면 허위 기재였고, 개인정보를 실제로 받는 **PostHog**(이메일·이름·전화번호)·**Resend**·**Neon** 이 빠져 있었다. 아래 §1-7 | S | §4 | — |
| ~~1~~ | ✅ **app favicon** — 완료·**배포·라이브 검증**(`app.findable.co.kr/favicon.ico` **404→200**, 2,962B 원본과 일치) | XS | 실측①② | — |
| ~~7~~ | ✅ **운영자 장애 알림** — 완료·**배포**. `captureOpsAlert` 신설 + 테스트 5건·뮤테이션 2건. 🔒배포 후 cron 가드 **401 재확인**(`x-vercel-cron` 스푸핑도 401) | S | BL-Day17-02 | — |
| 2 | 🟡 **요금제 「현재」 배지** — ⚠️**세션N-24 하향 정정**: 하단 CTA 가 `isCurrent` 일 때 **「이용 중」을 정상 표시**한다(`:271`, featured 무관). 그래서 *"자기 플랜을 못 본다"* 는 **과장이었다**. 진짜 결함은 `추천`·`현재` 가 **다른 축인데 한 슬롯을 다퉈** 「현재」를 희생시키는 것 → **P2** | S | `billing/page.tsx:216` | 없음 |
| 3 | **`/en` `<title>` 한국어 노출** — 영어 페이지 메타가 한국어 | S | `투두_마스터.md:1072` · §1 `[확인필요]` 선행 | 🟡확인 |
| ~~4~~ | ❌ **환상 항목이었다 — 삭제**. `min-h-[60vh]` 는 **코드에 없다**. `locked-surface.tsx:77` **주석 안**에만 있고, 그 주석은 *"왜 적용하지 않았나"* 를 남긴 것이다. 🎓 **"기각된 제안"을 "지울 죽은 코드"로 오독**했다 — 지웠으면 N-16 의 *"카드 상단 잘림"* 재발을 막는 경고문을 없앨 뻔했다 | — | 실측 | — |
| ~~5~~ | ✅ **결제 퍼널 계측 — 완료·배포·라이브 검증**(세션N-24). 아래 §1-3 상세. ⭐P0 정량근거 | M | BL-Day17-04 | — |
| ~~6~~ | ✅ **로그 도메인 — 완료·배포**(세션N-24). ⚠️**백로그 기재가 틀렸다**: 결제는 **이미 21개 키로 촘촘히 로깅**돼 있었고, 진짜 공백은 `auth` 웹훅 1곳이었는데 **그게 개인정보를 쏟고 있었다** → 아래 §1-4 | M | BL-Day17-03 | — |
| ~~8~~ | ✅ **crew route 스코핑 = 안전 확인(세션N-24)** — 아래 §1-2 상세. **코드 수정 불필요** | S(검증) | `실행백로그.md:66` | — |
| ~~9~~ | ✅ **타임아웃 정합 = 이미 맞다(세션N-24 검증) · 코드 변경 불필요** — 아래 §1-5 | S | `실행백로그.md:81` D16-4 | — |
| ~~14~~ | ❌ **전제가 틀렸다 — 기각(세션N-25 실측)**. 퍼널 감사가 *"진단↔계정 연결 코드가 없다"* 를 1위 결함이라 했고 나도 그대로 옮겼으나, **이미 연결돼 있다**: 대시보드·히스토리가 `identifiers=[로그인 이메일, "org:{orgId}"]` 로 `auditJob` 을 조회한다(`(authenticated)/page.tsx:47`·`history/page.tsx:23`) → **읽는 시점에 이메일로 자동 연결**. 별도 claim 로직은 **중복 기능**이었다. 이메일 프리필도 **의도적 미적용**(API 가 마스킹 주소만 줘서 그대로 넣으면 오연결 · `:3946` 주석에 근거). 🔴 대신 **직전 커밋이 만든 진짜 구멍**(새 가입 카드에 소유권 안내 누락 + 공유링크 이메일 노출)을 찾아 고쳤다 — `169a148` | — | 실측 | — |
| **15** | 🟠 **퍼널 계측 확장** — `funnel.ts` 에 **checkout 3종만** 있다. 진단시작·결과도달·crew클릭·가입클릭이 **미계측**이라 *"어디서 새는지"* 를 숫자로 못 본다. P0 인터뷰의 정량 근거와 직결 | S | 퍼널감사 | 없음 |
| **16** | 🟡 **IP 일일 신규도메인 상한 2건** — 대행사·에이전시(=지불의사 높은 ICP)가 클라이언트 3곳을 진단하면 **3번째에서 429**. 게다가 에러 문구가 *"문의해 주세요"* 인데 **문의 링크가 없다**(`route.ts:443`) | S | 퍼널감사 부차 | 없음 |
| 10 | **`/history/[jobId]` 앱 내재화** | **L** | N-23 §5-3. ⚠️공유링크(비로그인 `?shared=1`) 경로 유지 설계 선행. 🔬**세션N-25 실측**: `app` 에 `[jobId]` 라우트가 **없고**, 히스토리 목록이 `${webUrl}/ko/audit/${id}` 로 **web 으로 내보낸다**(`audit-history-list.tsx:124`). 그 web 페이지는 **로그인을 인식하지 않는다**(auth 참조 0건) → 유료 고객이 자기 데이터를 보려고 제품 밖으로 나간다 | **L** | 없음 |
| 11 | **lint 부채 23건** | M | `투두리스트.md:779`. ⚠️**단독으로 열지 말 것**(#10 이 그 파일 손댈 때 함께) | 🔗#10 |

### ⛔ 위 11건에서 **빼놓은 것**(하지 말라고 판정된 것 — 되살리지 말 것)
전부 **실측 근거**가 있다. 상세는 `_START_HERE_세션N23.md` §5·§5-2, `투두리스트.md:882~918`.
1. **엔진 수 7↔8 "통일"** — 오답. 본류=7 · 영어만=4 · 8=브리핑 별도. **재조사 금지**
2. **결제 상태 3줄**(다음 결제일·카드 4자리) — `Organization` 스키마에 **필드가 없다**
3. **처방 2건 병합** — 서로 **다른 질문**(영/한) + `@@unique([brandId,kind,target])` 라
   합치면 **고객 완료표시가 미완료로 되살아난다**
4. **감성 배지 변별력** — 데이터가 **실제로** 중립(18/3/null 8). 없는 차이를 만들면 조작
5. **순위 분모 노출** — `mentionListSize` 전부 null(N-10 이전 소급 없음). **없으면 감추는 게 정상**
6. **`x-vercel-cron` 폴백 부활** — 외부인이 유료 측정을 무제한 실행시킬 수 있었다
7. **업계 N분위 벤치마크** — 조직 1개라 표본 없음 = 숫자 조작
8. **naver 어댑터 Daum식 정합** — 구조가 다르다(naver=HyperCLOVA 합성). 없는 언급을 만든다
9. **팩트정합률 전면화** — 사실 검증을 안 하는데 이름이 사실 검증으로 읽힌다
10. **진실거울을 대시보드로 복사** — 유료 축(시간·비교·알림)을 안 바꾸는 오답

---

## 3. 💳 결제 트랙 — 카카오페이 승인이 관문 (3건)

> 4차 회신 발송 완료 · **답장 대기**. 👤 먼저 보낼 것 **없음**.
> ⚠️ 아래는 **승인 회신이 온 날** 착수. 그전에 하면 다시 손대야 한다.

- [ ] 🔴 **라이브 정기결제 채널키 교체** — `NEXT_PUBLIC_PORTONE_CHANNEL_KEY_BILLING`
      (지금은 테스트 CID `TCSUBSCRIP`) · 참조 2곳 실측 확인
- [ ] 🔴 **결제 예약(2회차 자동청구) 연결** — `POST /payments/{paymentId}/schedule`
      🔬**실측③: 생성 함수가 없다**(취소·삭제만 있음) → **신규 구현 필요**
- [ ] 🔴 **연결 후 "지금은 첫 회만 결제되며…" 줄 삭제**
      (`subscribe-button.tsx:186` 실측 확인) — 안 지우면 **거짓 표기**가 된다
- [ ] 👤 **`PORTONE_WEBHOOK_SECRET` 발급 + 콘솔 웹훅 URL 등록**
      🔴 **첫 실결제 전 필수**. 안 넣고 결제받으면 *"결제됐는데 plan 이 free"* 사고

🔴 **해지 순서 고정**: ①예약 취소 → ②빌링키 삭제. **뒤집으면 무한 과금 사고.**

---

## 4. 📊 데이터가 쌓여야 의미 생기는 것 (4건)

> 🔴🔴 **세션N-25 재분류 — "기다리면 되는 일"이 아니다.**
> 이 4건은 *"측정 5~6회 쌓이면 착수"* 로 적혀 있었으나 **실측하니 영원히 안 쌓인다**:
> 자동 재측정 cron 은 `planCapabilities(plan).autoRefreshHours != null` 인 org 만 돈다
> (`auto-refresh-tracking/route.ts:196`). 그런데 **`free: autoRefreshHours: null`**
> (`packages/auth/plan.ts:120`)이고 **유료 고객이 0명**이다.
> → **시계열은 1일에서 멈춰 있고, 시간이 지나도 안 늘어난다.**
> ⭐ 즉 이건 대기 항목이 아니라 **§5-1(P0 실고객)에 종속된 항목**이다.
> 다음 세션이 또 *"아직 데이터가 없네"* 하고 넘기지 않도록 여기 못박는다.

> 공통 근거(실측): `Tracking` 54행 · 측정 2회 · **시계열 1일** · 조직 1개.

- [ ] **D4 기간 선택 칩** — 시계열이 1일이라 **누를 게 없다**
- [ ] **D2 자동 이벤트핀** — 델타가 `[0,0,0,0,+97]`, +97 은 측정실패→복구라
      규격 적용하면 **핀이 측정 실패 지점에만 꽂힌다**
- [ ] **D1 이상감지 알림 UI** — 임계값을 정할 **분포 자체가 없다**
      🔴 **근거 없는 경계선 발명 금지**(*"40% 미만 경고"* 를 제안했다가 사용자 지적으로 철회한 전력)
- [ ] **`alerts` 실제 알림 메일** — 코드 완료·**기본 꺼짐**(`FINDABLE_ENABLE_DIGEST_EMAIL`).
      👤 **메일 본문을 눈으로 확인한 뒤** 켤 것

---

## 5. 👤 사람이 움직여야 하는 것 — **회사 병목** (2건)

### 5-1. 🔴🔴 P0 실고객 지불의사 검증 — **불변 1순위**
- 대상 = **마케터 10~15명**(외국 브랜드 한국법인 · K-뷰티 D2C)
- 📕 질문지 **이미 있음**: `P0_지불의사_시뮬레이션_결과.md`
- ⛔ **시뮬레이션으로 대체 불가**(r=0.20). *"비관적으로 돌리면 보수적 추정"* → **아니다, 노이즈다**
- 🤖 **내가 할 수 있는 것**: 질문지 다듬기 · 대상 리스트업 · 거절사유 사전정리 · 녹취 분석
- 🚫 **내가 못 하는 것**: 사람을 만나는 것 자체
- 🔗 **이게 풀려야** §4 의 before/after·게이팅도 데이터 근거를 갖는다
- ⭐ **§2-5(퍼널 이벤트)를 먼저 깔면** 이 검증의 정량 근거가 생긴다 ← **코드로 도울 수 있는 유일한 접점**

### 5-2. 🔵 KIPO 상표 출원 — **시간이 불리하게 간다**
- 근거 = `findableapp.com`(오스트리아 **동일 이름 GEO 회사**) + 한국 **선출원주의**
- 후보 = 제9류(SW) · 제35류(광고) · 제42류(SaaS)
- 🤖 **내가 할 수 있는 것**: 지정상품 분류 조사 · 기존 등록상표 검색 정리 · 서류 항목 정리
- 👤 실제 출원 · 수수료 납부

---

## 6. ⚪ 범위 큰 것 (데모데이 10월 이후 권고 · 4건)

- [ ] **GEO 실행 에이전트** — 진단→리라이트→발행 준비. **경쟁사 6곳 전원 공백 = 최대 차별화**
      ⚠️ 자사 통제 소스만(JSON-LD·자사 FAQ·구글비즈·위키데이터) + 인간 승인
      ❌ **나무위키 직접 편집 금지**(표시광고법)
      📕 정의 = [[project_findable_geo_agent_definition]] — 🔴**고객 사이트 수정이 아니라 자체 블로그 플랫폼**
- [ ] **RAG 파이프라인 4종** — `Document`/`DocumentChunk`/`Embedding` + pgvector
      ⚠️ 라이브 스키마 변경 **고위험** · `scopedVectorSearch` 는 **org 필터 손 주입 필수**
- [ ] **durable 실행 전환(Vercel Workflow)** — crew 최대 **226초 / 상한 270초** = 여유 44초
      🔗 §2-9(타임아웃 정합)와 같은 뿌리
- [ ] **Prisma Client Extension org 자동주입** — RLS 준하는 강제력. ⚠️전체 쿼리 영향 고위험

---

## 7. ⬜ 확인 대기 (코드로 앞당길 수 없음 · 2건)

- [ ] **crew 자기평가 실물 로그**(`refineApplied`) — 배포 후 crew 실행 0건.
      **새 진단 1건이 돌면 자동 확인**
      ⚠️ 실측 경로 함정: `crewResult.strategist.`**`output`**`.executiveSummary`
      (`strategist.executiveSummary` 로 읽으면 전건 null → "필드 없다"고 오진)
- [ ] **cron 실동작** — UTC 17시(=KST 새벽 2시) 교정 후 첫 자동 실행이 실제로 도는지 DB 1회 확인

---

## 8. ⚠️ 착수 전 필독 함정

- 🔴 **엔진 수 재조사 금지** — 본류=7 · 영어만=4 · 8=브리핑 별도(N-23 §1 확정본)
- 🔴 **`apps/web` 에 테스트 러너가 없다** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서 검증
- 🔴 새 `.test.tsx` = **`@vitest-environment jsdom` 도크블록 필수** → **단독 실행으로** 검증
  (전체 실행은 다른 테스트가 만든 DOM 에 편승해 **통과해버린다**)
- 🔴 **`verify-app.py` 는 1440px 고정** — 모바일은 검사된 적 없다 → **두 폭 다** 볼 것
- 🔴 **검사 스크립트가 UI 문구를 하드코딩하면 올바른 변경을 막는다** → 화면 문구 바꾸면
  `scripts/verify-app.py` 도 함께 볼 것 · ⭐**상태는 문자열 아니라 데이터 플래그로**
- 🔴 **biome 복잡도 20 · 중첩 삼항 금지** → 라벨 분기는 `let` + `if/else` 로 미리 계산
- 🔴 **DB 직접 조회 막힘**(한글 NFD + Prisma CJS) → **라이브 API `/api/audit/<id>`** 가 빠르다
- 🔴 **디자인 시스템 기본값을 열어라** — `SelectTrigger` 기본이 `w-fit`.
  *"내 파일에 없다" ≠ "적용 안 된다"*
- 🔴 **`Date.now()` 기반 상한은 테스트 불가**(가짜 타이머가 시계를 안 돌린다) → **틱 수로**
- 🔴 **테스트 mock 을 매 렌더 새 객체로 만들지 말 것** → effect 재구독 무한 폴링
- 🔴 **`apps/api` 는 죽은 앱**(Vercel 프로젝트 없음) — 새 코드 넣으면 **실행되지 않는다**
- 🔴 로컬 dev·build 는 **`VERCEL=1`** · `"use server"` 파일은 **async 함수만 export**
- 🚧 **로컬 dev 로그인 불가**(Clerk 개발 인스턴스) → 눈확인은 **배포 후**
- ⚠️ 요금제 문구는 **app·web 둘 다** · ⭐ **app 은 `lang="ko"`**
- ⚠️ 진단 결과 페이지에 **`h1` 추가 금지** · 크롤러용 텍스트를 **`sr-only` 로 숨기지 말 것**
- ⭐ **배포**: app = `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app`
  / web = `--project findable` (둘 다 **루트에서**)
- 👁️ **스크린샷 눈확인** — 7세션 연속 버그를 잡았다. 코드로 못 보는 것을 잡는다
- 🎓 **라이브에서 확인 못 하는 경로는 순수 함수로 빼서 테스트로 고정**

---

## 9. 🎓 이 프로젝트가 반복해서 배운 것

1. 🔴🔴 **문서가 분산되면 반드시 누락된다** — *"백로그 비었다"* 오진 **4회**.
   → **남은 일은 이 파일에만 적는다.**
2. 🔴 **진단서·인계문서는 낡는다** — 41건 중 **7건이 이미 고쳐져 있었고 4건은 기각**됐다.
   **착수 전 건별 실측.**
3. 🔴 **"묶음으로 적힌 항목"은 건별로 실측** — *"빈 상태 4화면 이식"* 중 **3건이 오탐**이었고
   대신 **문서에 없던 결함 2건**이 같은 화면에서 나왔다.
4. 👁️ **테스트는 아는 것만 검증한다** — 뮤테이션 23종 통과 후 스크린샷에서 버그 3건.
5. 🔴 **가드가 버그의 호위병이 된다** — 검사장치에 문구를 하드코딩하면 양방향으로 터진다.
6. ⭐ **없는 데이터를 화면에 만들지 않는다** — 표본 없는 분위·근거 없는 임계값은 **조작**이다.
