# 무료진단 결과 페이지 결함 감사 (2026-07-30)

> 발단: 사용자 문제제기 영상(`문제제기_영상/화면 기록 2026-07-30 오전 3.04.07.mov`) — nvidia.com 진단 결과.
> 전부 코드/DB 실측으로 원인 확정.

## ✅ 수정 결과 (같은 날 전량 수정·배포)

- **1~4, 6~14 전부 코드 수정 완료** (2026-07-30 후속 세션). 검증: audit·web·app tsc 0 + ultracite 0 + tsx 유닛 스모크(geo-score·strip-markdown·revenue-impact·competitor 필터) 통과.
- **핵심 구조 변화**: GEO 채점을 `packages/audit/geo-score.ts` **단일 진실**로 추출 — 결과 UI·리드메일·OG 이미지가 같은 숫자. 이 과정에서 **추가 결함 2건 발견·수정**: ①리드메일 점수 계산이 UI와 분모 불일치 ②OG 공유 이미지가 SoV를 GEO 점수처럼 표기(nvidia: 페이지 73 vs OG 85). PDF 템플릿도 "28개 AI 엔진 = 112회 호출" 같은 raw 길이 버그 수정.
- **채점 재설계 후 nvidia 재계산**: 감정 31/40(중립 무벌점) + 노출품질 13/20(HHI 집중도 반영) + 인지 20/20(오류 제외) + 점유 9 + 경쟁 8 = **81/100 "리더"** (기존 73 "경쟁 가능" → 헤드라인과 정합).
- **5번(Perplexity·Gemini)**: 원인=두 키가 www에만 있고 app에 없음(2일 전 사용자가 www에 등록, Sensitive라 재열람 불가). `GOOGLE_API_KEY`는 로컬(demo-lotteworld)에서 실값 발견·검증(200)→**app 등록 완료**. `PERPLEXITY_API_KEY`는 ✅해결(2026-07-30 후속): Perplexity 콘솔은 키 재열람 불가 → 새 키 발급, 클립보드→CLI로 app+www 동일키 등록(옛 키 www에서 제거)·재배포. kia.com 라이브 진단 **7엔진 28/28 오류 0** 검증. 실값=apps/web/.env.local 보관. 👤Perplexity 콘솔의 옛 키 삭제만 남음.
- 15번(저품질 인용 소스 필터)만 별도 과제로 잔존.

## ✅ 2차 문제제기 3건 (같은 날 후속, 수정·배포 완료)

16. **매출 임팩트 여전히 과소(사용자 지적: "대기업 336만도 말이 안 됨")** → **계산식 v2 재설계**. 리서치(Pew·Semrush·Adobe·Bain·네이버 AI 브리핑 27% 등 실측 벤치마크, 별도 문서=`매출임팩트_벤치마크_리서치_2026-07-30.md`) 기반 3요소 모델: C1 직접 유입(클릭률 8%×AI 전환 5%) + C2 제로클릭 영향(영향률 20%×전환 1%) + C3 광고 환산 가치(네이버 CPC, 별도 병기). 프리셋 노출 1만/10만/100만. 결과: 소규모 SoV30=월 210만, 대기업=월 2.1억. UI에 3요소 분해 카드+출처 고지+가정 7종 조정.
17. **네이버 AI 브리핑 답변에 고지문+자바스크립트 원문 노출(사용자 지적)** — 원인: 20,000자 고정 창이 `<script>` 중간을 잘라 닫는 태그가 없어 짝 매칭 제거 실패 → JS/JSON이 "답변"으로 노출(그 JSON에 검색어가 있어 언급 판정 오염 위험). 수정=`naver-briefing-adapter.ts`: 미종결 script/style 꼬리 통째 제거 + 네이버 고정 고지문("AI 브리핑 실험 단계로…연관성을 가지지 않습니다") 제거 + 20k→60k 재시도.
20. **브리핑 언급 판정이 영문 표기를 못 잡음(재측정 검증 중 발견)** — 2겹 원인. ①`briefing-runner.ts`가 brandVariants를 `[brand]` 1개만 전달 → `resolveBrandIdentity`로 복원하도록 수정. ②**진짜 뿌리 = `brand-identity.ts`**: 폼/DB 브랜드명이 주어지면 정적 사전 미스 시 LLM 추론을 건너뛰고 **변형 0개** 반환("엔비디아"만 알고 "NVIDIA" 모름). 사전 미스여도 LLM으로 변형 보강하도록 수정 — 본 러너(브랜드명 입력 org 측정)의 영문 답변 판정도 함께 개선됨. ⚠️기존 측정분은 DB 저장 산출물이라 재측정해야 반영(완료 건은 409 → DB에서 briefingStatus 초기화 후 재트리거). 이 결함이 사용자 지적 "진실거울은 '모름'인데 브리핑 섹션은 '언급'" 모순의 원인(옛 오염 판정과 새 판정이 화면에 공존).

21. **GEO 코파일럿 무응답(사용자 지적: 스피너만 돎)** — 원인 2겹. ①crew 4에이전트는 Letsur 우선 라우팅인데 **코파일럿만 무조건 `gateway()`** → Gateway 크레딧 0 → 헤더만 200으로 나가고 스트림이 비동기로 죽어 **빈 본문** → UI가 성공으로 오인. 수정=코파일럿도 Letsur 우선(copilotModel())+isCopilotConfigured에 LETSUR 키 인정. ②클라이언트가 "200+빈 스트림"을 성공 처리해 빈 말풍선 잔존 → 토큰 0이면 에러 표시로 수정. +부가: crew 미완료 시 chat API가 400인데 UI 안내 없음(코파일럿은 4에이전트 분석 후 사용 가능 — 게이트 자체는 설계).

22. **크루 4에이전트 간헐 "Bad Request"(재실행 검증 중 발견)** — 같은 job 재실행 시 일부 에이전트만 무작위 실패(전원 실패→민지만 성공 등). Letsur 직접 재현(단문·4병렬·json_schema·tool_choice·12만자 장문) 전부 200 → Mastra 페이로드에 대한 Letsur 간헐 400으로 판정. 수정=orchestrator 에이전트 호출에 3회 재시도(0.5s·1.5s 백오프).

23. **브랜드 변형 LLM 추론이 프로덕션에서 한 번도 안 돌았음(§20 최종 뿌리)** — `models.chat=openai("gpt-4o-mini")`가 OPENAI_API_KEY 전제인데 프로덕션 미설정 → 항상 조용히 실패. 수정=brand-identity의 LLM 추론을 Letsur 경유로 라우팅(crew와 동일).

24. **UX slop 제거(사용자 지시: 토스/애플 문법)** — impeccable 스킬 기준 금지패턴 제거: ①영문 모노스페이스 대문자 아이브로우 29곳→차분한 국문 라벨(text-xs font-medium, uppercase/tracking/mono 제거) ②이모지 CTA("📩 풀 리포트"→"풀 리포트 받기 · 무료", "🔗 카톡 자랑"→"카카오톡으로 공유") ③장식용 코너 글로우 2개 제거(hero·매출카드). 대시보드(app)는 해당 패턴 없음 확인. 🟢후속 후보: 카드 그리드 다양화·모션 정리(별도 세션).

- **크루 리포트 "짤림" 판정**: 잘리는 게 아니라 **설계상 글자수 제한**(제목 40자·이유 80자·whyItMatters 50자, zod 스키마 강제)의 요약형 출력 + 우측 "오늘 할 일"은 의도된 컴팩트 뷰. 단, 해당 리포트는 §19 오염 데이터("인용 출처 전무"·"8개 에러 엔진"=raw 응답수)로 분석된 것이라 내용 자체가 부정확 → 데이터 복구 후 crew 재실행 필요(completed는 409 → DB crewStatus 초기화).

19. **브리핑 병합이 인용 출처를 유실(라이브 검증 중 발견)** — `briefing-runner.ts`가 metrics 재계산 시 저장 응답을 `citedSources: []`로 복원 → `topCitedDomains`가 빈 배열로 덮여 출처 패널 소실+노출품질 축 0점. 수정=재계산 후 원본 topCitedDomains 보존(+excerpt 한도 1500→4000 정합). 오염된 tesla·nvidia job은 DB에서 원본 비율로 복구. 🟢잔여: 브리핑 미노출 검색어일 때 관련기사 스트립이 답변으로 저장되는 품질 이슈(§17 후속).

18. **4에이전트 심층분석이 소유자에게도 게이트(사용자 지적: "내가 접속해도")** — 원인: 게이트가 로그인 여부가 아니라 **AuditJob.email 문자열**을 env 화이트리스트와 대조. org 측정은 email=`org:{orgId}`라 무조건 "일반 리드" 판정. 수정=`usage-tier.ts` canRunDeepAnalysis: `org:` 프리픽스면 허용(org 측정은 인증+rate-limit 뒤에서만 생성). ⚠️www 무료진단에서 쓰려면 신청 이메일이 `FINDABLE_ADMIN_EMAILS`(www env)에 있어야 함 — 👤본인 이메일 포함 여부 확인.

## 🔴 치명 — 신뢰 붕괴급 (첫인상에서 데이터와 모순)

1. **헤드라인 자기모순** — SoV 85·언급률 71%인데 "AI 답변 절반 이상에서 누락"
   - `audit-result.tsx:333-360 mckinseyHeadline`. sov≥70 분기의 2번째 조건(mentioned ≥ live×0.8)이 실패하면 sov≥40 분기로 떨어지는데 그 카피가 "절반 이상 누락" 고정문구. 데이터와 정반대 문장이 첫 줄에 뜸.
   - 수정: 분기 재설계(수치 기반 동적 문장). 오류 엔진은 live 분모에서 제외.

2. **회복 가능 매출 +₩0 (SoV 85% → 85%)** — `revenue-impact.ts:70`
   - `clampedTarget = max(현재SoV, 60)` → SoV>60이면 목표=현재 → 회복분 영구 0. "이번 주 1건 액션으로 회복 가능"(1번 카피) 바로 아래서 +₩0 노출.
   - 수정: 목표 SoV를 `min(100, max(60, sov+10))`류로 — 잘하는 브랜드도 개선분이 보이게.

3. **매출 임팩트 월 ₩4만 (사용자 지적)** — 측정 오류가 아니라 **하드코딩 기본 가정**
   - `revenue-impact.ts:32 DEFAULT_ASSUMPTIONS.monthlyAiQueries=3000` 고정(브랜드 규모 무관). nvidia든 동네카페든 월 3,000질의.
   - 검산: 3000×(1−0.85)×8% = 36세션 ×2%×₩50,000 = ₩36,000 ≈ ₩4만. 코드는 가정대로 정확히 계산, 가정이 비현실.
   - 수정 방향: 카테고리/브랜드 규모별 질의량 추정(러너가 이미 브랜드 카테고리 앎 → LLM 1회 추정 or 규모 프리셋 선택 UI). 가정 조정 UI는 있으나 기본값이 첫인상.

4. **경쟁사 벤치마크에 "그래픽카드 추천"(용도 문구)이 브랜드로 등장** — `competitor-extract.ts`
   - 번호목록 파싱이 "1. 엔비디아 주식 추천 / 2. 그래픽카드 추천"(용도 분류 목록)을 브랜드 순위로 흡수.
   - 수정: 비브랜드 휴리스틱(동사/조사 포함·"추천"·"용" 등 접미 필터, 2회 미만 등장 컷 강화 등).

## 🟠 높음 — 기능이 고장나 보임

5. **Perplexity·Gemini 오류 (사용자 지적)** — 원인 실측 확정: **Vercel AI Gateway 크레딧 0**
   - DB 에러 원문: "A positive credit balance is required for all requests, including BYOK…"
   - `GOOGLE_API_KEY`(무료)·`PERPLEXITY_API_KEY` 미설정 → Gateway 폴백 → 크레딧 에러. chatgpt·claude는 Letsur 직접이라 무사.
   - 수정: 👤 키 2개 발급 + www·app env 등록(=투두 "엔진 복구" 건). 코드 수정 아님.

6. **진실의 거울 벽글(줄바꿈 소실) (사용자 지적)** — `truth-mirror.tsx:264`
   - blockquote에 `whitespace-pre-line` 누락(엔진별 응답 탭 `audit-result.tsx:2267`엔 있음). 줄바꿈 전부 붙어 출력.
   - + 카드 길이 불균형: ChatGPT 1500자 vs 타 카드 3줄. line-clamp+더보기 필요.

7. **Daum 카드 텍스트 넘침 (사용자 지적)** — 긴 URL 연속문자열에 `break-words`/overflow 처리 부재.

8. **엔진별 응답 표가 raw 파이프로 노출 (사용자 지적)** — `strip-markdown.ts`가 표 문법(`|---|`) 미처리. Claude 답변의 마크다운 표가 `| 제품 | 가격대 |` 그대로.
   - 수정: 표 행 파싱해 "제품: … / 가격대: …" 평문화 or 구분자 정리.

9. **응답 짤림 (사용자 지적)** — `packages/audit/runner.ts:193 excerpt = rawResponse.slice(0, 1500)`
   - 하드컷이라 "**GeForce RT"처럼 중간 절단 + 잘린 `**`는 stripMarkdown이 못 지워 raw 노출.
   - org 측정은 Tracking.rawResponse에 전문 저장됨. 무료진단은 excerpt뿐.
   - 수정: 한도 상향(비용 실측상 여유) or 문장 경계 컷 + "…" + (org면) 전문 보기.

## 🟡 중간 — 수치·서사 정합성

10. **"4개 프롬프트 × 28개 엔진 분석"** — `audit-result.tsx:1016`이 중복 포함 `enginesCovered.length`(28) 사용. 고유화(Set)하면 7. "×7개 엔진(28회 측정)"으로.

11. **"글로벌 AI는 100%" (Visibility Gap, 사용자 지적)** — `naver-vs-ai-gap.tsx:49`가 오류 엔진 제외 후 계산 → 실제로는 chatgpt·claude 2개(8측정)만으로 100%. 오류 카드 2장과 같은 화면에서 모순돼 보임.
    - + 섹션 목적 카피 불명확(사용자 지적): "이 카드가 뭘 알려주는지" 리드 문장 필요.

12. **긍정/부정 2/0 vs 진실거울 전부 "중립"** — KPI는 28응답 전체 집계, 진실거울 카드는 엔진별 **첫 프롬프트 응답만**(dedupeByEngine first-wins) 표시. 같은 화면에서 숫자 안 맞아 보임.

13. **진실거울 "7개 중 5개가 알고 있습니다"** — 오류 2개(측정 실패)를 "모른다" 분모에 포함. 측정 실패≠브랜드 미인지. 분모에서 제외하거나 "2개 측정 실패" 병기.

14. **GEO 점수 채점 설계 취약 2건**
    - 감정 분석(40점 만점): 부정 0·중립 다수면 22/40 — 최대 배점 축이 중립을 벌점 취급. GEO 73점의 주범.
    - 노출 품질(20점): `topCitedDomains.length × 4` — top5 리스트라 사실상 항상 20/20(집중도 무시. blog.naver.com 64%인데 만점).

## 🟢 낮음 — 데이터 품질/장기

15. **Naver·Daum 인용 원문이 저품질 소스**(디시 주식갤·Wi-Fi 공유기 글·무관 기사) — 검색증강 엔진 원천 한계. 관련성 필터(브랜드/카테고리 매칭 스코어) 별도 과제.

## 사용자 지적 ↔ 항목 매핑
- 매출 너무 낮음 → 3 (측정이 아니라 기본 가정 문제로 확정)
- 진실의 거울: 벽글 6 · 목적 불명확 6/13 카피 · Perplexity/Gemini 5 · Daum 넘침 7
- Visibility Gap 목적 불명확 → 11
- 엔진별 응답: 미작동 5 · 표 raw 8 · 짤림 9
