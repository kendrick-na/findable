# 🔬 0 결과·Tracking 전수 실측 (2026-08-16 세션N-34)

> ⛔ **재조사 금지.** 완료 회차 **95건 전량**(표본 아님) + `Tracking` 전 행.
> 이 문서는 v4 §H·§I 의 **전제를 뒤집은** 근거다.

## 1. 🔴 v4 「0 결과 화면」의 전제가 틀렸다

| v4 계획 | 실측 | 판정 |
|---|---|---|
| 「측정 성공했는데 언급 0곳」 화면 신설 | **0건 / 95건** | 🔴 만들면 **아무도 안 보는 화면** |
| `sov 0%` 회차 7건 | 전부 **성공 0행** = 측정 실패 | ✅ `MeasurementFailedView` 가 이미 담당(N-14) |

`sov 0%` 7건 정체: `nike.com` · `apple.com` · `innisfree.com` · `tonymoly.com` ·
`themedicube.co.kr` · `medicube.co.kr` ×2 — **전부 시도 28행 / 성공 0행**.

**최저 SoV = 39%**(`roundlab.kr`). **1~19% 구간은 0건.**
→ *"AI가 우리를 전혀 모른다"* 는 상태는 **우리 고객에게 오지 않는다.**

## 2. ✅ 대신 **엔진 단위 0** 은 실제로 뜬다 (= H 가 실제로 겨눌 곳)

측정 성공 회차 88건 중 **「모름」 카드가 뜬 회차 8건(9%) · 총 10장**

| 엔진 | 「모름」 카드 |
|---|:--:|
| **daum** | **6장** |
| hyperclova · perplexity · naver-briefing · gemini | 각 1장 |

### 🔴 8/8 회차에서 **다른 엔진은 알고 있었다**
→ *"이 AI만 모른다"* 는 **사실이다**(날조 아님). H 의 ②이유 문장 근거.

### 엔진별 언급률 (행 단위)
`daum` = 성공 **275행 중 언급 136행 = 49%**. 나머지 엔진은 **0~1%** 미언급.
→ 실측 자산 *"네이버92 · 다음41 · 51%p 격차"* 와 **일치**. 다음이 유일한 반복 0 지점.

## 3. ⚠️ `engineResponses` 에는 `citedSources` 가 **없다**

무료진단 결과가 들고 있는 필드 전량:
```
sov · isStub · excerpt · engineId · sentiment · durationMs · verdictVia
errorMessage · brandMentioned · mentionQuality · mentionListSize · mentionPosition
```
🔴 **인용 출처는 `Tracking` 에만 있다.** 그래서 진실거울 카드가
*"네이버 블로그에 쓰세요"* 같은 **채널 지목을 하면 날조**가 된다.

## 4. 🔴 I번(탭 재편) 선행 실측 — **데이터가 없다**

```
Tracking 합계 75행 · 브랜드 3개
  claude 13 · chatgpt 13 · hyperclova 11 · daum 11 · naver 10 · gemini 9 · perplexity 8
  (순위 있는 행: chatgpt 8 · hyperclova 9 · naver 6 · gemini 5 · claude 0 · perplexity 0)
```

**브랜드 3개 · 75행으로 탭 8개를 재편하면 빈 탭을 만드는 일이다.**
적재가 3중 조건이라 **무료진단(비로그인)분은 한 줄도 안 쌓인다** — v4 기재가 맞았다.

> 🔴 **I 는 UI 문제가 아니라 유입 문제다.** Tracking 이 쌓이려면 **로그인 사용자가
> 반복 측정**해야 하고, 그건 **P0 실고객·유통**에 걸려 있다. UI 재설계가 그걸 대신하지 않는다.

## 5. 📌 그래서 바꾼 것

| # | v4 | N-34 판정 |
|:--:|---|---|
| H | 0 결과 3요소 **전면 화면** | ✅ **엔진 카드 단위로 축소**해 구현(`4bc27bc`) |
| I | 탭 재편(L) | ⏸️ **보류** — 데이터 부재. P0 선행 |

## 6. 재현 방법 (원가 0)

`packages/database/` 안에서 `.mts` 로 실행(neon 모듈이 거기서만 잡힌다):
```bash
npx dotenv-cli -e ../../.env.local -- npx tsx ./질의.mts
```
⚠️ `packages/database/index.ts` 는 `import "server-only"` 라 **직접 import 불가**
→ `PrismaNeon` + `generated/client` 로 직접 붙는다.
