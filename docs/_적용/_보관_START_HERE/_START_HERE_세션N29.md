# 🧭 START HERE — 세션 N-29 (2026-08-13 세션N-28 종료 인계)

> ⛔ `_START_HERE_세션N28` 이하 전부 폐기.
> 🔴 **세션N-28 은 승인받은 8층을 전부 실행·배포·눈확인까지 끝냈다.** 재감사·재측정 금지.
> 🔴 **그리고 그 과정에서 훨씬 큰 것을 찾았다 — §1 을 먼저 읽을 것.**

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # c0cbf05 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 326/326 이어야 정상
```

**상태**: 테스트 **326/326** · web·app tsc **0** · web·app 둘 다 **프로덕션 배포 완료**
⚠️ `packages/payments/` 4건 + `_sov3.png` 는 **다른 세션 것** — 건드리지 말 것.
✅ 루트 `.vercel/project.json` = **web(`prj_azBb`)** 로 원복돼 있다(확인함).

---

## 1. 🔴🔴 최우선 — **로그인 화면이 20% 확률로 깨진다** (미해결)

### 증상
로그인 상태로 페이지를 **빠르게 이동하면** `"Oops, something went wrong"` 이 뜬다.
```
동시 요청 부하(4페이지 × 5라운드 = 20회) 실측:
  app.findable.co.kr        → 🔴 500 이 4/20 (20%)
  같은 빌드의 *.vercel.app  → ✅ 500 이 0/20
```
Vercel 로그: `Clerk: Unable to verify request … middleware did not run (code=auth_signature_invalid)`

⚠️ **단발 요청으로는 거의 안 잡힌다.** 동시 요청을 던져야 재현된다.

### 🔴 원인 = **코드가 아니라 도메인 계층**
**같은 빌드인데 접속 주소만 바꾸면 결과가 갈린다**(위 표). 이게 결정적 실험이었다.

### 👤 대표님 확인 대기 (코드로는 더 파면 추측이 된다)
Clerk 대시보드에서 4가지:
1. 프로덕션 인스턴스에 **`app.findable.co.kr` 등록**돼 있나
2. **DNS CNAME**(`clerk.`·`accounts.`) 전파 정상인가
3. **`CLERK_PROXY_URL` 과 `CLERK_DOMAIN` 을 동시에** 설정하지 않았나(문서: 둘 다 금지)
4. **satellite 도메인** — `www`(web)와 `app`(대시보드)이 같은 인스턴스면 필요할 수 있다

📕 상세 = **`docs/_적용/앱_500오류_조사기록_N28.md`** (오진 4번 기록 포함)

### 🎓 이 조사에서 배운 것 — **같은 실수 반복 금지**
> **"내 변경 이후 나타났다" 는 원인의 증거가 아니다.**
오진 4번: ①Analytics 404 ②내 병렬화 ③matcher `/__clerk/` ④DB헬퍼 인증 동시호출.
전부 그럴듯했고(①은 Clerk 공식 문서가 그 사슬을 설명한다) 전부 틀렸다.
⭐ **변수를 하나만 남기고 대조**해야 갈린다. 배포본을 비교할 때 **도메인까지 같이 바꿔버려서**
   3번을 헛짚었다.

---

## 2. ⬜ 다음 할 일 (우선순위)

### ① 🔴 500 — §1 대표님 답 받으면 재실측
재현 스크립트 패턴(그대로 쓰면 된다):
```js
// 동시 요청 4개 × N라운드. storageState 로 로그인 세션 재사용.
const codes = await Promise.all(['/','/billing','/brand','/history'].map(...))
```
세션 저장: `node scripts/save-ux-session.mjs` → `/tmp/findable-ux-session.json`

### ② ⬜ 대시보드 설계안 **승인 대기**
📕 `docs/_적용/대시보드_재설계_설계안_N28.md`
- 🔴 인계서(N-27)의 *"재설계 급"* 진단은 **과잉이었다** — `NextActionsCard` 가 이미 2번째
  자리에 있고 섹션 순서도 근거와 함께 설계돼 있다. **재배치 금지.**
- 진짜 구멍 하나: *"지금 할 일 보기"는 행동이 아니라 **이동***. 클릭 전엔 뭘 할지 모른다.
- **A/B/C 3안** + 여쭐 것 2개가 문서에 정리돼 있다. 👤 승인받고 실행.
- 🆕 **실측으로 추가된 것**: 모바일 빈 상태에서 **CTA 가 화면 밖**(1,025px).
  1·2·3단계 설명 카드 3장(≈450px)이 밀어냈다. 애플 HIG *Value First* 위반.

### ③ ⬜ 결과페이지 **10.6화면** 축소 (스킬·리서치 기반 축소안 → 승인 → 실행)
아직 **착수 안 했다.** `criteria.md` 는 화면 수를 *"실격 기준이 아니라 질문을 여는 숫자"* 로
쓰라고 명시한다 → *"왜 10.6화면이 필요한가"* 에 한 문장으로 답할 수 있는지부터.

### ④ 🟡 홈 콜드 **7.76초** 미해결
병렬화는 **철회했다**(§3). 답은 `lib/db/scoped.ts` 헬퍼가 `orgId` 를 **인자로 받게** 고쳐
호출부에서 `auth()` 를 1회만 부르는 것. **별도 설계·승인 대상.**

---

## 3. ✅ 세션N-28 이 한 일

### 3-a. 승인 8층 — 전부 실행·배포·눈확인
| 층 | 결과 |
|:--:|---|
| **①** | 진실의 거울 원문 **10줄→3줄** 클램프(원문 삭제 아님 · `전체 보기 ▼` 유지) |
| **②** | 모바일 엔진 탭 `overflow-x-auto` → `flex-wrap`. 잘림 **313px→0**, 네이버·다음이 화면 안으로 |
| **③** | 🔴 **고칠 것이 0건이었다** — 3-c 참고 |
| **④** | ⛔ **철회**(§3-d) |
| **⑤** | 한글 음수 자간 → **0**. 🔴 인계서는 4곳이라 했으나 **실제 5곳**(전수 grep) |
| **⑥** | truth-mirror 배지 = "AI 7곳 기준 인지율". 상단 KPI 는 N-14·N-17 이 이미 해놨다 |
| **⑦** | 요약 줄에 "**2회 실패**(점수에서 제외)" |

실측 전/후: 모바일 **11.0→10.6화면** · PC **6.8→6.6** · 가로스크롤 **없음** · 자간 **4→0**
눈확인: **모바일 4구간 + PC 3구간 = 7장 전부**(N-27 이 남긴 "미확인 구간" 해소)

### 3-b. 🔴 눈확인이 도구 3종이 통과시킨 **진짜 버그**를 잡았다
화면에 이렇게 떠 있었다: `우리를 아는 AI 7/6` · `117%` · 헤드라인만 `6곳`.
원인 = **같은 화면에 분모 규칙이 세 벌**. 실데이터: Perplexity 가 5회 중 **3성공·2 rate limit 실패**.
→ **모듈(`countMeasurementCoverage`)이 맞다**(답 받은 엔진을 빼면 축소 보고) → 통일.
**실화면 재검증: 7곳·7/7·100%, 모바일·PC 동일** ✅ 테스트 **+4**(문구 아니라 **계약** 검사).

같은 계통 **3곳 추가 통일**(커밋 `225c4b0`):
- 🔴 **챗** — `enginesCovered.length`(=응답 29)를 엔진 수로 써서 시스템 프롬프트에
  *"언급된 엔진 7/29개"* 로 나갔다. **AI 에게 거짓 맥락을 먹이고 있었다.**
- 🔴 **OG 이미지** — 카톡·X 공유 미리보기에 같은 계통 숫자가 나갔다
- 🟡 **업셀 카피** — 잠복(이번 회차엔 우연히 같은 값)
- ✅ **리드메일은 이미 정답** — 손대지 않았다

### 3-c. 🔴 인계서(N-27) 숫자가 **또 5건 틀렸다** (3세션 연속)
| 인계서 | 실제 |
|---|---|
| 자간 "4곳" | **5곳** — `audit/page.tsx` 가 한글 h1 에 `-0.03em` 무조건(목록 누락) |
| 근사 불일치 "16건" | 🔴 **전부 오탐** — 도구가 9,000px 떨어진 요소를 짝지었다 |
| 세로간격 "37종 문제" | 🔴 **기준 자체가 오류** — Linear 26·Stripe 32 도 같이 실격 |
| ⑥ "분모 통일 필요" | N-14·N-17 이 **이미 했다** |
| 대시보드 "재설계 급" | **이미 대부분 되어 있다** |

### 3-d. ⛔ ④ 병렬화 **전면 철회** (되살리지 말 것)
500 의 원인은 아니었지만, `scoped.ts` 헬퍼가 내부에서 `auth()` 를 부르므로
"DB 쿼리만 병렬화"도 실제로는 **인증 동시 호출**이다.
**효과 검증 없이 위험만 남는 변경**이라 `page.tsx`·`actions/page.tsx` 를 N-27 상태로 복원.
같이 뺀 것: matcher `/__clerk/(.*)`(문서 권장이나 우리 앱에서 효과 0 — `frontendApiProxy` 미사용).
유지한 것: **Analytics 404 제거**(독립적으로 옳다 — 지표가 안 쌓이는데 404 만 내고 있었다).

### 3-e. ⭐ 스킬 도구 개선 3건 (회귀 전건 통과)
| 무엇 | 왜 |
|---|---|
| 🆕 **측정 4종에 로그인 세션 지원**(`UX_STORAGE_STATE`) | 그동안 대시보드·관리자는 **"검사된 적 없는 화면"**. 로그인 벽에 막힌 리다이렉트를 재고 "이상 없음"이라 보고할 위험도 있었다. `scripts/save-ux-session.mjs` 신설 |
| `align.mjs` **근사 불일치 판정 교체** | 값만 가까운 무관 요소를 짝지어 오탐 **22건**. → "세로 1.5화면 + 조상 공유"로 **22→2**. 출력에 어느 요소인지 병기 |
| `align.mjs` **세로간격 임계값 철회** | "종류 ≤6"·"4배수" 가 **Linear 26·Stripe 32 를 전부 실격**시켰다 |

🎓 회귀가 **과잉수정을 잡았다** — body 를 조상 사슬에서 빼자 픽스처의 진짜 결함(형제 카드
`p-4` vs `px-4`)이 0건이 됐다. 원인: `.card > span` 둘은 **부모 공유가 아니라 부모가 형제**.

---

## 4. ⚠️ 함정 (계속 유효 + 이번 추가)

- 🔴🔴 **인계서 숫자를 그대로 믿지 말 것.** N-28 이 5건 정정했다(3-c). 착수 전 실측·검산.
- 🔴🔴 **도구가 통과시켜도 눈으로 봐야 한다** — `7/6`·`117%` 는 measure·align·axe **전부 통과**.
- 🔴🔴 **"내 변경 이후 생겼다" ≠ 원인** — 변수 하나만 남겨 대조(§1 오진 4번).
- 🆕 ⭐ **app 배포는 swap 필요 없다**: `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app` (**루트에서**).
  ⚠️ `apps/app` 안에서 실행하면 rootDirectory 겹쳐 실패. 🔴 배포 후 **`vercel inspect` 로 Aliases 확인**.
- 🆕 **로그인 실측**: `node scripts/save-ux-session.mjs` → `UX_STORAGE_STATE=/tmp/findable-ux-session.json node ~/.claude/skills/ui-ux-quality/scripts/measure.mjs <URL> both`
  ⚠️ QA 계정(`+qa`)은 **측정 이력 0건** → "데이터 있는 대시보드"는 아직 못 봤다(숫자 창고 문제의 무대).
- 🔴 **`git stash pop` 금지** — 7월 「브랜딩전_백업」 stash 가 **아직 살아 있다**(`stash@{0}`).
- 🔴 **엔진 수 재조사 금지** — 본류 **7** · 영어만 4 · 8=브리핑 별도.
- 🔴 **`FINDABLE_ADMIN_EMAILS` ≠ 관리자 권한**(심층분석 원가 게이트). 운영자는 **Clerk metadata**.
- 🔴 **BotID 가 curl 차단** → 진단 생성은 브라우저로만. 단 **결과 조회 API 는 curl 가능**
  (`/api/audit/<id>` — N-28 이 `7/6` 원인 규명에 썼다. **실데이터 검증에 매우 유용**).
- 🔴 **`apps/web` 에 테스트 러너 없음** → 고정할 로직은 `packages/` 나 `apps/app` 으로.
- ⚠️ **biome 이 대괄호 경로**(`[jobId]`)를 못 읽는다 → 상위 디렉터리로 lint.
  (경로에 따옴표 붙이면 그 따옴표째 경로로 읽어 "No such file" 이 난다)
- ⚠️ **lint 부채 47건은 기존 것**(복잡도·중첩 삼항). N-28 변경 줄과 안 겹침을 diff 로 확인했다.
- ⚠️ **playwright 스크립트는 Findable 폴더에서 실행**(scratchpad 에 두면 모듈을 못 찾는다).
- ⚠️ **`vercel logs <옛배포>` 가 비어 있는 건 "에러 없음"이 아니다** — 트래픽이 없어서다.
  옛 배포본과 비교하려면 **직접 트래픽을 보내야** 한다(세션 쿠키 도메인 복제 필요).

---

## 5. 🔴 잊으면 안 되는 것

> **① 화면 작업이면 `Skill(ui-ux-quality)` 를 먼저 호출한다.** 말 안 해도.
> **② 도구 4종은 서로 다른 걸 잡는다** — 그리고 **넷 다 못 잡는 것을 눈이 잡는다**(이번에 2번 증명).
> **③ 승인 전 코드 금지**(배포된 실사용 화면).
> **④ ⚠️ `packages/payments/` 는 다른 세션 작업 중.**
> **⑤ 🔴 회사 병목은 여전히 P0 실고객·유통이다.** 유저 **0명**.
>    N-28 이 고친 것도 전부 *"고객이 왔을 때 배신하지 않게"* 하는 작업이다.
>    ⭐ 단 §1 의 500 은 **고객이 오는 순간 첫 화면이 깨지는 문제**라 유통 전에 닫는 게 맞다.
