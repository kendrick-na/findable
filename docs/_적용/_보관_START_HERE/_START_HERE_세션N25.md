# 🧭 START HERE — 세션 N-25 (2026-08-12 세션N-24 인계)

> ⚠️ **이 파일 + `_투두_단일진실_N24.md` 를 열 것.**
> ⛔ `_START_HERE_세션N23` 이하 **전부 폐기**(단, N23 §1 엔진수·§6 함정은 여전히 유효).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 7ea6c80 이면 인계 시점 그대로
git status --short        # 비어 있어야 정상
```

**현재 상태**: 테스트 **196/196** · app lint 기준선 **4** · tsc 0(web·app) · 워킹트리 깨끗
**세션N-24 커밋 12건 · app·web 전부 배포 완료**

---

## 1. 🔴🔴 새 세션이 가장 먼저 알아야 할 것

### ⛔ "코드 할 게 없다"는 오진을 **5번째 반복하지 말 것**
N-10·N-11·N-13·N-17·**N-23** 이 각각 *"백로그 비었다"* 고 틀렸다.
N-23 은 *"S7 소진, 남은 건 P0·상표뿐"* 이라 했으나 **실제로는 코드 11건이 남아 있었다**.
원인은 게으름이 아니라 **문서 분산**이다. → **남은 일은 `_투두_단일진실_N24.md` 에만.**

### 🎓 세션N-24 가 반복한 실수 — **같은 실수를 3번 했다**
🔴 **`grep` 결과가 주석인지 실행 코드인지 확인하지 않았다.**
- `min-h-[60vh]` → 주석 안에만 있었다(지웠으면 경고문 삭제)
- `Reddit 40% 강조` → 파일 상단 주석이었다(지웠으면 **준법 가드** 삭제)

🔴 **어느 경로가 실제로 도는지 확인하지 않았다.**
crew(유료 버튼 전용)만 3번 팠고, **모든 무료진단이 지나가는 `buildGeoActions`** 는 안 봤다.
→ **착수 전 "이 코드가 언제 실행되나"를 먼저 확인할 것.**

---

## 2. ✅ 세션N-24 완료 (전부 배포·검증)

| # | 무엇 | 검증 |
|---|---|---|
| 1 | **app favicon** — `app.findable.co.kr/favicon.ico` **404→200** | 라이브(2,962B 해시 일치) |
| 2 | **운영자 장애 알림** `captureOpsAlert` — stuck 스윕이 로그만 남기고 아무도 안 보던 것 | 뮤테이션 2건 |
| 3 | **보안 감사** — 심층분석 유료 게이트가 `z.email()` 하나에 의존함을 발견·테스트로 봉인 | 뮤테이션 1건 |
| 4 | **결제 퍼널 계측** — 시도·실패단계(intent/widget/verify)·완료 구분 | 뮤테이션 2건 |
| 5 | 🔴 **개인정보 로그 유출 차단** — Clerk 웹훅이 **원문 JSON 전체**(이메일·이름·전화번호)를 Logtail 에 쏟고 있었다 | 뮤테이션 2건 |
| 6 | 🔴 **처방 시장 적합성** — 해외 브랜드에 *"네이버 지식iN"* 나가던 것 | 뮤테이션 2건 |
| 7 | ⭐ **`/admin/evidence`** — 고객사 before/after 근거 화면 | 뮤테이션 2건 |
| 8 | 📕 **KIPO 상표 조사** · **개인정보 위탁·동의 전략** | 1차출처 + 반증 |

### 🔬 "코드 불필요"로 판정한 것 3건 (되살리지 말 것)
1. **잠금카드 `min-h-[60vh]`** — 코드에 **없다**(주석 안). 환상 항목
2. **타임아웃 정합(D16-4)** — 이미 맞다(270 < 300 < 900). 2026-07-28 수정됨
3. **BL-Day17-01 sweep 이중구현** — 이미 web 단독으로 정리됨

---

## 3. 📋 남은 일 — **단일 진실 = `_투두_단일진실_N24.md`**

### 🖥️ 코드 (지금 가능 · 원가 0)
| 항목 | 크기 | 비고 |
|---|:--:|---|
| 🔴 **처리방침 개정** — 수탁자 공개 + 사례활용 목적 추가 | S | ⭐**다음 착수 1순위**(§4 참조) |
| `/en` `<title>` 한국어 노출 | S | 🟡 해외 IP 확인 필요(한국에선 재현 불가) |
| 요금제 「현재」 배지(`추천`과 슬롯 다툼) | S | P2 — 하단 CTA 는 이미 「이용 중」 정상 표시 |
| `/history/[jobId]` 앱 내재화 | **L** | ⚠️ 공유링크(비로그인 `?shared=1`) 경로 설계 선행 |
| lint 부채 23건 | M | ⚠️ 단독으로 열지 말 것(위 L 작업과 함께) |

### 📊 데이터가 쌓여야 의미 생기는 것 (4건)
D4 기간선택 · D2 이벤트핀 · D1 알림UI · `alerts` 실제 메일
→ 공통 근거: **측정 2회 · 시계열 1일**. 지금 만들면 "작동 안 하는 버튼".

### 💳 결제 (카카오페이 승인 대기 — 먼저 보낼 것 없음)
라이브 채널키 교체 · **결제예약 생성함수 신설**(취소만 있고 생성 없음 실측) ·
"첫 회만 결제" 문구 삭제 · 웹훅 시크릿

---

## 4. 🔴 다음 착수 권고 = **처리방침 개정** (승인 불필요·근거 확정)

**왜 1순위인가**: 위탁 여부 판정과 **무관하게 확정적으로 필요**하다.
우리는 가입자·무료진단자 이메일을 **직접 수집**하므로 **독자적 개인정보처리자**다.

- [ ] **수탁자 공개**(제26조 제2항) — AWS·Vercel·Clerk·PortOne·AI API
      🔴 **"업무 내용"과 "수탁자"를 둘 다** 적어야 한다(업체명 누락으로 과태료 사례 있음)
- [ ] **목적에 "사례 활용" 추가** — 현재 방침 제2조 목적 5개에 **없다**
      (`apps/web/app/[locale]/legal/[slug]/page.tsx` · KO/EN 둘 다)

📕 근거·문안 재료 = `개인정보_위탁_동의_전략_2026-08-12.md`

---

## 5. 👤 사용자 결정 대기

### 🔴 P0 실고객 지불의사 — **불변 1순위 · 유일한 회사 병목**
마케터 10~15명을 **실제로 만나야** 한다. 질문지는 `P0_지불의사_시뮬레이션_결과.md` 에 있다.
⛔ 시뮬레이션으로 대체 불가(r=0.20). *"비관적으로 돌리면 보수적"* → **아니다, 노이즈다.**
⭐ 세션N-24 가 **퍼널 계측**을 깔아둬서 이제 *"결제창까지 갔다 포기 N명"* 을 숫자로 들고 갈 수 있다.

### 🔵 KIPO 상표 — 📕`KIPO_상표출원_조사_2026-08-12.md`
관납료 3개류 **약 74만원** · **상표는 감면 대상 제외** · 심사 평균 12.6개월 ·
우선심사 1류당 16만원(**우리는 서비스 운영 중이라 요건 충족 가능성 높음**)
🔴 최대 리스크 = **"Findable" 이 효능을 직접 기술**해 식별력 거절 위험(제33조 제1항 제3호)
⬜ 다음 = **KIPRIS 무료 선행상표 검색**("Findable"·"파인더블" 둘 다 · 5분)

### 🟠 카카오페이 — 4차 회신 발송 완료 · **답장 대기**

---

## 6. ⚠️ 함정 (착수 전 필독)

### 세션N-24 신규
- 🔴 **`grep` 결과가 주석인지 코드인지 확인할 것**(3세션째 반복한 실수)
- 🔴 **"이 코드가 언제 실행되나"를 먼저 확인할 것** —
  `buildGeoActions`=모든 무료진단 / crew 4에이전트=**유료·승인자가 버튼 눌러야만**
- 🔴 **법률 결론일수록 반증(cross-verify)을 붙일 것** — 코드는 테스트가 잡지만
  법률 오판은 **아무도 안 잡고 사고 난 뒤에 드러난다**. 실제로 3건 중 2건이 뒤집혔다
- 🔴 **판례를 근거로 달 때 판시 범위를 확인할 것** — 대법원 2020도13960 을
  *"위탁 불성립"* 근거로 달았는데 그 판결은 **오히려 위탁을 인정**했다
- ⚠️ 새 `.test.ts` 파일 헤더에 `/**` 쓰면 biome `useSingleJsDocAsterisk` 걸림 → `/*` 로
- ⚠️ 정규식은 **최상위 상수로**(`useTopLevelRegex`) · `test.each` 는 포매터가 재배치함

### 기존 함정 (계속 유효)
- 🔴🔴 **엔진 수 재조사 금지** — 본류=**7** · 영어만=**4** · 8=브리핑 버튼 따로(N23 §1 확정)
- 🔴 새 `.test.tsx` = **`@vitest-environment jsdom` 도크블록 필수** → **단독 실행으로 먼저**
- 🔴 **`apps/web` 에 테스트 러너 없음** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서 검증
- 🔴 **`verify-app.py` 는 1440px 고정** — 모바일은 따로 볼 것 · `FINDABLE_TEST_PW` 필요
- 🔴 **biome 복잡도 20 · 중첩 삼항 금지**
- 🔴 **DB 직접 조회 막힘**(한글 NFD + Prisma CJS) → **라이브 API `/api/audit/<id>`** 가 빠르다
- 🔴 **`apps/api` 는 죽은 앱** — 새 코드 넣으면 실행되지 않는다
- 🔴 **cron 은 UTC** · **`x-vercel-cron` 폴백 부활 금지**
- 🔴 로컬 dev·build 는 **`VERCEL=1`** · `"use server"` 파일은 **async 함수만 export**
- ⚠️ 요금제 문구는 **app·web 둘 다** · ⭐ **app 은 `lang="ko"`**
- ⭐ **배포**(둘 다 루트에서):
  ```bash
  # app — project.json swap 필요(끝나면 반드시 원복)
  cp .vercel/project.json /tmp/vroot.bak
  cp apps/app/.vercel/project.json .vercel/project.json
  npx vercel --prod --yes --scope nayoy2-7791s-projects
  cp /tmp/vroot.bak .vercel/project.json     # 🔴 원복 필수
  # web — 루트가 기본값이라 swap 불필요
  npx vercel --prod --yes --scope nayoy2-7791s-projects
  ```

---

## 7. ⛔ 되살리지 말 것 (전부 실측 근거)

1. **엔진 수 "28곳 통일"** — 오답. 두 숫자는 다른 것을 센다
2. **결제 상태 3줄** — `Organization` 스키마에 필드가 **없다**
3. **처방 2건 병합** — 서로 **다른 질문**(영/한) + `@@unique([brandId,kind,target])` 라
   합치면 **고객 완료표시가 미완료로 되살아난다**
4. **감성 배지 변별력** — 데이터가 **실제로** 중립(18/3/null 8)
5. **순위 분모 노출** — `mentionListSize` 전부 null(N-10 이전 소급 없음)
6. **잠금카드 `min-h`** — 🔴 **코드에 없다**(주석 안). 환상 항목
7. **`x-vercel-cron` 폴백** — 외부인이 유료 측정을 무제한 실행시킬 수 있었다
8. **업계 N분위 벤치마크** — 조직 1개라 표본 없음 = 숫자 조작
9. **naver 어댑터 Daum식 정합** — 구조가 다르다. 없는 언급을 만든다
10. **팩트정합률 전면화** — 사실 검증을 안 하는데 이름이 사실 검증으로 읽힌다
11. **`z.email()` 완화** — 🔴 **보안 경계**다. 느슨하게 하면 유료 게이트가 뚫린다
12. **대법원 2020도13960 인용** — 그 판결은 **오히려 위탁을 인정**했다

---

## 8. 🎓 이 프로젝트가 반복해서 배운 것

1. 🔴 **문서가 분산되면 반드시 누락된다** — *"백로그 비었다"* 오진 **5회**
2. 🔴 **진단서·인계문서는 낡는다** — 착수 전 **건별 실측**
3. 🔴 **묶음으로 적힌 지적은 건별로 실측** — N-24 에서 *"결제 무로그"* 가 틀렸고
   (실제 21개 키 존재) 대신 **개인정보 유출**이라는 더 중대한 것이 나왔다
4. 👁️ **테스트는 아는 것만 검증한다** — 스크린샷·라이브 확인이 코드로 못 보는 것을 잡는다
5. 🔴 **가드가 버그의 호위병이 된다** — 문구 하드코딩 금지, **구조로 판정**
6. ⭐ **없는 데이터를 화면에 만들지 않는다** — 표본 없는 분위·근거 없는 임계값은 **조작**
7. ⭐ **근거 없는 임계값을 발명하지 말 것** — N-24 에서 백로그의 `SWEEP_ALERT_THRESHOLD=3`
   을 **구조로 대체**했다(1차 가드가 270초라 `count>0` 자체가 이미 예외)
8. 🔴 **법률·정책 판단도 근거로 한다** — 👤 *"요즘 세상에 변호사가 왜 필요해"*
   → 1차 출처(법령·안내서·의결례)에 사실관계를 대입해 **잠정 판정**까지 낸다.
   변호사는 *"틀리면 책임지는 사람"* 이 필요할 때(엔터프라이즈 계약·분쟁) 쓴다

---

## 9. 🔴 다음 세션이 잊으면 안 되는 것

> **① 남은 일은 `_투두_단일진실_N24.md` 에만 있다.** 다른 문서의 `⬜` 를 믿지 말 것
> **② 착수 전 "주석인가 코드인가" + "언제 실행되나"를 확인**(N-24 가 3번 틀렸다)
> **③ 다음 코드 착수 권고 = 처리방침 개정**(§4 · 승인 불필요·근거 확정)
> **④ 회사 병목은 여전히 P0 실고객** — 코드로는 퍼널 계측까지가 최선이었다
> **⑤ 법률 결론에는 반드시 반증을 붙일 것**(3건 중 2건이 뒤집혔다)
