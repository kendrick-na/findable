# 🧭 START HERE — 세션 N-33 (2026-08-15 세션N-32 종료 인계)

> ⛔ `_START_HERE_세션N32` 이하 **전부 폐기**.
> ✅ **A·B·C·D·E 완료**(커밋 8개 · ⚠️미배포). 다음 = **F(지표 사전)** 부터.
> 🔴 C 는 **취소됐다** — 재실측 결과 `keep-all` 은 이미 0곳이고 정렬 경고는 **중앙정렬 오탐**이었다(중심오차 0.00px).

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3     # 7343224 · 98d7ea1 · f22ecd4 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 334/334
```

**상태**: 테스트 **334/334** · web·app tsc **0** · ⚠️ **미배포**(N-31부터 누적 **11커밋**)

📕 **단일 진실**: `docs/_적용/재설계안_v4_전면_2026-08-15.md` (§6-e 착수순서 · §8 미해결)

---

## 1. ⛔ 절대 금지 (N-32에서도 그대로 지켰다)

| 대상 | 이유 |
|---|---|
| 🔴 **`packages/payments/*`** | **다른 세션이 미커밋 작업 중**. 카카오페이 심사 중 |
| 🔴 **트라이얼·결제 코드** | 👤 지시: *"기획에 보존하되 **반영은 하지 말아줘**"* |
| 🔴 `checkout/success:51-53` | `테스트 결제`·`평가위원 시연용` 문구 — **결제트랙이라 손대지 말 것** |
| 🔴 `ultracite` 폴더 단위 | N-30 사고. **파일 단위로만** · 대괄호 경로는 못 읽는다(tsc 로 대체) |
| 🔴 `git stash pop` | 7월 백업 stash 생존 |

---

## 2. ✅ 세션 N-32가 한 것

### 2-a. B번 — 사실 정직성 (커밋 `b73ca5a` · 16파일 · **-474줄**)

`e55f5d6`이 놓친 **잔존분**을 걷어냈다. 🔴 **소스 grep 으로는 4건을 놓쳤다.**

| 고친 것 | 내용 |
|---|---|
| **난수를 실측처럼** | `sov-chart` 가 `seedRandom()` 점 분포에 `Last 30 days`·`medicube.co.kr`·`n ≈ 200 responses`·`+4% vs last 30d` 를 붙여 **30일 200회 측정**처럼 보이게 하던 것 → 가상 도메인 + 「실제 측정 결과 보기」(=`/report` 실데이터) 링크 |
| 🔴 **EN 브랜치 잔존** | `Minji · GEO analyst` — KO 만 고쳐져 있었다. `Medicube: Perplexity SoV down 12%` · `Medicube Q1 SoV report` · 아바타 `민`/`M` |
| **실존사·날조수치** | `아모레, Amorepacific`(KO·EN) · `$ findable audit medicube.co.kr` · `Korean Moat Score 89%` · `+18/+14/+9%` |
| **미출시를 라이브로** | Cafe24·네이버·WordPress `Connected` → `v1.5 예정`(같은 파일 `:51`은 `(v1.5)` = 한 화면 안 모순이었다) |
| **가짜 후기 4건** | 사전 **7개 전부**. 지어낸 인용 + **실존 개발자 얼굴 4명**(leerob·shadcn·haydenbleasel·pontusab) + 경쟁사 가격 단정 + 실존사 `조선미녀` |
| **없는 고객 주장** | `home.cases` = *"글로벌 K-브랜드가 선택한 플랫폼"*(고객 0명) |
| **없는 직원 잔존** | 사전 `features` 의 `민지·Alex·수진·준호` → 역할 기반. IBK 사고 동일 패턴 |
| 🔴 **철회됨** | ~~"3사 SoV 0 을 공개"~~ — **이건 내가 틀렸다.** 페이지의 `sov:0` 자체가 오류였고(JSONL 원본은 92~96) 나는 그걸 믿고 **없는 사실**을 덧붙였다. `0fdc25d` 로 데이터·문장 모두 정정 |
| 버그 | `blog/[slug]` params 타입 `locale` 누락 · `header.tsx` `href="#"` 죽은 링크 · **390px 줄바꿈 겹침** |

### 2-b. A번 — 모션 (커밋 `cb73940`)

**실측 근거**(추정 아님): 우리 = `getComputedStyle` 전수 · 경쟁사 = **60fps 영상 프레임차분**

| | 전 | 후 |
|---|---|---|
| 히어로 완료 | **1.40초** | **0.90초** |
| 🔴 **CTA 등장** | **1.20초** | **0.72초** |
| 최대 duration | 0.8s(extra-long ⛔) | **0.5s** |
| Reveal(스크롤 15곳) | 0.6s · stagger 80 | **0.4s · 60** |
| Reveal 이징 | `(0.16,1,0.3,1)` | **`(0,0,0,1)`** MD3 감속 |

**경쟁사 실측**: Profound 헤드라인 **83~100ms** · Peec 카드호버 **50·83·150ms** → 우리 800ms 는 **8배**
🟢 **안 건드림**: `transition 0.15s` **251곳**(이미 맞다) · 브랜딩 색·폰트(👤 확정)

---

## 3. 🔴 인계서 오류 정정 — **6세션 연속**

| N-32 이전 기록 | 실측 |
|---|---|
| *"`blog/[slug]` 런타임 크래시 · 글 1개 발행하면 즉시 500"* | ❌ **크래시 안 난다.** 라우트가 `[locale]/blog/[slug]` 라 런타임엔 `locale` 이 **실제로 온다**(형제 `blog/page.tsx:11` 이 증거). **타입 선언만 빠진 것** |
| *"testimonials 에 leerob 얼굴"* | ⚠️ 위치가 다르다. 컴포넌트가 아니라 **사전 7개**(`:103`). 파일만 지우면 데이터가 남는다 |
| *"reduced-motion 위반 가능성"* | ❌ **정상.** 스크롤 후 15개 항목 ON/OFF 모두 표시. 처음 잰 `opacity:0`은 **화면 밖 대기상태** 오독 |
| *"무한 pulse 가 시선 뺏음"* | ❌ **안 돈다.** `stats.isLive=false` 라 비활성 |
| *"홈 진단 0건 = 죽은 제품처럼 보임"* | ❌ **로컬만 그렇다.** DB 미연결 catch 값. **운영은 88건**(라이브 fetch 확인) |

### 🔴🔴 이번에 배운 것 — **렌더된 화면을 봐야 잡힌다**
소스 grep 은 **KO 만 고치고 EN 을 놓친 4건**을 전부 통과시켰다.
⭐ **curl 로 HTML 받아 대조 + 스크린샷 눈확인** 이 유일하게 잡았다.
⭐ **390px 줄바꿈 겹침**도 PC 만 봤으면 못 봤다 — 두 뷰포트 다 찍어야 한다.

---

## 4. ⬜ 다음 할 일 (v4 §6-e)

| 순 | 묶음 | 규모 | 상태 |
|:--:|---|:--:|:--:|
| A | 모션·비주얼(MD3) | M | ✅ **완료** `cb73940` |
| B | 남은 결함 | M | ✅ **완료** `b73ca5a` |
| ~~C~~ | ~~랜딩 정렬·타이포~~ | M | ❌ **취소**(재실측: keep-all 0곳 · 정렬은 중앙정렬 오탐) |
| D | Showcase → 실제 진단결과 | M | ✅ `3c50d17`·`f22ecd4` |
| E | 소셜프루프 밴드 | S | ✅ `98d7ea1`·`7343224` |
| **F** | **지표 사전** — 🔴**분모 축 3종 명명 먼저**(불일치 8종) | M | ⬜ **다음** |
| ~~F-b~~ | app `AI N곳에 물어`→`측정한 AI N곳` | XS | ✅ `4993b0c` |
| G | 「평가」 탭(쿼리 변경 0) | S | ⬜ |
| H | 순위 충격 공개 + 0결과 3요소 | M | ⬜ |
| I | 탭 재편 + `loading.tsx` 신설 | **L** | ⬜ **행수 실측 선행** |
| — | ⛔ 트라이얼·결제·전환이벤트 | L | ⛔ **심사 후. 기획만 보존** |

### 🔴 I번 전 필수 — `Tracking` 행 수 실측 (원가 0)
```sql
SELECT "engineId", count(*), count(sentiment), count("mentionPosition"),
       count(*) FILTER (WHERE "citedSources"::text != '[]')
FROM "Tracking" GROUP BY "engineId";
```
적재가 **3중 조건**이라 **무료진단(비로그인)분은 한 줄도 안 쌓인다.**

### 🆕 F 이후 같이 정리할 것 (N-32·33 이 본 것)
- 홈에 **임포터 0인 컴포넌트**가 더 있다: `cta.tsx`·`faq.tsx`·`features.tsx`·`stats.tsx`
  (next-forge 보일러플레이트 잔존). 사전 키는 살아있다 → **F 이후 정리 권장**
- 사전 5개 언어(de·es·fr·pt·zh)의 `features` 4번째 항목이 **번역 안 된 보일러플레이트**
  (*"엔터프라이즈급 보안·암호화"* — 우리 기능 아님). 현재 미렌더

---

## 5. 🔴 확정 사항 (다시 논의하지 말 것)

| # | 결정 |
|:--:|---|
| 1 | **「지금 할 일」 = 탭 밖 최상단 + 사이드바** (경쟁사 탭에 넣은 곳 0곳) |
| 2 | **모바일 = 하단 탭 4개 + 「더보기」 시트** (햄버거 쓴 2곳 다 실패) |
| 3 | **다국어 현행 유지** (대시보드 한국어 전용 · 랜딩 사전 7개) |
| 4 | **부분가림 = Peec f041 3겹**(블러+페이드+⭐선명 하나) · 코파일럿 **3회 제한** |
| 5 | ⛔ **`Sample`·`(예시)` 라벨 남발 금지** — 단 `MockFrame` 의 「예시 화면」 배지 1개는 유지 |
| 6 | **ThreePillars 유지**(한국 엔진 = 유일 차별점) |
| 7 | `/` 이력 섹션 제거(`/history` 유지) |
| 8 | 🆕 **모션은 MD3 토큰만 따른다** — short 50~200 / medium 250~400 / long 450~600. **extra-long(700~) 금지**. 외부 스킬의 "150~250ms" 는 출처 없음 |

---

## 6. ⭐ 전략적으로 잊으면 안 되는 것

### 🔴 "무료진단은 우리만"은 **틀렸다**
Scrunch·Otterly·Profound 전부 무료툴이 있다(라이브 확인).

### ⭐ 진짜 차별점 = **한국 엔진** (4곳 랜딩 fetch 실측)
`naver|hyperclova|clova|daum|kakao` 언급 → **4곳 전부 0건**
🔴 헤드라인 주장은 "무료"가 아니라 **「아무도 안 재는 곳을 잰다」**여야 한다.

### ⭐ 4곳 공통 규율 = **회사가 만든 성과 숫자 0곳**
성과 수치는 **전부 실명 인물 인용**으로만 등장한다. → B번이 이 규율을 코드에 적용한 것이다.

### 🔴 4곳 공통 실패 = 우리 자리
①**0 결과에 침묵** ②한국 엔진 0 ③한국 브랜드 자동감지 실패 ④한국어 생성 붕괴 ⑤로딩 실패 미설계
⭐ ①은 아직 **안 했다**. (N-32 가 `case/a-brand` 에서 했다고 썼으나 그건 **오류였고 `0fdc25d` 로 철회**됐다.)
   → **H번(0결과 3요소)** 에서 제대로 한다.

### 🔴 기술 스택은 1군과 동일
Next.js App Router + Clerk + Vercel = Profound·Otterly 와 같다.
🔴 **Peec·Scrunch 는 SPA라 SSR 이 없다** → 공개 결과 페이지를 **구조적으로 못 만든다**(우리 우위).

---

## 7. 🔴 잊으면 안 되는 절차

> ① **화면 작업이면 `Skill(ui-ux-quality)` 먼저.** 말 안 해도.
> ② 🔴 **4층 모델** — ①뭘 넣나/빼나 ②얼마나 ③배치 ④보이기. ①②를 안 고치고 ③④만 손대면 반복된다.
> ③ 🔴 **도구 4종은 서로 다른 걸 잡는다**(measure·align·squint·a11y).
> ④ 🔴 **fullPage 를 모바일(390)·PC(1440) 두 장 다 찍어 눈으로 볼 것.**
> ⑤ ⛔ **승인 전 코드 금지**(배포된 실사용 화면).
> ⑥ 🔴 **인계서 숫자를 믿지 말고 실측하라** — **6세션 연속** 틀렸다.
> ⑦ 🆕 🔴 **소스 grep 을 검증으로 쓰지 말 것.** `curl` 로 렌더된 HTML 을 받아 대조하라.
>    KO 만 고치고 EN 을 놓친 날조 4건이 grep 을 전부 통과했다.
> ⑧ 🔴 **회사 병목은 여전히 P0 실고객·유통. UI 재설계가 그걸 대신하지 않는다.**
