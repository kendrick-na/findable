# 🧭 START HERE — 세션 N-31 (2026-08-14 세션N-30 종료 인계)

> ⛔ `_START_HERE_세션N30` 이하 **전부 폐기**.
> 🎯 **다음 할 일 = 재설계안 v3 §6 의 1번부터 순서대로.** 방향은 👤 승인받았고, **설계안 최종 승인만 남았다.**

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # 0711532 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 334/334 · Errors 0
```

**상태**: 테스트 **334/334** · web·app tsc **0** · app **배포 완료**(`dpl_H6r3CTuVSbP1EHZJcKAPqLK8DJCn`)
⚠️ `packages/payments/` 3건 = **다른 세션 것**(카카오페이 심사 중). 건드리지 말 것.

📕 **먼저 읽을 것 (딱 2개)**
1. `docs/_적용/재설계안_v3_전면_경쟁사기반_2026-08-14.md` ← **단일 진실. 남은 일 전부 여기.**
2. 이 파일 §3(함정) · §4(남은 판단)

---

## 1. 👤 이번 세션에서 받은 지시 (방향의 근거 — 잊으면 안 됨)

원문 그대로:

| # | 👤 지시 | 뜻 |
|:--:|---|---|
| 1 | *"프로파운드 ui ux 화면대로 만들되, 대시보드나 무료진단을. 우리걸로 더 좋게 만들자"* | 구조는 경쟁사, 완성도는 우리가 더 |
| 2 | *"Profound, Peec · Otterly · Scrunch 회사 화면들을 전면적으로 분석해. 따라서 만들자. 메뉴, 기능 등등 모두"* | 4곳 전부 · 메뉴 · **기능까지** |
| 3 | 🔴 *"**전체적으로 다 뜯어고쳐야해. 내가 계속 ui ux가 별로라고 했던 이유임.** 4곳처럼 만들고. 그다음 내걸로, 최고의 ui ux로 개선하면돼"* | **1단계=4곳 수준 도달 → 2단계=우리 것으로 앞서기** |
| 4 | *"로그인 화면도 다. 싹다"* | 로그인 포함 전 화면 |
| 5 | *"그것도 다 따라서 만들자. 그거까지 함께 재설계안 기획해야해. 기능까지"* | **대시보드 탭 분할도 함** (내가 "안 함"이라 했다가 정정) |
| 6 | ✅ *"우리만의 브랜딩 컬러 좋음"* | **색은 우리 것**(오렌지 `#ff7a4d`·Instrument Serif·다크). 마젠타·로열블루 이식 금지 |
| 7 | *"교차검증하면서 잘 진행해"* · *"전략적으로 검증하면서 감사하면서"* | 실측·교차검증을 절차로 |

---

## 2. ✅ 세션N-30 이 한 것

### 2-a. 재설계안 v2 §7 완료 · 배포 · 검증 (커밋 `275cbb6`·`0711532`)
- **등록 필수 입력 2→1개**(브랜드명 선택화) · **등록 = 자동 측정** · **대기 화면 신설**(`/brand/measuring`)
- 🔴 조용한 실패 차단: 반환 타입에 `measurement: started|rate_limited|failed` 자리 신설 + 화면 분기
- **뮤테이션 4/4 전건 검출** · 테스트 326→**334** · **실화면 13/13 통과**(💰200원 1회로 검증)
- ⭐ **대기 화면에 가짜 진행률을 안 만들었다** — 실측상 러너가 `processing`을 **1번만** 쓰고(`runner.ts:224`)
  `AuditJob`에 엔진별 진행 필드가 **없다**. 설계안이 자기 조건을 어길 뻔한 걸 막았다.

### 2-b. 🔴 인계서 오류 **6건** 정정 (N-25·27·28에 이어 **4세션 연속**)
| 낡은 기록 | 실측 |
|---|---|
| *"대시보드 10.6화면"* | ❌ **1.8화면.** 10.6은 **무료진단 모바일** 숫자였다(두 화면을 뒤섞음) |
| *"설명 없는 지표: 대부분"* | ❌ 히어로 3장·추세·질문별 성적 **전부 이미 있었다** → §7의 1번은 **이미 됨** |
| *"`empty-state.test.tsx`가 화면 문구를 잠근다"* | ⚠️ 실제로는 자기가 넘긴 prop. 진짜 대상은 `empty-state.tsx:40` 기본값 |
| *"오늘 배포 6회로 무료 한도 100%"* | ❌ **100회 중 17회**(83회 여유). 한도는 배포 횟수가 아니라 빌드 시간 쪽 |
| *"Profound 대기화면 진행 표시 없음"* | ❌ **상태 메시지 8종 회전** |
| *"Profound 다크 대시보드"* | ❌ **라이트.** 다크는 가입·대기·페이월만 |

### 2-c. ⭐ 경쟁사 4곳 실계정 화면 전면 분석 (이 세션 최대 자산)
- 👤 가 직접 4곳 가입·녹화 → **42분 · 프레임 279장 · OCR 8,226줄**
- 병렬 에이전트 4개가 **프레임 이미지를 눈으로 확인**하며 분석(OCR만으론 위계·레이아웃을 못 읽는다)
- 📁 `docs/_경쟁사_UIUX/{Profound,Peec,Otterly,Scrunch}/_frames*/` + `_ocr*.txt`
  ⚠️ **영상 원본은 `.vercelignore` 로 배포 제외됨**(`docs`·`*.mov`)
- 🔴 **재분석 금지** — 결과가 v3 설계안 §1에 표로 정리돼 있다. 프레임은 근거 확인용으로만.

### 2-d. 🔴 "별로다"의 정체를 **숫자로 확정** (스킬 도구)
| 화면 | 좌측 시작선 | 근사 불일치(1~3px) | 기타 |
|---|:--:|:--:|---|
| 랜딩 `/ko` | 🔴 **8종** | 🔴 **5건** | 🔴 `keep-all` 없음 **10곳** · 12.2화면 |
| 무료진단 `/audit/[id]` | 🔴 **8종** | 🔴 모바일5·PC4 | 🔴 라벨 없는 입력 1개 · 줄높이 불일치 4건 · 10.8화면 |
| 로그인 `/sign-in` | 4종 | 없음 | 🔴 **PC 좌측 절반이 빔**(4곳 전부 소셜프루프로 채움) |

⭐ **좌측선 8갈래 = 눈이 여덟 번 턱에 걸린다.** 이게 *"배치가 이상하다"* 의 물리적 정체다.
🔴 **1~3px 어긋남은 자동으로만 잡힌다** — 사람 눈은 *"뭔가 이상"* 까지만 간다. **재는 도구가 없어서 못 고쳤다.**

### 2-e. 배포 청소
`문제제기_영상/`(94MB)이 **매번 업로드되고 있었다** → `.vercelignore` 에 추가(커밋 `0711532`).
🔴 `vercel --prod` 는 git 이 아니라 **로컬 폴더를 통째로** 올린다.

---

## 3. ⚠️ 함정 (N-30 신규 + 계속 유효)

- 🔴🔴 **읽는 코드가 없는 필드에 컨트롤을 만들지 말 것**(v2 교훈, 여전히 유효)
- 🔴 **가짜 진행률·가짜 숫자 금지.** 러너는 진행 상황을 알려주지 않는다(위 2-a)
- 🔴 **화면 수를 실격 기준으로 쓰지 말 것** — Stripe **24.1화면**인데 잘 만든 제품이다(스킬 실측).
  *"왜 이만큼 필요한가"* 에 한 문장으로 답하면 통과.
- 🔴 **글자·줄높이·세로간격 "종류 수"에 합격선 두지 말 것** — 스킬이 실측으로 **철회**했다
  (토스 11종 = 나쁜 화면 11종. 변별력 0). ⭐ 변별력 확인된 건 **한글 음수 자간**과 **근사 불일치**뿐.
- 🔴 **`ultracite fix` 를 폴더 단위로 돌리면 남의 파일을 건드린다** — N-30 에서 `subscription.ts`
  (카카오페이 심사 트랙)를 건드려 되돌렸다. **파일 단위로 지정하거나 diff 확인 후 커밋.**
- ⚠️ **괄호 경로는 셸 인용이 안 먹는다** — `app/(authenticated)/...` 를 ultracite 에 넘기면 실패.
  폴더(`app`)로 넘기고 결과를 확인하는 편이 확실하다.
- ⚠️ **playwright·스킬 스크립트는 Findable 폴더에서 실행**(scratchpad 는 모듈을 못 찾는다)
- ⚠️ **로컬 dev 로그인 불가**(Clerk 개발 인스턴스) → 화면 검증은 **배포 후** `scripts/verify-app.py`
- 🔴 **`git stash pop` 금지**(7월 백업 stash 가 살아 있다)
- ⚠️ **Vercel Sensitive env 는 `pull` 하면 `[SENSITIVE]`** → CLI 로 값 판정 불가

### 🆕 검증 자산 (N-30 신설 · 재사용할 것)
| 스크립트 | 용도 |
|---|---|
| `scripts/verify-measuring-flow.py --domain X` | 💰**원가 발생**(~200원). 등록→측정→대기→자동이동 **13계약** 검증 |
| `scripts/verify-measuring-guard.py` | 원가 0. 대기화면 방어(없는·빈·틀린·주입 job id) **5건** |
| `scripts/verify-app.py [경로들]` | 원가 0. 라이브 화면 렌더·콘솔에러 |
| `~/.claude/skills/ui-ux-quality/scripts/{measure,align,squint,a11y}.mjs` | 🔴 **넷은 서로 다른 걸 잡는다. 하나만 돌리면 놓친다** |

---

## 4. 🔴 남은 판단 (👤 몫)

| # | 판단 | 상태 |
|:--:|---|---|
| **1** | 🔴 **재설계안 v3 최종 승인** → 승인 시 §6 의 **1번부터** 착수 | ⏳ **대기 중** |
| 2 | 카카오페이 심사 | 🟠 심사 중(2~3주). `packages/payments/` 건드리지 말 것 |
| 3 | Vercel Pro $20/월 | 🟡 **급하지 않음** — 배포 한도는 100회 중 17회 사용 |
| 4 | 지역·페르소나 스키마 신설(2단계) | ⬜ 1단계 끝난 뒤 판단 |
| 5 | 🔴 **P0 실고객·유통** | 🔴 **회사 최대 병목. 유저 0명.** UI 를 고쳐도 이건 안 풀린다 |

---

## 5. ⬜ 다음 착수 = 재설계안 v3 §6

📕 `docs/_적용/재설계안_v3_전면_경쟁사기반_2026-08-14.md`

| # | 묶음 | 규모 | 핵심 |
|:--:|---|:--:|---|
| **1** | **정렬·타이포 기본기** | M | 좌측선 8→2 · 근사불일치 10→0 · `keep-all` 10곳 · 입력 라벨 |
| **2** | **지표 사전 신설** | M | `packages/audit/metric-dictionary.ts` — `{label, question, description, format, direction, denominator}` |
| **3** | **대시보드 탭 7개 재편** | **L** | 🟢 **있는 데이터를 꺼내는 것**(AI별·출처·평가 신설) |
| **4** | 순위 충격 공개 + 모바일 처방 상단화 | M | Profound f031 방식 |
| **5** | 0 결과 3요소 + 로딩 실패 상태 | S | 🔴 4곳 **공통 실패** = 우리 기회 |
| **6** | 로그인 PC 소셜 프루프 | S | 지금 좌측 절반이 빔 |

⚠️ **2번이 3번보다 먼저다** — 탭 7개면 지표가 7화면으로 퍼진다. 사전 없이 하면 같은 지표를
7곳에서 각자 설명하게 되고, 그게 이 저장소가 **세 번 겪은 사고**다(N-14 → N-28 `7/6곳·117%` → N-30 `95% vs 7곳`).

### 🔬 탭 7개가 "빈 탭"이 안 되는 근거 (DB 실측)
`Tracking` 에 이미 다 쌓인다: `engineId`(→AI별) · `citedSources`(→출처) · `sentiment`(→평가) ·
`promptId`(→질문별·화면 있음) · `rawResponse`(→진실의 거울) · `shareOfVoice` · `mentionPosition/ListSize`
🔴 **없는 것은 지역·페르소나뿐**(`Prompt` 에 축 없음) → **2단계로 미뤘다.**

---

## 6. 📊 경쟁사 4곳 요약 (v3 §1 이 전문 · 여기는 핵심만)

| | Profound | Otterly | Scrunch | Peec |
|---|:--:|:--:|:--:|:--:|
| 가격/월 | $99·$399 | 불명 | **$250** | $80·$205·$420 |
| 필수 입력 | **도메인 1** | 4개 | 2개 | **도메인 1** |
| 지표 설명 | ⭐**항상 표시** | ⓘ툴팁 | ⓘ+**방향** | ⓘ툴팁 |
| 대기 안내 | 메시지 8종 | 메시지7종+**워크스루** | 🔴**전무** | 로고+6엔진 |
| 한국어 UI | ⭐완전(단 요금제·대시보드는 영어) | 🔴기계번역 | ❌ | ❌ |
| 한국 엔진 | ❌ | ❌ | ❌ | ❌ |
| 무료 진단 | ❌ | ❌ | ⚠️약속만 | ❌ |

### 🔴 4곳 **공통 실패** = 우리 자리
1. **첫 결과 0에 침묵** — Otterly는 4분 기다려 `Position 0`(순위 0등은 논리적으로 없다) + 가장 눈에 띄는 건 `Start Subscription`
2. **한국 엔진 0곳**
3. **한국 브랜드 자동감지 실패** — Otterly: AI에이전시→**아동복** · Peec: 마케팅사→**영어학원 4곳**
4. **한국어 생성 붕괴** — Otterly `무 integrated 마케팅 전략…`
5. **로딩 실패 상태 미설계** — Otterly `Content Checker` 60초 영구 스켈레톤인데 버튼은 활성

### ⭐ 가져올 것 1~3위
1. **순위 충격 공개** — `#1 Omnicom / … / #51 우리`(자사만 하이라이트) + *"이미 5+개 기회를 찾아두었어요"*
2. **지표 3단** — `이름 ⓘ` → **평문 한 줄** → `숫자` (툴팁은 누르는 사람만 읽는다)
3. **방향 표시** — `Direction: Higher is better`(Scrunch)

---

## 7. 🔴 잊으면 안 되는 것

> ① **화면 작업이면 `Skill(ui-ux-quality)` 먼저.** 말 안 해도.
>    그리고 🔴 **참조파일을 골라 읽지 말 것** — N-30 에서 16개 중 2개만 읽고 답했다가 👤 지적받았다.
>    **서비스 전체를 손대면 `ia.md` 를 화면 감사보다 먼저.** 가장 중요한 건 `quality.md`(4층 모델).
> ② **도구가 통과시켜도 눈으로 봐야 한다** — N-30 에서 도구가 통과시킨 화면에서 눈으로
>    *"로그인 PC 좌측 절반이 빔"* 을 잡았다. 반대로 **라벨 없는 입력은 도구만 잡았다.**
> ③ ⛔ **승인 전 코드 금지**(배포된 실사용 화면). 방향은 승인받았지만 **설계안 최종 승인은 아직**.
> ④ 🔴 **인계서 숫자를 믿지 말고 실측하라** — **4세션 연속** 틀렸다(N-30 만 6건).
> ⑤ 🔴 **회사 병목은 여전히 P0 실고객·유통.** UI 재설계가 그걸 대신하지 않는다.
