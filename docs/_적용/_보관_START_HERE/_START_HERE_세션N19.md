# 🧭 START HERE — 세션 N-19 (2026-08-11 세션N-18 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` + `UIUX_대개선_설계_v3_2026-08-10.md` 를 열 것.**
> ⛔ `_START_HERE_세션N18` 이하 **전부 폐기**. ⛔ `UIUX_대개선_설계_v2` 폐기(근거만 유효).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 9eff045 근처면 세션N-18 종료 상태
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

## 1. ⭐ 다음에 할 일 (우선순위 순)

| # | 무엇 | 왜 | 누가 |
|:--:|---|---|---|
| **1** | 🎯 **S2 — 빈 상태 공용화** | 가입 6명 중 **5명이 로그인하면 빈 화면**을 본다. 병목은 유입이 아니라 **착지** | 나 |
| 2 | S3 첫 화면 정리 → S4 용어 한국어화 → S5 진단결과 SSR → S6 IA 3단 재편 | UIUX 결함 88건 트랙 | 나 |
| ⏳ | 🟠 **카카오페이 답장 대기** | 4차 회신 발송 완료. **먼저 보낼 것 없음** | 👤 |
| 🔴 | **P0 실고객 10~15명 지불의사** | **불변 1순위·유일한 회사 병목**. 시뮬 대체 불가(r=0.20) | 👤 |

⚠️ **S0~S6 는 전부 화면 변경 → 착수 전 사용자 승인 필요**(프로젝트 규칙).
⚠️ **각 단계마다 스크린샷 눈 확인**(테스트는 아는 것만 검증한다).

---

## 2. 🏁 세션N-18 성과 (커밋 6건 · web 3회·app 3회 배포)

| 무엇 | 결과 |
|---|---|
| 🔴 **cron 인증 구멍** | 닫음 · 라이브 401 검증 (`135676e`) |
| 🔴 **cron UTC 오해** | `0 2`(=오전11시) → `0 17`(=새벽 2시 KST) 교정 · **실제 자동 실행 확인** |
| ✅ **PDF 3개월 실패** | **복구 확정**(7건 전부 pdfUrl 생성) — 확인 과제 **종결** |
| 💰 **`/billing` Scale 누락** | 추가·배포 (`2f6cdd3`) |
| 🖼 **og:image 전 페이지 0개** | 발행됨 · 라이브 확인 |
| ⚖️ **alerts 거짓 판매문구** | 제거 |
| 🆕 **정기결제(월 자동결제) 구현** | 빌링키·첫결제·웹해지·사전고지 (`f806bbf`·`9eff045`) |
| 🆕 **카카오페이 3·4차 회신** | 발송 완료 |

---

## 3. 🔴 세션N-18 이 뒤집은 것 (**되살리지 말 것**)

| 이전 기재 | 실측 결과 |
|---|---|
| *"새벽 2시 크론이 돌면 PDF 확인 가능"* | ❌ **cron schedule 은 항상 UTC**. `0 2` = **오전 11시 KST** 였다 |
| *"PDF 복구됐는지 확인 필요"* | ✅ **복구 확정** — 측정 7건 전부 `pdfUrl` 생성 |
| *"S1-3 = 거짓표시 2건(`alerts`·`/brand`)"* | ⚠️ **`/brand` 는 오탐**(정직한 화면). 진짜는 `alerts` **미결제자 잠금화면**뿐 |
| *"크론이 11일째 일을 안 한다(로직 결함)"* | ❌ **로직 정상.** 호출이 안 됐을 뿐(UTC 건) |
| *"Hobby 는 cron 1개 제한"* | ❌ **100개 허용**. 제약은 하루 1회 + 정밀도 ±59분 |
| *"정기결제는 CID 없으면 못 만든다"* | ❌ **테스트 CID `TCSUBSCRIP` 로 지금 만들 수 있다**(실제로 만들었다) |
| *"카카오페이는 채널키 하나면 된다"* | ❌ **정기결제용 채널 별도 필요**(내가 근거 없이 단정했다가 정정) |

🎓 **교훈: 문서·요약을 믿지 말고 코드·DB·화면·공식문서 원문을 실측하라.**
N-18 도 **7건**이 틀렸다(N-17 7건 · N-14 5건 — **매 세션 반복**).

---

## 4. 🔴🔴 세션N-18 최대 발견 — 보안 구멍 (닫음)

**`x-vercel-cron: 1` 헤더 한 줄로 외부인이 유료 측정을 무제한 실행**시킬 수 있었다.
- 이전 가드가 `CRON_SECRET` **없으면** 그 헤더로 폴백 → 프로덕션에 시크릿이 **없었다**
- 🔬 내가 스푸핑 테스트를 하다 **우연히 발견**(측정 5건·약 435원 실행됨 = 👤승인 없이 원가 쓴 사고)
- ✅ `packages/security/cron.ts` 로 단일화(**fail closed**·`timingSafeEqual`)·3개 cron 적용
- ✅ 라이브 검증: 진짜 시크릿 **200** / 틀린 시크릿·스푸핑 **401**

🔴 **`x-vercel-cron` 폴백을 되살리지 말 것.**
🔴 **원가 나가는 엔드포인트는 찌르기 전에 가드를 코드로 먼저 읽을 것.**

---

## 5. 🟠 카카오페이 = 답장 대기 (먼저 보낼 것 없음)

📕 SoT = **`_카카오페이_심사보완/⭐월요일_아침_실행카드.md`** 최상단

**진행 경위**: 심사관이 *"정기결제 상품이 없으면 심사 불가"* → 👤대표님 판단
*"만들고서 메일 보내는 게 어때? 정기결제는 꼭 필요한 거야"* (**맞는 판단이었다**)
→ 정기결제를 **실제로 구현**해서 회신. 왕복 한 번을 없앴다.

| 심사관 답 | 할 일 |
|---|---|
| **승인** | 실행카드 §"되돌릴 것" **0~5번**(🆕0번=정기결제 마무리) |
| "자동 청구 안 되는데요" | **이미 정직하게 밝혔다**(회신 참고사항 2번) |
| 화면 재요청 | `python3 scripts/capture-payment-path.py` 재실행 |

🔴 **승인 후 필수**: 라이브 채널키 교체 + **결제 예약(2회차 자동청구) 연결** +
`subscribe-button.tsx` 의 *"지금은 첫 회만 결제되며…"* 줄 **삭제**(안 지우면 거짓 표기).

---

## 6. ⚠️ 함정 (착수 전 필독)

### 세션N-18 신규
- 🔴 **cron schedule 은 항상 UTC**(KST=UTC+9). Hobby=**하루 1회만**(더 잦으면 배포 실패)·**±59분**.
- 🔴 **원가 엔드포인트는 가드부터 읽고 찌를 것**(내가 435원 썼다).
- ⚠️ **`vercel.json` 은 `additionalProperties:false`** — 주석용 커스텀 키 금지(스키마 거부).
- ⭐ **app 배포에 project.json swap 불필요**:
  `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app` (루트에서).
  ⚠️ `apps/app` 안에서 실행하면 rootDirectory 겹쳐 실패.
- ⚠️ **PortOne 인증 헤더는 `PortOne {secret}`** — 문서 요약은 "Bearer"라고 하지만 **틀렸다**.
  우리 작동 코드(`packages/payments/portone.ts`)가 진실.
- ⚠️ **DB write 전 `ensureOrgExists()`** — Clerk 웹훅 지연 시 org row 부재(relationMode=prisma).
- ⚠️ **PPT 좁은 컬럼 = 마지막 줄 잘림** — 텍스트 컬럼 4.0M EMU 는 좁다(4.7M 로 넓혔다).
  **렌더해서 눈으로 볼 것**(python-pptx 로그만 보면 못 잡는다).
- ⚠️ `app.findable.co.kr/favicon.ico` **404** = 기존 결함·기능 영향 없음(web 엔 있고 app 엔 없다).
- ⚠️ 한글 경로는 **NFD** — `cd` 가 깨지면 절대경로나 python 으로 우회.

### 기존 함정 (계속 유효)
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요** · 🔴 **`apps/api` 는 죽은 앱**
- 🔴 **`"use server"` 파일은 async 함수만 export** — tsc·lint 통과인데 **빌드만 실패**
- 🔴 **`audit-result.tsx` M1 주석 존중** · 🔴 **날짜 계산해 넣지 말 것** · 🔴 **BotID 우회 금지**
- 🔴 **`git stash` 금지**(기준선은 `git worktree add … HEAD --detach`)
- ⚠️ **DB 조회**: `packages/database/` 안에서 + **`.mts`** + `PrismaNeon` 어댑터 + `.env.local`
- ⚠️ **괄호 경로(`(authenticated)`)** 는 Bash·Biome 인용이 깨진다 → 그 폴더로 `cd` 후 상대경로
- ⚠️ **오탐률 12~32%** — 진단서를 그대로 다 고치지 말 것
- ⭐ **화면 검증**: `python3 scripts/verify-app.py /경로 --expect "문구"` (실제 로그인·스크린샷)
  ⚠️ QA 계정(`nayoy2+qa@`)은 측정 데이터가 없어 **빈 상태만** 보인다

---

## 7. 👤 사용자 결정 대기

1. 🔴 **P0 실고객 지불의사 검증**(마케터 10~15명) — 불변 1순위. 질문지=`P0_지불의사_시뮬레이션_결과.md`
2. 🔴 **잠긴 유료 3종 결정** — `alerts` 는 껍데기(`compare`·`sources` 는 작동). ⭐유력=채널톡식 "시간 축"
3. 🟠 **카카오페이 답장 대기** (위 §5)
4. 🔵 P4: 파트너 특전 정책 · KIPO 상표 출원
5. `alerts` 실제 알림 구현 = **보류**(임계값 분포 없음). 근거 없는 임계값 발명 금지

---

## 8. 💰 원가 방침

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · 브라우저 검증 · 빌드 · 배포 | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인** |

⚠️ **엔드포인트 호출도 "새 진단 실행"이 될 수 있다.**

---

## 9. 🔴 하지 말 것

- ❌ **`x-vercel-cron` 폴백 부활**(보안 구멍) · ❌ **`vercel.json` 커스텀 키**
- ❌ **정기결제 해지에서 예약 취소 생략** — 빌링키만 지우면 **무한 과금**
- ❌ 표본 없는 **"업계 N분위"** · **지어낸 예시 숫자** · **임계값 발명**
- ❌ **소급 데이터 수정** · ❌ **daum 표시 필터**(근거 77건이 빈 카드가 된다)
- ❌ **FAQPage 로 AI 인용 노리기**(2026-05 폐지) · **llms.txt** · **자체 별점**
- ❌ **조작 수치 인용**: *"FAQPage→Perplexity +34%"* · *"YouTube 상관 0.737"*
- ❌ **`(준비 중)` 딱지 제거** — 전부 정직한 표기다
- ❌ **`tracking-tight`** · **crew 자기평가에 zod `.max()`**
- (전체는 `투두리스트.md` §만들지 말 것)

---

## 10. 🔴 다음 세션이 잊으면 안 되는 한 줄

> **S0·S1 끝·배포됨. 다음은 S2(빈 상태) — 가입자 6명 중 5명이 보는 화면이다.**
> **카카오페이는 답장 대기, 먼저 보낼 것 없다.**
> **판정 기준은 "예쁨"이 아니라 "고객사 직원이 3초에 이해하는가"다.**
> **문서를 믿지 말고 실측하라 — N-18 에서도 7건이 틀렸다.**
