# 🧭 START HERE — 세션 N-26 (2026-08-12 세션N-25 인계)

> ⚠️ **이 파일 + `_투두_단일진실_N25.md` 두 개만 열면 된다.**
> ⛔ `_START_HERE_세션N25` 이하 **전부 폐기** · `_투두_단일진실_N24` 는 **근거 보관용으로 격하**
> (남은 일은 N24 에 없다 — **N25 파일이 단일 진실**).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # efee78c 이면 인계 시점 그대로
git status --short          # ⚠️ packages/payments/ 4건은 **다른 세션 것**(아래 §1)
cd apps/app && pnpm vitest run   # 249/249 여야 정상
```

**현재 상태**: 테스트 **249/249** · web·app tsc **0** · 세션N-25 커밋 **10건 전부 배포·라이브 확인**

---

## 1. 🔴🔴 착수 전에 반드시 확인할 것 — **다른 세션이 결제를 작업 중이다**

```
 M packages/payments/index.ts
 M packages/payments/portone.ts      (+68줄)
?? packages/payments/billing-cycle.ts        (신설: addMonthsClamped·nextBillingDate)
?? apps/app/__tests__/billing-cycle.test.ts
```

이건 **세션N-25 가 만든 것이 아니다.** 결제 예약(2회차 자동청구) 관련으로 보인다.
🔴 **결제 트랙(`_투두_단일진실_N25.md` §4)을 착수하려면 그 세션과 충돌하는지 먼저 확인**할 것.
⚠️ 커밋하지 말 것 — 남의 작업물이다. 세션N-25 는 손대지 않았다.

---

## 2. ✅ **§2-1 「429 문의 링크」는 세션N-26 이 완료했다** (`b7d890b` · 배포·라이브 확인)

> **세션N-26 은 코드 5건을 끝냈다** — 429 문의 링크(`b7d890b`) ·
> 🔒 **공유링크 이메일 노출 정석 해결**(`3633629`) · 요금제 「현재」 배지(`f46d6bc`) ·
> 🆕 **가입·결제 고객에게 「가입하세요」**(`75438e1`) ·
> 🆕 **메일 발송 실패해도 「곧 보내드려요」**(`4760a39`).
> 검증: **309/309** · web·app tsc 0 · 린트 증가 0 · web·app **둘 다 배포·라이브 확인**.
>
> ⭐ **#7·#8 은 목록에 없던 결함이다.** 둘 다 같은 렌즈로 나왔다 —
> **"화면이 약속한 것을 제품이 실제로 지키는가"**.
> 목록을 따르는 것보다 **목록을 의심하는 것**이 값을 냈다.
>
> 🎓 **가드가 두 번 뚫렸다**(둘 다 뮤테이션으로 발견 · `_투두_단일진실_N25.md` §7-b):
> ①**정규식 블록주석 제거가 40,570자를 삼켰다**(가드가 빈 문자열을 검사 중이었다)
> ②**"형태"로 판정하니 다른 형태의 되돌림이 통과**했다 → *"어디에 있는가"* 로 교체.
> ⚠️ **가드를 새로 짜면 반드시 뮤테이션을 돌릴 것.** 통과는 증거가 아니다.
>
> 🔴 **투두리스트 자체를 감사해 4건을 정정했다** — `_투두_단일진실_N25.md` 의
> *"세션N-26 투두리스트 자체 감사"* 절을 **먼저** 볼 것:
> **#2 는 재현 실패**(해외 IP 없이 확인 불가) · **#4 는 과장**(이미 새 탭) ·
> **#5 는 23건이 아니라 65건** · **#6 이 가장 심각했는데 맨 아래 있었다**.
>
> ⬜ **남은 코드**: #4(축소 재정의됨 · L) · #5 lint 65건 · #2(보류).
> 🔴 **IP 상한 값은 아직 올리지 말 것** — `ip_capped` 계측이 쌓인 뒤 분포를 근거로.

<details>
<summary>완료된 원래 지시(근거 보관)</summary>

**무엇**: 서버가 429 를 뱉을 때 문구는 *"문의해 주세요"* 인데 **클릭할 데가 없다.**

| 위치 | 문구 |
|---|---|
| `apps/web/app/api/audit/route.ts:425` | *"오늘 무료 진단이 모두 소진되었습니다. 내일 다시 시도하시거나 **문의해 주세요**."* |
| `apps/web/app/api/audit/route.ts:443` | *"무료 진단은 24시간 동안 N개 도메인까지… 더 필요하시면 **문의해 주세요**."* |

**왜 지금**: `:443`(IP 상한)은 **대행사·에이전시**(=지불의사 가장 높은 ICP)가 클라이언트
3곳을 진단하면 3번째에서 걸리는 지점이다. **고의도 리드가 여기서 그냥 사라진다.**

**어떻게**: 서버 문구만 고치지 말고 **폼이 링크를 렌더**하게 한다
(`audit-form.tsx` 는 이미 `existingJobHref` 로 링크를 띄우는 패턴이 있다 — 그걸 재사용).
`/contact` 라우트 존재는 실측 확인할 것.

### 🔴 함께 판단해야 할 것 — **IP 상한 값(2건)은 지금 올리지 말 것**
세션N-25 가 `audit_submitted(outcome: "ip_capped")` 계측을 깔았다.
→ *"IP 상한이 실제로 대행사를 막고 있나"* 를 **며칠 뒤 숫자로** 답할 수 있다.
⛔ 지금 2→5 로 올리면 **근거 없이 숫자를 발명**하는 것이다(이 프로젝트 금지사항).
📌 **판단 조건**: PostHog 에서 `ip_capped` 가 유의미하게 잡히면 그때 분포를 근거로.

</details>

---

## 3. 📋 남은 일 전체 — **`_투두_단일진실_N25.md`** (여기 요약만)

### 🔴 회사 병목은 코드가 아니다
```
트래픽 0 → 유저 0 → 유료 0 → 매출 0
```
세션N-25 가 고친 건 전부 *"고객이 왔을 때 배신하지 않게"* 다. **아직 고객이 오지 않는다.**
- **P0** 🔴 마케터 10~15명 **실제 인터뷰**(👤 사람이 만나야 함 · 시뮬 대체 불가 r=0.20)
- **P1** 🔵 KIPO 상표 출원(선출원주의 — 시간이 불리하게 간다)
- **P2** 🟠 카카오페이 회신 대기

⭐ 세션N-25 가 퍼널 계측을 깔아서, 이제 인터뷰에 *"제출 N건 중 거절 M건"* ·
*"무료 체험 다 쓰고 가입 화면 본 K명"* 을 **숫자로** 들고 갈 수 있다.

### 🖥️ 코드 (5건 · 원가 0)
| # | 할 일 | 크기 |
|:--:|---|:--:|
| 1 | ~~429 문의 링크~~ | ✅ **완료**(N-26) |
| 7 | ~~🆕 가입·결제 고객에게 「가입하세요」~~ | ✅ **완료**(N-26) |
| 8 | ~~🆕 메일 실패해도 「곧 보내드려요」~~ | ✅ **완료**(N-26) |
| 2 | `/en` `<title>` 한국어 (🟡 해외 IP 확인 선행) | S |
| 3 | 요금제 「현재」 배지 (P2) | S |
| 4 | `/history/[jobId]` 앱 내재화 — 유료 고객이 자기 데이터 보려고 **제품 밖으로 나간다** | **L** |
| 5 | lint 부채 23건 (⚠️ #4 와 함께) | M |

**+ 미해결 보안 1건**: 공유링크 이메일 노출(`[jobId]/route.ts:130` 이 **소유 검사 없이**
`emailMasked` 를 준다). 현 방어 `?shared=1` 은 **공유 버튼 링크에만** 붙는다.

### 🔗 P0 종속 4건 — **기다려도 안 풀린다**
D4 기간칩 · D2 이벤트핀 · D1 알림UI · alerts 메일.
🔬 근거: 자동 재측정 cron 은 `autoRefreshHours != null` org 만 도는데
**`free: null`**(`plan.ts:120`) + **유료 0명** → 시계열이 1일에서 **멈춰 있다**.
🔴 **순서 경고**: 유료 축(알림=0·비교=0·시간=절반)을 먼저 채우고 무료를 줄인다.
   반대로 하면 **게이팅 이탈만** 얻는다(최대 30% — Artisan).

---

## 4. ✅ 세션N-25 가 한 일 (커밋 10건 · 전부 배포·라이브 확인)

| # | 무엇 | 검증 |
|:--:|---|---|
| 1 | **처리방침 개정** — 수탁자 6곳 공개 + **AI 미학습 고지** 신설 + 기재오류 3건 정정 | 라이브(KO) |
| 2 | **crew 전역 일일 상한**(KST 자정 · `packages/audit/kst-day.ts` 순수함수) | 뮤테이션 6건 |
| 3 | ⭐ **crew 무료 1회 개방** + *"무료"* 라 해놓고 403 뱉던 **거짓 제거** | 라이브 번들 |
| 4 | **실패가 체험분을 먹던 것** 수정(우리 잘못인데 고객이 손해) | 뮤테이션 2건 |
| 5 | **가입 카드 소유권 안내** + **공유링크 이메일 노출 방지** | 뮤테이션 2건 |
| 6 | **퍼널 계측 3종** + 🔴 완료를 *"시작"* 으로 세던 이벤트 정정 | 뮤테이션 3건 |

### 🔬 실측으로 **인계문서를 정정한 것 4건** (다음 세션이 다시 믿지 않도록)
1. 🔴 **수탁자 목록이 양방향으로 틀렸다** — `AWS` 는 SDK **0건**(적었으면 **허위 기재**),
   개인정보를 실제로 받는 **PostHog**(이메일·이름·전화번호)·**Resend**·**Neon** 은 누락.
2. 🔴 **"crew 원가 67%" 는 오독** — 원문은 *"crew 를 **빼면** 67% 절감"*(sonnet 시절).
   실측 = 진단 87원 / crew 수십 원 · **원가율 0.09%**.
3. 🔴 **N-24 의 "Logtail 유출" 위험평가가 어긋났다** — BetterStack 은 **프로덕션 미활성**.
   실제 개인정보 전송처는 **PostHog** 였다(N-24 의 코드 수정 자체는 옳다).
4. 🔴 **"진단↔계정 연결 없음" 은 전제 오류** — 대시보드가
   `identifiers=[로그인 이메일, "org:{orgId}"]` 로 조회 → **읽는 시점에 자동 연결**된다.

---

## 5. 🎓 세션N-25 가 배운 것 (다음 세션이 같은 실수를 하지 않도록)

1. 🔴🔴 **인계문서·감사 결과 둘 다 틀린다.**
   이번에 에이전트 **1위 지목이 두 번 틀렸고**(*"연결 코드 없음"*·*"돈 내도 crew 안 열림"*),
   내가 **인계문서 숫자를 검증 없이 인용**해 전략 추천 전체를 그 위에 세웠다(67%→0.09%).
   → **감사 결과는 탐색의 시작점이지 결론이 아니다.** 건별 실측.
   ⭐ 그래도 감사는 값을 했다 — 틀린 1위를 파는 과정에서 **실제 구멍 2개**를 찾았다.
2. 🔴 **방향을 뒤집기 전 `git log -S` 로 그 결정의 이력을 볼 것.**
   내 초안(*"crew 유료화"*)은 `12a00de`(8-10)가 **이미 되돌린 결정**을 다시 잠그는 것이었다.
   👤 그 커밋의 지시 원문: *"MVP 가 아니라 **고객 페인을 해결하는 최고의 제품**"*.
3. 🔴 **가드가 두 번 뚫렸다** → **존재 검사 금지**:
   ①부정 고지의 *존재*만 검사 → **섹션 제목**이 만족시켜 통과
   ②`toContain("헬퍼이름")` → **import 줄**이 만족시켜 통과
   → *"긍정 서술이 **없는가**"* · *"**호출 형태**인가"* 로 판정한다.
4. ⭐ **계측이 있는데 거짓말할 수 있다** — `audit_started` 가 완료 시점에 발화해
   퍼널 이탈이 **0으로** 읽혔다. **없는 것보다 나쁘다**(틀린 답을 자신 있게 준다).
5. ⭐ **근거 없는 임계값 대신 관측 가능한 것으로** — crew 는 원가를 기록하지 않아
   금액 상한 대신 **건수 상한**을 걸었다(📌 cost 계기 붙으면 금액으로).
6. 🔴 **어뷰징 방어와 고객 경험을 한 함수에 섞지 않는다** —
   상한=방어 / 쿼터=체험. 섞으면 *"우리 잘못인데 고객이 손해"* 가 된다.

---

## 6. ⚠️ 함정 (착수 전 필독)

- 🔴🔴 **엔진 수 재조사 금지** — 본류=**7** · 영어만=**4** · 8=브리핑 별도
- 🔴 **BotID 가 curl 을 차단한다** → 진단·crew 경로는 **브라우저로만** 라이브 검증 가능
  (세션N-25 가 이걸로 실사용 검증을 못 했다 → 순수함수+정적가드로 대체)
- 🔴 **`apps/web` 에 테스트 러너 없음** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서
- 🔴 **`apps/api` 는 죽은 앱** — 새 코드 넣으면 실행되지 않는다
- 🔴 **DB 직접 조회 막힘**(한글 NFD + Prisma CJS) → **라이브 API `/api/audit/<id>`**
- 🔴 **cron 은 UTC** — `0 17 * * *` = KST 새벽 2시. 일일 상한은 **KST 자정**으로 끊는다
  (`packages/audit/kst-day.ts` 재사용 — 두 벌 만들지 말 것)
- 🔴 **biome: 복잡도 20 · 중첩 삼항 금지 · 정규식은 최상위 상수 · `/**` 대신 `/*`**
- ⚠️ **biome 이 대괄호 경로(`[jobId]`)를 직접 못 읽는다** → 상위 디렉터리로 lint
- 🔴 **`Date.now()` 기반 상한은 테스트 불가** → 기준 시각을 **인자로 받는다**
- 🔴 로컬 dev·build 는 **`VERCEL=1`** · `"use server"` 는 **async 함수만 export**
- ⚠️ 요금제 문구는 **app·web 둘 다** · ⭐ **app 은 `lang="ko"`**
- ⭐ **배포**: web = `npx vercel --prod --yes --scope nayoy2-7791s-projects`(루트 기본값)
  / app = `project.json` swap 후 **🔴 원복 필수**
- ⭐ **라이브 검증법**(세션N-25 확립): 배포 후 결과페이지 HTML 의 청크 URL 을 긁어
  **번들에 문구가 실제로 들어갔는지** 확인한다(curl + python 매칭 · 셸 한글 매칭은 깨진다)
- 👁️ **스크린샷 눈확인** — 테스트는 아는 것만 검증한다

---

## 7. ⛔ 되살리지 말 것 (세션N-25 신규 4건 + 기존)

### 🆕 세션N-25 추가
13. **crew 를 유료 전용으로 되돌리기** — `12a00de` 가 되돌린 결정이고, 업계 게이팅 축
    4개(프롬프트·엔진·브랜드·갱신주기)에 **처방은 없다**. HubSpot Grader·AthenaHQ
    무료티어는 처방을 **공짜로 준다**. 원가도 막지 않는다(0.09%)
14. **처방(`buildGeoActions`) 잠그기** — *"우리가 이기는 유일한 지점"*
15. **AWS 를 수탁자로 기재** — SDK **0건**. 런타임 감지 플래그일 뿐(**허위 기재**)
16. **실패한 crew 를 체험분에 카운트** — 우리 잘못(270초 타임아웃)으로 고객이 손해본다

### 기존 (계속 유효)
1. 엔진 수 "28곳 통일" 2. 결제 상태 3줄(스키마에 필드 없음) 3. 처방 2건 병합
4. 감성 배지 변별력 5. 순위 분모 노출 6. 잠금카드 `min-h`(코드에 없음)
7. `x-vercel-cron` 폴백 8. 업계 N분위 벤치마크 9. naver 어댑터 Daum식 정합
10. 팩트정합률 전면화 11. **`z.email()` 완화**(🔴 보안 경계) 12. 대법원 2020도13960 인용

---

## 8. 🔴 다음 세션이 잊으면 안 되는 것

> **① 남은 일은 `_투두_단일진실_N25.md` 에만.** 다른 문서의 `⬜` 를 믿지 말 것
> **② 다음 착수 = 429 문의 링크**(§2). IP 상한 **값은 데이터 후 판단**(지금 올리면 발명)
> **③ ⚠️ `packages/payments/` 는 다른 세션 작업 중** — 충돌 확인 후 착수
> **④ 착수 전 실측** — 인계문서·감사 결과 **둘 다 틀린 적이 있다**(§5-1)
> **⑤ 가드는 존재 검사 금지** — 구조·호출 형태로 판정(§5-3)
> **⑥ 회사 병목은 P0 실고객.** 코드로는 계측까지가 최선이었다
