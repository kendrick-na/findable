# 🧭 START HERE — 세션 N-18 (2026-08-11 세션N-17 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` + `UIUX_대개선_설계_v3_2026-08-10.md` 를 열 것.**
> ⛔ `_START_HERE_세션N17` 이하 **전부 폐기**. ⛔ `UIUX_대개선_설계_v2` 폐기(근거만 유효).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 500be5f 이면 세션N-17 종료 상태
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

## 1. 🔴🔴 지금 당장 할 일 (다른 것보다 먼저)

### ① **`500be5f`(S0) 이 미배포다** — 배포하고 라이브 검증
S0 = 무료진단 숫자 정합성 7건. **커밋만 했고 배포 안 했다.**
👤사용자에게 *"지금 배포 vs S1까지 묶어서 배포"* 를 물었고 **답을 못 받은 상태**로 세션이 끝났다.
⭐ 내 추천은 **S1까지 만들고 한 번에 배포**였다(원가 0·검증 1회).
```bash
npx vercel --prod --yes    # web. app 은 이번 변경에 없음
```

### ② **PDF 복구 실물 확인** (원가 0 · 자동)
🔬 세션N-17 이 원인 확증·수정·배포 완료(`0e79b29`). **남은 건 확인뿐.**
새벽 2시 크론(`/api/cron/auto-refresh-tracking`)이 돌면 자동 생성된다.
🔬 발동 조건 실측 확인됨: growth org 1개 · 브랜드 7개 전부 263~286시간 경과(주기 24h) ·
   `MAX_TRIGGERS_PER_RUN=5` → **5건 실행 예정**(원가 750~1,500원, 원래 도는 작업).
**확인법** — `AuditJob.pdfUrl` 이 null 이 아니면 3개월 실패 복구 성공:
```bash
cd packages/database   # ⚠️ 반드시 이 폴더에서 + .mts 확장자 + npx tsx
# PrismaClient 는 ./generated/client.ts + PrismaNeon 어댑터 필수(아래 §6 참조)
```
기준선: 최근 완료 6건 전부 `pdfUrl=null` · 마지막 성공 **2026-05-03**.

---

## 2. 🎯 이 프로젝트의 판단 기준 (불변 · 가장 중요)

> 👤 *"MVP(최소기능)가 아니라 **고객 페인을 해결해줄 최고의 제품**을 만들어."*
> 👤 *"고객사·회사·**그 회사 직원이 봐도 직관적이고 이해 쉬울 것**."*
> 👤 판정(2026-08-11): ***"고객 입장에서 직관적이지 않다. 고객들이 잘 쓸 상품이 아니다."***

🔴 **평가 대상은 "예쁨"이 아니라 "이해 가능성"이다.** 판정 문장:
> *"우리를 전혀 모르는 대기업 마케팅 직원이 3초 보고 ①무슨 화면인지 ②뭘 눌러야 하는지 ③왜 좋은지 아는가?"*

🎯 고객의 진짜 페인(리서치 확정·재리서치 금지): ***"측정은 잘하는데 무엇을 해야 할지는 안 알려준다"***

---

## 3. 🏁 착수 순서 S0~S7 (세션N-17 확정 · 이 순서대로 계속)

📕 상세 = `투두리스트.md` §전략적 착수 순서 · 기획서 = `UIUX_대개선_설계_v3_2026-08-10.md`

| 단계 | 무엇 | 상태 |
|:--:|---|---|
| **S0** | 🔴🔴 숫자 정합성 7건 | ✅**완료**(`500be5f`) · ⬜**미배포** |
| **S1** | 🔧 고장수리 3건 — ⭐**다음 착수** | ⬜ |
| **S2** | 🎯 빈 상태 공용화(원인①) | ⬜ |
| **S3** | ✂️ 첫 화면 정리(원인④) | ⬜ |
| **S4** | ✍️ 용어 한국어화 + 가격 표기 | ⬜ |
| **S5** | 🔍 진단결과 요약부 SSR | ⬜ |
| **S6** | 🧭 IA 3단 재편(측정·분석·**실행**) | ⬜ |
| S7 | 나머지 medium/low | ⬜ |

### ⭐ S1 = 다음에 할 것 (전부 원가 0)
| # | 내용 | 왜 |
|---|---|---|
| 1 | **`/billing` 에 Scale 추가** | 💰**매출 직결.** 홈 "Scale 시작하기"가 `/billing` 으로 보내는데 **상품이 없다**. 결제 카탈로그·권한 위계엔 이미 있음(월 99만/청구 108.9만) |
| 2 | **`og:image` 기본값** | 카톡·슬랙 공유 시 썸네일 없음. 파일은 200 서빙되는데 메타태그 미발행. `createMetadata` **한 곳**만 고친다(호출부 전부 고치지 말 것) |
| 3 | **거짓 표시 2건** | ⚖️표시광고 리스크: `alerts` 잠금카드가 "이메일 알림"을 파는데 **발송 꺼져 있음** · `/brand` 는 "측정 시작"인데 **등록만 함** |

---

## 4. 🔬 UI/UX 결함 88건 (최우선 트랙)

📕 진단서 = `_진단_UIUX_v3/` 4개 파일 · 기준 = **Apple 공식 8원칙 + NN/g 10 휴리스틱 + Vercel WIG**

| 진단 | 원 지적 | ✅확인 | 오탐률 | critical |
|---|---|---|---|---|
| **무료진단 결과화면** | 41 | **36** | **12%** | **10** |
| 대시보드 6화면 | 47 | 32 | 32% | 7 |

🔴 **근본 원인 5개** (개별 수정보다 이것을 고치는 게 본질 · 기획서 v3 §2):
| # | 원인 | 상태 |
|:--:|---|---|
| ⑤ | **숫자가 서로를 반박한다** | ✅ S0 로 주요 7건 해소 |
| ① | 빈 상태가 제품을 설명 안 함 | ⬜ S2 |
| ② | 파는 것과 실제가 다름 | ⬜ S1 |
| ③ | 파는 자리에서 이름·가격 숨김 | ⬜ S1·S4 |
| ④ | 내부 용어·영업이 화면을 덮음 | ⬜ S3·S4 |

⚠️ **오탐률이 12~32%다** — 진단서 항목을 **그대로 다 고치지 말 것.**
반박된 것 중엔 *"이미 코드에 해결책이 있다"*·*"제안이 지어낸 숫자를 넣게 된다"* 가 많았다.

---

## 5. 🔴 세션N-17 이 실측으로 뒤집은 것 (되살리지 말 것)

| 이전 주장 | 실측 결과 |
|---|---|
| *"PDF 실패 원인 = 66MB > Vercel 50MB 한도"* | ❌ **한도는 250MB**. 진짜 원인은 **바이너리가 배포본에 0개 함수**에만 포함(런타임 문자열 경로라 nft 가 못 봄) |
| *"`serverExternalPackages` 누락"* | ❌ **no-op** — chromium·puppeteer-core 는 Next 가 이미 자동 opt-out |
| *"Growth 결제해도 열리는 게 없다"* | ❌ **`compare`·`sources` 는 작동한다.** `alerts` 만 껍데기 |
| *"`(준비 중)` 3건이 잘못된 표기"* | ❌ **전부 정직한 표기다**(메일 발송 꺼짐·Notion 미구현). **떼지 말 것** |
| *"자동 재측정이 Scale 전용"* | ❌ Starter=주간·Growth=매일 **이미 작동 중**이었고 표에서 누락돼 **못 팔고 있었다** → 표기 추가 완료 |
| *"UI/UX 는 리서치대로 다 개선했다"* | ❌ 감사 대상이 **무료진단 1화면뿐**이었다(`billing` 문서 언급 **0회**) |
| *"코드로 할 게 없다"* | ❌ **미완 코드 11건**. ⚠️N-13 도 같은 실수 → **3번째 반복** |

🎓 **교훈: 문서를 믿지 말고 코드·DB·화면을 실측하라. 그리고 투두는 끝까지 읽어라.**

---

## 6. ⚠️ 함정 (착수 전 필독)

### 세션N-17 신규
- 🔴 **`git stash` 금지**(기존 규칙). lint 기준선 대조는 **파일을 `/tmp` 로 복사해 규칙별 비교** —
  ⚠️ `/tmp` 파일명이 kebab-case 가 아니면 `useFilenamingConvention` **오탐 1건**이 뜬다(내가 헤맸다).
- 🔴 **`audit-result.tsx` 의 M1 주석(2026-08-07)을 존중할 것** — 5축 %/괄호 분모는
  **두 대안을 이미 검토해 내린 결정**이다. 바꾸면 과거에 고친 오독이 되살아난다.
  (S0 은 그래서 **계산을 안 건드리고 설명만 추가**했다.)
- 🔴 **날짜를 계산해 넣지 말 것** — 진단 시점이 과거라 *"다음 월요일"* 을 찍으면 **없는 날짜를 지어낸다.**
- 🔴 **BotID 때문에 진단 자동 실행 불가** — Playwright(headless·실제 Chrome 채널 **둘 다**) **403**.
  `tier==="admin"` 우회는 BotID **뒤에** 있어 못 넘는다. ⚠️**BotID 를 끄거나 우회하지 말 것**(방어 1층).
  → 진단이 필요하면 👤**사람이 브라우저로** 제출.
- ⚠️ **로컬 검증 시 로케일 함정**: `localhost:3210/audit/...` 은 **영문**으로 뜬다(`/ko` 는 307).
  한국어 확인은 **`NEXT_LOCALE=ko` 쿠키 + `locale="ko-KR"` 컨텍스트**로.
- ⚠️ **DB 조회**: `packages/database/` 안에서 + **`.mts`**(top-level await) +
  `PrismaClient` 는 `./generated/client.ts` + **`PrismaNeon` 어댑터 필수**(Prisma 7 은 옵션 없이 생성 불가) +
  `neonConfig.webSocketConstructor = ws` + `.env.local` 로드.
- ⚠️ **`enginesCovered.length` = 응답 수(29)** / `enginesCoveredUnique.length` = 엔진 수(8). **헷갈리기 쉽다.**
- ⚠️ **한글 경로 glob 이 zsh 에서 깨진다**(NFD) → `ls`·python 으로 우회.
- ⚠️ **괄호 포함 경로(`(authenticated)`·`[locale]`)는 Bash·Biome 인용이 깨진다** → **디렉터리 단위**로 lint.

### 기존 함정 (계속 유효)
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요**(app·web).
- 🔴 **`apps/api` 는 죽은 앱** — cron 은 `apps/web/app/api/cron/`, 웹훅은 `apps/app/app/webhooks/`.
- 🔴 **`"use server"` 파일은 async 함수만 export** — tsc·lint 통과인데 **빌드만 실패**.
- ⚠️ **app 배포는 `.vercel/project.json` swap → 원복 필수**(trap 으로 보장). web 은 그대로 `--prod`.
- ⚠️ **QA 계정(`nayoy2+qa@`)은 측정 데이터가 없다** → 대시보드 빈 상태만 보인다.
- ⭐ **화면 검증 도구**: `python3 scripts/verify-app.py / --expect "문구"`(실제 로그인·스크린샷).

---

## 7. 👤 사용자 결정 대기 (내가 못 하는 것)

1. 🔴 **S0 배포 시점** — 지금 vs S1 묶어서(내 추천: 묶어서)
2. 🔴 **P0 실고객 지불의사 검증** — 마케터 10~15명. **불변 1순위이자 유일한 회사 병목.**
   ⚠️시뮬로 대체 불가(r=0.20). 질문지=`P0_지불의사_시뮬레이션_결과.md`
3. 🟠 **카카오페이 심사** = 답장 대기. 지금 할 일 없음.
   🔴단, VAT 정정으로 제출 스크린샷(99,000)과 실제(108,900)가 어긋남 → **먼저 움직이지 말 것**
4. 🔵 P4: 파트너 특전 정책 · KIPO 상표 출원
5. `alerts` 실제 알림 구현 = **보류**(임계값 분포 없음 · 시계열 1일). 근거 없는 임계값 발명 금지.

---

## 8. 💰 원가 방침 (유지)

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · 브라우저 검증 · 빌드 · 배포 | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인** |

---

## 9. 🔴 하지 말 것

- ❌ **표본 없는 "업계 N분위" 벤치마크**(조직 1개 = 표본 없음) · **지어낸 예시 숫자** · **임계값 발명**
- ❌ **소급 데이터 수정**(마이그레이션) — 표시층에서 고친다
- ❌ **daum 표시 필터** — 만들면 멀쩡한 근거 77건이 빈 카드가 된다
- ❌ **FAQPage 로 AI 인용 노리기**(2026-05 리치결과 폐지) · **llms.txt** · **자체 별점 `aggregateRating`**(manual action)
- ❌ **조작 수치 인용**: *"FAQPage→Perplexity +34%"* · *"YouTube 상관 0.737"* = **원 출처에 없다**
- ❌ **`(준비 중)` 딱지 제거** — 전부 정직한 표기다
- ❌ **`tracking-tight`**(Pretendard 자간 내장) · **crew 자기평가에 zod `.max()`**(리포트 전멸)
- ❌ **나무위키 직접 편집**(표시광고법)
- (전체는 `투두리스트.md` §만들지 말 것)

---

## 10. 🔴 다음 세션이 잊으면 안 되는 한 줄

> **S0(숫자 정합성)은 커밋됐지만 배포 안 됐다. 그것부터 처리하고 S1(Scale 누락·og:image·거짓표시)로 간다.**
> **판정 기준은 "예쁨"이 아니라 "고객사 직원이 3초에 이해하는가"다.**
> **그리고 문서를 믿지 말고 실측하라 — 세션N-17 에서도 7건이 틀렸다.**
