> ⛔ **폐기됨 (2026-08-08 세션N-11 종료).** 최신 인계 = **`_START_HERE_세션N12.md`**.
> 이 파일엔 실측으로 **틀렸다고 확인된 내용**이 있다(웹훅 경로 제안 `app/api/webhooks/` →
> 실제 관례는 `app/webhooks/` · "vitest 2 failed 는 기존 결함" → 지금은 29/29 통과).
> 세션N-12 문서 §4 의 "정정 목록"을 먼저 볼 것.

# 🧭 START HERE — 세션 N-11 (2026-08-08 세션N-10 종료 인계)

> ⚠️ **이 파일 "하나만" 읽으면 안 된다.** 여기는 **방향·함정·다음 순서**만 있고,
> **실제 잔여 목록은 `투두리스트.md`** 에 있다 — 세션N-10이 이걸 몰라서
> "백로그가 비었다"고 잘못 보고했다(§0). **둘 다 열 것.**
> ⛔ `_START_HERE_세션N10.md`·`세션N9`·`세션E`·`세션L`·`다음세션`은 **폐기**.
> 세션N-10은 감사 8번 + 투두 3건을 끝내고 종료. 워킹트리 깨끗 · web·app 배포 완료.

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 f7d5337 이면 세션N-10 종료 상태 그대로
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

### 🔴 이번 세션이 배운 가장 큰 교훈 — **인계문서 1개 ≠ 전체 백로그**

세션N-10 전반에 내가 `_START_HERE` 만 읽고 **"코드 백로그가 비었다"** 고 보고했다.
사용자가 *"나머지 할것들 많은데?"* 라고 지적해서 열어보니 **`투두리스트.md` 에 잔여가 따로 있었다.**

**→ 새 세션은 반드시 이 3개를 다 열 것:**
1. 이 파일 (방향·함정)
2. **`docs/_적용/투두리스트.md`** ← ⭐**실제 잔여 목록. 빠뜨리지 말 것**
3. `docs/_적용/실행백로그.md` (Day별 상세)

⚠️ **단 그 목록들도 낡았다.** 세션N-10에서 ⬜로 적힌 60·61·69·42·54·77번이
**전부 이미 구현돼 있었다**. → **착수 전 항상 코드로 실측**할 것.

---

## 1. ⭐ 다음에 할 일 (우선순위순)

### 🔴 0순위 — **결제 웹훅이 죽은 앱에 들어가 있다** (세션N-10이 만든 문제)

세션N-10이 PortOne 결제 웹훅을 `apps/api/app/webhooks/payments/route.ts` 에 구현·커밋했다
(`5a0d171`). 코드·테스트는 정상이나 **배포될 수 없는 위치**다.

**실측(2026-08-08)으로 확인한 사실:**
| 확인 | 결과 |
|---|---|
| Vercel 프로젝트 목록 | **`findable`(web) · `findable-app`(app) 둘뿐**. `api` 프로젝트 **없음** |
| `apps/api/.vercel` | **없음**(링크 자체가 안 됨) |
| `api.findable.co.kr` | **응답 000**(도메인 미존재) |
| `apps/api` 라우트 | `health` · `webhooks/payments` · `cron/keep-alive` 3개뿐 |
| 실제 도는 cron | **전부 `apps/web/app/api/cron/`** (sweep·digest·auto-refresh) |
| `PORTONE_*` env | **`findable-app` 에 3개** (web엔 0개) |
| `payments/keys` 를 로드하는 앱 | **`apps/app/env.ts`** (web은 안 씀) |

> 💡 **패턴**: 이 저장소는 `apps/api` 를 **버리고** 전부 web/app 으로 옮겨왔다.
> cron 도 원래 `apps/api` 설계였는데 실제로는 web 에 있다.

**할 일**: 웹훅 라우트를 **`apps/app/app/api/webhooks/payments/route.ts`** 로 옮긴다.
- 근거: PortOne env·`grantPlan`(Clerk)·`payments/keys` 가 전부 app 쪽이다.
- 이동 후 `apps/api/app/webhooks/payments/route.ts` 는 삭제(죽은 코드 남기지 말 것).
- ⚠️ 이동 뒤 **`PORTONE_WEBHOOK_SECRET` 를 `findable-app` env 에 추가**해야 한다
  (현재 미설정 → 라우트가 200 `not_configured` 로 조용히 닫힘. 기존 동작엔 무해).
- ⚠️ app 배포는 `project.json` swap → **원복 필수**.
- 검증 자산: 서명 9종 테스트를 세션N-10이 이미 돌렸다(로직은 검증됨). 이동만 하면 된다.
- 👤 그 뒤 **사용자가 PortOne 콘솔에서 웹훅 URL 등록 + 시크릿 발급** 해야 최종 작동.

### 1순위 — `투두리스트.md` 잔여 중 코드 항목
| # | 항목 | 메모 |
|---|---|---|
| **#68** | 주간 재측정 + 이메일 알림 | ⚠️`auto-refresh-tracking` cron 이 **이미 있다**(`0 2 * * *`) → **이메일만 붙이면 된다**. 단 "보낼 변화"가 있어야 의미 |
| **#50** | 엔진수·갱신주기 게이팅 레버 | 🔬**원가 제약 없음이 실측으로 확인됨**(아래 §3) → 이제 막는 건 원가가 아니라 **시간** |
| **#58** | 팩트정합률 전면화 + before/after 1건 | vitamin→painkiller 증거 |
| — | `audit-result.tsx` lint 부채 | 이 파일 크게 손댈 때. ⚠️`poll()` 복잡도 31(max 20)은 **기존 결함**(세션N-10이 건드리지 않음을 확인) |
| #6 | naver 어댑터 Daum식 정합 | 우선순위 낮음(실측 4/4로 이미 잘 나옴) |

### 🔴 하지 말 것 (데이터 대기)
**D4 기간선택 · D2 자동핀 · D1 알림** — `Tracking` 54행·측정 2회·시계열 1일.
지금 만들면 **작동 안 하는 버튼**이 된다. 측정 5~6회 쌓이면 셋을 한 묶음으로.

---

## 2. ✅ 세션N-10이 한 일 (커밋 7건, 전부 배포됨)

| 커밋 | 내용 |
|---|---|
| `116a333` | **감사 8번** — 가짜 1위 제거 + 분모 도입 + competition 채점식 개정 |
| `d3b95ad` | crew orchestrator 주석의 거짓 수치 정정 |
| `5a0d171` | **PortOne 결제 웹훅** ⚠️죽은 앱(`apps/api`)에 있음 → §1 0순위 |
| `63b528a` | **유닛 이코노믹스 실측** 문서 |
| `4762687` | **무료진단 히스토리** ("지난번보다 나아졌나") |
| `8acc91f`·`070a6d1`·`f7d5337` | 인계·투두 문서 갱신 |

### 🔬 감사 8번 — 인계문서의 "보류 권고"가 틀렸다
세션N-9가 *"순위 26건 중 23건이 1위 = 변별력 없음"* 으로 보류를 권했으나,
**그 23건 중 11건은 코드가 지어낸 1위**였다.
- 폴백이 *"브랜드가 앞 30% 안 + 번호줄 3개↑"* 면 1위를 줬는데 **목록 안에 브랜드가 있는지를 안 봄**
- 실측: *"나이키 **외에** 다른 러닝화"* 답변(아디다스·아식스만, 나이키 부재)이 **나이키 1위**로 저장
- 조치: 폴백 제거 · `mentionListSize`(분모) 신설 · `competition = 10 × 자리 × 경쟁규모`
- ✅**소급 안전**: 기존 71 job 점수 변동 **0건**(분모 없으면 옛 식 유지)
- ⚠️분모는 **신규 측정부터** 채워진다 → 기존 job 은 설계대로 옛 문구로 폴백

### 🔬 무료진단 히스토리 — `completed` ≠ "잘 측정됨"
- 🔒매칭키 = **이메일 + 정규화 도메인**. ⚠️도메인만 쓰면 안 된다 —
  실측 `medicube.co.kr` 한 도메인에 **이메일 15개**(남의 진단 노출).
- ⚠️`sulwhasoo.com`↔`www.sulwhasoo.com` 따로 저장돼 있어 **매칭 시에만** `www.` 제거.
  저장값은 안 건드림(24h 캐시 키가 domain).
- 🔴별건 발견: `nike.com` job 이 `completed` 인데 **28엔진 전부 error**(0점).
  직전으로 잡으면 **"+80점 개선" 거짓말** → `usable` 판정으로 망가진 회차 건너뜀.
- ✅프로덕션 브라우저 검증: `지난번보다 -6점 / 이전 75점 · 2번째 측정` · 첫 측정 job 은 **미표시**

---

## 3. 💰 유닛 이코노믹스 (📕`유닛이코노믹스_실측_2026-08-07.md` · **재실측 금지**)

**진단 1건 = 평균 87원 · p50 181초.**

| 엔진 | 건당 | 비중 |
|---|---|---|
| claude | 8.59원 | **50.5%** |
| chatgpt | 7.09원 | **41.7%** |
| **hyperclova** | **0.30원** | **1.7%** |
| gemini·daum | 0원 | 0% |

- 🔬**한국 엔진은 거의 공짜** → 차별점을 원가 부담 없이 유지 가능
- **매일 측정해도 원가율 2.6%**(Starter 99,000원) → 게이팅 레버에 원가 제약 **없음**
- 🔴**진짜 리스크 = 시간**: 최대 **298초 vs 함수 상한 300초(여유 2초)**.
  프롬프트 4→5개 job 이 곧바로 최댓값 → **프롬프트 늘리면 시간이 먼저 터진다**
- ⚠️단가표는 2026-07 **추정**이며 청구서 미보정. claude·chatgpt 가 92%라 이 둘이 틀리면 전체가 틀림

---

## 4. 👤 사용자 몫 (내가 못 하는 것)

1. ⭐**실고객 10~15명 지불의사** — 회사 **단일 장애점**. 시뮬 대체 불가(r=0.20)
2. 🆕**PortOne 콘솔 웹훅 등록 + 시크릿 발급**(§1 0순위와 세트)
3. **카카오페이 정기결제 CID**(일반결제와 별도)
4. **로그인해서 신규 화면 눈으로 확인** — 세션N-9·N-10 신규 화면(헤더 상주·브랜드 칩·
   질문별 성적·순위 스파크라인·"N개 중 M번째"). 숫자는 DB 교차대조로 맞춰뒀고 볼 건 **배치·가독성**
5. `/compare`·프롬프트 마법사 직접 써보기(만들었으나 써본 기록 없음)

---

## 5. ⚠️ 함정 (착수 전 필독)

- 🔴 **`git stash` 금지** — `stash@{0}` 에 2026-07 낡은 stash. pop 하면 17파일 충돌.
  변경 전/후 대조는 **`git show <커밋>:<경로>`** 또는 **`git worktree add <경로> HEAD --detach`**
  (+`node_modules` 심링크). 세션N-10이 lint·vitest 기준선을 이 방법으로 확정했다.
- 🔴 **로컬 dev는 `VERCEL=1` 필요**(app·web) — 없으면 Bun 에서 Neon 핸드셰이크가 깨져 500
- 🔴 **Prisma TCP 차단(P1001)** → 스키마 변경은 **neon HTTP 직접 DDL**
  (세션N-10이 `Tracking.mentionListSize` 를 이 방법으로 추가)
- 🔴 **`Tracking`·`Annotation`·`ActionCompletion` 은 org 컬럼이 없다** →
  반드시 `apps/app/lib/db/scoped.ts` 헬퍼로만 접근
- 🔴 **`AuditJob.result` 에는 `rawResponse` 가 없다**(`excerpt` 만). 원문 재실행이 필요하면
  **`Tracking`** 을 쓸 것. 세션N-10이 이걸 모르고 2,026응답 재실행해 "0건"이라는 무의미한
  결과를 냈다가 자체 적발했다.
- 🔴 **토큰 이름을 상상해서 쓰지 말 것** — CSS 폴백 때문에 화면은 멀쩡해 보인다.
  실존 토큰은 `packages/design-system/styles/globals.css` 에서 확인
- ⚠️ **ultracite 는 `(authenticated)` 괄호 경로를 못 읽는다** → 해당 디렉터리로 `cd` 후 실행
- ⚠️ **app 배포는 `project.json` swap → 원복 필수**. www 는 swap 불필요
- ⚠️ **vitest 2 failed(`sign-in`·`sign-up`, Clerk)는 기존 결함** — HEAD 워크트리에서 동일 재현 확인
- ⚠️ **검증 hook 오탐 2종**: "모델슬러그 하이픈"(세션 제목에 반응) · "generateObject removed in v6"
  · route 파일 "no observability"(이미 log 있음)

---

## 6. 📕 읽을 것

1. ⭐**`투두리스트.md`** — 실제 잔여 목록(§0 교훈)
2. `유닛이코노믹스_실측_2026-08-07.md` — 원가·시간 (재실측 금지)
3. `UIUX_감사결과_3중_2026-08-07.md` — 미수정 항목 (재감사 금지)
4. `_리서치원본_UIUX_2026-08-06/` (6파일 1,626줄) — **재리서치 금지**(검색 200회 소진)
   - `05:118-123` 유료 축 정의(**가장 중요**)

**흔들리면 안 되는 축**: 유료가 이겨야 할 축 = **"시간·비교·알림"**.
⚠️진실거울(무료 aha)을 대시보드로 복사하는 건 **오답** · 유료 축을 먼저 채우고 무료 축소는 맨 마지막.

---

## 7. 🎓 세션N-10 교훈

1. 🔴**인계문서 1개만 읽고 "백로그 비었다"고 하지 말 것**(§0). 사용자 지적으로 발견.
2. **"보류 권고"도 전제를 실측할 것** — 감사 8번 보류 근거가 숫자는 맞고 해석이 틀렸다.
3. ⚠️**내가 설계한 것도 경계 테스트로 깰 것** — 상대위치만 쓴 1차 설계는
   "2개 중 1위=50개 중 1위=만점"이라 **고치려던 문제가 그대로 남았다**. 합성 경계 8종으로 자체 적발.
4. **분모는 `matches.length` 가 아니라 번호 최댓값** — 한 답변에 "1.2.3."이 두 번 나오면
   6이 되어 "6개 중 1번째"라는 새 거짓말(나이키/Gemini 실측).
5. **`completed` 를 "성공"으로 읽지 말 것** — 28엔진 전부 error 인데 completed 인 job 이 있다.
6. **템플릿 잔재를 의심할 것** — 결제 웹훅이 next-forge **Stripe 보일러플레이트** 그대로였고
   (PortOne 아님) **한 번도 동작한 적 없는 죽은 코드**였다. `apps/api` 자체가 죽은 앱인 것도 같은 계열.
7. **lint 증감은 워크트리 기준선과 대조** — 파일이 커지면 없던 진단이 보고되기도 한다.
   세션N-10에서 "+1"이 내 탓인 줄 알았으나 **내 파일만 되돌려 재측정**하니 5→5로 무관했다.
