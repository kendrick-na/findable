> ⛔⛔ **폐기됨 — 최신은 `_START_HERE_세션N25.md`** (2026-08-12 세션N-24 종료).
> ✅ 여전히 유효한 것: §1(엔진 수 진상) · §3~§5-2(실측 기각 근거) · §6(함정).

# 🧭 START HERE — 세션 N-23 (2026-08-12 **S7 1~4차 완료** 인계)

> 🔴🔴 **세션N-24 정정 (2026-08-12)**: 아래 §0-1·§5-3·§8 의 *"코드는 할 게 거의 없다 ·
> S7 사실상 소진"* 은 **틀렸다.** 4개 문서 전량 통독 + 실측 결과 **코드로 지금 가능한 일 11건**이
> 남아 있었다(`실행백로그.md` 에만 있던 5건 + 신규 실측 3건 포함).
> → ⭐ **남은 일의 단일 진실 = `_투두_단일진실_N24.md`. 그것부터 열 것.**
> ✅ 이 파일에서 **여전히 유효한 것** = §1(엔진 수 진상) · §3~§5-2(실측 기각 근거) · §6(함정).

> ⚠️ **이 파일 + `_투두_단일진실_N24.md` + `UIUX_대개선_설계_v3_2026-08-10.md` 를 열 것.**
> ⛔ `_START_HERE_세션N22` 이하 **전부 폐기**.

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 ca30df0 이면 이 인계 시점 그대로
git status --short        # 비어 있어야 정상
```

---

## 0-1. 🔴🔴 **새 세션이 가장 먼저 할 일 — 코드로 뛰어들지 말 것**

**S0~S6 + S7(1~4차)이 전부 끝났다. 코드 트랙은 수확체감 구간이다.**
진단서 41건 중 **7건은 이미 고쳐져 있었고, 4건은 실측으로 기각**됐다.
남은 S7 2건은 **large 1개 + 미발현 1개**(§5-3)라 지금 집을 이유가 약하다.

🔴 **그러니 새 세션은 "다음 투두 뭐지?"로 시작하지 말고, 👤사용자에게 이걸 먼저 물어라:**

> **"코드는 지금 할 게 거의 없습니다. 회사 병목은 ①P0 실고객 지불의사 검증
> ②KIPO 상표 출원 두 개인데, 둘 다 사람이 움직여야 하는 일입니다.
> 제가 도울 수 있는 건 **질문지 다듬기 / 상표 출원 서류 정리 / 인터뷰 대상 리스트**
> 정도입니다. 무엇부터 할까요?"**

⚠️ 사용자가 *"전략적으로 알아서 진행해"* 라고 답해도 **코드부터 파지 말 것.**
그 지시는 지금까지 4세션 연속 있었고 그 결과가 지금 상태다 — **남은 건 코드가 아니다.**
(사용자가 명시적으로 §5-3 을 지목하거나 새 요구를 주면 그때 코드로 간다.)

---

## 1. 🔴🔴 이번 세션 최대 성과 — **엔진 수 "진상"을 확정했다** (재조사 금지)

진단서·에이전트가 *"7↔8 불일치, 28곳을 고쳐라"* 라고 했는데 **그게 오답이었다.**
**두 숫자는 서로 다른 것을 센다:**

| 상황 | 실제 엔진 | 근거 |
|---|:--:|---|
| **본류 무료진단(기본 `both`)** | **7** | `runner.ts` `DEFAULT_7` · 주석 `:306` *"본류 audit 는 항상 7 엔진만 호출한다"* |
| 한국어만 | **7** | 〃 |
| 🔴 **영어만** | **4** | `runner.ts` `GLOBAL_4` — 한국 검색엔진에 영어 질의가 무의미해 naver·daum·hyperclova 제외 |
| 결과 화면에 8이 뜨는 경우 | **8** | 고객이 **네이버 브리핑 버튼을 따로 누른 뒤**(`briefing-runner.ts`, on-demand) |
| `ENGINES` 정의 목록 | 9 | `packages/ai/lib/engines/types.ts:33` — 정의일 뿐 전부 도는 게 아니다 |

⭐ **결론: 랜딩·요금제의 "7개"는 틀리지 않았다.** 28곳 일괄 수정은 **하면 안 되는 일**이었다.
🔴 **진짜 구멍은 하나였다 — 「영어만」을 고른 고객이 7을 약속받고 4로 측정된다.** → 고쳤다.

---

## 2. ✅ S7 1차 완료 (커밋 `505ddce` · `1a11e3f` · app·web 배포 READY)

| 묶음 | 내용 | 상태 |
|---|---|---|
| **S7-a** 숫자·돈 | ①「영어만」 선택지에 **AI 4곳** 명시 + 빠지는 엔진 설명 ②**Enterprise VAT 안내 누락**(연 3천만원에 300만원이 계약 때 튀어나옴) ③`연 ₩30M~` → **`연 3,000만원~`**(app·web) ④히어로·단계라벨의 무조건 "7개" | ✅ |
| **S7-b** 죽은 화면 | `/history` 가 서버 컴포넌트라 **측정이 끝나도 영원히 「측정 중」** → 진행 중일 때만 15초 폴링 + **상한 40회** + 수동 새로고침 | ✅ |
| **S7-c** 표시·조판 | 처방 제목 12px회색 → **h2 text-xl/2xl**(우리가 이기는 섹션이 가장 약했다) · 내부용어 **"다른 층"** 교체 · **`↗ -46%p`** 화살표 제거(방향 오독) · 업종 드롭다운 `w-fit` → 전폭 | ✅ |

### 📊 검증 수준
- app lint **0** · tsc **0**(app·web) · build **양쪽** · 테스트 **133/133**(128→+5)
- **뮤테이션 2건** — 낭비폴링 가드·상한 가드를 죽여 실제 실패 확인
- 🔴 **web lint 는 기존 5건 유지**(내가 늘리지 않았다. 한때 7로 늘렸다가 중첩 삼항을 걷어냈다)
- 눈확인 **1440px + 390px 양쪽**

---

## 3. 🔴 이번 세션이 뒤집은 것 (**되살리지 말 것**)

| 이전 기재 | 실측 결과 |
|---|---|
| *"엔진 수 7↔8 불일치 → 28곳 통일"* | ❌ **오답.** 두 숫자는 다른 것을 센다(§1). 진짜는 **영어전용 4개** 1건 |
| *"S7 = 나머지 medium/low"* (범위 불명) | 🔬 **41건 전건 실측** → **7건은 이미 고쳐져 있었다** |
| *"S6-c #3 업종 드롭다운 = 재확인 필요"* | ✅ 진짜였다 — `SelectTrigger` 기본이 **`w-fit`**(N-22 때 "오탐"이라 한 판정이 틀렸다) |

⚠️ **N-22 에서 내가 #3 을 "오탐"이라고 적었는데 그 판정이 틀렸다.** 그때는 `w-fit` 을
`grep` 으로만 찾아 **디자인 시스템 기본값을 안 봤다**. 이번에 `select.tsx` 를 열어 확인했다.
🎓 **"내 파일에 없다"는 "적용 안 된다"가 아니다 — 상속·기본값을 봐야 한다.**

---

## 4. 🐛 이번 세션에 **테스트·스크린샷이 잡은 내 버그 3건**

1. 🔴 **effect 의존성에 `timedOut` 을 넣어 폴링이 되살아났다** — 상한에 닿아
   `setTimedOut(true)` → 리렌더 → effect 재구독 → 타이머 재장착. 테스트가 **60회 vs 40회**로 잡았다.
2. 🔴 **테스트 mock 이 `useRouter: () => ({ refresh })`** 였다 — 매 렌더 **새 객체**라
   의존성이 계속 바뀌어 같은 무한 폴링을 유발. 실제 Next router 는 안정적이므로 **모의도
   같은 객체를 돌려줘야** 현실과 맞는다.
3. 👁️ **히어로가 선택과 무관하게 "7개 AI"를 약속** — 폼에서 「영어만·AI 4곳」을 고른
   바로 위에서 7을 말해 **같은 화면에서 숫자가 반박**했다. **코드로는 안 보였다**
   (히어로=서버 컴포넌트, 폼=클라이언트, 다른 파일) → **모바일 스크린샷이 잡았다.**

⭐ 그리고 **벽시계로 상한을 재면 테스트가 불가능하다**(가짜 타이머는 `Date.now()` 를 안 돌린다)
→ **틱 수로 센다**(`MAX_POLLS`). 검증 가능성이 설계를 바꾼 사례.

---

## 5. ✅ S7 2차도 완료 (커밋 `f6ed9bf` · app·web 배포·눈확인)

| 항목 | 무엇 | 상태 |
|---|---|---|
| **www 이탈 복귀불가** | 결과 링크가 대시보드를 벗어나는데 돌아올 길이 없었다 → **새 탭 + "새 탭" 표기**. 이력·브랜드 카드 **둘 다**(한쪽만 고치면 같은 링크가 화면마다 다르게 움직인다) | ✅ |
| **인용 등급 범례** | 칩의 자사·매체·커뮤니티가 **뜻 없이** 색만 달랐다 → **"내가 고칠 수 있나"** 축으로 한 줄 | ✅ |
| **임팩트·노력 척도** | `4/5` 만 있고 5의 뜻이 없어 카드 3장 비교 불가 → 설명 툴팁 | ✅ |
| **빈 상태 카드 폭** | 전폭(1130px)이라 "만들다 만 화면" → `max-w-2xl mx-auto`. **4화면 동시 개선** | ✅ |
| **영어 혼재** | `리포트 Export`→`리포트 내보내기` · `STEP 1`→`1단계` | ✅ |

### ⛔ 하지 않기로 **판정한** 2건 (실측 근거 — 되살리지 말 것)
1. 🔴 **결제 상태 3줄**(다음 결제일·카드 끝 4자리) — `Organization` 스키마에 **두 필드가 없다**
   (`planExpiresAt` 은 만료일이지 결제 예정일이 아니다). 없는 데이터를 화면에 만들 수 없고,
   PortOne 조회 신설은 **카카오페이 승인 대기 중**에 할 일이 아니다. (해지 버튼은 **이미 있다**.)
2. 🔴 **잠금 카드 `min-h-[60vh]`** — 잠금 3화면(`alerts`·`sources`·`compare`)이 **전부
   `preview` 를 넘겨** 그 분기를 안 탄다 = **닿지도 않는 죽은 코드**. 게다가 이 자리는 높이를
   만져 **카드 상단이 잘린 전력**이 있다(N-16 주석).

---

## 5-1. ✅ S7 3차도 완료 (커밋 `3b50c85`·`2f3c8b7`·`b59d3f9` · web 배포·눈확인)

| 항목 | 무엇 | 상태 |
|---|---|---|
| **순위 표기 단일화** | 같은 페이지가 순위를 두 방식으로 말했다(KPI=분모 밝힘 / 격차카드=`평균 3.2위` 맨 숫자). **"3.2위"는 5개 중인지 50개 중인지 뜻이 다르다** → `@repo/audit/rank-label` 신설(계산 1곳) | ✅ |
| **가정 7필드 근거** | 곱해져 "예상 손실"이 되는 값들의 출처가 **한 글자도 없었다** → 칸마다 출처 한 줄(Pew·Semrush·Bain·네이버 CPC) | ✅ |

🔬 **실측으로 드러난 사실**: 분모(`mentionListSize`)는 **엔진 응답에 원래 있던 값**인데
격차 카드가 그 필드를 **안 읽고 있었다**. 데이터가 없어서가 아니라 안 쓴 것이다.

⚠️ **분모가 화면에 안 보여도 버그가 아니다** — 샘플 회차(`d732a13a…`)는 29개 응답 **전부**
`mentionListSize` 가 null 이다(세션N-10 이전 측정분은 소급 없음). **없으면 감추는 게 정상**이고
그 판정을 테스트 8건 + 뮤테이션으로 고정했다. `rank-label.ts` 헤더에도 못박아 뒀다.

---

## 5-2. ✅ S7 4차도 완료 (커밋 `ed41515`·`de54576` · app·web 배포·눈확인)

| 항목 | 무엇 | 상태 |
|---|---|---|
| **`/history` 총 건수** | `take: 50` 인데 총 건수도 상한도 없어 **51번째부터 말없이 잘렸다** → "지금까지 N번" + 잘렸을 때만 "최근 50건만". 목록·카운트가 **같은 스코프 상수**를 본다 | ✅ |
| **감성 배지 설명** | "중립"이 왜 개선 대상인지 없었다 → *"밋밋해서 안 골라진다"* 를 `title` 로 | ✅ |

### 🔬 이번에도 **진단서 지적 2건을 기각**했다 (실측 근거 — 되살리지 말 것)
1. ⛔ **"처방 2건이 사실상 같으니 병합"** → **틀렸다.** 실측한 두 건은 **서로 다른 질문**이다
   (영어 `"Top 5 popular brands…"` / 한국어 `"…인기 브랜드 5가지 추천해줘"`). 질문이 다르면
   고칠 페이지도 다르다 → 병합은 **정보 손실**.
   🔴 게다가 완료 표시 정체성이 `@@unique([brandId, kind, target])` 이고 `target` = **질문
   원문**이라, 합치면 **고객이 이미 완료한 항목이 미완료로 되살아난다**. 기각 사유를
   `packages/audit/actions.ts` 주석에 남겼다.
2. ⛔ **"감성 배지가 전부 중립이라 변별력 0"** → **데이터가 실제로 그렇다**
   (라이브 실측 **중립 18 / 긍정 3 / null 8**). 없는 차이를 만들면 그건 조작이다.
   (null 8건은 호출부 `cited && engine.sentiment` 가 이미 막아 "중립"으로 표시되지 않는다.)

### 🎓 이번 세션의 방법론 교훈
**"라이브에서 확인할 수 없는 경로는 순수 함수로 빼서 테스트로 고정한다."**
`/history` 건수 문구를 서버 컴포넌트 삼항에 뒀더니 **QA 계정이 0건이라 "잘림" 경로를
눈으로 볼 방법이 없었다** — "확인했다"고 말할 수 없는 상태였다. → `historyCountLabel` 로
빼고 3경우를 고정(뮤테이션 검증 완료).

---

## 5-3. ⭐ 다음 착수 = **S7 잔여 2건**

1. **`/history/[jobId]` 내재화**(large) — 새 탭은 단기 처방이고, 결과를 앱 안에서 보는 게 본안.
   ⚠️ 결과 화면은 **공유 링크(비로그인)도 받는다**(`?shared=1`) → 앱으로 옮기면 그 경로를
   어떻게 유지할지부터 설계해야 한다.
2. ⚪ 배지 배타 조건(`추천`↔`현재`) — **유료 고객이 생기기 전엔 미발현**(지금 고칠 이유 없음)

📌 **S7 은 사실상 소진됐다.** 남은 1번은 large 이고 2번은 미발현이라, 다음 세션은
**S7 을 이어가기보다 §7 의 👤사용자 결정 대기 항목**(P0 지불의사·KIPO 상표)이
회사 병목이라는 점을 먼저 짚어주는 게 맞다.

---

## 6. ⚠️ 함정 (착수 전 필독)

### 세션N-23 신규
- 🔴 **엔진 수는 재조사하지 말 것** — §1 이 확정본이다. "7"을 8로 바꾸는 수정은 **오답**.
- 🔴 **`Date.now()` 기반 상한은 테스트로 검증할 수 없다** → 틱·횟수로 세라.
- 🔴 **테스트 mock 객체를 매 렌더 새로 만들지 말 것**(effect 의존성이 흔들려 헛버그가 난다).
- 🔴 **디자인 시스템 기본값을 보라** — `SelectTrigger` 는 `w-fit` 이 기본이다.
  내 파일에 클래스가 없다고 "기본이 전폭"이 아니다.
- 🔴 **biome 복잡도 20 상한 + 중첩 삼항 금지** — 라벨 분기는 `let` + `if/else` 로 미리 계산.
  suppression 이 필요하면 선례를 따를 것(`copilot-chat.tsx:142`).
- ⚠️ **DB 직접 조회는 여전히 막힌다**(한글 NFD 경로 + Prisma CJS). 스키마 확인은
  **라이브 API 응답**이나 코드 판독으로. 시간 낭비하지 말 것.

### 기존 함정 (계속 유효)
- 🔴🔴 새 `.test.tsx` 는 **`@vitest-environment jsdom` 도크블록 필수** → **단독 실행으로 먼저** 검증
- 🔴 `apps/web` 에는 **테스트 러너가 없다** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서 검증
- 🔴 **검사 스크립트가 UI 문구를 하드코딩하면 올바른 변경을 막는다**(N-22 에 겪음).
  화면 문구를 바꾸면 **`scripts/verify-app.py` 도 함께 볼 것**
- 🔴 **`verify-app.py` 는 1440px 고정** — 모바일은 **검사된 적이 없다**. 화면 변경 시 **두 폭 다** 볼 것
- ⚠️ 요금제 문구는 **app·web 둘 다** · ⭐ **app 은 `lang="ko"`**
- ⚠️ 진단 결과 페이지에 **`h1` 추가 금지** · 크롤러용 텍스트를 **`sr-only` 로 숨기지 말 것**
- 🚧 **로컬 dev 로그인 불가**(Clerk 개발 인스턴스) → 눈확인은 **배포 후** `verify-app.py`
- 🔴 **cron 은 UTC** · **`x-vercel-cron` 폴백 부활 금지** · 🔴 **`apps/api` 는 죽은 앱**
- 🔴 로컬 dev·build 는 **`VERCEL=1`** · **`"use server"` 파일은 async 함수만 export**
- ⭐ **app 배포**: `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app`
  **web 배포**: `--project findable` (둘 다 **루트에서**)
- ⚠️ `app.findable.co.kr/favicon.ico` **404** = 기존 결함·기능 영향 없음

---

## 7. 👤 사용자 결정 대기 — **여기가 지금 진짜 남은 일이다**

### 1. 🔴🔴 P0 실고객 지불의사 검증 — **불변 1순위 · 유일한 회사 병목**
- 대상 = **마케터 10~15명**(실제로 만나서 물어야 한다)
- 📕 질문지는 **이미 있다**: `P0_지불의사_시뮬레이션_결과.md`
- ⛔ **시뮬레이션으로 대체 불가**(r=0.20 · [[reference_llm_persona_simulation_limits]]).
  *"비관적으로 돌리면 보수적 추정 아니냐"* → **아니다. 노이즈다.**
- 🤖 **내가 도울 수 있는 것**: 질문지 다듬기 · 인터뷰 대상 리스트업 ·
  거절 사유 사전 정리 · 인터뷰 후 녹취 정리/분석
- 🚫 **내가 못 하는 것**: 사람을 만나는 것 자체

### 2. 🔵 KIPO 상표 출원 — **시간이 불리하게 간다**
- 근거 = `findableapp.com`(오스트리아 **동일 이름 GEO 회사**) 충돌 + 한국 **선출원주의**
- 🤖 **내가 도울 수 있는 것**: 출원 서류 항목 정리 · 지정상품 분류(제9·35·42류 후보) 조사 ·
  기존 등록 상표 검색 정리
- ⚠️ 실제 출원·수수료 납부는 👤

### 3. 🟠 카카오페이 답장 대기 — **먼저 보낼 것 없음**
4차 회신 발송 완료. 승인 오면 실행카드 §"되돌릴 것" 0~5번(라이브 채널키·결제예약 연결·
"첫 회만 결제" 문구 삭제).

### 4. 🔵 보류
P4 파트너 특전 정책 · `alerts` 실제 알림(임계값 분포가 없어 **근거 없는 임계값 발명 금지**)

---

## 8. 🔴 다음 세션이 잊으면 안 되는 것

> **🔴 첫 행동 = §0-1 을 읽고 👤에게 "코드 말고 P0/KIPO 중 뭘 도울까요"를 먼저 물을 것.**
> **S0~S6 완료 · S7 1·2·3·4차 완료·배포됨. S7 은 사실상 소진(§5-3 잔여 2건).**
> **⛔ "하지 않기로 판정한 4건"을 다시 집어들지 말 것 — 전부 실측 근거가 있다:**
>   결제상태 3줄(스키마 없음) · 잠금카드 `min-h`(죽은 코드) ·
>   처방 병합(다른 질문 + 완료표시 파괴) · 감성 배지 변별력(데이터가 실제로 중립)
> **⚠️ 순위에 분모가 안 보이는 건 정상 동작이다(§5-1) — "고치러" 오지 말 것.**
> **🎓 라이브에서 확인 못 하는 경로는 순수 함수로 빼서 테스트로 고정하라(§5-2).**
> **🔴 엔진 수는 §1 이 확정본 — 재조사·일괄수정 금지.**
> **🎓 진단서는 낡는다 — 41건 중 7건이 이미 고쳐져 있었다. 착수 전 건별 실측.**
> **🎓 "내 파일에 없다" ≠ "적용 안 된다" — 디자인 시스템 기본값을 열어라.**
> **👁️ 스크린샷이 코드로 못 보는 것을 잡는다 — 이번엔 서로 다른 파일의 숫자 모순.**
> **👁️ 그리고 뷰포트도 가드다 — `verify-app.py` 는 1440px 고정, 모바일은 따로 봐야 한다.**
