# 🧭 START HERE — 세션 N-34 (2026-08-16 종료 인계)

> ⛔ `_START_HERE_세션N33` 이하 **전부 폐기**.
> ✅ **v4 소진** — F · G · H · A · B · D 완료. **커밋 12개** · ⚠️ **미배포**(N-31부터 **24커밋**)
> 🔴 **I(탭 재편)·C(AI별)는 기각/보류** — 데이터·정책이 막는다(코드 문제 아님).
> 🎯 **테스트 377/377** · web·app tsc 0

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3     # c2e5c01 · cd032c3 · 19339fb 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 377/377
```

**상태**: 테스트 **377/377**(N-33 334 → **+43**) · web·app tsc **0** · ⚠️ **미배포 24커밋**

📕 **먼저 열 것** = `docs/_적용/_투두_단일진실_N34.md` ← **남은 일의 단일 진실**
📕 설계 근거 = `재설계안_v4_전면_2026-08-15.md` · 실측 = `_실측_0결과_분포_N34.md`

### 🔴 다음 세션이 할 일 (우선순위)
1. 🔴 **배포** — 24커밋이 고객에게 안 갔다. 👤 지금은 **보류 지시 중**
2. 🔴 **배포 후 눈확인 부채 3건** — G-1·G-2·D 를 **실제 대시보드로 못 봤다**
   (로컬은 Clerk 개발 인스턴스라 로그인 구조적 불가). 특히 **로딩 상태는
   느린 회선 스로틀링**으로 실제로 뜨는 걸 봐야 완결된다.
3. 👤 **유료 경계 결정** — 순위 카드(`/compare`)·「AI별」을 무료로 풀지 = 가격 정책
📕 🆕 **실측 근거**: `docs/_적용/_실측_0결과_분포_N34.md` ← **§H·§I 전제를 뒤집은 문서**

---

## 1. ⛔ 절대 금지 (N-34도 그대로 지켰다)

| 대상 | 이유 |
|---|---|
| 🔴 **`packages/payments/*`** | **다른 세션이 미커밋 작업 중**. 카카오페이 심사 중 |
| 🔴 **트라이얼·결제·전환이벤트** | 👤 지시: *"기획에 보존하되 **반영은 하지 말아줘**"* |
| 🔴 `checkout/success:51-53` | `테스트 결제` 문구 — 결제트랙이라 손대지 말 것 |
| 🔴 `ultracite` 폴더 단위 | **대괄호·괄호 경로를 못 읽는다**(`[locale]`·`(authenticated)`) → tsc 로 대체 |
| 🔴 `git stash pop` | 7월 백업 stash 생존 |

---

## 2. ✅ 세션 N-34가 한 것

| 커밋 | 내용 |
|---|---|
| `44edaf2` | **F** — 지표 사전 신설(`packages/audit/metric-dictionary.ts`) + 화면 4곳 연결 |
| `7bbe478` | **F** — 엔진 권역 사설 복제본 제거(`filterByRegion` 단일화) |
| `4bc27bc` | **H(축소)** — 「모름」 카드 0 결과 3요소 |
| `e8337b0` | **G-1** — 감성 정직화(화면이 상태를 좋게 반올림하고 있었다) |
| `3cd65a7` | **G-2** — 감성 섹션 신설 + 카드 재연결 |
| `e4af992` | 죽은 홈 컴포넌트 4개 + 사전 키 5종×7언어 **-833줄** |
| `9ba3acf` | **F-⑤** 「인용됨」→「우리를 말함」(한 화면 용어 충돌) |
| `065204a` | **A·B** `/` 이력 중복 제거 · 고아화면 2개(삭제/연결 **정반대 판정**) |
| `19339fb` | **F-④** `sources-board` 용어 잔존 2건 |
| `cd032c3` | **감사** 긍정 비율 **3벌 단일화** + A 회귀 기록 |
| `c2e5c01` | **D** 로딩 상태 신설(v4 **P0-2** — 탭 재편에 묶여 멈춰 있었다) |

### 2-c. G — **탭을 만들기 전에 데이터를 감사했다**

🔴 **G-1: 화면이 상태를 좋게 반올림했다.** `sentimentTier` 가 부정은 비중으로 판정하는데
**긍정만 `positive > 0`** 이라 1건만 있어도 「우호적」이었다.
실측 브랜드 3개 전량이 **긍정 12~15%(=85%+ 중립)인데 셋 다 「우호적」** →
같은 3할 경계를 긍정에도 적용(새 임계값 발명 안 함). 이제 셋 다 「중립적」.
그리고 `별로` 가 **조사**(`용도별로`·`목적별로`)를 부정으로 세고 있었다 →
실측 4건 전부 조사형·진짜 부정 0건이라 키워드에서 제거.

🔴 **G-2: 「좋게 말하나?」 카드가 약속을 배신하는 링크였다.**
`/sources` 로 갔는데 **그 화면엔 감성이 한 줄도 없다**(`grep` 0건) →
무료 숫자를 눌렀더니 **딴 주제 결제 벽**. `/actions`(무료) 섹션으로 재연결 + `locked` 제거.

⭐ **경쟁사 4곳 실측이 「독립 화면 신설」을 기각했다** — Peec(3대 지표 한 줄)·
Profound(탭)·Otterly(등장률과 한 덩어리)·Scrunch(타일). **감성만의 화면은 0곳.**
→ 라우트를 늘리지 않았다.

⚠️ **부정은 여전히 전 데이터셋 0건**이다(분류기 한계). 화면이 그 한계를 밝힌다 —
"부정 0 = 문제 없음"으로 읽히면 못 잰 걸 좋은 소식으로 파는 것.
진짜 해법은 **v1.5 정밀 분석**(별개 트랙).

### 2-a. F — 지표 사전
- **분모 축 3종 명명**(응답 / 고유엔진 / 성공행) ← v4 가 지정한 1순위 임무
- 🔒 **계산하지 않는다.** measurement-coverage·geo-score·rank-label·market-scope 가 계속 단독 담당
- ⚠️ **이름을 발명하지 않았다** — 화면에 이미 있던 말을 골라서 **문구 변경 0**
- 연결: 순위 카드 **방향 표식**(`낮을수록 좋음`) 신설 · `truth-mirror` truthy 버그 ·
  `sources-board` 분모 0 → `—` · 한 화면 두 이름(`언급률`↔`등장률`) 통일

### 2-b. H — **범위를 실측으로 바꿨다**
📕 근거 = `_실측_0결과_분포_N34.md`. 전면 「0 결과 화면」은 **만들지 않았다**(대상 0건).
대신 **엔진 카드 단위**로 3요소를 넣었다(실제로 9% 회차에 뜬다).

---

## 3. 🔴 실측이 뒤집은 것 — **7세션 연속**

| 이전 기록 | 실측 |
|---|---|
| v4 §4-a-1 ④ *"`measurement-coverage.ts` 운영코드 0건"* | ⚠️ **앱을 잘못 짚었다.** `apps/web` 은 운영 3곳에서 쓴다. 안 쓰는 건 `apps/app` |
| v4 §4-a-1 ③ app `AI N곳에 물어` | ✅ **이미 고쳐져 있었다**(`4993b0c`) |
| v4 §H 「0 결과」 대상 | 🔴 **0건 / 95건**(전수). 전면 화면은 낭비 |
| v4 §I 탭 재편 | 🔴 **Tracking 75행 · 브랜드 3개**. 빈 탭을 만드는 일 |

🔴 **`apps/app` 이 성공행 축을 쓰는 건 버그가 아니다** — `Tracking` 이 실패 행을
저장 안 해서 **구조적으로** 그렇게 된다(`dashboard-data.ts:376-380` 이 명시).
→ **고칠 대상이 아니라 이름 붙일 축**이라 사전에 `successRow` 로 넣었다.

---

## 4. ⬜ 다음 할 일

| 순 | 묶음 | 규모 | 상태 |
|:--:|---|:--:|:--:|
| A·B·D·E | — | — | ✅ N-32·33 |
| **F** | 지표 사전 | M | ✅ **N-34** |
| **H** | 0 결과 3요소 | S | ✅ **N-34**(축소) |
| **G** | 감성 정직화 + 섹션·재연결 | M | ✅ **N-34**(G-1·G-2) |
| ~~I~~ | 탭 재편 | L | ⏸️ **보류** — 데이터 부재. **P0 선행** |
| — | ⛔ 트라이얼·결제 | L | ⛔ 심사 후. 기획만 보존 |

### 🔴 v4 는 **소진됐다** — 남은 건 코드 밖이다
| 순 | 무엇 | 성격 |
|:--:|---|---|
| 1 | 🔴 **배포 24커밋** | 👤 **보류 지시 중** |
| 2 | 🔴 **눈확인 부채 3건**(G-1·G-2·D) | **배포 후에만** 가능 |
| 3 | 유료 경계(순위카드·「AI별」) | 👤 **가격 정책** |
| 4 | 🔴 **P0 실고객·유통** | 사업 — Tracking 브랜드 **3개**가 모든 시간축 기능의 뿌리 |

⛔ **기각·보류된 것**(되살리지 말 것): I 탭 재편(데이터 없음) · C 「AI별」 분리
(이미 `sources-board:151` 에 있고, 무료 쪽엔 G-2 `byEngine` 이 있다) ·
전면 0결과 화면(대상 0건) · 랜딩 정렬(오탐 확정)

---

## 5. 🔴 이번에 확정된 규율

1. 🔴 **`curl` 로도 부족하다** — 결과 페이지는 **클라이언트 렌더**라 curl 이 `Loading` 만 받는다.
   **Playwright 로 브라우저를 띄워야** 진짜 화면이 보인다.
2. 🔴 **화면을 만들기 전에 그 상태가 실제로 나는지 DB 로 세라.**
   H 는 전수 조사에서 **대상 0건**이 나와 범위가 바뀌었다. 안 셌으면 헛일할 뻔했다.
3. 🔴 **없는 앵커로 링크를 만들지 말 것.** 결과 페이지 `id=` 는 `g-good`·`g-warn`·`g-bad` **셋뿐**.
4. 🔴 **가드는 문구가 아니라 계약을 검사한다.** 뮤테이션으로 **무는지 확인**할 것
   (이번에 5종 확인: 방향 뒤집기·축 붕괴·분모 감추기·거짓말 가드 제거·상태이름 삭제).
5. ⚠️ **DB 질의는 `packages/database/` 안에서 `.mts`** — `index.ts` 는 `server-only` 라
   직접 import 불가. `PrismaNeon` + `generated/client` 로 붙고 `dotenv-cli -e ../../.env.local`.

---

## 6. 🔴 잊으면 안 되는 절차

> ① **화면 작업이면 `Skill(ui-ux-quality)` 먼저.**
> ② 🔴 **fullPage 를 모바일(390)·PC(1440) 두 장 다 찍어 눈으로 볼 것.**
> ③ 🔴 **인계서 숫자를 믿지 말고 실측하라** — **7세션 연속** 틀렸다.
> ④ 🔴 **소스 grep 을 검증으로 쓰지 말 것.**
> ⑤ ⛔ **승인 전 코드 금지**(배포된 실사용 화면).
> ⑥ 🔴 **회사 병목은 여전히 P0 실고객·유통.** UI 재설계가 그걸 대신하지 않는다.
>    — 이번 I 보류가 그 증거다(데이터가 없어서 UI 를 못 만든다).
