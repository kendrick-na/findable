# 🧭 START HERE — 세션 N-22 (2026-08-11 S6 완료 인계)

> ⚠️ **이 파일 + `투두리스트.md` + `UIUX_대개선_설계_v3_2026-08-10.md` 를 열 것.**
> ⛔ `_START_HERE_세션N21` 이하 **전부 폐기**. ⛔ `UIUX_대개선_설계_v2` 폐기(근거만 유효).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 96b9fe7 이면 이 인계 시점 그대로
git status --short        # 비어 있어야 정상
```

---

## 1. ✅ S6 완료 = **S0~S6 가 전부 끝났다**

커밋 `bcde96e`(S6) · `96b9fe7`(검증도구 수리) · app 프로덕션 배포 READY · **라이브 6화면 눈확인**.

| 단계 | 무엇 | 결과 |
|---|---|---|
| **S6-a** | `/brand` 이름 3개→1개(「브랜드·측정」)·폼 「브랜드 추가」·**거짓 설명문** | ✅ |
| **S6-b** | 사이드바 **3단**(⭐실행 → 측정 → 분석) | ✅ |
| **S6-c** | 비주얼·인터랙션 — **4건이 아니라 3건**(#3 오탐) | ✅ |

👤 승인 이력: **"ⓐ+ⓑ+ⓒ 전부 묶기"** + **"잠금 4개는 「분석」 그룹으로 그대로 노출"**.

### 📊 검증 수준 (다음 세션도 이 수준 유지)
- lint **0** · tsc **0** · build 통과 · 테스트 **128/128**(122→+6)
- **뮤테이션 검증 2건** — 가드를 죽여 실제로 실패함을 확인(`isDone=true` / 라벨에 문자열 복원)
- 라이브 눈확인 **6화면**(`/` `/brand` `/history` `/billing` `/compare` `/actions`)

---

## 2. 🔴 이번 세션이 뒤집은 것 (**되살리지 말 것**)

| 이전 기재(N-21) | 실측 결과 |
|---|---|
| *"S6-c = 비주얼·인터랙션 **4건**"* | ❌ **3건** — #3 업종 드롭다운은 **오탐**(`w-fit` 은 배지 2곳뿐·`SelectTrigger` 는 기본 전폭) |
| *"#4 = `/history` 가 실패 행에도 **문구**를 띄운다"* | ⚠️ **더 심각했다** — 문구뿐 아니라 **행 전체가 결과 링크**였다(`<a>` 무조건) |
| *"백업은 `docs/_적용/_백업/` 에 있다"* | ⚠️ **위치 틀림** — 실제는 **루트 `_백업/`**(파일 자체는 존재) |

🎓 **교훈(또 반복됐다): 묶음으로 적힌 항목은 건별로 실측하라.** N-19 는 3건 중 2건이,
N-22 는 4건 중 1건이 오탐이었다. **문서를 그대로 믿고 코드를 쓰면 없는 걸 고친다.**

---

## 3. 🔴🔴 이번 세션 최대 교훈 — **검사 장치가 올바른 변경을 막았다**

`scripts/verify-app.py` 는 로그인 성공 신호로 **`text=워크스페이스`** 를 기다렸다.
그런데 그건 **사이드바 그룹 라벨**이었고, S6-b 가 IA 를 3단으로 재편하면서 그 라벨이 사라졌다.
→ **정상 배포인데 "🔴 로그인 실패"로 오탐**했다. 스크린샷은 새하얀 빈 화면이었다.

⭐ **이것은 `[[feedback_guard_defends_the_bug]]` 의 거울상이다**:
- 그때: 가드가 **틀린 문구**를 기대값으로 박아 **버그를 ✅통과**시켰다.
- 이번: 가드가 **낡은 문구**를 기대값으로 박아 **올바른 변경을 ❌실패**시켰다.
- 답은 같다 → **검사는 문구가 아니라 구조/계약으로 판정한다.**
  (`[data-slot="sidebar"], nav` 로 교체. 문구는 계속 다듬을 것이고 그때마다 도구가 깨지면 안 된다.)

---

## 4. ⭐ 다음 착수 = **S7 (나머지 medium/low)** — 🔴 **감사도 설계도 안 됐다**

⚠️ S6 는 "설계·승인 완료, 코드만 남음" 상태였지만 **S7 은 그렇지 않다.**
착수하려면 **먼저 감사부터** 해야 하고, 그 결과를 👤에게 보여 승인받아야 한다.

🔴 **착수 전 반드시 할 것**: 기획서(`UIUX_대개선_설계_v3`)의 **6개 축**(IA·플로우·인터랙션·
비주얼·카피·SEO)을 **전부** 훑을 것. N-21 에서 내가 IA 한 축만 보고 "설계 끝"이라 했다가
👤가 잡아냈다.

### 🟡 이번 세션에 발견했으나 고치지 않은 것 (S7 후보)
- 🟡 **Growth 카드의 "Notion · Google Docs Export"가 두 줄로 접히면서 `(준비 중)` 배지가
  둘째 줄로 밀린다** — 읽히긴 하지만 아이콘과 시각적으로 멀어진다(스크린샷 확인).
  기능 결함 아님 · 카드 폭 제약 문제라 별도 판단 필요.
- ⚠️ `app.findable.co.kr/favicon.ico` **404**(콘솔 에러) — 기존 결함·기능 영향 없음.

---

## 5. ⚠️ 함정 (착수 전 필독)

### 세션N-22 신규
- 🔴🔴 **검사 스크립트가 UI 문구를 하드코딩하면 올바른 변경을 막는다**(§3). 화면 문구를
  바꿀 때는 **`scripts/verify-app.py` 도 같이 볼 것.**
- 🔴 **biome 복잡도 상한 20** — `.map()` 콜백에 분기를 더 넣으면 걸린다.
  답은 조건을 줄이는 게 아니라 **행 컴포넌트로 빼는 것**(`FeatureRow` 가 그 사례).
- ⚠️ **`_백업/` 은 루트에 있다**(`docs/_적용/_백업/` 아님). 인계 문서가 틀리게 적고 있었다.

### 기존 함정 (계속 유효)
- 🔴🔴 **새 `.test.tsx` 는 `@vitest-environment jsdom` 도크블록 필수** — 없으면 **로컬 전체
  실행은 통과하는데 Vercel 빌드만 깨진다**. → **`npx vitest run <파일>` 단독으로 먼저** 돌릴 것.
- 🔴 **`apps/web` 에는 테스트 러너가 없다** → 고정할 로직은 `packages/` 로 빼고 `apps/app` 에서 검증.
- 🔴 **`metrics.enginesCovered` 는 엔진 목록이 아니라 응답 목록**(29개=고유 8) → `new Set()`.
- ⚠️ **요금제 문구는 `apps/app/.../lib/pricing.ts` 와 `apps/web/.../pricing/page.tsx` 둘 다** 고칠 것.
- ⭐ **`app` 은 `lang="ko"`** — 되돌리면 한국어 줄바꿈이 다시 깨진다.
- ⚠️ 진단 결과 페이지에 **`h1` 추가 금지**(`audit-result.tsx:1405` 에 이미 있다).
- ⚠️ 크롤러용 텍스트를 **`sr-only` 로 숨기지 말 것** → 중복이 부담이면 `<details>`.
- 🚧 **로컬 dev 로그인 불가**(Clerk 개발 인스턴스) → 눈확인은 **배포 후 `verify-app.py`**.
- 🔴 **cron schedule 은 항상 UTC** · **`x-vercel-cron` 폴백 부활 금지**(보안 구멍)
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요** · 🔴 **`apps/api` 는 죽은 앱**
- 🔴 **`"use server"` 파일은 async 함수만 export** — tsc·lint 통과인데 **빌드만 실패**
- ⭐ **app 배포**: `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app`
  (루트에서). **web 배포**: `--project findable`. ⚠️`apps/app` 안에서 실행하면 실패.
- ⚠️ **PortOne 인증 헤더는 `PortOne {secret}`** · ⚠️ **오탐률 12~32%**

---

## 6. 👤 사용자 결정 대기

1. 🔴 **P0 실고객 지불의사 검증**(마케터 10~15명) — **불변 1순위·유일한 회사 병목**.
   시뮬 대체 불가(r=0.20). 질문지=`P0_지불의사_시뮬레이션_결과.md`
2. 🟠 **카카오페이 답장 대기** — 4차 회신 발송 완료. **먼저 보낼 것 없음**.
3. 🔵 **KIPO 상표 출원** — `findableapp.com`(오스트리아 동일명 GEO) 충돌 + 선출원주의
4. 🔵 P4 파트너 특전 정책
5. `alerts` 실제 알림 = **보류**(임계값 분포 없음 · 근거 없는 임계값 발명 금지)

---

## 7. 💰 원가 방침 · 🔴 승인 규칙

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · 브라우저 검증 · 빌드 · 배포 | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인** |

⚠️ **화면 변경은 원칙적으로 착수 전 승인**(프로젝트 규칙). S6 는 설계안을 보여 승인받았다.
새 세션에서는 **다시 확인할 것.**

---

## 8. 🔴 다음 세션이 잊으면 안 되는 것

> **S0~S6 전부 끝·배포됨. 다음은 S7 — 단 S7 은 감사도 설계도 안 된 상태다.**
> **🎓 묶음으로 적힌 투두는 건별로 실측하라** — N-19 는 3중 2, N-22 는 4중 1이 오탐이었다.
> **🎓 기획서는 6개 축이다** — 한 축만 보고 "설계 끝"이라 하지 말 것.
> **🔴 검사 장치가 UI 문구를 하드코딩하면 올바른 변경을 막는다** — 문구 바꿀 때 검증 도구도 볼 것.
> **👁️ 스크린샷 눈확인** — 이번엔 텍스트 추출로는 안 보이는 **줄바꿈 문제**를 스샷이 잡았다.
