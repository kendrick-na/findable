# 🧭 START HERE — 세션 N-28 (2026-08-13 세션N-27 인계)

> ⚠️ **이 파일 + `_투두_단일진실_N25.md` 두 개만 열면 된다.**
> ⛔ `_START_HERE_세션N27` 이하 전부 폐기.
> 🔴 **이번 세션의 남은 일 = 아래 §2 설계안을 대표님께 승인받고 실행하는 것.**
>    감사는 이미 끝났다. **재감사하지 말 것.**

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # b825bd1 이면 인계 시점 그대로
git status --short          # ⚠️ packages/payments/ 2건은 **다른 세션 것** — 건드리지 말 것
cd apps/app && npx vitest run   # 322/322 여야 정상
```

**현재 상태**: 테스트 **322/322** · web·app tsc **0** · 세션N-27 커밋 **12건 전부 프로덕션 배포·실측 확인**

⚠️ **`apps/web/lib/audit/crew-runner.ts` 는 격리했다**(scratchpad 로 이동).
낡은 중복본이라 web tsc 를 2건 깨뜨렸다. 정식 파일은 `packages/audit/crew-runner.ts`. **되살리지 말 것.**

---

## 1. 🔴🔴 지금 어디까지 왔나 — **감사는 끝났고, 승인 대기 중이다**

세션N-27 은 두 단계를 했다:
1. **전반부** — 인계서가 준 U1~U9 실행 + 새로 발견한 U10~U15 (커밋 12건, **배포 완료**)
2. **후반부** — 👤 지시로 **`Skill(ui-ux-quality)` 정식 절차**를 처음부터 다시 밟았다
   → **측정만 하고 코드는 안 건드렸다.** 설계안까지 만들어 **승인을 요청한 상태에서 컨텍스트가 끝났다.**

🔴 **다음 세션이 할 일 = §2 설계안을 대표님께 다시 제시하고, 승인받은 범위만 실행.**

### ⚠️ 세션N-27 전반부의 실패 (반복 금지)

인계서가 *"재감사하지 말고 바로 고쳐라"* 라고 해서 **스킬을 호출하지 않고** a11y 스크립트 하나만 썼다.
👤 가 *"어떤 워크플로우로 개선한 거지?"* 라고 물어 드러났다.

| 스킬이 시키는 것 | 전반부에 한 것 |
|---|---|
| `SKILL.md` 호출 → 참조 16개 중 관련 파일 정독 | ❌ 안 읽음 |
| 측정 **4종**(measure·align·a11y·squint) + sitemap | ❌ **a11y 하나만** |
| IA 인벤토리를 **화면 감사 전에** | ❌ 안 함 |
| 설계안 → 승인 → 코드 | ❌ 바로 코드 |

🎓 **메모리 `reference_ux_product_quality_bar` 에 이미 적혀 있던 실수를 그대로 반복했다.**
   (세션N-26 도 *"16개 중 2개만 읽고 감사"* 로 같은 지적을 받았다)
⭐ **후반부에 정식 절차로 다시 재니 전반부가 못 본 것이 무더기로 나왔다**(아래 §2).
   → **a11y 0건은 "접근성 위반 없음"일 뿐 "UI/UX 좋음"이 아니다.** 도구가 출력에 그렇게 경고한다.

---

## 2. 📋 승인 대기 설계안 — **8층 표 (그대로 제시할 것)**

> 🔴 **배포된 실사용 화면이라 스킬 규칙상 승인 전 코드 금지.**
> 근거는 전부 **실측**이다. 재측정하지 말고 바로 제시할 것.

| 층 | 할 것 | 근거(실측) | 🔴 안 할 것 |
|---|---|---|---|
| **①IA** | 진실의 거울에서 **원문 접기**(요약·판정만), 원문은 엔진별응답 단독 | 두 섹션이 **글자 단위로 같은 원문** 출력 | ⛔ **둘 중 하나 삭제 금지** — 역할이 다름 |
| **②플로우** | 모바일 엔진 탭 **2줄 wrap**(7개 다 보이게) | 653px 내용이 340px 칸 → **313px 잘림** | ⛔ 드롭다운 전환 금지(1클릭 증가) |
| **③레이아웃** | 근사 불일치 16건 · 세로간격 37종 축소 | `align.mjs` 실측 | ⛔ 전면 재배치 안 함 |
| **④인터랙션** | 대시보드 홈·`/actions` **`Promise.all` 병렬화** | 홈 `await` 7개 **직렬** | ⛔ 캐싱·구조변경 안 함 |
| **⑤비주얼** | 한글 음수자간 **4곳 → 0** | `measure.mjs` 실측 | ⛔ 브랜드 색·폰트 안 건드림 |
| **⑥카피** | **분모를 이름에 넣기** — "AI 7곳 중 7곳 인지(100%)" / "응답 27건 중 26건 등장(96%)" | 같은 화면 다른 분모 | ⛔ 한쪽 숫자 삭제 금지(**둘 다 사실**) |
| **⑦상태정직성** | 요약 카드에 **오류 1건** 명시(지금은 아래 각주에만) | 27/29 성공 | 해당 없음 |
| **⑧기본기** | — | | 해당 없음 |

**예상 숫자 변화**: 근사 불일치 16→0 · 한글 자간 4→0 · 모바일 탭 잘림 313px→0 · 홈 직렬7→병렬

### 🔴 이 설계로도 **안 고쳐지는 것 3개** (낙관 보고 금지)
1. **결과페이지 11화면 그대로** — 줄이려면 IA 재설계(L). `criteria.md` 는 화면 수를 **실격 기준으로 쓰지 말라**고 명시
2. **대시보드가 "숫자 창고"** — `b2b.md` 판정 3문 중 ③(다음 행동)이 약하다. **재설계 급**
3. **squint 2·3·4구간 미확인** — 세션 이미지 한도 초과. **"검사된 적 없는 것"으로 보고했다**

### 👤 여쭙고 답을 못 받은 것 2개
- **Q1. 범위** — A안(①~⑦ 전부·반나절) / B안(④느림+⑥숫자만) / C안(승인만, 순서는 알아서)
- **Q2. 대시보드 비주얼 개선**을 이번에 묶을지, 별도 설계안으로 뺄지
  ⚠️ 재설계는 되돌리기 비용이 커서 **별도 승인 대상**

---

## 3. 🔬 실측 근거 — **재조사 금지 (전부 프로덕션에서 잼)**

측정 대상 = `https://www.findable.co.kr/ko/audit/105fa34f-924e-440a-9544-4ebbe63d54e8` (엔비디아 · 실데이터)

### 3-a. 👤 가 지목한 5건 — **전부 진짜 문제였다**

| # | 지적 | 실측 판정 |
|:--:|---|---|
| ① | 엔진 열에 왜 스크롤? | 🔴 **모바일 313px 잘림**(내용 653 vs 칸 340). PC 는 0. **네이버·다음이 화면 밖** = 차별점을 숨김 |
| ② | 엔진별응답 vs 진실의 거울 차이? | 🔴 **중복 확정**. 두 섹션이 *"어떤 엔비디아를 추천받고…"* 이하 **동일 원문**. `ia.md` 기준 = 지워도 정보 안 사라짐 |
| ③ | 96% vs 100% 통일 | 🔴 **분모가 다름**(둘 다 산술은 맞음) — 96%=응답 26/27 · 100%=엔진 7/7 |
| ④ | 대시보드가 무료진단과 똑같다 | 🔴 **구조가 실제로 같다** — **같은 러너 `runAuditJob`** 사용. 유료가 더 받는 건 *자동 재측정·이력*이지 다른 측정이 아님 |
| ⑤ | 메뉴 누르면 느림 | 🟡 **홈만** 7.76초(콜드) → 재측정 0.5~1.8초. **나머지 9개는 0.49~0.75초 정상** |

### 3-b. 도구가 잡은 것 (눈·axe 로는 못 봄)

```
measure.mjs  모바일 11.0화면(9305px) · PC 6.8화면 · 가장 큰 글자 "78"(72px) · 질문 0개
             🔴 한글 음수자간 4곳
align.mjs    🔴 근사 불일치 16건 (70vs71 · 90vs92 · 98vs101 · 154vs156 …)
             🔴 세로 간격 37종 (목표 ≤6) · 좌측선/근접성역전/경계침범은 0 ✅
squint       1구간만 눈확인 → 78 점수링이 압도적 1순위 ✅ 위계 양호
sitemap      공개 화면 15개 인벤토리 확보
```

### 3-c. 🔴 정확한 수정 위치 (다음 세션이 바로 찾게)

```
한글 음수자간 4곳
  apps/web/app/[locale]/audit/[jobId]/components/revenue-impact-card.tsx:141,182,207
  apps/web/app/[locale]/audit/[jobId]/components/naver-vs-ai-gap.tsx:256,277,350
  → isKo 분기 패턴은 hero.tsx·step-sections.tsx 에 이미 있다(그대로 따를 것)

엔진 탭 스크롤
  audit-result.tsx:3324  `flex gap-1 overflow-x-auto`

대시보드 홈 직렬 await (7개)
  apps/app/app/(authenticated)/page.tsx:38,39,41,42,53,68,89
  ⭐ /history·/admin/ops·/admin/audits 는 **이미 Promise.all 로 병렬화돼 있다** — 그 패턴을 복사할 것
  /actions 도 await 10개·Promise.all 0 → 같이 고칠 후보
```

---

## 4. ✅ 세션N-27 이 한 일 (커밋 12건 · 전부 배포·실측)

| # | 무엇 | 커밋 |
|:--:|---|---|
| U1~U3 | 대비 위반 결과페이지 **51곳→0** | `164d282` |
| U9~U12 | 🔴 **헤더 흰배경으로 로고가 안 보이던 것** | `56d04b9` |
| U4·U7 | 모바일 엔진라벨 겹침 · `prefers-reduced-motion` 신설 | `36a245a` |
| U5·U6·U8·U13 | 200%확대 · 이메일 래핑 · 한글자간(랜딩) · 가로넘침 | `2574391` |
| **A안** | `/admin/audits` 관리자 진단 목록 **신설** | `cb3b9c8` `985d984` |
| 🔴 **U14** | **web 이 라이트 OS 방문자에게 깨져 있었다** | `c4bb662` |
| U15 | 진단 시간 약속 **"3분" 통일**(화면마다 달랐다) | `43be74e` `f4b8d8f` |
| 🔴 **B정정** | admin 게이트 테스트 **0개**였던 것(+8) | `72b3813` |

배포: app `findable-mtsva9z6t` · web `findable-2s3zofrjp` (둘 다 `vercel inspect` 로 Aliases 확인)

### 🎓 이번 세션이 잡은 **인계서·문서 오류 6건** (같은 실수 반복 금지)

1. 🔴 **B안이 통째로 틀렸다** — `FINDABLE_ADMIN_EMAILS`(Vercel) 가 아니라 **Clerk `publicMetadata.role`**.
   그 env 는 **심층분석 원가 게이트**(`packages/audit/usage-tier.ts:17`)일 뿐 `/admin/*` 와 **무관**.
   → 지시대로 했으면 **아무 일도 안 일어났다.** 실제로는 **이미 `{"plan":"growth","role":"admin"}` 로 되어 있었다.**
2. **U2 제안값 `#72767d`** 는 검정 배경에서만 통과 → 카드 위 3.82. **`#7e8289`** 로 정정
3. **`text-zinc-600`(2.70:1)** 이 목록에 아예 없었다 → U1 후 재측정에서 14곳 발견
4. **"로고 1.06:1 = 오탐"** 판정이 **오판**이었다 → 헤더가 진짜 흰색, **로고가 안 보였다**
5. **U6 "break-all 위치"** 는 코드에 없었다(증상만 맞고 원인 서술이 틀림)
6. **#5 lint 부채** 65건 → 실측 **81건**

⭐ **전부 "실행 전에 검산해서" 잡았다.** 인계서 숫자를 그대로 믿었으면 전부 통과했을 것이다.

---

## 5. ⚠️ 함정 (계속 유효)

- 🔴🔴 **엔진 수 재조사 금지** — 본류=**7** · 영어만=**4** · 8=브리핑 별도
- 🔴 **`FINDABLE_ADMIN_EMAILS` ≠ 관리자 권한**(위 §4-1). 운영자는 **Clerk metadata**
- 🔴 **`git stash pop` 조심** — 7월 「브랜딩전_백업」 stash 가 튀어나와 **19개 파일 충돌**을 냈다.
  복구했지만 시간을 크게 잃었다. **stash 대신 `git checkout HEAD -- <파일>` 을 쓸 것**
- 🔴 **BotID 가 curl 을 차단** → 진단 생성은 브라우저로만. 단 **결과 조회 API 는 curl 로 된다**
  (`/api/audit/<id>` → 실데이터 검증에 유용. 세션N-27 이 96%/100% 검산에 씀)
- 🔴 **`apps/web` 에 테스트 러너 없음** → 고정할 로직은 `packages/` 나 `apps/app` 으로
- 🔴 **`apps/api` 는 죽은 앱**
- 🔴 **biome**: 복잡도 20 · 중첩 삼항 금지 · `*` 선택자는 `noDescendingSpecificity` 를 **파일 전체 기준**으로 건드림(→ `:where()` 로 감쌀 것)
- ⚠️ biome 이 **대괄호 경로**(`[jobId]`)를 직접 못 읽는다 → 상위 디렉터리로 lint
- 🔴 로컬 build 는 **`VERCEL=1`**
- ⚠️ **로컬 dev 는 `/ko` 프리픽스에서 리다이렉트 루프** → 로컬 측정은 `/audit/<id>`(프리픽스 없이)
- ⭐ **배포**: web = `npx vercel --prod --yes --scope nayoy2-7791s-projects`(루트)
  / app = `VERCEL_PROJECT_ID=prj_krY2Z1RCQUymaENTPtkUK51rbjOA VERCEL_ORG_ID=team_Hf0ltKNpfGZ7j88gcouH57LK` 를 앞에 붙여 루트에서
  🔴 **배포 후 `vercel inspect <url>` 로 Aliases 를 반드시 확인**(도메인 자동연결이 안 될 때가 있다)
- ⚠️ **이미지 한도** — 한 세션에 스크린샷을 많이 보면 API 가 거부한다. **눈확인이 필요한 것부터** 먼저 볼 것

---

## 6. 🔴 다음 세션이 잊으면 안 되는 것

> **① 감사는 끝났다. §2 설계안을 대표님께 제시하고 승인받는 것부터.** 재측정 금지
> **② 승인 전 코드 금지** — 배포된 실사용 화면이다(스킬 규칙)
> **③ 화면 작업이면 `Skill(ui-ux-quality)` 를 먼저 호출한다.** 말 안 해도.
>    전반부에 이걸 건너뛰어 👤 에게 지적받았다
> **④ 도구 4종은 서로 다른 걸 잡는다** — 하나만 돌리면 놓친다(실측으로 증명됨)
> **⑤ ⚠️ `packages/payments/` 는 다른 세션 작업 중** — 건드리지 말 것
> **⑥ 🔴 회사 병목은 여전히 P0 실고객·유통이다.** 세션N-27 이 고친 12건은 전부
>    *"고객이 왔을 때 배신하지 않게"* 하는 작업이고, **유저는 여전히 0명**이다.
>    UI/UX 감사가 그 사실을 가리지 않게 할 것(`audit-loop.md` 4절)
