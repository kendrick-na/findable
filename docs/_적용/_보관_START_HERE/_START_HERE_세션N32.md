# 🧭 START HERE — 세션 N-32 (2026-08-15 세션N-31 종료 인계)

> ⛔ `_START_HERE_세션N31` 이하 **전부 폐기**.
> 🎯 **다음 할 일 = ①모션·비주얼(MD3 토큰) ②남은 결함 정리.** 방향·설계는 **전부 확정됐다.**

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # e55f5d6 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 334/334
```

**상태**: 테스트 **334/334** · web·app tsc **0** · ⚠️ **미배포**(코드 변경 후 배포 안 함)

📕 **먼저 읽을 것 (딱 1개)**
- `docs/_적용/재설계안_v4_전면_2026-08-15.md` ← **단일 진실. 남은 일 전부 여기.**
  ⚠️ 1,100줄 넘는다. **§6-e(착수순서) · §6-i(전수조사) · §8(미해결)** 부터.

---

## 1. ⛔ 절대 금지 (어기면 복구 불가)

| 대상 | 이유 |
|---|---|
| 🔴 **`packages/payments/*`** | **다른 세션이 미커밋 작업 중**(`portone.ts`·`index.ts`·`billing-cycle.ts`). 카카오페이 심사 중 |
| 🔴 **트라이얼·결제 코드** | 👤 지시: *"카카오페이 심사에 위배되는건 기획에 보존해두되, **반영은 하지 말아줘**"* |
| 🔴 `ultracite` **폴더 단위** | N-30 사고(결제 파일 오염). **파일 단위로만** |
| ⚠️ `ultracite` **대괄호 경로** | `app/[locale]/...` 를 **못 읽는다**(내부 IO 에러). **tsc 로 대체 검증** |
| 🔴 `git stash pop` | 7월 백업 stash 생존 |

---

## 2. ✅ 세션N-31이 한 것

### 2-a. v4 완성 (커밋 `2f1eec4`→`0de2383`)
경쟁사 **442프레임**(데스크톱 279 + 모바일 162 · 👤 직접 촬영) + 코드 실측 + 라이브 fetch 로
v3 를 전면 재검증. **결정 4개 확정**(액션 자리·모바일 내비·다국어·부분가림).

### 2-b. 🔴 코드 반영 (커밋 `e55f5d6`) — **사실 정직성**
| 고친 것 | 내용 |
|---|---|
| `/synergy` 노출 | sitemap 제외 + robots 차단(**네이버 D2SF 투자 피치가 검색 색인 중이었다**) |
| **Gartner 날조** | `<blockquote cite='gartner.com'> "한국 콜라겐 시장 1위"` → FAQ 구조화 예시로 교체 |
| **없는 직원** | `민지 · GEO 분석가` → `에이전트`. **IBK 운영매니저 사고와 동일 패턴** |
| 실존사 사칭 | 메디큐브·아모레퍼시픽·조선미녀 → 가상 브랜드 |
| 가짜 수치 | `42→78`·`63%→91%`·`11%→18%` 삭제 |
| 엔진 수 | `8 AI 엔진` 5곳 → **7**(데이터셋 `engine_summary` 키 7개로 확인) |
| 플랜 고지 | starter `1개 브랜드` → **3개**(`plan.ts:121` brandLimit=3) |

### 2-c. 🔴 인계서 오류 정정 **5세션 연속** (N-31만 3건)
| 낡은 기록 | 실측 |
|---|---|
| *"로그인 PC 좌측 절반이 빔"* | ❌ **안 비었다**(로고·헤드라인·엔진7 있음). 수직분포 문제 |
| *"Profound 모바일 고장"* | ❌ **페이월이다**(f021 요금제 화면). 👤가 먼저 지적 |
| *"경쟁사 한국어 UI 없음"* | ❌ **OCR이 한글을 못 읽었다**. Profound 는 네이티브 |

⛔ **언어 판정에 OCR 쓰지 말 것. 프레임을 눈으로 볼 것.**
⛔ **프레임 샘플링으로 결론 내지 말 것** — f020(백지)만 보고 "고장"이라 했는데 f021이 답이었다.

---

## 3. 🔴 다음 할 일 ①: 모션·비주얼 (👤 요청)

**아직 아무도 안 봤다.** v4 §3-b ⑤비주얼에 *"우리 브랜딩 유지"* 만 적혀 있다.

### 3-a. 판정 기준 = **구글 MD3 공식 토큰**
```
short  : 50~200ms
medium : 250~400ms      ← 🔴 외부 스킬이 "150~250ms"라 해서 이 구간을 통째로 빠뜨린다
long   : 450~600ms
```
📕 `~/.claude/skills/ui-ux-quality/reference/motion.md` 가 단일 진실.
⛔ `impeccable` 등 외부 스킬 숫자 금지(1차 출처 없음).

### 3-b. 프레임에서 관찰된 경쟁사 비주얼 (재분석 금지 · 아래가 전부)
| | 비주얼·모션 |
|---|---|
| **Profound** | ⭐**헤드라인 엔진명 회전**(ChatGPT→Gemini→Perplexity + 로고 동반) · 다크 |
| **Peec** | 라이트 · 두톤 헤드라인 · **문장 안에 지표 칩 인라인** · ⭐**궤도 로더**(로고 중심 AI 6종 회전) |
| **Scrunch** | 크림 · **세리프 이탤릭**(가장 편집디자인적) · **코드 스캔 레이저 라인** |
| **Otterly** | 라이트 + 마젠타 |

📁 영상 원본: `docs/_경쟁사_UIUX/_모바일/{4개사}/*.MP4` · `docs/_경쟁사_UIUX/{4개사}/*.mov`
→ **모션은 영상에서 다시 뽑아야 한다**(프레임 정지 이미지로는 못 잰다).

### 3-c. ⛔ 안 할 것
🔴 **우리 브랜딩 유지**(오렌지 `#ff7a4d` · Instrument Serif · 다크). 👤 확정: *"우리만의 브랜딩 컬러 좋음"*
⛔ 마젠타(Otterly)·로열블루(Scrunch)·크림 이식 금지.

---

## 4. 🔴 다음 할 일 ②: 남은 결함 (전수조사 §6-i)

### 4-a. 아직 안 고친 가짜 숫자 — **6건**
| 위치 | 내용 |
|---|---|
| `sov-chart.tsx:69,79,143-151` | 🔴 **RNG 난수를 `Last 30 days` 실측처럼** · `n ≈ 200 responses` · `medicube.co.kr` |
| `step-sections.tsx:262-264` | 메디큐브 `n:47` · 아모레퍼시픽 `n:32` · 조선미녀 `n:28` — **예시 배지조차 없음** |
| `step-sections.tsx:273,282-284` | `Korean Moat Score: 89%` · `+18%/+14%/+9%` |
| `step-sections.tsx:301-303` | Cafe24/Naver/WordPress **`Connected`** ↔ 같은 파일 `:51`엔 `(v1.5)` = **미출시를 라이브로** |
| `testimonials.tsx`·`cases.tsx` | 가짜 후기 4건 + **실존 개발자 얼굴**(`github.com/leerob.png`) + `Logo 1~15`. **현재 미렌더(importer 0)** → **파일 삭제 권장** |
| `case/a-brand:62-90` | 3개사가 `sov:0`인데 *"5사 28회 라이브 측정"* 에 포함 |

### 4-b. 확정 버그
| 위치 | 내용 |
|---|---|
| 🔴 `blog/[slug]/page.tsx:52` | `const { slug, locale } = await params` — 타입엔 `slug`만(`:22-26`) → `locale.startsWith` **런타임 크래시**. `@ts-nocheck`(1행)가 은폐 |
| | ⚠️ **현재 미발현**(블로그 글 0건). **글 1개 발행하면 즉시 500** |
| `checkout/success:51-53` | `테스트 결제` · `평가위원 시연용 환경` 문구 ⛔**결제트랙이라 손대지 말 것** |
| `header.tsx:66` | `<BreadcrumbLink href="#">` 죽은 링크 |

### 4-c. 🟢 손대지 말 것 (검증 완료)
- `/report`·`/research`·`/case` **숫자는 진짜다** — JSONL 원본 대조: 평균 SoV **93.2**·응답 **140건** 정확히 일치
- `revenue-impact-card.tsx:59-85` — **출처 명기 + "추정" 라벨** = ⭐모범 사례
- `apps/app` 대시보드 14개 — 거의 전부 깨끗

---

## 5. ⬜ 착수 순서 (v4 §6-e · 감사 반영본)

| 순 | 묶음 | 규모 | 결제 | 상태 |
|:--:|---|:--:|:--:|:--:|
| 0 | IA 인벤토리 + ROT | S | ✅ | ✅ **완료**(§6-f) |
| 0-b | 랜딩 섹션 근거 | S | ✅ | ✅ **완료**(§6-g) |
| — | **위험 결함 1차** | M | ✅ | ✅ **완료**(`e55f5d6`) |
| **A** | 🆕 **모션·비주얼(MD3)** | M | ✅ | ⬜ **다음** |
| **B** | **남은 결함 6+2건**(§4) | M | ✅ | ⬜ **다음** |
| **C** | **랜딩 정렬·타이포** — 좌측선 8→2 · 근사불일치 5→0 · keep-all 10곳 · 줄높이 4건 | M | ✅ | ⬜ 실측 4/4 일치 |
| D | **Showcase → 실제 진단결과**(Peec f003 방식) | M | ✅ | ⬜ |
| E | 소셜프루프 밴드(LiveCounter 재구성) | S | ✅ | ⬜ |
| F | 지표 사전 — 🔴**분모 축 3종 명명 먼저** | M | ✅ | ⬜ |
| F-b | ⭐**1줄**: app `AI N곳에 물어`→`측정한 AI N곳` | XS | ✅ | ⬜ |
| G | 「평가」 탭(쿼리 변경 0) | S | ✅ | ⬜ |
| H | 순위 충격 공개 + 0결과 3요소 | M | ✅ | ⬜ |
| I | 탭 재편 + `loading.tsx` 신설 | **L** | 🔴 | ⬜ **행수 실측 선행** |
| — | ⛔ 트라이얼·결제·전환이벤트 | L | 🔴🔴 | ⛔ **심사 후. 기획만 보존** |

### 🔴 I번 전 필수 — `Tracking` 행 수 실측 (원가 0)
```sql
SELECT "engineId", count(*), count(sentiment), count("mentionPosition"),
       count(*) FILTER (WHERE "citedSources"::text != '[]')
FROM "Tracking" GROUP BY "engineId";
```
**적재가 `AUDIT_DUAL_WRITE_ENABLED` && `organizationId` && `brandId` 3중 조건**이라
**무료진단(비로그인)분은 한 줄도 안 쌓인다.** 유저 0명이면 탭 8개가 전부 빈다.

---

## 6. 🔴 확정 사항 (다시 논의하지 말 것)

| # | 결정 |
|:--:|---|
| 1 | **「지금 할 일」 = 탭 밖 최상단 + 사이드바.** 근거: Profound 사이드바 그룹명이 문자 그대로 `Action`, Otterly 는 `Recommendations`. **탭에 넣은 곳 0곳** |
| 2 | **모바일 = 하단 탭 4개 + 「더보기」 시트.** 햄버거 쓴 2곳 다 실패(Scrunch 는 **햄버거가 화면 밖으로 나감**) |
| 3 | **다국어 현행 유지.** 대시보드 한국어 전용 · 랜딩은 사전 7개 + 선택기 이미 있음 |
| 4 | **부분가림 = Peec f041 3겹**(블러 + 페이드 + ⭐**선명 하나**). 코파일럿 챗 = **3회 제한** |
| 5 | ⛔ **`Sample`·`(예시)` 라벨 금지** — Peec 은 진짜 데이터에 `Sample` 이라 써서 스스로 신뢰를 깎는다 |
| 6 | **ThreePillars 유지**(한국 엔진 = 유일 차별점). 영어 제목도 유지 |
| 7 | `/` 이력 섹션 제거(`/history` 유지) — 같은 컴포넌트·같은 데이터 중복 |

---

## 7. ⭐ 전략적으로 잊으면 안 되는 것

### 🔴 "무료진단은 우리만"은 **틀렸다**(라이브 fetch 확인)
```
scrunch.com/platform/agent-traffic
→ "Enter your website URL" · "Run AI visibility audit" · "No credit card required"
```
Otterly 무료툴 5종 · Profound 무료 AEO Report 도 있다.

### ⭐ 진짜 차별점 = **한국 엔진** (4곳 랜딩 fetch 실측)
```
naver|hyperclova|clova|daum|kakao 언급 → 4곳 전부 0건
```
🔴 **헤드라인 주장은 "무료"가 아니라 「아무도 안 재는 곳을 잰다」여야 한다.**

### ⭐ 4곳 공통 규율
**회사가 만든 성과 숫자를 쓰는 곳은 0곳.** 성과 수치(`2x`·`4x`)는 **전부 실명 인물 인용**.

### 🔴 4곳 공통 실패 = 우리 자리
①**0 결과에 침묵** ②한국 엔진 0 ③한국 브랜드 자동감지 실패 ④한국어 생성 붕괴 ⑤**로딩 실패 미설계**
⭐ **Otterly 는 모바일에 「큰 화면을 쓰세요」 사과문을 띄운다**(f010).

### 🔴 우리 기술 스택은 **1군과 동일**
Next.js App Router + Clerk + Vercel + Tailwind/Radix = Profound·Otterly 와 같다.
🔴 **Peec·Scrunch 는 SPA라 SSR이 없다** → 공개 결과 페이지를 **구조적으로 못 만든다**(우리 우위).

---

## 8. 🔴 잊으면 안 되는 절차

> ① **화면 작업이면 `Skill(ui-ux-quality)` 먼저.** 말 안 해도.
>    🔴 **참조파일을 골라 읽지 말 것.** 서비스 전체면 `ia.md` 를 화면 감사보다 먼저. 가장 중요한 건 `quality.md`.
> ② 🔴 **4층 모델** — ①뭘 넣나/빼나 ②얼마나 ③배치 ④보이기.
>    *"①②를 안 고치고 ③④만 손대면 「고쳤는데도 별로다」가 반복된다"*(3회 연속 사고).
>    ⚠️ **탭 재편은 총량을 안 줄이는 ④순위 수단이다.**
> ③ 🔴 **도구 4종은 서로 다른 걸 잡는다**(measure·align·squint·a11y). 하나만 돌리면 놓친다.
> ④ 🔴 **fullPage 를 모바일(390)·PC(1440) 두 장 다 찍어 눈으로 볼 것.** 안 본 뷰포트는 "검사된 적 없는 것".
> ⑤ ⛔ **승인 전 코드 금지**(배포된 실사용 화면).
> ⑥ 🔴 **인계서 숫자를 믿지 말고 실측하라** — **5세션 연속** 틀렸다.
> ⑦ 🔴 **회사 병목은 여전히 P0 실고객·유통. 유저 0명.** UI 재설계가 그걸 대신하지 않는다.
