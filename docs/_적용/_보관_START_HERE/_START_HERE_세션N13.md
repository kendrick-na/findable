# 🧭 START HERE — 세션 N-13 (2026-08-09 세션N-12 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` 를 둘 다 열 것.** 여기는 방향·함정, 저기는 남은 일.
> ⛔ `_START_HERE_세션N12`·`N11`·`N10`·`N9`·`세션E`·`세션L`·`다음세션`은 **전부 폐기**.
> 🔴 **`실행백로그.md` 의 `⬜` 는 믿지 말 것**(표본 5건 실측 전부 이미 완료).
> 🔴 **`투두_마스터.md` 는 완료 기록용**(남은 일은 `투두리스트.md` 에만).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 efef8c2 계열이면 세션N-12 종료 상태
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

---

## 1. ⭐ 다음에 할 일

### 🔴 지금 코드로 할 수 있는 일은 **거의 없다**
세션N-11·N-12 가 잔여 코드 항목을 **전건 실측·처리**했다. 남은 12건 중 코드는
`audit-result.tsx` lint 부채(3,696줄이라 그 파일 손댈 때) + 범위 큰 것(RAG·durable·
GEO 에이전트, 데모데이 이후)뿐이다.

### 👤 회사를 막고 있는 것 = 사용자 몫 (2026-08-10 재정렬)

1. 🟠**결제 심사 트랙 — 마감이 있는 유일한 트랙. 기록이 8/4 에서 멈춰 있다.**
   📕SoT=`_카카오페이_심사보완/`. 문안 2종은 **완성돼 있고 발송 여부만 미확인**:
   - `포트원_단건결제_추가문의.md` — 카카오페이는 **일반결제 CID 와 정기결제 CID 가 각각**
     발급되는데 지금은 **정기결제만 접수**된 상태다.
   - `회신메일_2차_문안.md` — ⚠️**「전체 답장」**(카카오페이 심사관 + pg_on 동시 수신) ·
     첨부 불필요 · **포트원 문의를 먼저 보낸 뒤** 발송(본문이 그걸 전제한다).
2. ⭐**실고객 10~15명 지불의사** — 회사 단일 장애점. 시뮬 대체 불가(r=0.20).
   🕓**사용자 판단(8/10): 나중에.** 🔗 이게 풀려야 before/after·게이팅도 데이터로 만들 수 있다.
3. 🕓**`PORTONE_WEBHOOK_SECRET` — 지금 하지 말 것. 카카오페이 승인 후로 미룸**(8/10 판정).
   웹훅은 **배포 완료**(라이브 200)고, 시크릿이 없으면 `not_configured` 로그 후 **200 으로
   조용히 닫는다** → 영향 0·**위조 시도도 무해**. 무엇보다 **결제 채널 승인 전이라 결제가 0건**
   이라 메울 구멍이 없고, 시크릿은 **웹훅 URL 등록과 세트로 발급**되므로 지금 하면 다시 손댄다.
   🔴**단, 첫 실결제 전에는 반드시** — 안 넣고 결제받으면 "돈 냈는데 무료 화면" 사고가 난다.

### ✅ 세션N-12 가 남긴 확인 과제 — **세션N-13 이 교차검증 완료**(2026-08-10)
프로덕션 **31건**(N-12 는 18건)을 `critiqueStrategist` 로직 그대로 재현해 대조한 결과
**N-12 의 수치가 전건 재현**됐다. 배선도 정상이다.

| 항목 | N-12 문서 | N-13 재현(31건) |
|---|---|---|
| `executiveSummary` 위반 | 18/18 · 347자 | **31/31=100% · 347자** ✅ |
| `whyThisOne` 위반 | 18/18 · 234자 | **31/31 · 234자** ✅ |
| 배수 2.5 발동률(채택) | 33% | **32%** ✅ |
| strategist 시간 | 중앙 65초·최대 80초 | **중앙 65초·최대 82초** ✅ |

- 🔬 **시간 위험 0건**: crew total 실측 **최대 226초**(문서의 "최대 130초"는 낡았다).
  단 그 회차는 재작성 **비대상**이고, 발동 10건 중 최악이 `181+45=226초` < **270초**.
  → **270초 초과 위험 0/31**. 회귀 테스트 `crew-critique.test.ts` **9/9 통과**.
- ⚠️ **실측 경로 함정**: `crewResult.strategist.`**`output`**`.executiveSummary` 다.
  `strategist.executiveSummary` 로 읽으면 **전건 `null`** 이 나와 "필드가 없다 → 자기평가가
  헛돈다"고 **오진한다**(세션N-13 이 실제로 한 번 틀렸다가 구조 조회로 잡음).
- ⬜ **유일한 잔여**: 배포(8/9) 이후 **crew 실행 0건**(최신 8/1)이라 `refineApplied=true`
  실물 로그만 아직 없다. **새 진단 1건이 돌면 자동 확인**되고, 코드로 앞당길 수 없다.
```
로그: audit.crew.completed → refineAttempted / refineApplied / refineViolations / refineDurationMs
DB : AuditJob.crewResult.refinement
```

---

## 2. ✅ 세션N-12 가 한 일 (커밋 `efef8c2`, 배포됨)

### crew 자기평가 1패스(critique→refine) + `MODEL_FAST` 죽은 코드 해소

🔴 **착수 전 전수 감사에서 진짜 결함이 나왔다** — 이게 이번의 핵심이다.
스키마 `.describe()` 와 에이전트 지시문이 **둘 다** 길이 규칙을 말하는데,
프로덕션 18건 감사 결과:

| 필드 | 약속 | 위반 | 최장 |
|---|---|---|---|
| `executiveSummary` | 80자 | **18/18 전건** | **347자**(4.3배) |
| `mondayActionOne.whyThisOne` | 80자 | 18/18 전건 | 234자 |
| 액션 `step` | 50자 | 280건 | 128자 |

`executiveSummary` 는 *"임원이 한 문장만 읽어도 결정 가능"* 이 존재 이유인데 347자면
한 문장이 아니다. **제품 약속이 100% 깨진 채 화면에 그대로 렌더**되고 있었다.

- 🔒 **zod `.max()` 로 강제하지 않았다** — 그러면 검증 실패로 `output: null` 이 되어
  **18/18 이 위반인 지금 리포트가 전멸**한다. → 감지 후 **1회 압축 재작성**,
  실패하면 원본 유지 = **현재보다 나빠질 수 없는 구조**.
- 🔬 **허용 배수 2.5는 실측으로 결정**: 1.5=**100%** 발동 · 2.0=78% · **2.5=33%**(채택) · 3.0=17%.
  crew 실측 최대 **130초** + 전략가 1회 최대 **80초** vs `CREW_TIMEOUT_MS` **270초** →
  100% 발동은 예산을 위협한다. + `REWRITE_TIMEOUT_MS`(45초)로 이중 방어(최악 175초).
  🔬**N-13 정정**: 표본을 31건으로 넓히면 crew total 최대는 130초가 아니라 **226초**다.
  그래도 그 회차는 재작성 비대상이라 **270초 초과 위험은 0건**(위 §1 표 참조).
- 🔒 **사실이 바뀌면 재작성을 버린다** — rank·channel·impact·effort·princetonStrategy 중
  하나라도 달라지면 채택 안 함. 압축이지 재분석이 아니다(숫자가 바뀌면 `/actions` 카드와
  결과 페이지가 다른 우선순위를 말한다 — 세션N-8 "화면 점수 ≠ 메일 점수"와 같은 계열).
  더 길어져도 버린다.
- ⚠️ **`MODEL_FAST` 는 지금 이득이 없다**: 실측상 FAST 와 DEFAULT 가 **같은 haiku-4.5** 다.
  `FINDABLE_CREW_MODEL_FAST` 로 더 싼 모델을 지정해야 효과가 난다(배선만 열어둔 상태).

---

## 3. 🎓 교훈 (세션N-11·N-12 누적)

1. 🔴 **"투두에 있으니 만든다"가 아니라 "지금 만들 수 있나 / 만들면 좋아지나"를 먼저 볼 것.**
   #58 은 **데이터가 원천 부재**(조치 기록 0건), #40 은 **적용하면 가짜 언급이 생기는** 항목이었다.
2. 🔴 **기능을 넣기 전에 그 기능이 고칠 대상을 실측할 것.** 이번엔 "crew 품질 개선"이라는
   막연한 목표로 시작했다가 **전수 감사로 약속 위반 100%** 라는 구체적 표적을 찾았다.
   실측 없이 만들었으면 엉뚱한 걸 고쳤을 것이다.
3. 🔴 **스키마 검증을 조이면 기존 데이터가 전멸할 수 있다** — `.max()` 를 넣기 전에
   **현재 위반율**을 먼저 볼 것(18/18이었다).
4. 🔴 **`curl` 로 "사용자 화면"을 판정하지 말 것**(쿠키 미전송 → `/en` 오진 사고).
5. **문서가 흩어지면 반드시 누락된다** — 남은 일은 `투두리스트.md` 한 곳에만.
6. ⚠️ **lint 총계는 착시**(Biome 파일당 진단 상한). 규칙별·행별로 기준선과 대조할 것.
7. ⚠️ **`@ts-nocheck` 를 지우면 숨은 오류가 나온다** — 덮지 말고 원인을 고칠 것.

---

## 4. ⚠️ 함정 (착수 전 필독)

- 🔴 **`git stash` 금지** — `stash@{0}` 에 2026-07 낡은 stash. 대조는 `git show <커밋>:<경로>`
  또는 `git worktree add <경로> HEAD --detach`.
- 🔴 **vitest 에서 서버 전용 모듈 테스트** = `server-only` 를 빈 모듈로 alias
  (`apps/app/vitest.config.mts`). ⚠️`resolve.conditions` 에 `react-server` 를 **전역** 추가하면
  **React 가 깨진다**(실측). 환경을 node 로 바꿔도 안 풀린다.
- 🔴 **`packages/ai`·`packages/audit`·`packages/payments` 엔 vitest 가 없다** →
  순수함수 테스트도 **`apps/app/__tests__/`** 에 둔다(새 의존성 추가는 사용자에게 먼저 물을 것).
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요**(app·web) — 없으면 Bun 에서 Neon 핸드셰이크 깨져 500.
- 🔴 **Prisma TCP 차단(P1001)** → 스키마 변경은 **neon HTTP 직접 DDL**.
  DB 조회 스크립트는 **`packages/database/` 안**에서 실행해야 `@neondatabase/serverless` 가 잡힌다.
- 🔴 **`Tracking`·`Annotation`·`ActionCompletion` 은 org 컬럼이 없다** → 헬퍼로만 접근.
  ⚠️경로는 **`apps/app/lib/db/scoped.ts`** 다(N-13 정정 — `packages/database/` 에는 없다).
- 🔴 **`AuditJob.result` 에 `rawResponse` 없음**(`excerpt` 만). 원문은 **`Tracking`**.
- 🔴 **`apps/api` 는 죽은 앱** — Vercel 프로젝트 자체가 없다. 새 코드 넣지 말 것.
  cron 은 `apps/web/app/api/cron/`, 웹훅은 `apps/app/app/webhooks/`.
- ⚠️ **app 배포는 `.vercel/project.json` swap → 원복 필수**. www 는 swap 불필요.
  배포는 `npx vercel --prod --yes`.
- ⚠️ **ultracite 는 `(authenticated)`·`[jobId]` 괄호 경로를 못 읽는다** → 디렉터리로 `cd` 후 실행.
- ⚠️ **검증 hook 오탐 4종**(무시할 것): "모델슬러그 하이픈"(세션 제목·Letsur 슬러그에 반응) ·
  "generateObject removed in v6" · route 파일 "no observability"(이미 log 있음) ·
  **`setTimeout` workflow 경고**(이 코드는 Workflow 샌드박스가 아니라 일반 Node 함수다).

---

## 5. 📕 읽을 것 (재조사 금지 표시 확인)

1. ⭐**`투두리스트.md`** — 남은 일 단일 진실 + **"만들지 말 것"** 섹션
2. `유닛이코노믹스_실측_2026-08-07.md` — 원가·시간 (**재실측 금지**)
3. `엔진수_게이팅_판단재료_2026-08-08.md` — #50 판단 근거
4. `UIUX_감사결과_3중_2026-08-07.md` — 미수정 항목 (**재감사 금지**)
5. `_리서치원본_UIUX_2026-08-06/` — **재리서치 금지**(검색 200회 소진).
   `05:118-123` 유료 축 정의(**가장 중요**)

**흔들리면 안 되는 축**: 유료가 이겨야 할 축 = **"시간·비교·알림"**.
⚠️진실거울(무료 aha)을 대시보드로 복사하는 건 **오답**.
