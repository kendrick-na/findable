# 🧭 START HERE — 세션 N-17 (2026-08-10 세션N-16 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` 를 둘 다 열 것.** 여기는 방향·판단·함정, 저기는 남은 일.
> ⛔ `_START_HERE_세션N15` 이하 **전부 폐기**(N15 는 아래 §2 에서 3건이 틀렸음이 확정됐다).
> 🔴 `실행백로그.md` 의 `⬜` 는 믿지 말 것 · `투두_마스터.md` 는 완료 기록용.

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 78bd1fd 이면 세션N-16 종료 상태
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

---

## 1. 🔴 이 프로젝트의 판단 기준 (👤 사용자 정정 — 가장 중요)

> 👤 **2026-08-10 원문**: *"내가 얘기했던 'MVP'라는 표현은 정정할게. MVP가 최소 기능 제품이라는
> 뜻인지 몰랐어. **고객들에게 선보일, 고객들의 페인을 해결하고 니즈를 해결해줄 최고의 제품**을 만들어."*

🔴 **"작게 내보내자"는 더 이상 근거가 아니다.** 우선순위 다툼은 **"이게 고객 페인을 실제로 푸는가"** 로 판정한다.

🎯 **고객의 진짜 페인**(리서치 확정 · ⚠️재리서치 금지):
> 12개 도구 리뷰어의 **독립 반복 지적** = *"측정은 잘하는데 무엇을 해야 할지는 안 알려준다"*
> 사용자 반응 원문 = *"이걸 어떻게 고치는지 더 알고 싶다"* 📕`_리서치원본_UIUX_2026-08-06/06번:176`

**페인은 "점수를 모르는 것"이 아니다**(점수는 경쟁사도 다 보여준다).
**"그래서 내일 아침에 뭘 하면 되는데?"** 가 페인이고, 그게 **카테고리 전체의 공백**이다.

---

## 2. 🔴 세션N-16 이 실측으로 뒤집은 것 (되살리지 말 것)

| 이전 문서 주장 | 실측 결과 |
|---|---|
| *"AI Gateway 크레딧 -$0.11 이라 새 진단이 안 돈다"* | ❌ **오보.** 엔진별 개인 키로 **이미 우회돼 있다**(chatgpt·claude=Letsur · gemini=구글무료 · perplexity=공식API · 한국3종=개인키). 프로덕션 env 7개 키 전부 등록. 크레딧 에러는 **7/29 이전에만**, **7/30 이후 0건**. → **새 진단은 지금도 돈다** |
| *"월요일 아침 카카오페이 ①→②"* | ❌ **이미 8/10 저녁 완료.** 포트원 문의는 **폐기**(콘솔에서 이미 접수됨) · "8/4 접수" 날짜 함정도 발송본에서 이미 삭제됨. 현재 = **심사 대기** |
| *"저장된 옛 회차 daum 오염 → 표시 필터 신설"* | ❌ **만들면 안 된다.** 전수 실측(40회차·1,186블록) 결과 필터 적용 시 **근거 전멸 77건**. 그 응답들은 **이미 `brandMentioned=false`·`sov=0`**(점수 정확) 이고 `dedupeByEngine` 이 엔진당 1장만 써서 **화면에 렌더되지도 않는다**. 화면 노출 무관 블록은 **15%뿐** |
| *"B 원문신뢰 3종을 만들어야"* | ⚠️ **2/3 은 이미 있었고 리서치 기준보다 낫다**(출처 4단 분류 · StatusPill 3상태). 실제로 없던 건 **①브랜드 하이라이트뿐** |
| *"섹션 15개"·"파일 3,696줄"* | ❌ 실측 **큰 블록 10개** · **3,812줄** |

🎓 **교훈은 그대로 유효**: **문서를 믿지 말고 코드·DB·화면을 실측하라.** 이번 세션도 **5건**이 틀렸다.

---

## 3. ✅ 세션N-16 이 배포한 것 (커밋 9건 · web 3회 · app 3회)

| 커밋 | 내용 | 고객 페인 |
|---|---|---|
| `7f0fa79` | **VAT 부가세 10% 미청구 수정**(99,000→108,900 청구) | 매출 누수 |
| `040a173` | **D 밀도축소** — 진실거울 1,409→**676px** · 전체 7,167→**6,434px** | *"너무 길다"* |
| `b6dd63b` | **A1 이어붙이기** — 빈 상태에 *"진단은 그때 쓴 이메일로 이어져요"* + 로그인 주소 표기 | *"내 진단이 사라졌다"* |
| `6c074b6` | **A2 샘플 리포트** — 실제 측정 회차 링크(`?shared=1`) | *"뭔지 모르겠다"* |
| ⭐`12a00de` | **C 처방 5건 전면 공개** + 실행방법 펼침 | ⭐**최대 페인** |
| `f6de40a` | **B 브랜드 하이라이트** — 원문에서 우리 이름만 형광 | *"어디 나오는지 못 찾겠다"* |
| `4292055` | **잠금화면 가짜 숫자 제거** — 실제 진단 링크로 교체 | 규칙 위반 |
| `2e9c84b`·`e3fbc58`·`78bd1fd` | 문서 재편(제품 기준·로드맵) | — |

### ⭐ 가장 중요한 것 = `12a00de` (처방 전면 공개)
🔬**우리가 이기는 지점을 우리 손으로 80% 잠가두고 있었다** — 생성 **5건** / 무료 화면 노출 **1건**.
DB 실측: 처방 **10건 전수 100% 가 `how`(구체 실행방법)를 이미 갖고 있었다**(Princeton 논문 근거 포함).
→ **잠금 제거.** 1건 펼침 + 4건 `<details>` 접힘.
🔒**유료 축은 안 무너진다**: 가치역전 원인은 *"무료가 **반복 이유**까지 주는 것"* 인데
**처방은 1회성 aha 라 반복 이유가 아니다**(반복 이유=**시간·비교·알림**은 그대로 유료).

---

## 4. 🔴 다음 세션이 가장 먼저 알아야 할 것

### **화면에서 코드로 고칠 수 있는 건 사실상 소진됐다.**

🔬 세션N-16 재감사 결론 — 남은 3건은 **기술이 아니라 판단·돈·고객이 관문**이다.

| 남은 것 | 진짜 관문 | 코드로 가능한 것 |
|---|---|---|
| **실행 추적**(처방 완료→before/after) | 🔴**실고객이 처방을 눌러야** 데이터가 생긴다(`ActionCompletion` **0건**) | **이미 준비됨** — 더 만들 것 없음 |
| **PDF 복구** | 💰**진단 1건(150~300원) 승인** 없이 원인 확증 불가 | 승인되면 즉시 |
| **시간축 게이팅** | 👤**"기능이냐 기간이냐" = 가격 정책** | 가짜 숫자 제거는 이미 완료 |

⭐ **그래서 지금 회사를 움직이는 건 코드가 아니라 👤`P0 실고객 지불의사 검증`이다.**
셋 다 그 뒤에 줄 서 있다. (사용자 판단은 *"나중에"* 였으나 **이제 그것이 유일한 병목**임이 드러났다.)

---

## 5. 👤 사용자 결정 대기 3건 (내가 못 하는 것)

### ① 🔴 실고객 지불의사 검증 — **불변 1순위이자 이제 유일한 병목**
마케터 10~15명에게 수동 진단 리포트 → *"월 ₩99K 낼까?"* 관찰. 질문지=`P0_지불의사_시뮬레이션_결과.md`.
⚠️ **시뮬로 대체 불가**(r=0.20 · [[reference_llm_persona_simulation_limits]]).

### ② 💰 PDF 원인 확증에 진단 1건 승인
🔬**마지막 PDF = 2026-05-03.** 이후 **71건 중 0건**(코드는 매번 호출되나 `try/catch` 로 조용히 삼켜짐).
🔴**리서치상 리텐션 핵심** — *"Day 30 종착점은 내보내기·보고"* · *"shareability 가 리텐션의 핵심"*.
⚠️**내 가설 2개가 이미 반박됐다 — 되풀이 금지**:
| 가설 | 반박 |
|---|---|
| `app` 에 `BLOB_READ_WRITE_TOKEN` 없어서 | 토큰이 **있는** `web` 실행분도 똑같이 실패 |
| 300초 타임아웃 | 최근 40건 **평균 19초·최대 54초** |
🟡**남은 후보(미확증)**: `@sparticuz/chromium` **66MB** vs Vercel 함수 **50MB**(바이너리 폴더 날짜 **5/2**).
⬜**확증법 = 새 진단 1건 → `audit.pdf.failed` 로그 실시간 확인.**
✅**급하지 않은 이유**: 공유링크·CSV **둘 다 작동 중**(3대 내보내기 중 2개 정상).

### ③ 👤 잠긴 유료 3종의 게이팅 축 — **가격 정책**
`compare`·`sources`·`alerts` 가 **전부 껍데기**라 **Growth 결제해도 열리는 게 사실상 없다**.
⭐**유력안 = 채널톡식 "시간 축"**: 기능을 잠그지 말고 **보관 기간·갱신 주기·엔진 수**로 나눈다.
> *"기능 잠금은 **당했다**는 느낌을 주지만 **보관 기간은 그렇지 않다**"* (채널톡 5개 티어 기능 동일)
우리는 `autoRefreshHours` 가 있어 **축이 절반 구현돼 있다**.
⚠️`alerts` 는 **지어낸 수치가 아니라 설정 항목 설명**이라 가짜숫자 제거 대상이 아니었다 —
*"없는 기능을 판다"* 는 문제는 **이 결정과 함께** 다뤄야 한다.

### 🟠 (마감 있음) 카카오페이 = **심사 대기 중** — 지금 할 일 없음
🔴**단, VAT 정정으로 제출 스크린샷과 금액이 어긋난다**(`C_결제창.png` 99,000 vs 실제 108,900).
⭐**먼저 움직이지 말 것** — 심사관이 금액을 묻지 않았고, 묻지 않은 쟁점을 열면 심사가 길어진다.
📕 상세 = `_카카오페이_심사보완/⭐월요일_아침_실행카드.md` **최상단**.

---

## 6. ⚠️ 함정 (착수 전 필독)

### 세션N-16 에서 새로 확인
- 🔴 **`LockedSurface` 의 `preview` 는 `pointer-events-none`+`aria-hidden`** — 거기 링크를 넣으면
  **눌리지 않는다.** 실제 링크는 **카드 안**(`sampleUrl` prop)에 둘 것.
- 🔴 **오버레이 `absolute inset-0` 는 프리뷰 높이에 의존한다** — 프리뷰를 짧게 바꾸면
  **카드가 잘리고 본문이 화면 밖으로 밀린다**(라이브 스크린샷에서만 발견).
- 🔴 **`geoActions` 의 `how`·`evidence` 는 마크다운 `**` 를 포함한다** → `stripMarkdown` 필수.
- 🔴 **브랜드 하이라이트는 `brandName`(원본)을 넘길 것** — `brand` 는 빈 값일 때
  *"당신의 브랜드"* 로 대체되는 **표시용 문구**라, 그걸 넘기면 **거짓 형광**이 칠해진다.
- 🔴 **진실거울 카드를 자를 땐 "모름" 카드를 보장할 것**(`pickInitialCards`) —
  정렬이 `인용됨→모름→오류`라 앞 3장만 자르면 **GEO 개선 1순위(=우리가 파는 것)가 통째로 접힌다**.
- ⚠️ **DB 조회 스크립트는 `packages/database/` 안에서** + `PrismaClient` 는 **`./generated/client.ts`**
  (`@prisma/client` 직접 import 는 실패) + **`npx tsx`** 로 실행.
- ⚠️ **`AuditJob.result` 의 엔진 배열 필드명은 `engineResponses`**(`engineResults` 아님 — 이걸로 헤맸다).
- ⚠️ **괄호 포함 경로(`(authenticated)`·`[locale]`)는 Bash 인용이 깨진다** →
  **스크립트 파일로 만들어 실행**하거나 **디렉터리 단위**로 lint 할 것.
- ⚠️ **검증 hook 의 "Model slug uses hyphens" 9건은 오탐** — `세션N-14` 같은 라벨을 모델명으로 오인.

### 기존 함정 (계속 유효)
- 🔴 **`git stash` 금지** — 대조는 `git show <커밋>:<경로>`.
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요**(app·web).
- 🔴 **`apps/api` 는 죽은 앱** — cron 은 `apps/web/app/api/cron/`, 웹훅은 `apps/app/app/webhooks/`.
- 🔴 **`"use server"` 파일은 async 함수만 export 가능** — tsc·lint 통과인데 **빌드만 실패**.
- ⚠️ **app 배포는 `.vercel/project.json` swap → 원복 필수**(trap 으로 보장할 것).
  web 은 그대로 `npx vercel --prod --yes`.
- ⚠️ **lint 기준선 대조법**: `git show HEAD:<경로>` 를 `/tmp` 에 복사해 **규칙별로** 비교
  (Biome 는 파일당 진단 상한이 있어 총계만 보면 착시).
- ⚠️ **QA 계정(`nayoy2+qa@`)은 측정 데이터가 없다** → 대시보드 **빈 상태만** 보인다.
  데이터 있는 화면은 **무료진단 URL**(`www.findable.co.kr/audit/<jobId>`)로.
- ⭐ **화면 검증 도구가 있다**: `python3 scripts/verify-app.py / --expect "문구"` (실제 로그인·스크린샷).

---

## 7. 💰 원가 방침 (사용자 지시, 유지)

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · Clerk API · 브라우저 검증 · 카카오 검색 API | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인받고 실행** |

---

## 8. 📕 읽을 것

1. ⭐**`투두리스트.md`** — 상단 **§제품 기준 → §로드맵** 부터. 남은 일 단일 진실 + **"만들지 말 것"**
2. `UIUX_대개선_설계_v2_2026-08-10.md` — UI/UX 단일 진실
3. `_리서치원본_UIUX_2026-08-06/` **7개 파일** — ⚠️**재리서치 금지**(검색 200회 소진)
   · `06번` = GEO 12개 실제 화면(밀도·Day30) · `07번` = B2B SaaS 21개(빈상태·잠긴유료·나쁜점수 **직답**)
4. `_카카오페이_심사보완/⭐월요일_아침_실행카드.md` — 👤 결제 심사(최상단에 VAT 불일치 판단)

---

## 9. 🔴 하지 말 것

- ❌ **표본 없는 "업계 N분위" 벤치마크** — 조직 1개라 표본이 없다. 쓰면 **숫자 조작**.
- ❌ **지어낸 예시 숫자** — 이번에 `compare`·`sources` 에서 제거했다. **되살리지 말 것.**
  가치를 보여줘야 하면 **실제 진단 회차를 `?shared=1` 로 링크**한다.
- ❌ **임계값 발명**(*"N% 미만이면 경고"*) — 근거 없는 경계선. **분모를 밝히는 것**으로 해결.
- ❌ **소급 데이터 수정**(마이그레이션) — 점수·시계열이 흔들린다. **표시층에서 고친다.**
- ❌ **daum 표시 필터** — 만들면 멀쩡한 근거 **77건이 빈 카드**가 된다(§2).
- ❌ **진실거울(무료 aha)을 대시보드로 복사** — 유료 축(시간·비교·알림)을 안 바꾸는 오답.
- ❌ **`tracking-tight`** — Pretendard 는 자간이 이미 −0.5px 내장.
- ❌ **crew 자기평가에 zod `.max()` 강제** — 위반율 100% 라 리포트가 전멸한다.
- ❌ **나무위키 직접 편집** — 표시광고법.
- (전체는 `투두리스트.md` §만들지 말 것)

---

## 10. 🔴 다음 세션이 잊으면 안 되는 한 줄

> **제품 기준은 "최소 기능"이 아니라 "고객 페인을 푸는 최고의 제품"이다.**
> **화면 코드는 이번에 거의 다 끝냈고, 남은 3건(실행추적·PDF·게이팅)은 전부
> 👤사용자 결정·실고객·돈이 관문이다. 그래서 다음 최우선은 `P0 실고객 지불의사 검증`이다.**
