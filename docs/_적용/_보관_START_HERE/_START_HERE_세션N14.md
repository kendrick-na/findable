# 🧭 START HERE — 세션 N-14 ⛔ **폐기됨**

> 🔴🔴 **이 파일은 끝났다. 새 세션은 [`_START_HERE_세션N15.md`](_START_HERE_세션N15.md) 를 열 것.**
> 여기 적힌 "지금 할 일"(전면 감사 미완 5건)은 **4건이 완료**됐고 남은 1건은
> 👤크레딧 충전(사용자 몫)뿐이다. **이 문서를 그대로 따라가면 이미 한 일을 또 한다.**

---

# (아래는 기록 보존용) START HERE — 세션 N-14 (2026-08-10 세션N-13 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` 를 둘 다 열 것.** 여기는 방향·함정, 저기는 남은 일.
> ⛔ `_START_HERE_세션N13`·`N12`·`N11`·`N10`·`N9`·`세션E`·`세션L`·`다음세션`은 **전부 폐기**.
> 🔴 **`실행백로그.md` 의 `⬜` 는 믿지 말 것** · **`투두_마스터.md` 는 완료 기록용**.

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 2b81f5d 이면 세션N-13 종료 상태
git status --short        # 비어 있어야 정상
cat .vercel/project.json  # projectName 이 "findable"(=web) 이어야 정상
```

---

## 1. 🔴 지금 바로 이어서 할 일 — **전면 감사 미완 3건**

세션N-13은 **사용자 캡처 제보**(네이버 브리핑 화면에 HTML 코드 노출)에서 시작해
전수감사를 돌렸고, **오염 유형 4개 중 1개만 고치고 끝났다.** 나머지가 아래다.

### 🔴 A. daum 무관 문서가 "AI 답변"으로 화면에 노출 (**최우선**)

**실측**: daum 응답 **279건 중 129건(46%)** 에 브랜드와 무관한 문서가 섞여 있다.

| 브랜드 | 화면에 나간 "AI 답변" |
|---|---|
| 조선미녀 | *"나무위키:접근 제한/문서 목록… 화성의과학대학교, 나사렛대학교, 푸바오"* |
| 메디큐브 | *"아이폰 단축어, 노션, 자기관리, 앱 추천"* |
| SK하이닉스 | *"말왕 - 나무위키"*, *"애플TV 4K 리뷰"* |
| 올리브영 | *"High Guardian Spice - 나무위키"* |

**근본 원인** = `packages/ai/lib/engines/korean-adapters.ts:~330` (P0-c, 2026-07-27)
```ts
const [promptDocs, brandDocs] = await Promise.all([
  searchKakao(query.prompt),      // 질문으로 검색
  searchKakao(brandQuery),        // 🔴 브랜드명으로도 검색해서 뒤에 붙임
]);
const docs = [...promptDocs, ...brandDocs];   // 합쳐서 "AI 답변"으로 저장
```
의도는 *"이 브랜드가 다음에 실재하나"* 확인이었는데, 카카오 검색이 엉뚱한 문서를
반환하면 **그게 그대로 고객 화면의 "AI 답변"이 된다.**

🔬 **조사가 확인해준 것**: 이 구조의 정식 명칭은 **contamination(오염) / label leakage** 다.
   *"측정으로 고칠 수 있는 문제가 아니라 파이프라인부터 분리해야 한다."*

⚠️ **단, 내 첫 가설은 실측으로 틀렸다 — 반드시 읽을 것**:
   - ❌ "가짜 언급이 점수를 부풀린다" → **틀렸다.** daum 언급률은 **46%로 8개 엔진 중 최저**
     (claude 99% · chatgpt 99%). daum 을 빼면 SoV 가 **평균 +6.7 올라간다.**
   - 명백한 오탐(브랜드명이 본문에 아예 없는데 언급=true)은 **279건 중 6건(2%)** 뿐.
   - ⭐ **그러므로 점수 로직은 건드리지 말 것.** 고칠 것은 **화면에 나가는 근거 텍스트**다.

**권고 수정**:
1. `brandDocs` 를 **판정용으로만** 쓰고 `rawResponse`(화면 근거)에서는 분리
2. 또는 브랜드명이 안 들어간 스니펫은 근거 목록에서 제외
3. 🔒 **소급 데이터는 건드리지 말 것**(표시층만 고치면 기존 점수 불변)

### 🟡 B. 감성 분포가 분모 없이 개수로 나간다
```
{"neutral":20,"negative":0,"positive":6}   합=26   ← 퍼센트가 아니라 건수
{"neutral":0,"negative":0,"positive":0}    합=0    ← 🔴 0으로 나눌 위험
```
화면은 "긍정 15%"로 보여주는데 분모가 없다. **합이 0인 회차가 실재**한다.

### 👤 C. AI Gateway 크레딧 — **사용자 확인 필요**
진단 71건에서 **에러 응답 301건**이 나왔고 원인이 하나다.
```
A positive credit balance is required for all requests   ~120회
AI Gateway requires a valid credit card on file           32회
Free credits temporarily have restricted access           21회
```
🔴 **크레딧이 비어 있으면 새 진단이 아예 안 돈다.** 5/1·7/27 회차는 통째로 실패했고
설화수 진단은 **분석가 0/3 성공** = 처방 없이 화면이 렌더됐다.
→ 고쳐도 검증할 데이터가 안 생기므로 **A 착수 전에 사용자에게 확인할 것.**

---

## 2. ✅ 세션N-13 이 한 일 (커밋 9건, app 배포됨)

### ① 화면 텍스트 오염 전수 해소 (`2b81f5d`) — **195건 → 0건**
사용자 캡처에서 시작. 저장된 응답 **1,779건** 전수감사.

| 오염 | 건수 | 엔진 |
|---|---|---|
| HTML 엔티티 | **868회** | daum(`&#39;`610·`&#34;`257) · naver-briefing |
| 마크다운 링크 | 32건 | perplexity·hyperclova·gemini·claude |
| 네이버 UI(`Keep에 저장` 등) | 6건 | naver-briefing |
| HTML 태그(`<br>`) · 미종결 태그 | 5건 | perplexity · naver-briefing |

🔴 **근본 원인 = `stripHtml` 이 세 가지가 틀렸다**
①`/&[a-z]+;/gi` 라 **숫자형을 아예 못 잡음**(868회 통과) ②엔티티를 **공백으로 치환**
(디코딩이 아니라 `It&#39;s`→`It s` 로 글자 훼손) ③태그를 먼저 지워 **순서 역전**
\+ 네이버가 **고지문 문구를 바꿨는데** 우리 목록이 안 따라감.

→ **`packages/ai/lib/engines/sanitize.ts` 신설 = 정제의 단일 진실**, 4개 어댑터 전부 적용.
   판정·저장·표시가 **같은 텍스트**를 쓴다(화면과 점수가 어긋나는 사고 차단).
   ⚠️**이중 인코딩**(`&amp;#39;`) 실재 → 상한 **3패스**(무한 반복하면 원문 `&amp;amp;` 훼손).
   ⚠️**잘린 마크다운 링크**(응답이 끊겨 `)` 없음) 5건 별도 처리.

### ② 무료 진단자도 액션 완료 체크 (`bc48646`·`e311556`·`977cc15`, **배포됨**)
`ActionCompletion` **0건**의 원인은 버그가 아니라 **입구가 좁아서**였다 —
처방은 보이는데 `brandId` 가 없어 **저장할 곳이 없었다**.
→ 완료를 누르면 서버가 `domain` 으로 Brand 를 도출/생성해 연결.
🔗 조치 기록이 쌓여야 **before/after** 를 만들 수 있다(투두 "만들지 말 것"의 유일한 해소 경로).

🔬 **교차검증 2회로 결함 5건** — 전부 통상 검사를 통과한 상태였다:
① `"use server"` 는 **동기 함수 export 불가** → tsc·lint·테스트 통과인데 **빌드만 실패**
② 도메인 형식 검증 누락 ③ 브랜드 한도 우회 ④ **완료 SoV 유실**(기능 목적 무력화)
⑤ Brand 생성 후 `/brand` 미갱신

### ③ 검증 도구 2종 (`10d1c57`) — **"직접 눌러보세요"를 없앴다**
```bash
python3 scripts/verify-app.py                       # 화면 5개 자동 검증 + 스크린샷
python3 scripts/verify-app.py /actions --expect "완료로 표시"
python3 scripts/findable_admin.py users             # 사용자·plan 현황
python3 scripts/findable_admin.py plan <email> growth  # 유료 화면 검증용
python3 scripts/findable_admin.py token <email>     # 로그인 폼 건너뛰기
```
- 계정: `apps/app/.env.local` 의 `FINDABLE_TEST_EMAIL`(`nayoy2+qa@gmail.com`)·`FINDABLE_TEST_PW`
- Clerk 프로덕션 키: 같은 파일 `CLERK_PROD_SECRET_KEY` (**git 제외 확인됨**)
- 🔴 도구를 만들자마자 **실제 결함 1건 발견**: 승인된 파트너가 폐기된 `plan=insider` 라
  `free` 로 폴백돼 **특전을 못 받고 있었다** → `growth` 정정 완료.

### ④ 문서 정정
- `UIUX_감사결과_3중` 이 **8/7 에 멈춰** 이미 만든 **8건을 "⬜ 남음"** 으로 적고 있었다 → 실측표로 정정
- 투두 전면 재정리 + **결제 심사 트랙 편입** + **정기결제 미구현 발견**
- crew 자기평가 교차검증(표본 18→31건, N-12 수치 전건 재현)

---

## 3. 🎓 교훈 (이번 세션에서 값비싸게 배운 것)

1. 🔴 **"내부 정합성"만 고치고 있으면 고객이 보는 화면이 깨진 걸 못 본다.**
   N-9~N-13 이 대시보드·crew·완료체크를 고치는 동안, 정작 **무료 진단 화면에
   HTML 코드가 그대로 나가고 있었다.** 사용자가 캡처를 보내고서야 알았다.
2. 🔴 **`tsc`·lint·테스트가 다 통과해도 빌드는 깨질 수 있다** (`"use server"` 동기 export).
   **배포 전 `next build` 를 반드시 돌릴 것.**
3. 🔴 **가설을 실측으로 검증하기 전에 고치지 말 것.** "daum 가짜 언급이 점수를 부풀린다"는
   내 가설은 **정반대**였다(daum 이 점수를 낮추고 있었다).
4. 🔴 **추천하기 전에 DB를 볼 것.** `plan=insider` 를 `free` 로 정리하자고 추천했다가,
   DB 에서 **승인된 파트너**임을 확인하고 `growth` 로 뒤집었다. 그대로 했으면 권한을 없앨 뻔했다.
5. **교차검증은 2회까지 새 결함을 낸다.** 1차에서 3건, 2차에서 2건이 더 나왔다.
6. ⚠️ **뮤테이션 테스트로 가드 실효성을 확인할 것** — 테스트가 통과한다고 지키는 게 아니다.

---

## 4. ⚠️ 함정 (착수 전 필독)

### 이번 세션에서 새로 확인한 것
- 🔴 **`"use server"` 파일은 async 함수만 export 가능** — 동기 헬퍼는 별도 파일로
  (순수 함수는 `apps/app/lib/domain.ts` 처럼 `"use server"` **밖**에).
- 🔴 **`AuditJob.domain` 은 정규화가 보장되지 않는다** — 실측 29종 중 3종이 `www.`·경로 포함.
  **`sulwhasoo.com` 과 `www.sulwhasoo.com` 이 DB 에 둘 다 존재.** 정규화 없이 Brand 를
  도출하면 같은 브랜드가 **두 건으로 갈라진다** → `@/lib/domain` 을 반드시 통과시킬 것.
- 🔴 **로그인 성공을 URL 로 판정하지 말 것**(리다이렉트가 늦게 끝나 오탐).
  앱 셸(사이드바 "워크스페이스")이 떴는지로 판정한다.
- ⚠️ **Cloudflare 봇 차단(Turnstile)** 때문에 **가입은 자동화 불가**. 로그인은 가능.
- ⚠️ **`nayoy2@gmail.com`(사용자 본인)은 비밀번호가 없다** — 이메일 코드 인증이라 자동화 불가.
  검증은 `nayoy2+qa@gmail.com` 으로.
- ⚠️ 이 환경의 **python ssl 이 api.clerk.com 을 못 뚫는다** → `curl` 로 호출(도구에 반영됨).
- ⚠️ `Tracking` 의 시각 컬럼은 **`trackedAt`**(`createdAt` 아님).
- ⚠️ `_vercel/insights/script.js` **404** 는 Analytics 미설정 탓 — **기능 영향 없음**.

### 기존 함정 (계속 유효)
- 🔴 **`git stash` 금지** — 대조는 `git show <커밋>:<경로>` 또는 `git worktree add … HEAD --detach`.
- 🔴 **vitest 서버 전용 모듈** = `server-only` 를 빈 모듈로 alias.
  ⚠️`resolve.conditions` 에 `react-server` **전역 추가하면 React 가 깨진다**.
- 🔴 **`packages/ai`·`audit`·`payments` 엔 vitest 가 없다** → 테스트는 **`apps/app/__tests__/`** 에.
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요**(app·web) — 없으면 Bun 에서 Neon 핸드셰이크 깨져 500.
- 🔴 **Prisma TCP 차단(P1001)** → 스키마 변경은 neon HTTP 직접 DDL.
  DB 조회 스크립트는 **`packages/database/` 안**에서 실행(`@neondatabase/serverless` 해석).
- 🔴 **`Tracking`·`Annotation`·`ActionCompletion` 은 org 컬럼이 없다** →
  **`apps/app/lib/db/scoped.ts`** 헬퍼로만 접근(⚠️`packages/database/` 에 없다).
- 🔴 **`AuditJob.result` 에 `rawResponse` 없음**(`excerpt` 만). 원문은 **`Tracking`**.
- 🔴 **`apps/api` 는 죽은 앱** — Vercel 프로젝트가 없다. cron 은 `apps/web/app/api/cron/`,
  웹훅은 `apps/app/app/webhooks/`.
- ⚠️ **app 배포는 `.vercel/project.json` swap → 원복 필수**. `npx vercel --prod --yes`.
- ⚠️ **검증 hook 오탐 4종**(무시): "모델슬러그 하이픈" · "generateObject removed in v6" ·
  route "no observability" · `setTimeout` workflow 경고.

---

## 5. 💰 원가 방침 (사용자 지시, 2026-08-10)

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · Clerk API · 브라우저 검증 | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인받고 실행** |

> 사용자 원문: *"함부로 새로 진단하고 하면 안됨. 돈 들어가니까. 진짜 필요한 경우에만."*

---

## 6. 📕 읽을 것 (재조사 금지 표시 확인)

1. ⭐**`투두리스트.md`** — 남은 일 단일 진실 + **"만들지 말 것"** 섹션
2. `_카카오페이_심사보완/⭐월요일_아침_실행카드.md` — 👤**결제 심사(마감 있는 유일한 트랙)**
3. `유닛이코노믹스_실측_2026-08-07.md` (**재실측 금지**)
4. ⭐**`UIUX_대개선_설계_v2_2026-08-10.md`** — **UI/UX 단일 진실**(IA·유저플로우·인터랙션·비주얼 4축)
   ⚠️ `UIUX_대개선_기획서_2026-08-06.md`(v1)는 **5건이 낡았다** — 상단 경고표 먼저 볼 것
5. `UIUX_경쟁사_화면대조_2026-08-10.md` — 경쟁사 15개 툴 vs 우리 화면 항목별 대조
6. `UIUX_감사결과_3중_2026-08-07.md` — ⚠️**상단 N-13 실측표를 먼저 볼 것**(본문은 8/7에 멈춰 있다)
7. `_리서치원본_UIUX_2026-08-06/` — **재리서치 금지**(A트랙 검색 200회 + B·C트랙 2회 소진)
   **7개 파일** — 06번(GEO 도구 실제 화면·인터랙션) · 07번(B2B SaaS 21개)이 **2026-08-10 추가분**

**흔들리면 안 되는 축**: 유료가 이겨야 할 축 = **"시간·비교·알림"**.
⚠️진실거울(무료 aha)을 대시보드로 복사하는 건 **오답**.

---

## 7. 🎯 사용자가 세션 말미에 지적한 것 (다음 세션이 답해야 할 질문)

> *"UI/UX 이전에 개선한 게 뭘 개선했다는 건지 모르겠음. 리서치했던 경쟁사·선두주자
> 화면 구성은 반영한 게 없음? 지금 화면 구성이 곧 UI/UX 인데 고객 입장에선 납득할 수
> 없을 것 같은데? 뭐가 뭔지 하나도 모르겠고."*

✅ **이 지적에 답했다 (2026-08-10 세션N-14).**
1. ✅ 엔진 응답 텍스트 오염 전수 (**완료**)
2. ✅ **화면 텍스트 실물 확인** — 라이브 13개 캡처. 🔴**"오염 195건→0건"이 사실과 달랐음을 여기서 잡았다**
   (저장 시점만 고쳐서 **옛 회차 49건이 그대로**였다 → `stripMarkdown` 표시층에서 해소·배포·재검증)
3. ✅ **경쟁사 화면 구성 대조** — `UIUX_경쟁사_화면대조_2026-08-10.md`
4. ✅ **리서치 3트랙** — A(기존자산) + B(경쟁사 실제화면 12개) + C(B2B SaaS 21개) → **설계 v2**

🎯 **답의 요지**: *"반영한 게 없냐"* → **업계 표준은 이미 따라가 있었다**(히어로 4지표·질문형 라벨).
   안 보였던 이유는 ①**측정 데이터가 없으면 빈 화면만** 뜨고 ②**화면이 실제로 틀린 말을 하고 있었다**
   (HTML 코드 노출 49건 · 측정 0건인데 "월 800세션 손실" 단언 7건 — **둘 다 오늘 수정·배포**).
   → **지적이 정확했다.**

🔴 **그리고 대조에서 새로 드러난 것**: 유료 축 3개(`compare`·`alerts`·`sources`)가 **전부 껍데기**라
   **Growth 를 결제해도 열리는 화면이 사실상 없다.** → 투두 **P1.6** 에 등록(👤 결정 대기).

### 🧭 GEO 에이전트 = **자체 블로그 플랫폼** (사용자 정의, 오해 주의)
> *"GEO에 최적화된 미디엄 같은 = 자체 블로그 플랫폼을 만드는 거임."*
> *"진단만 하는 게 아니라 뭘 해야하는지 직관적으로 알려주고, 실제 해야할 것 바로 해주는 에이전트까지."*

🔴 문서의 *"진단→리라이트→발행"* 기재는 **"고객 사이트를 대신 고친다"로 오해된다**.
   실제 의도는 **우리가 운영하는 플랫폼**이라 **권한 문제가 없고 지금도 착수 가능**하다.
   (내가 *"실고객 0명이라 승인할 사람이 없어 불가"* 라고 판단한 건 **오답**이었다.)
