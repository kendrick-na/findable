# 🧭 START HERE — 세션 N-30 (2026-08-14 세션N-29 종료 인계)

> ⛔ `_START_HERE_세션N29` 이하 전부 폐기.
> 🎉 **N-29 는 로그인 500(20%)·로그아웃·빌드실패를 전부 닫았다.** 재조사 금지.
> 🔴 **다음 할 일 = 재설계안 v2 §7 의 1~4번.** 승인은 받았다(범위: 흐름만).

---

## 0. ⚡ 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -1        # f84988e 이면 인계 시점 그대로
cd apps/app && npx vitest run   # 326/326 · Errors 0
```

**상태**: 테스트 **326/326** · web·app tsc **0** · app **배포 완료** · 500 **0/60**
⚠️ `packages/payments/` 2건 + `_sov3.png` + `billing-cycle.test.ts` = **다른 세션 것**(카카오페이 심사 중). 건드리지 말 것.

📕 **먼저 읽을 것**: `docs/_적용/재설계안_v2_온보딩_대시보드_2026-08-14.md`

---

## 1. ✅ N-29 가 해결한 것 (재조사 금지)

### 1-a. 🎉 로그인 500 (20% → **0/60**) — 원인 확정
**원인 = `CLERK_ENCRYPTION_KEY` 부재.** Vercel Production 에 추가하니 사라졌다.
**로그아웃 실패·재로그인 루프도 같은 뿌리**였고 함께 해소(구조 판정: 쿠키 소멸 + 보호페이지 차단).

🔴 **인계서(N-29)의 "코드가 아니라 도메인 계층" 결론은 무효였다:**
대조군 `*.vercel.app` 은 Clerk 프로덕션이 CNAME 을 요구해 **로그인이 성립하지 않는 주소**다
→ 서명 검사 코드에 도달조차 못 한다. **0/20 은 통과가 아니라 시험을 안 본 것.**
에러 문구 `"middleware did not run"` 도 거짓 — `assertTokenSignature` 는 미들웨어가
`AuthStatus: SignedIn` 을 심은 뒤에만 도달한다.

### 1-b. 🎉 Vercel 빌드 실패 복구
`empty-state.test.tsx` 가 render 7회를 **한 번도 정리하지 않아** vitest 가 jsdom 을 철거한 뒤
React 타이머가 깨어나 `window` 를 찾았다 → `afterEach(cleanup)` 한 줄.
⚠️ **테스트는 326/326 통과하는데 빌드는 실패**했고 **로컬에서 재현 안 됐다**(로컬 2.1초 / CI 18.3초).
🎓 `326/326` 이 **경쟁 조건 위에** 있었다.

### 1-c. 소셜 버튼 국내 관행화 + 한국어 UI
아이콘 정사각형 3개 → **전체폭 3줄 + 로고 + "~로 시작하기"**. 약관 링크 0개→2개.
- `@clerk/localizations` koKR + `socialButtonsBlockButtonManyInView` 덮어쓰기
  (🔴 제공사 3개 이상이면 Clerk 가 짧은 라벨을 써서 "~로 시작하기" 가 **안 나온다**)
- 🔴 런타임은 `options` 에서 읽는데 타입은 `layout` 만 안다 → **둘 다 전달**(타입만 우회)
- 🔴 세로 1열은 **Tailwind 로 못 이긴다**(`cl-internal-*` 이 3열 고정) → `elements` **객체 형식**으로

---

## 2. 🔴🔴 최우선 — 재설계안 v2 §7 의 1~4번

📕 `docs/_적용/재설계안_v2_온보딩_대시보드_2026-08-14.md` (⛔ v1 폐기)

| # | 무엇 | 위험 | 조건 |
|:--:|---|:--:|---|
| **1** | 지표에 **설명 한 줄** | 없음 | — |
| **2** | 브랜드명 **선택 입력** | 낮음 | `required` 제거 + `assign.ts` 검사 1줄 삭제 |
| **3** | 등록 시 **자동 측정** | 중간 | 🔴 **아래 3가지를 같은 커밋에** |
| **4** | **대기 화면** 신설 | 중간 | 러너 진행 구조 선행 확인 |

### 🔴 3번의 필수 조건 3가지 (교차검증에서 나옴)
1. **반환 타입 확장** — 지금 `{ok}|{error}` 라 24h 한도에 걸리면 *"등록했어요"* 만 뜨고
   **측정은 안 되는 조용한 실패**가 난다 → `{ok, measurement:"started"|"rate_limited"|"failed"}`
2. **거짓이 되는 문구 6곳** 같은 커밋에서 수정(*"이제 측정 시작을 눌러…"* 등)
   ⚠️ 코드 주석에 *"바로 측정을 시작할 수 있어요"* 를 **거짓이라 지웠다**는 기록이 있다 → 이번에 다시 참이 된다
3. **`empty-state.test.tsx` 가 `"측정 시작하기"` 를 하드코딩** → 자동측정 후 거짓인데 **테스트는 통과**한다
   (가드가 거짓말을 지킨다) → 구조 검사로 교체

---

## 3. 👤 대표님 대기 2건

1. 🔴 **네이버 버튼 Disable** — Clerk → SSO connections → Naver → Danger zone → `Disable`
   (§5 참고. **깨진 버튼이 지금 로그인 화면에 그대로 있다**)
2. 🟡 **Vercel Pro $20/월** — 오늘 배포 6회로 무료 한도 100% 도달. 소모원은 **배포**(크론 무죄)

---

## 4. ⬜ 다음 설계 (범위 밖으로 미룬 것)

| 무엇 | 왜 미뤘나 |
|---|---|
| **대시보드 탭 분할** | 👤 결정: *"흐름 먼저, UI/UX 는 다음 설계"*. Profound 는 탭 8개, 우리는 **10.6화면**(실측·미해결) |
| 기간 비교(`vs 이전기간`) | 무료는 **8일 이상** 지나야 보임 + 🔴 **심사 중 `plan.ts` 금지** |
| 경쟁사 저장 | 🔴 **읽는 곳이 없으면 써도 무의미**. 그리고 타입이 3종으로 갈림 |
| 확인 단계 화면 | 🔴 **언어 축이 무효**(아래 §5) — 정직하게 못 만든다 |
| Content(글 생성) | `buildGeoActions` 가 이미 근거·실행방법·우선순위를 준다. 겹침 |
| **유료 근거**(알림 메일) | 크론 완성·스위치 기본 꺼짐. ⚠️ 켜면 판매 문구가 바뀌어 **심사 영향** |

---

## 5. ⚠️ 함정 (N-29 신규 + 계속 유효)

- 🔴🔴 **읽는 코드가 없는 필드에 컨트롤을 만들지 말 것.** `Brand.language` 는 **읽는 곳 0**이다.
  v1 설계안이 *"영어만 고르면 4곳"* 컨트롤을 넣을 뻔했다 — **렌더되는 순간 거짓말**이다.
  (엔진 선택은 `Prompt.language`/`generateAuditPrompts` 가 정한다 — `runner.ts:271`)
- 🔴🔴 **`marketScope` 를 측정 후 바꾸면 분모가 갈린다** → 추세 그래프가 두 기준을 한 선으로 그린다.
- 🔴 **네이버 로그인은 Clerk 로 못 고친다**(Debug 로 확정): 네이버가 `response` 중첩으로 주는데
  Clerk 가 **중첩 경로 매핑을 실제로 못 읽는다**(화면엔 입력되지만 값이 `-`).
  ⛔ 시도해서 막힌 것: 대시보드 기본값 True · 네이버 앱 설정(이미 정상) · 중첩 매핑 · **OIDC 전환**
  (`scopes_supported` 에 email 이 **없어** 인증창에도 못 감).
  → **중계 서버**(`apps/web/app/api/auth/naver/userinfo/route.ts`, 미배포)가 유일한 길이나
  **B2B 에 네이버는 표준이 아니다**(경쟁사 9곳 중 채택 **0곳**) → **내리는 게 답**.
- 🔴 **`vercel --prod` 는 git 이 아니라 로컬 폴더를 통째로 올린다** — 미추적 파일도 같이 간다.
- 🔴 **Vercel Sensitive env 는 `pull` 하면 `[SENSITIVE]`** → CLI 로 값 판정 불가. 대시보드에서 확인.
- ⚠️ **playwright 스크립트는 Findable 폴더에서 실행**(scratchpad 는 모듈을 못 찾는다).
- ⚠️ **이미지 크기 제한**: 여러 장 볼 때 어느 변이든 2000px 초과 금지. 영상 분석은 **OCR**(`/tmp/ocr.swift`)이 유효했다.
- 🔴 **`git stash pop` 금지** — 7월 백업 stash 가 아직 살아 있다.

---

## 6. 📊 Profound 실측 (영상 2개 · `docs/_경쟁사_UIUX/Profound/`)

| 항목 | Profound |
|---|---|
| 로그인 | **소셜 0개** · `name@company.com` |
| 온보딩 | 도메인 → 국가 → 언어 (**업종 안 물음**) |
| 자동 산출 | 경쟁사 순위 · 사업요약 · 엔진별 추세 |
| **대기 화면** | 배경 검정 · 중앙 **7%만** · 로고+도메인 2줄 · **2분 20초** · 🔴 진행 표시 **없음** |
| 대시보드 | 탭 **8개** · `Last 7 Days` × `vs Previous` · 지표마다 설명 한 줄 |
| Content | 템플릿 → 브리프 → 초안 |
| 무료 진단 | 🔴 **없음** (선두 9곳 중 익명 무료진단 **0곳**) |

⭐ **우리만 있는 것**: 무료진단 15섹션 · 네이버/다음 · 한국어 개체 그라운딩 · 진실의 거울 ·
처방에 **근거**(`GeoAction.evidence/how/priority`)

---

## 7. 🔴 잊으면 안 되는 것

> ① **화면 작업이면 `Skill(ui-ux-quality)` 먼저.** 말 안 해도.
> ② **도구가 통과시켜도 눈으로 봐야 한다** — N-29 에서 소셜 버튼 문구가 `Googl…` 로 **잘렸는데
>    도구는 "블록버튼 3 · 문구 정상"으로 통과**시켰다.
> ③ **승인 전 코드 금지**(배포된 실사용 화면).
> ④ ⚠️ `packages/payments/` = 다른 세션 + **카카오페이 심사 중**(2~3주, 현 상태 유지 요구).
> ⑤ 🔴 **회사 병목은 여전히 P0 실고객·유통.** 유저 **0명**.
