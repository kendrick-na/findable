# 🧭 START HERE — 세션 N-21 (2026-08-11 세션N-19/20 종료 인계)

> ⚠️ **이 파일 + `투두리스트.md` + `UIUX_대개선_설계_v3_2026-08-10.md` 를 열 것.**
> ⛔ `_START_HERE_세션N20` 이하 **전부 폐기**. ⛔ `UIUX_대개선_설계_v2` 폐기(근거만 유효).

---

## 0. ⚡ 새 세션 첫 3분

```bash
cd /Users/easymilli/Downloads/바이브코딩/Findable
git log --oneline -3      # 최신이 07b91f1 이면 이 인계 시점 그대로
git status --short        # 비어 있어야 정상
```

---

## 1. 🔴🔴 다음 착수 = **S6** (승인 완료 · 설계 확정 · **코드는 한 줄도 안 씀**)

👤 **사용자가 "2번"으로 승인했다** = **S6-a + S6-b 둘 다 진행**.
그 뒤 사용자 지적으로 **S6-c(비주얼·인터랙션)가 추가**됐다 — 아래 §2 마지막 질문만 답받으면 착수.

### ⛳ 착수 직전 상태
- ✅ 감사 완료 · ✅ 설계안 확정 · ✅ 사용자 승인 · ✅ **백업 완료**
  (`_백업/sidebar_v1_평평한9항목.tsx.bak` · `_백업/brand_page_v1_이름3개.tsx.bak`)
- 🔴 **아직 코드 변경 0건.** 작업트리 깨끗.

### S6-a — 이름 통일 + 🔴거짓 설명문 (위험 낮음)

🔬 **실측 확인**: `/brand` 를 부르는 이름이 **3개**다.

| 위치 | 현재 | → 제안 |
|---|---|---|
| 사이드바 (`sidebar.tsx:73`) | `측정 시작` | **브랜드·측정** |
| 페이지 title·Header·h1 (`brand/page.tsx:14,66,70`) | `브랜드 측정` | **브랜드·측정**(동일) |
| 폼 제목 (`brand/page.tsx:149`) | `새 브랜드 등록` | **브랜드 추가** |

🔴 **거짓 설명문 (같은 파일 `brand/page.tsx:152-153`)**:
현재 *"브랜드 이름과 도메인만 있으면 **바로 측정을 시작할 수 있어요**"*
→ 🔬**코드 실측: 등록만 하고 측정은 안 한다.** 등록 후 토스트가 스스로
  *"이제 측정 시작을 눌러 첫 측정을 실행해 보세요"* 라고 실토한다
  (`features/brand/assign-brand-form.tsx:77`). 실제 측정 트리거는 브랜드별
  `StartTrackingButton`(`brand/page.tsx:133`)이다.
→ 제안 문구: **"등록하면 측정 버튼이 생겨요. 측정은 1~3분 걸려요."**

⚠️ **N-18 이 이 화면을 "오탐"이라고 판정했는데 그 판정이 부분적으로 틀렸다.**
측정 버튼은 실재하므로 화면 전체가 거짓은 아니지만 **이 설명문 한 줄은 거짓**이다(원인②).

### S6-b — 사이드바 3단 그룹화 (위험 중간)

```
현재 (평평한 9개 · 4개 자물쇠)        → 제안 (3단 · 실행을 최상단)
────────────────────────────         ────────────────────────────
대시보드                              ⭐ 실행
측정 시작                                지금 할 일        ← 우리가 이기는 지점
측정 이력                             측정
지금 할 일                               대시보드
경쟁사 비교      🔒                      브랜드·측정
출처 링크        🔒                      측정 이력
추적 알림        🔒                   분석
데이터 내보내기  🔒                      경쟁사 비교     🔒
                                        출처 링크       🔒
                                        추적 알림       🔒
                                        데이터 내보내기 🔒
```

📕 **리서치 근거 (원본 확인함 · `06_GEO도구_실제화면_인터랙션_2026-08-10.md`)**:
- **Gauge**: `[VERIFIED]` 3단계 **`Monitor → Analyze → Act`** (`:152`)
- **Scrunch AI**: `[VERIFIED]` 최상위 IA **2분할** `Monitoring`/`Insights` (`:83`)
- 리서치 결론 §5: *"**조직 구조가 IA 를 결정**"* (`:231`)

🔴 **단, 내가 선례를 그대로 베끼지 않은 곳이 한 군데 있다 — 반드시 알고 시작할 것**:
**Gauge 는 `Act` 를 마지막에 두는데 나는 「실행」을 최상단에 올렸다.**
근거 = 리서치 확정 페인이 *"측정은 잘하는데 뭘 해야 할지 안 알려준다"* 이고 `geoActions` 가
우리 유일한 구조적 우위인데 **지금은 사이드바 4번째**라서다. **의도된 변경**이지 실수가 아니다.
(사용자가 순서를 되돌리라고 하면 Gauge 원형대로 실행을 맨 아래로 두면 된다.)

### ⛔ S6 에서 **하지 말 것** — 기획서 §3-1 "잠금 4개 접기" 는 **보류 권고**

기획서는 잠긴 4개를 「더보기」로 접거나 1개로 줄이라고 하지만 **반대 근거가 있다**:
- 🔬 `compare`·`sources` 는 **실제로 작동하는 기능**이다(N-17 실측). 접으면 **팔 수 있는 것을
  숨기는** 셈이고, 이는 N-17 이 고친 *"있는 기능을 안 팔고 있었다"* 결함의 **재발**이다.
- "잠긴 게 절반이라 쓸 수 없는 제품으로 읽힌다"는 문제는 **접어서가 아니라 그룹화로** 풀린다
  — 「분석」 그룹에 모이면 "이 묶음은 상위 플랜"으로 읽힌다.
👤 **사용자가 접길 원하면 그대로 하면 된다**(내 권고는 보류일 뿐).

### 🆕 S6-c — 비주얼·인터랙션 4건 (**사용자 지적으로 추가 · 마지막 질문 미답변**)

🔴 **내 누락을 사용자가 잡았다**: 기획서 v3 는 **6개 축**(IA·유저플로우·인터랙션·비주얼·카피·SEO)인데
내 S6 제안은 **IA 하나뿐**이었다. §7(비주얼·인터랙션)은 **미착수**다.

🔬 **4건 전부 코드로 아직 열려 있음을 확인했다**:

| # | 결함 | 실측 위치 |
|:--:|---|---|
| 1 | 🔴 블러 프리뷰 대비 **1.74:1**(WCAG 4.5 미달) | `locked-surface.tsx:59` `opacity-30 blur-[5px]` |
| 2 | 🔴 **`(준비 중)` 이 완성 기능과 똑같은 주황 체크(✓)** | `billing/page.tsx:211` `CheckIcon` 하나로 전부 |
| 3 | 🟡 업종 드롭다운 너비(`w-fit`) 불일치 | `assign-brand-form.tsx` — grep 무결과, **재확인 필요** |
| 4 | 🟡 **`/history` 가 실패·대기 행에도 "결과 보기 →"** | `audit-history-list.tsx:90` **조건 없음** ↔ `brand/page.tsx:117` 은 `completed` 만 = **자기모순** |

⭐ **#2·#4 는 원인②(파는 것과 실제가 다름) 계열**이다 — 체크는 "제공됨"으로 읽히고,
실패한 측정에 "결과 보기"는 **없는 결과를 약속**한다.
⭐ **묶는 이유**: #2·#4 의 파일(`billing/page.tsx`·`audit-history-list.tsx`)은 **이번 세션에 이미
만진 파일**이다. S6 와 함께 고치면 같은 파일을 세 번째로 여는 것을 막는다.

### 🔴 사용자에게 물어야 할 마지막 한 가지 (이것만 답받으면 바로 착수)
> **"S6 = S6-a + S6-b + S6-c 로 묶어서 진행할까요, 아니면 §7(비주얼·인터랙션)은 별도 단계로 미룰까요?"**
(내 권고 = **묶어서 진행**. 같은 파일을 다시 열지 않아도 되고, #2·#4 는 표시 정직성 문제라 미룰 이유가 없다.)

---

## 2. ✅ 세션N-19/20 성과 (커밋 9건 · app 3회·web 3회 배포 전부 READY)

| 단계 | 무엇 | 커밋 |
|---|---|---|
| **S2'** | 빈 상태 3대 결함 | `87c1a5e`·`fa7ebff` |
| **S3'+S4** | 용어 7종 한국어화·잠금 가격·`lang="ko"`·web/app 동기화 | `0611c2d`·`11f63cb` |
| **S5** | 진단결과 요약부 SSR | `d76a22c`·`eb1f115` |

### 🏆 이번 세션 최대 성과 3가지
1. 🔴 **`/history` 완전 공백** — `jobs.map()` 이 빈 `<ul>` 렌더. tsc·lint·빌드 **전부 통과하는
   침묵 결함**이었다. 가입자 6명 중 5명이 보는 경로.
2. 🔴🔴 **`app` 이 `lang="en"` 이었다** — `:lang(ko) { word-break: keep-all }` 이 CSS 에 **이미
   있는데** 앱이 스스로를 영어라고 선언해 **한 번도 적용된 적이 없었다**. 한 단어로 **앱 전
   화면 한국어 줄바꿈 수리** + 스크린리더 발음 정상화.
3. 🔴 **S5 아이러니 해소** — "AI 인용" 파는데 우리 결과가 크롤러에게 빈 페이지였다.
   라이브 정적 HTML 실측: 브랜드명 **0→10회** · 처방 **0→3회** · 엔진명 **0→2회**.

### 📊 검증 수준 (다음 세션도 이 수준을 유지할 것)
- web·app **tsc 0** · lint **0** · build 양쪽 · 테스트 **122/122**(신규 16건)
- **뮤테이션 검증 4건** — 가드를 죽여 테스트가 실제로 실패함을 확인
- 라이브 재검증 전건 + **스크린샷 눈확인** → 이번 세션에 **버그 6건을 스크린샷이 잡았다**

---

## 3. 🔴 세션N-19/20 이 뒤집은 것 (**되살리지 말 것**)

| 이전 기재 | 실측 결과 |
|---|---|
| *"S2 = 4화면에 빈 상태 이식"* | ❌ **3화면은 이미 4요소 충족**(오탐). 진짜는 `/history` 1건 |
| *"S1 에서 거짓 판매문구 다 제거"* | ❌ **`UpgradeLadder` 누락**(꺼진 이메일 알림을 팔고 있었다) |
| *"S4(용어) → S5(SSR)"* 순서 | 🔄 **뒤집었다** — S4 먼저면 영문 용어가 색인돼 두 번 일한다 |
| *"`/brand` 지적은 오탐"*(N-18) | ⚠️ **부분적으로 틀렸다** — 설명문 한 줄은 **진짜 거짓**(→S6-a) |
| *"P1.6 단일 진실 = 설계 v2"* | ❌ **v3 이 최신**(투두 헤더가 폐기본을 가리켰다 → 정정) |

🎓 **교훈 2개 (매 세션 반복되는 것)**
1. **묶음으로 적힌 항목("4화면에 X 적용")은 건별로 실측하라** — 3건 중 2건이 오탐이었다.
2. **기획서의 축 전체를 확인하라** — 내가 IA 하나만 보고 "S6 설계 끝"이라 했는데
   **사용자가 6개 축을 물어 §7 미착수를 잡아냈다.**

---

## 4. ⚠️ 함정 (착수 전 필독)

### 세션N-19/20 신규
- 🔴🔴 **렌더링 테스트에 `@vitest-environment jsdom` 도크블록 필수.** 이 저장소는 vitest
  설정 파일이 없어 환경 기본값이 `node` → `window is not defined`.
  ⚠️**로컬 전체 실행은 통과한다**(다른 테스트가 만든 DOM 에 편승) → **단독 실행에서만
  드러나고 Vercel 빌드가 그 조건이다.** 내가 이걸로 배포를 깨뜨렸다(`dpl_7iejFziJ…`).
  → 새 `.test.tsx` 는 **`npx vitest run <파일>` 단독으로 먼저** 돌릴 것.
- 🔴 **`apps/web` 에는 테스트 러너가 없다**(vitest 미설치) → 규칙을 고정해야 하는 로직은
  **순수 함수로 `packages/` 에 빼고 `apps/app/__tests__` 에서 검증**
  (`measurement-coverage`·`digest-filter`·`ssr-summary` 가 이 패턴).
- 🔴 **`metrics.enginesCovered` 는 엔진 목록이 아니라 응답 목록**이다(실측 29개 = 고유 8엔진).
  `.length` 를 엔진 수로 쓰면 *"AI 29곳에서 측정"* 이라는 **거짓 숫자**가 된다 → `new Set()`.
  ⛔ `result.geoScore`·`metrics.recognitionRate` 는 실측 회차에 **없었다**(null) — 있다고 가정 금지.
- ⚠️ **요금제 문구는 `apps/app/.../lib/pricing.ts` 와 `apps/web/.../pricing/page.tsx` 둘 다** 고칠 것.
  한쪽만 고치면 **같은 상품이 두 화면에서 다른 이름**으로 불린다(내가 처음에 app 만 고쳤다).
- ⭐ **`app` 은 `lang="ko"`** 다. 되돌리면 **한국어 줄바꿈이 다시 깨진다**.
- ⚠️ **진단 결과 페이지에 `h1` 추가 금지** — 클라이언트 뷰(`audit-result.tsx:1405`)에 이미 있다.
- ⚠️ **크롤러용 텍스트를 `sr-only` 로 숨기지 말 것**(구글: 안 보이는 콘텐츠 마크업 금지).
  중복이 부담이면 **`<details>` 로 접는다** — 접힌 내용도 HTML 에 있어 크롤러가 읽는다.
- ⚠️ **JSX 는 속성 사이에 중괄호 주석을 못 넣는다**(TS1005) → 요소 위로. 그리고 **JSX 주석 안에
  주석 종료 기호를 글자로 쓰면 거기서 끊긴다**(TS1381). 둘 다 겪었다.
- ⚠️ **JSX prop 정렬**(`useSortedAttributes`) lint 에러 → `npx biome check --write <파일>`.
- 🚧 **로컬 dev 로그인은 구조적으로 불가** — dev 는 Clerk **개발 인스턴스**라 QA 계정이 없다
  (*"Couldn't find your account"* + *"Development mode"* 스크린샷 확정).
  → 화면 눈확인은 **배포 후 `python3 scripts/verify-app.py`**(프로덕션 대상).
- ⚠️ dev 서버 대상 Playwright 는 **`networkidle` 금지**(HMR 웹소켓) → `domcontentloaded`.
- ⚠️ 한글 **NFD** 경로 때문에 `packages/database` 에서 `node --experimental-strip-types` 가
  모듈을 못 찾는다 → DB 스키마 확인은 **라이브 API 응답**이 빠르다.

### 기존 함정 (계속 유효)
- 🔴 **cron schedule 은 항상 UTC** · **`x-vercel-cron` 폴백 부활 금지**(보안 구멍)
- 🔴 **로컬 dev·build 는 `VERCEL=1` 필요** · 🔴 **`apps/api` 는 죽은 앱**
- 🔴 **`"use server"` 파일은 async 함수만 export** — tsc·lint 통과인데 **빌드만 실패**
- 🔴 **`audit-result.tsx` M1 주석 존중** · 🔴 **날짜 계산해 넣지 말 것** · 🔴 **BotID 우회 금지**
- ⭐ **app 배포**: `npx vercel --prod --yes --scope nayoy2-7791s-projects --project findable-app`
  (루트에서). **web 배포**: `--project findable`. ⚠️`apps/app` 안에서 실행하면 실패.
- ⚠️ **PortOne 인증 헤더는 `PortOne {secret}`**(문서 요약의 "Bearer"는 틀렸다)
- ⚠️ **오탐률 12~32%** — 진단서를 그대로 다 고치지 말 것
- ⚠️ `app.findable.co.kr/favicon.ico` **404** = 기존 결함·기능 영향 없음

---

## 5. 👤 사용자 결정 대기 (S6 외)

1. 🔴 **P0 실고객 지불의사 검증**(마케터 10~15명) — **불변 1순위·유일한 회사 병목**.
   시뮬 대체 불가(r=0.20). 질문지=`P0_지불의사_시뮬레이션_결과.md`
2. 🟠 **카카오페이 답장 대기** — 4차 회신 발송 완료. **먼저 보낼 것 없음**.
   승인 시 실행카드 §"되돌릴 것" 0~5번(라이브 채널키·결제예약 연결·"첫 회만 결제" 문구 삭제)
3. 🔵 **KIPO 상표 출원** — 앞으로 당겼다(`findableapp.com` 오스트리아 동일명 충돌 + 선출원주의)
4. 🔵 P4 파트너 특전 정책
5. `alerts` 실제 알림 = **보류**(임계값 분포 없음 · 근거 없는 임계값 발명 금지)

---

## 6. 💰 원가 방침 · 🔴 승인 규칙

| 작업 | 원가 | 방침 |
|---|---|---|
| DB 조회 · 브라우저 검증 · 빌드 · 배포 | **0원** | 자유롭게 |
| **새 진단 실행** | **150~300원** | 🔴 **매번 승인** |

⚠️ **엔드포인트 호출도 "새 진단 실행"이 될 수 있다.** 가드부터 코드로 읽을 것.

⚠️ **화면 변경은 원칙적으로 착수 전 승인**(프로젝트 규칙). 단 👤사용자가 2026-08-11 에
**"전략적으로 감사하면서 알아서 진행해"** 를 **3회 연속 지시**했고 S6 는 **설계안을 보여
승인("2번")을 받았다**. 새 세션에서는 **다시 확인할 것**(그 지시가 세션 한정인지 모른다).

---

## 7. 🔴 다음 세션이 잊으면 안 되는 것

> **S0·S1·S2'·S3'·S4·S5 끝·배포됨. 다음은 S6 — 감사·설계·승인까지 끝났고 코드만 남았다.**
> **S6 = ⓐ이름 통일+거짓문구 ⓑ사이드바 3단(실행 최상단) ⓒ비주얼·인터랙션 4건.**
> **착수 전 물을 것 = "ⓒ를 함께 묶을까, 미룰까" 하나뿐.**
> **⛔ 기획서 §3-1 '잠금 4개 접기'는 보류 권고** — 작동하는 기능을 숨기는 셈이다.
> **🎓 기획서는 6개 축이다(IA·플로우·인터랙션·비주얼·카피·SEO) — 한 축만 보고 "설계 끝"이라 하지 말 것.**
> **🎓 묶음으로 적힌 투두는 건별로 실측하라 — N-19 에서 3건 중 2건이 오탐이었다.**
> **👁️ 스크린샷 눈확인이 이번 세션에 버그 6건을 잡았다 — 테스트는 아는 것만 검증한다.**
