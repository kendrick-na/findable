> ⛔ **폐기된 인계 문서입니다. 여기서 지시를 따르지 마세요.**
> 👉 최신 인계 = **`_START_HERE_세션N10.md`** (2026-08-07 세션N-9 종료분)

# 🧭 START HERE — 세션 N-9 (2026-08-07 세션N-8 종료 인계)

> **이 파일 하나만 읽으면 방향을 잃지 않는다.** 상세 근거는 §7 링크로.
> 세션N-8은 **컨텍스트 소진**으로 종료. 코드·문서·메모리 전부 커밋됨(워킹트리 깨끗).

---

## 1. 지금 무엇을 하고 있었나 (한 문단)

**리서치 기반으로 무료진단(web)과 대시보드(app)의 UI/UX를 고치는 중.**
근거는 세션N-4에 확보한 리서치 원본 1,626줄(경쟁사 15개 실측·검색 200회 소진, **재리서치 금지**)과
세션N-7의 3중 감사 결과다. 무료진단 쪽은 **코드 항목이 전부 끝났고 배포까지 됐다.**
지금은 **대시보드(app)** 를 고치는 중이며, D3·D7·D2를 끝내고 **D4 차례**다.

**핵심 전략(리서치 결론, 흔들리면 안 되는 축):**
> 무료가 이미 "반복 확인의 이유"까지 줘버린 게 가치역전의 진짜 원인.
> 유료가 이겨야 할 축은 "더 많은 정보"가 아니라 **"시간·비교·알림"**.
> ⚠️ **진실거울(무료의 aha)을 대시보드로 복사하는 건 오답** — 축이 안 바뀐다.
> ⚠️ **순서를 뒤집지 말 것**: 유료 축을 먼저 채우고, 무료를 줄이는 건 맨 마지막.

---

## 2. ⭐ 다음에 할 일 (이 순서대로)

### 0순위 — **app 배포 + 실화면 확인** 🔴
**이유**: app 변경 3건(D3·CSV·D2)을 **아직 아무도 눈으로 못 봤다.**
특히 D2 주석 UI는 화면 요소가 많다(폼·핀·삭제 버튼·빈 상태).
세션N-5가 "코드만 믿고 넘겼다가" 거짓 숫자를 살려둔 전례가 있다.

```bash
# app 배포 — ⚠️ project.json swap 필요 → 끝나면 반드시 원복
cd /Users/easymilli/Downloads/바이브코딩/Findable
bash scripts/check-app-env-parity.sh   # PORTONE_API_SECRET MISS는 오탐
# (swap 절차는 §6 참조)
```
⚠️ `packages/security`가 web·app **공용**이라 web도 함께 재배포해야 한다
(web 배포 이후 `c802917` 미배포 상태이기도 하다).

### 1순위 — **D4 기간 선택**
리서치가 정의한 유료 축 **"시간"에 정면 대응**하는 유일한 항목. 무료↔Starter 경계선.
- 기간 칩(GSC: *"기본 최근 3개월 + 토글"*)
- ⚠️ **아래 이력 리스트도 같은 기간으로 동기화** — GSC 차트↔표 실시간 바인딩이
  리서치에서 *"가장 베끼기 쉬운 버전"* 으로 **두 번** 꼽혔다(D4와 함께 설계하면 화면 1개로 두 요구 충족)
- 현재 상태: 기간 UI **0건**, `page.tsx:53` `take:20` 고정 · `scopedTracking` `take:1400` 고정

### 2순위 — **D5·D6·D8·D10 묶음** (전부 데이터 있음·화면만)
| # | 할 일 | 실측 |
|---|---|---|
| D6 | 히어로 3장 → `/history`·`/compare`·`/sources` 링크 | ⭐**목적지 라우트 전부 존재**. `dashboard-kpis.tsx:44`가 `div`(링크 아님) → **링크만 걸면 끝** |
| D8 | 감성 카드 3분할 스택바 | 데이터 있음(`SentimentSummary`), 지금은 텍스트로만 |
| D5 | 감성 카드에 이전 기간 병기 | 3장 중 **감성만 없음**(SoV=배지 있음, 순위=힌트 있음) |
| D10 | 브랜드 전환 UI | `dashboard-data.ts:376`이 **조용히 최근 브랜드 자동선택**. 브랜드 9종 보유 유저 실존 |

### 3순위 — 나중
- **D9 순위 스파크라인** — ⚠️겉보기와 달리 **데이터 계층 변경 필요**(`SovTrendPoint`에 `position` 필드 없음)
- **D11 헤더 상주 지표** — 슬롯은 있고 14곳 모두 비어 있음. P2
- **감사 8번 진짜 분모** — `estimateMentionPosition`이 `matches.length`를 버림 → 저장해야 *"5개 중 1번째"* 가능
- **밀리는 질문 리스트** — `runner.ts:354` `promptStats` 계산 후 버림. 채택률 **8/15**로 벤치마크 1위 공백

---

## 3. 🔴 보류 중인 것 (하지 말 것 · 이유 있음)

| 항목 | 왜 보류 |
|---|---|
| **D2 자동 이벤트 핀** | 실측 델타 `[0,0,0,0,+97]` · +97은 **측정실패→복구** 아티팩트. Sistrix 규격 15.5%p 적용 시 **핀이 측정 실패 지점에만** 꽂힌다. 주1회 갱신이라 분포 형성에 **2~3개월** → 재측정 쌓인 뒤 |
| **D1 알림 실구현** | 위와 같은 이유(보낼 변화가 없음). ⚠️단 **cron은 이미 돌고 있다** — 없는 건 "알려주는" 절반뿐이라 데이터만 쌓이면 싸다 |
| **오인 배지·게이트 확장** | 표본 71건 중 **48건이 사용자 본인 테스트** → 어떤 결과든 일반화 불가 |
| **한국 엔진 유료화** | **되돌리기 어려운 순서**라 맨 마지막. 실고객만 판정 가능 |
| **점수 임계값 조정** | F11 이후 등급이 박해진 게 변별력 확보의 결과일 수 있음. 정답이 데이터에 없음 |
| **무료 축소** | 리서치: *"유료 축을 먼저 채우고 그다음 무료를 줄인다. 반대로 하면 게이팅 이탈만 얻는다"* |

---

## 4. 👤 사용자 몫 (내가 못 하는 것)

1. ⭐**실고객 10~15명 지불의사 확인** — 회사의 **단일 장애점·불변 1순위**.
   시뮬레이션으로 **대체 불가** 확정(r=0.20·세그먼트 오지목 50%). §3 보류 항목이 전부 여기에 묶여 있다.
2. **카카오페이 정기결제 CID** — 일반결제와 **별도 발급**. 받으면 바로 구현(설계 조사 완료)
3. **`/compare`·프롬프트 마법사 직접 써보기** — 만들었으나 **써본 기록이 없다**(30분)

---

## 5. 📌 현재 상태 (사실만)

```
브랜치   feature/login-branding-2026-07   (main보다 122개 앞섬 · main이 앞선 건 0 = 충돌 없음)
워킹트리  깨끗
검증     web tsc 0 · app tsc 0 · lint 증가 0

🟢 web  프로덕션 배포됨 (닥터오엣 70→60 검증 완료)
        ⚠️ 단 `c802917`(공유 리드젠 분기)는 배포 후 커밋 = 미반영
🔴 app  전부 미배포 (D3·CSV·D2)
```

**🔑 배포 방식 = CLI 직접**(`npx vercel --prod --yes`). **GitHub 자동배포 아님.**
근거3: 전 배포 Username=`nayoy2-7791` · `vercel inspect`에 **Git 메타데이터 없음** · 별칭 직접 연결.
→ **브랜치 신경 쓸 필요 없다. main 병합 없이 지금 브랜치에서 바로 배포.**

---

## 6. ⚠️ 함정 (착수 전 필독 · 실제로 사고를 냈던 것들)

- 🔴 **`git stash` 금지** — `stash@{0}`에 2026-07 낡은 stash가 그대로 있다. `pop`하면 17개 파일에 충돌 마커.
  대조가 필요하면 **`git show <커밋>:<경로>`**.
- 🔴 **로컬 dev는 `VERCEL=1` 필요**(app·web 공통) — 없으면 Bun에서 Neon 핸드셰이크가 깨져 API 500.
  **코드 수정하지 말 것**(프로덕션은 정상).
- 🔴 **Prisma TCP가 sandbox에서 막힘(P1001)** → 스키마 변경은 **neon HTTP로 직접 DDL**.
  (`Annotation` 테이블도 이 방식으로 만들었다. `prisma generate`는 정상 동작.)
- 🔴 **`Tracking`·`Annotation`·`ActionCompletion`은 org 컬럼이 없다** — brand 경유가 **유일 방어**.
  반드시 `apps/app/lib/db/scoped.ts` 헬퍼로만 접근. 직접 쿼리하면 **다른 조직 데이터가 샌다.**
- 🔴 **grep 0건이면 부재가 아니라 내 검색어를 의심할 것** (세션N-5에서 실제 오판).
- 🔴 **토큰 이름을 상상해서 쓰지 말 것** — CSS 폴백 때문에 **화면상 정상으로 보이는 조용한 결함**이 된다.
  실존 토큰은 `packages/design-system/styles/globals.css`.
- ⚠️ **app 배포는 `project.json` swap → 원복 필수**. www 배포는 swap 불필요(루트 `.vercel`이 이미 `findable`).
- ⚠️ 검증 hook의 **"모델 슬러그 하이픈"·"generateObject removed"는 오탐**(세션 제목 `세션N-8`에 반응).
- ⚠️ `scripts/화면검증/screenshot_ko.py`의 **OUT 경로가 옛 세션 scratchpad로 하드코딩**돼 있다.
  결과 PNG가 거기 쌓인다(인자화 미완).
- ⚠️ ultracite가 **`[jobId]`·`(authenticated)` 괄호 경로를 못 읽는다** → 해당 디렉터리로 `cd` 후 실행.
- ⚠️ **`.env.local` 접근이 이 세션에서 차단됐다** → 로컬 app 화면 확인이 막혔다.
  필요하면 사용자에게 Clerk 키 3개 복사 권한을 요청할 것(§7 절차).

---

## 7. 📕 읽을 것 (이 순서로)

1. **`UIUX_감사결과_3중_2026-08-07.md`** — 미수정 항목 단일 진실. ⚠️**재감사 금지**
2. `투두_마스터.md` START HERE — 세션별 이력
3. `_리서치원본_UIUX_2026-08-06/` (6파일 1,626줄) — ⚠️**재리서치 금지**(검색 200회 소진)
   - `01_GEO경쟁사_12개_UI실측.md` — 경쟁사 기능 채택률의 1차 출처
   - `02_SEO_SaaS_대시보드_패턴.md` — GSC·Sistrix·Semrush. **D4 근거가 여기**
   - `05_설득_전환_리텐션.md:118-123` — **유료 축 정의(가장 중요)**
4. `scripts/화면검증/README.md` — 로컬 화면 확인 절차

---

## 8. 🎓 이 프로젝트에서 반복 확인된 교훈

1. **문서의 결론이 아니라 전제를 코드로 확인할 것.**
   세션N-8에서 감사 문서 사례를 실측했더니 **3번 다 달랐다**(D3 사례 오류 · D7 불필요한 DB변경 ·
   D2를 자동 감지로만 좁혀 읽은 내 오독). 실측이 3번 다 옳았다.
2. **완료 보고를 grep 패턴까지 의심할 것.** N-5의 "해요체 26→0"은 따옴표만 세서 백틱 5건을 놓쳤다.
3. **"불가능"을 에러 실물 없이 쓰지 말 것.** N-5가 넘긴 "Clerk 키 없음"은 틀렸고 진짜 원인은 CSS였다.
4. **표시 구조를 바로잡으면 숨은 계산 결함이 따라 나온다.**
   M1(5축 배치)을 고치자 **3개월 묵은 점수 불일치**(71건 중 58건)가 드러났다.
5. **스크린샷 눈대중 금지.** 화면 85% vs DB 68%였던 적이 있다. 렌더 성공 ≠ 숫자 정확.
6. **"이미 고객을 확보한 서비스의 기록"은 우리 표본보다 강한 근거다.**
   단, **우리 데이터에 없는 것**과 **근거가 없는 것**은 다르다 — D2가 그 차이로 갈렸다.
