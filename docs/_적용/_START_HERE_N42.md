> ⚠️ **이 파일은 N-42(2026-08-18) 시점 기록이다.**
> **현재 남은 일은 `docs/_남은일_단일진실.md`** 를 본다(N-43 에 코드 실측으로 갱신).

# 🧭 세션 N-42 인계 (2026-08-18)

> **새 세션은 이 파일부터.** 상위 = [[project_findable_master_todo]] · 함정 = [[reference_findable_traps]]

## 지금 상태

✅ **8커밋 · 워킹트리 깨끗 · 576/576 · app·web tsc 0 · biome 153(신규 0)**
🔴 **전부 미배포** — 배포하면 405→413커밋
브랜치 `feature/login-branding-2026-07` · 최신 `f4ae4d7`

## 🔴 다음 할 일 한 줄

**로그인 뒤 구간을 Storybook 으로 캡처 → 끊긴 지점 확인 → 온보딩 범위 확정.**
그다음 **배포**.

---

## 이번 세션이 한 것 (8커밋)

| 커밋 | 내용 |
|---|---|
| `d595f3e` | **측정 전 자동추정 노출** — Profound `f049` 대응. `reason`·`confidence` 가 렌더 0곳이었다 |
| `c16e534` | 🔴 **배지에 영어가 새던 버그** — 라벨 키가 DB 저장값과 한 번도 안 맞았다 |
| `03e6f6a` | **유형 태깅 2종→7종** — 배열 단위 부여 + `CATEGORY_MAP` 이 2값으로 접고 있었다 |
| `01133c2` | **유형별 묶음(Topics)** — 경쟁사 3/4 보유. **1칸이면 안 묶는다** |
| `a6c6503` | **lint 326→153** — 안전 자동수정만. 49파일 변경이라 무해함을 증명 후 커밋 |
| `352d5d5` | **무료진단 랜딩 제거 + 초대 코드** — 기간제 권한 |
| `f3c21f5` | **운영 콘솔 `/admin/orgs`** — 가입 조직·초대 코드 관리 |
| `f4ae4d7` | 가입 비밀번호 칸 영어 잔존 한글화 |

---

## ⭐ 오버엣지 기업 수용 — 준비 완료

| | 상태 |
|---|---|
| 초대 코드 | ✅ `KAISTOVEREDGE2026` **프로덕션에 생성됨**(growth · 30일 · 한도 20 · 유효 2026-12-31) |
| 코드 입력 화면 | ✅ `/billing` 상단 |
| 만료 처리 | ✅ 크론(매일)이 `planExpiresAt` 지난 org 를 free 강하 |
| 운영 콘솔 | ✅ `/admin/orgs` — 코드 생성·기간수정·조직목록·`+30일`·`회수` |

🔴 **배포해야 작동한다**(코드는 DB 에 있지만 화면·액션이 미배포).

⛔ **`trial` 플랜은 만들지 않았다** — `Plan` enum 확장 = 상품 구성 변경 = 카카오페이 심사
   항목(~9월 초). 기존 `growth` 를 **기간제로** 주는 방식이라 심사와 무관하다.

---

## 🔴 N-42 신규 함정 6건

1. 🔴🔴 **다른 프로젝트 서버를 찍고 오진할 뻔했다.** 포트 3000 이 **「놀맵」**이었다
   (Findable app 이 아니다) → *"가입 404·로그인 404"* 로 보고할 뻔.
   → ⭐ **포트를 점유한 프로세스가 무엇인지 먼저 확인**(`curl /` 로 `<title>` 을 본다).
   Findable app 은 3010 에 따로 띄웠다(`npx next dev -p 3010`).
2. 🔴🔴 **"고쳤다"를 화면으로 확인하지 않으면 안 고쳐진다.** 비밀번호 문구를
   `formFieldInputPlaceholder__password` 로 덮었는데 **tsc 0·빌드 통과인데 화면은 그대로**였다.
   그건 **로그인용** 키고 가입은 `__signUpPassword` 다.
   → ⭐ 키 이름을 추측하지 말고 **원본**(Clerk `en-US.ts`)에서 확인 · **DOM placeholder 를 직접 읽어** 검증.
3. 🔴 **내 정규식이 「에러 화면」을 오탐했다.** `/ko/pricing` 을 에러로 판정했으나
   스크립트 내용을 본문으로 센 것. 라이브·로컬 **둘 다 200**.
   → 에러 판정은 **보이는 요소**(`h1,h2,h3,p`)로만.
4. ⚠️ **`networkidle` 은 Clerk 화면에서 안 끝난다**(30초 타임아웃 → 전부 `-1`).
   → `domcontentloaded` + 고정 대기로 찍는다.
5. ⚠️ **localhost 는 i18n 리다이렉트 루프가 난다**(`ERR_TOO_MANY_REDIRECTS`).
   N-39 함정 재확인 — **라우팅·SEO 판정은 라이브 curl 로**.
6. ⚠️ **playwright 브라우저 경로는 매번 다르다** — 이번엔
   `chromium_headless_shell-1223`(N-41 기록은 `1234`). **`find` 로 실경로를 잡을 것.**

---

## 📸 전 과정 캡처 (홍보·가이드 영상 소재)

📁 **`_캡처_전과정_20260818/`** — 19장 · 13MB · 레티나(PC 2배 · 모바일 3배)
- 비로그인 구간 전부: 랜딩·가격·무료진단·리포트·가입·로그인·로그인벽 3종
- PC 1440 · 모바일 390 두 벌
- ⬜ **로그인 뒤 구간은 아직 없다** → 다음 세션이 Storybook 으로 찍을 것

---

## ⬜ 남은 일 (우선순위)

| 순 | 무엇 | 규모 | 비고 |
|:--:|---|:--:|---|
| **1** | 🔴 **로그인 뒤 Storybook 캡처 → 끊김 분석** | M | 온보딩 범위를 이걸로 정한다 |
| **2** | 🔴 **온보딩 신설** | **L** | 경쟁사 대비 **가장 뒤진 것**(아래) |
| **3** | **배포** + 라이브 실화면 검증 | S | 8커밋 대기 |
| 4 | 경쟁사 전체 재점검 | M | 배포 후 |
| 5 | ⏸️ P4 엔진 필터 | S | 선행조건(P0) 철회 → **재판정 필요** |
| 6 | ⏸️ lint 잔여 153 | M | 69건이 테스트의 `useTopLevelRegex`(기계적) |
| 7 | ⛔ 로그인 소셜프루프 | S | **심사동결**(결제 경로 입구) + 고객 0명 |
| 8 | ⛔ 블로그/RSS | M | **BaseHub 레포가 비어있다**(스키마 0) → **MDX 로 확정**(👤) |

### 🔴 2번 온보딩 — 왜 가장 뒤졌나 (경쟁사 실측)

| 단계 | Scrunch | Profound | **우리** |
|---|---|---|---|
| 용도 선택 | ✅ | — | ❌ |
| 국가 | ✅ 드롭다운 | ✅ 감지표시 | 🟡 **표시만**(N-42) |
| **"무엇을 파나"** | ✅ 1~2문장 | ✅ 9줄 `X`/`+` 편집 | ❌ |
| 페르소나 | ✅ 0/3 자동제안 | — | ❌ |
| 경쟁사 | ✅ 0/5 자동제안 | — | ❌ |
| 형태 | **전용 온보딩 화면** | **전용 `/welcome`** | 🔴 **폼 안의 회색 박스 하나** |

→ 경쟁사는 **가입 후 별도 여정**, 우리는 **브랜드 등록 폼에 한 줄**. 규모가 다르다.
→ ⚠️ 다만 **6단계를 그대로 베끼면 안 된다**: Scrunch 는 그 6단계를 다 하고도
  우리를 **암호화폐 마케팅**으로 잡았다. ⚠️ **2026-08-19 정정(👤)**: 이건 **오분류가 아니었다** —
  인디고차일드는 실제로 암호화폐 마케팅사였다. 이 문단을 오분류 근거로 인용하지 말 것.
  **우리 강점(도메인 1개)을 버리지 말 것.**

---

## 👤 사람이 해야 할 것

| # | 무엇 |
|:--:|---|
| 1 | **admin 확인** — `app.findable.co.kr` 사이드바에 「운영 현황」이 보이나? 안 보이면 Clerk → Users → 본인 → Public metadata 에 `{"role":"admin"}` |
| 2 | **BaseHub** — 토큰은 살렸으나(401→200) **레포가 비어있다**. MDX 로 가기로 확정했으므로 BaseHub 는 이제 불필요 |
| 3 | 구글·네이버 수집 현황 확인 |
| 4 | 카페24 문의 `eco_bizops@cafe24corp.com` |
